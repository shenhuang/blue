# Nightly verify + publish — 2026-06-04

**Verdict:** All green (build + 20/20 regressions + exploratory probes). **Publish SKIPPED** — working tree dirty with a change I did not make, and local `main` is not ahead of `origin/main`. No code changed.

## Tree state — DIRTY (not publishable)
- `git fetch origin main` → OK.
- Read-only status (`git --no-optional-locks status --porcelain`):
  - ` M .deploy-token.example` — a change **I did not make this run**. Diff is a single trailing-newline removal; the visible text is byte-identical (`# Delete it any time to disable unattended publishing.` loses its final `\n`). Left untouched per policy (never commit/stash/discard work I didn't make).
  - `?? CLAUDE.md` — always-untracked, ignored per task policy.
- `.deploy-token` present and non-empty (40 bytes) → unattended publishing **is** configured.
- Because the tree is dirty, I did **not** rebase/stash. Sync was a no-op anyway: local `HEAD` already equals `origin/main`.

## Synced commit
- `HEAD` = `origin/main` = **`4ac45b5`** (`部署修复：删冗余 static.yml… + 加 .deploy-token 无人值守推送机制`).
- Ahead/behind vs `origin/main`: **0 / 0**.

## Build + typecheck — GREEN
- `node_modules` present → skipped `npm ci`.
- `npx tsc -b` → exit 0, no type errors.
- `npx vite build --outDir /tmp/nightly-dist --emptyOutDir` → exit 0; 90 modules; `index.js` 340.11 kB (gzip 136.44 kB), `index.css` 15.66 kB. (Built to `/tmp`; the mount can't overwrite `./dist`.)

## Regression suite — 20/20 PASS
All `scripts/playthrough*.ts` via `tsx`, exit 0:

| script | result |
|---|---|
| playthrough.ts | ok |
| playthrough-bands.ts | ok |
| playthrough-bluecaves.ts | ok |
| playthrough-chart.ts | ok |
| playthrough-combat.ts | ok |
| playthrough-combat-scenarios.ts | ok (9/9) |
| playthrough-corpse.ts | ok |
| playthrough-decay.ts | ok |
| playthrough-economy.ts | ok |
| playthrough-lighthouse.ts | ok |
| playthrough-lighthouse-scenarios.ts | ok |
| playthrough-mapgen-scenarios.ts | ok (4/4 + 60/60 seeds) |
| playthrough-mimic.ts | ok* |
| playthrough-outpost.ts | ok |
| playthrough-save.ts | ok |
| playthrough-scenarios.ts | ok (110/110) |
| playthrough-sensors.ts | ok |
| playthrough-stealth.ts | ok |
| playthrough-upgrades.ts | ok |
| playthrough-wreckyard.ts | ok |

\* `playthrough-mimic.ts` exits 0 and prints `✓ … 回归通过`. The crude grep heuristic (`✗`/`断言失败` = fail) flagged it on a **false positive**: the line `isPoiLit ✓ / isPoiExplainedByLighthouse ✗（宏观 tell）✓` uses `✗` as a *semantic* symbol describing the mimic "tell" (a lit chart POI that no lighthouse explains), not as an assertion failure. Genuine pass.

## Exploratory testing
Throwaway probes under `/tmp` only (`/tmp/probe-nightly.ts`, `/tmp/probe-outpost.ts`) — never written into the repo. Recent history is entirely the 深水区 Phase 0–3 series (bands, clarity/sensors, alert/stealth, outposts, mimic capstone) plus the deploy wiring, so I targeted those systems and the newest content pools.

**1 · Full-dive invariant fuzz** — 70 runs = all 5 bands (`reef_deep`, `trench_mouth`, `trench_throat`, `abyssal`, `hadal`) × 14 seeded plays; half with sonar + battery upgrades, randomly toggling light / pinging sonar at each node. `Math.random` seeded per run (reproducible). **2,567** invariant checks at every step:
- no throws; every stat (`stamina/oxygen/sanity/nitrogen`), `power`, `alert`, `currentDepth`, `turn` finite;
- bounds held: oxygen ∈ [0, max], sanity & nitrogen ∈ [0,100], power ∈ [0, powerMax], alert ∈ [0,100], depth ≤ map max;
- no soft-locks — every run terminated within the 400-step guard (0 guard trips). Outcomes: 12 reached ascent/resolution, 38 ended in an ambush encounter, 0 oxygen/sanity deaths. The high ambush rate is expected Phase 0b behaviour (lit lamp in deep water → alert → predator approach) and is not a defect; combat itself is covered by the combat playthroughs.

**2 · Content stress (recently-added + base pools)** — resolved **every option of every event** in `trench` (6), `abyssal` (5), `hadal` (4), `mimic` (2), plus `reef`, `wreck_graveyard`, `lighthouse`, `blue_caves`. Each option resolved under 8 seeds × high/low sanity to exercise both `onSuccess` and `onFailure` check branches: **1,920** resolutions, no throws, narrative always an array, stats stayed finite, no negative gold.

**3 · Save round-trip** — a deep mid-dive snapshot (`zone.blue_caves` @ 60m, power 40/40, the trench-mouth band) survived `serialize → deserialize` with `sensors / power / powerMax / alert / bandAlertFactor / activeFlags(Set) / stats` all preserved.

**4 · Outpost economy bounds (Phase 2b)** — both real outposts (`outpost.reef_deep` static, `outpost.trench_deep` water-current/2× decay) across raw stage 0–3 × elapsed runs 0–12: **104** checks. `outpostDecayLevel` ∈ [0,4], `effectiveOutpostStage` ∈ [0, raw], energy `capacity ≥ 0`, online facilities ⊆ built, Σ online draw ≤ capacity, and recharge/oxygen bonuses consistent with online state. Observed graceful degradation on the current outpost as it goes unmaintained: `decay 0→2→4`, `capacity 3→1→0`, facilities drop offline in deterministic order (recharge first, then oxygen supply), saturating at decay 4 — no negatives, no crash.

**Findings:** no clear defects, no invariant violations, no crashes. No ambiguous notes that would block publishing.

## Fixes
None — nothing red, nothing to fix.

## Publish — SKIPPED
Two independent blockers (either alone is sufficient):
1. **Working tree dirty** with `.deploy-token.example` (a change I did not make) — policy forbids publishing changes I didn't author.
2. **Local `main` not ahead of `origin/main`** (0 ahead) — nothing new to publish.

No push attempted. `.deploy-token` is present, so unattended publishing remains available for a future clean + ahead night. `.deploy-token.example` left untouched for the user to resolve (a one-character trailing-newline diff).
