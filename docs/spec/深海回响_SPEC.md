# 深海回响（暂定名）· 游戏设计文档

> 状态：**最早的总纲文档，内容随开发大量迁出（最新一次文档口径核对 2026-06-06，随 docs 分拆治理）**——§1 设计支柱/§2 核心循环等宏观口径仍是 canon 起点，但多节（§10 战斗「占位」、§8 装备背包、附录 A 数值等）已被后续专门 SPEC（战斗系统/负伤系统/物品栏与装备/探深深度柱等）取代或细化，读具体机制请优先查对应子 SPEC，本档仅作历史总览。
>
> 一款潜水题材的文字冒险 Roguelike。
> 从港口出发，下潜未知海域，与体力、氧气、理智、氮气的拉扯中追逐宝藏，
> 在深处与不可名状之物相遇——若能活着回来。

---

## 1. 设计支柱

1. **每一次下潜都是一次取舍**：氧气是时间，深度是回报，氮气是债务，理智是边界。
2. **死亡是叙事而非惩罚**：尸体留在海底，下一次出航你可能成为别人的传说。
3. **越深越不可知**：写实 → 异样 → 克苏鲁的渐进过渡，由理智值与深度共同驱动。
4. **数据驱动可扩展**：所有事件、装备、升级以 JSON 配表，方便后续大规模内容填充。

---

## 2. 核心循环

```
出港 → 选择下潜海域 → 随机生成节点图（浅海池 → 深海池过渡）
     ↓
进入节点（回合制：每回合消耗氧气，深度叠加氮气，事件可额外加成）
     ↓
触发事件 → 选项检定 → 属性增减 / 物品获得 / 链式剧情
     ↓
判断：继续深入？ 选择上浮？ 应急上浮？
     ↓
上浮（含减压停留 mini-loop） → 回港
     ↓
结算：变卖宝藏（金币）、记录探索深度（建设值）
     ↓
升级港口、购买装备 → 再次出航
```

死亡时，本次身上的宝藏与一次性物资留在海底；探索程度转化为**港口建设值**，永久投资升级树。

---

## 3. 四属性系统

| 属性 | 起始 | 上限 | 主要消耗 | 主要恢复 | 备注 |
|---|---|---|---|---|---|
| 体力 Stamina | 100 | 装备决定 | 挣扎、战斗、强游 | 休息回合、补给 | 影响"逃跑/拼搏"类选项检定 |
| 氧气 Oxygen | 由气瓶决定 | 由气瓶决定 | 每回合按深度系数消耗 | 仅靠备用气瓶/事件 | 归零 = 死亡 |
| 理智 Sanity | 100 | 100 | 深度衰减 + 事件冲击 | 回港、特定装备、安抚事件 | **隐藏属性**，玩家从描述变化感知 |
| 氮气浓度 Nitrogen | 0 | 100 | 深度持续累积 | 上浮+减压停留、混合气 | 过高 → 减压病；高浓度+深度 → 氮醉扣理智 |

### 3.1 属性耦合

- **体力 ↓** → 动作消耗氧气加倍
- **氧气 ↓** → 触发"喘息""窒息恐慌"等扣理智事件
- **氮气 ↑** → 高深度下，每回合按比例扣理智（氮醉）
- **理智 ↓** → 解锁/暴露幻觉选项，描述文字开始失真

### 3.2 UI 关键预警

属性面板必须显示两个派生值，让玩家明确感知风险边界：

- **氧气剩余回合数**（按当前深度基础消耗预估）
- **安全上浮所需回合数**（按当前氮气浓度的减压方案预估）

当后者 > 前者，UI 标红警告 —— 玩家知道自己已经"过界"。

---

## 4. 上浮与减压系统

### 4.1 三种上浮方式

| 类型 | 触发 | 代价 |
|---|---|---|
| 正常上浮 | 玩家主动选择 | 经过若干上浮节点，可能触发事件 |
| 应急上浮 | 任意时刻可触发 | 跳过减压停留，**几乎必得减压病** |
| 强制上浮 | 氧气≤危险线、理智 0、特定事件 | 等同应急上浮，无玩家选择空间 |

### 4.2 减压病分级

| 分级 | 触发条件 | 后果 |
|---|---|---|
| I 型 皮肤型 | 轻微氮气超标 | 回港治疗费扣金币，无长期后果 |
| II 型 关节型 | 中度氮气超标 | 下次下潜起始体力 −30% |
| III 型 神经型 | 重度氮气超标 | 下次下潜起始理智 −50%，部分选项文字模糊 |
| IV 型 致命 | 极重 + 高深度 | **当场死亡**，尸体保留全部战利品 |

> MVP 阶段只实现 II 型与 IV 型，I/III 型后续扩展。

---

## 5. 理智与克苏鲁渐进

### 5.1 理智阶段

| 区间 | 表现 |
|---|---|
| 80–100 | 完全正常 |
| 50–80 | 偶现"红字选项"（幻觉诱饵） |
| 20–50 | 节点描述失真，出现假节点 |
| 0–20  | UI 文字偶发故障，看到"不该看见的东西" |
| 0 | **疯狂上浮**（自动忽略减压，大概率致命） |

理智不是另一条死亡条，而是**叙事和决策质量的滑坡**。

### 5.2 深度 × 内容调性

| 深度 | 区域 | 调性 | 典型事件 |
|---|---|---|---|
| 0–20m | 港口浅滩 | 写实 | 海胆、章鱼、生锈渔具、新手沉船 |
| 20–50m | 大陆架 | 写实+悬念 | 商船残骸、洋流、鲨鱼、第一具人类骸骨 |
| 50–100m | 暮光带 | 异样 | 错误的回声、应该死掉的鱼群、灯光下的眼睛 |
| 100–200m | 午夜带 | 克苏鲁 | 不会破碎的"骨头"、规律气泡、看到死去的同伴在游 |
| 200m+ | 渊海带 | 不可名状 | 巨物轮廓、几何错乱的"建筑"、听见自己的声音 |
| 300m+ | 沉没之城 | 终极 | 隐藏 BOSS，理智不可恢复 |

---

## 6. 探索与节点系统

### 6.1 节点图生成

每次出海生成一张**有向图**，节点带深度属性。事件池按深度分段（`shallow` / `coastal` / `twilight` / `midnight` / `abyssal`），随推进自然过渡。

每个节点暴露 2–3 个下一节点选项，玩家选择即推进。某些节点有特殊标签：

- **上浮口**：可发起正常上浮
- **休息点**：可消耗回合恢复体力/部分氧气
- **沉没遗迹**：固定特殊事件链
- **尸体定位**：与玩家历史死亡点匹配，触发尸体回收

### 6.2 回合制时间粒度

每回合自动结算：

```
氧气 -= baseOxygenCost × depthFactor × staminaFactor × weightFactor
氮气 += baseNitrogenGain × depthFactor
理智 -= sanityDecayAtDepth(currentDepth) × nitrogenNarcosisModifier
```

某些事件附加 `oxygenTurnCost: 3`，代表"这次行动额外消耗 3 回合氧气"。

---

## 7. 死亡、尸体与元进度

### 7.1 死亡时

- 本次身上**所有战利品与一次性物资**留在海底
- 本次永久装备：**留在海底**（需找回尸体）
- 金币：**全部留在海底**（除非已存入港口银行，见 §9.2）
- 探索程度 → 转化为**港口建设值**：
  ```
  建设值 = 最深深度系数 + 触发事件数 × α + 完成事件链 × β
  ```

### 7.2 尸体系统

- 尸体在**死亡区域相近深度**生成，下次出海有概率匹配进节点池
- 节点选择时给出文字提示："这一带似乎有熟悉的东西…"
- 回收时受限于当前背包容量与负重
- 尸体随"水下时间"（实际下潜次数）逐步腐烂：物品失效、信息丢失
- 隐藏机制：尸体在同一海域累积过多 → 触发"亡者之径"事件链（吸引深海某物）

### 7.3 港口建设值升级树

四条独立线，玩家需在有限建设值下取舍：

| 线 | 主题 | 典型升级 |
|---|---|---|
| 船坞 | 出海起点 | 起始水深、跳过浅水跑图、出海前补给 |
| 气瓶库 | 呼吸方案 | 氧气上限、混合气解锁、备用气瓶 |
| 打捞行会 | 信息与回收 | 尸体定位、宝藏地图、节点预览 |
| 教堂/灯塔 | 心理建设 | 理智上限、识别幻觉选项、抗深海污染 |

---

## 8. 装备与背包

### 8.1 装备槽（5 槽固定）

| 槽位 | 关键效果 |
|---|---|
| 气瓶 | 氧气上限、混合气类型 |
| 潜水服 | 体力上限、保暖（深度负面修正抗性） |
| 灯具 | 节点视野（提前看清下一节点类型）、影响特定事件 |
| 副手工具 | 鱼叉/切割器/相机等，影响事件选项 |
| 护身符 | 理智抗性、抵抗幻觉（后期解锁线） |

装备每个槽位有等级（1–5）+ 可选词缀，词缀通过事件、稀有掉落、港口高级升级获得。

> MVP 阶段先做前 4 槽，护身符在克苏鲁层扩展时引入。

### 8.2 背包

- **格子上限**：基础 8 格，港口升级可至 16 格
- 高价值物品占多格（一只完整的鱼叉可能占 2 格）
- **负重**：所有物品累积负重影响上浮速度与每回合氧气消耗
- 取舍设计：拿一件 4 格的稀有宝物 vs 多塞 2 件中级宝物

---

## 9. 经济系统

### 9.1 双货币

- **金币**：通过变卖战利品获得，购买装备、消耗品
- **建设值**：通过探索程度获得，升级港口

两套货币独立运作，避免"刷钱通关"的退化路径。

### 9.2 港口银行（升级解锁）

港口升级解锁银行后，玩家可在出海前**存入部分金币**，存入部分死亡后保留。
鼓励"分批冒险"策略，但要交手续费。

### 9.3 消耗品

- 额外氧气瓶（途中补气）
- 急救包（恢复体力）
- 镇静剂（恢复理智）
- 修复包（损坏装备修复）

消耗品占背包格，深海下决定带几个是核心策略。

---

## 10. 战斗系统（占位）

MVP 阶段：生物事件走选项检定，无独立战斗系统。

后续设计：轻量回合制独立战斗，不沿用 catacomb 战斗机制。
- 候选方向：动作点（AP）系统，玩家每回合 2–3 AP，可分配到攻击/防御/呼吸调节
- 敌人有"威胁等级"和"行为模式"两个轴
- 战斗中氧气仍持续消耗，强迫"快速解决"心理压力

**待开新文档单独设计。**

---

## 11. 多结局

| 结局 | 触发 | 主题 |
|---|---|---|
| 渔夫结局 | 港口经济达到某阈值，主动退役 | 平静、富足 |
| 探险家结局 | 累计抵达多个深度里程碑后安全归港 | 荣耀、传奇 |
| 真相结局 | 完成沉没之城事件链，击败/对话隐藏存在 | 不可名状 |
| 殉海结局 | 在指定深度死亡且持有特定物品 | 悲剧、归属 |
| 疯狂结局 | 理智永久归零且达到一定深度 | 克苏鲁 |

不同结局对应不同的港口最终状态与玩家档案标记，鼓励多周目。

---

## 12. 事件数据 Schema

```typescript
type DiveEvent = {
  id: string;
  // —— 触发条件 ——
  depthRange: [min: number, max: number];
  zoneTags?: ('wreck' | 'cave' | 'reef' | 'abyss' | 'ruins')[];
  sanityRange?: [min: number, max: number];      // 仅在此理智区间出现（幻觉事件）
  weight: number;                                 // 抽取权重
  cooldown?: number;
  oncePerRun?: boolean;
  oncePerSave?: boolean;                         // 剧情向事件
  prereqEventIds?: string[];
  prereqFlags?: string[];

  // —— 文本与呈现 ——
  title: string;
  body: string;                                  // 模板变量 {player.name} {depth}
  tone: 'realistic' | 'uncanny' | 'cosmic';    // 影响 UI 配色、字体故障

  options: EventOption[];
};

type EventOption = {
  label: string;
  visibleIf?: Condition;                         // 需要装备/技能/物品
  hallucination?: boolean;                       // true = 理智低时才出现且结果是坏的
  check?: {
    stat: 'stamina' | 'sanity' | 'nitrogen' | 'oxygen';
    dc: number;
    onSuccess: Outcome;
    onFailure: Outcome;
  };
  outcome?: Outcome;
};

type Outcome = {
  text: string;
  deltas?: Partial<Record<Stat, number>>;
  oxygenTurnCost?: number;                       // 额外消耗 N 回合氧气
  loot?: LootRoll[];
  applyFlags?: string[];
  triggerEventId?: string;                       // 链式事件
  endDive?: 'forceAscend' | 'death';
};
```

### 12.1 数据流

```
designer 编辑 events.json / xlsx
   ↓ （可选 dataconv 脚本）
   游戏读取 JSON
   ↓
   按当前 depth/sanity/flags 过滤、按 weight 抽取
   ↓
   渲染事件 → 玩家选项 → 结算 → 链式触发
```

---

## 13. 技术栈

- **前端**：TypeScript + Vite + 简洁 DOM/Canvas 渲染
- **数据**：JSON 直接手写（事件多分支适合 JSON）；如有协作需求加 xlsx → JSON 转换层
- **状态管理**：单一 GameState store，所有事件都是 `(state, choice) → newState` 的纯函数
- **持久化**：LocalStorage 存档（多存档槽）+ JSON 导出（便于备份和分享）
- **部署**：纯静态站点，可发到 GitHub Pages / Gitee Pages，无后端

---

## 14. MVP 范围（第一里程碑）

### 必须有

- [ ] 深度 0–100m，三段事件池
- [ ] 四属性 + UI 预警
- [ ] 节点图生成 + 节点内事件
- [ ] 回合制氧气/氮气结算
- [ ] 正常上浮 + 应急上浮（含 II 型与 IV 型减压病）
- [ ] 理智驱动的红字选项 + 暮光层异样事件
- [ ] 死亡 → 尸体留存 + 建设值结算
- [ ] 港口：船坞 + 气瓶库两条升级线
- [ ] 4 个装备槽 + 基础背包（8 格 + 负重）
- [ ] 30–50 个事件，覆盖三段深度
- [ ] 单存档系统

### 不做（后续）

- 克苏鲁深层（100m+）
- 护身符槽位
- 词缀系统
- 独立战斗系统
- 多结局（MVP 只有"活着回港"和"死亡"两个简单终态）
- 多语言

---

## 15. 路线图

| 里程碑 | 内容 | 估计 |
|---|---|---|
| M1 引擎 | 状态机、回合制、节点图、事件 schema 与渲染 | 2–3 周 |
| M2 MVP 玩法 | 上浮/减压、死亡/尸体、港口升级（2 线）、装备槽 | 2 周 |
| M3 MVP 内容 | 40 个事件 + 10 件装备 + UI 完善 | 2 周 |
| M4 内测 | 闭门测试、平衡调整 | 1 周 |
| M5 深海扩展 | 100–200m、克苏鲁事件、护身符、幻觉系统强化 | 3 周 |
| M6 战斗系统 | 独立设计文档 + 实装 | 4 周 |
| M7 多结局 + BOSS | 渊海层、沉没之城、5 结局 | 4 周 |

---

## 16. 开放问题（待后续讨论）

1. **战斗系统细节**：AP 还是行动点列表？敌人 AI 复杂度？
2. **音效与配乐**：纯静态站如何处理？要不要做？
3. **国际化**：是否需要英文版？影响事件文本编写规范。
4. **特殊节点平衡**：休息点是否限次？尸体定位是否消耗资源？
5. **战利品稀有度**：是否引入"传说级"装备，及其在事件中的获取设计？
6. **港口 NPC**：是否引入有名字的 NPC（船长、商人、酒馆怪人），承载小剧情？
7. **教学关**：第一次下潜是否是特制的"引导海域"？
8. **存档机制细节**：是否允许在下潜中保存？还是只在港口存档（强化 roguelike 张力）？

---

## 附录 A · 默认数值预案（待平衡）

```
起始 体力 100 / 100
起始 氧气 60 回合（一级气瓶）
起始 氮气 0 / 100
起始 理智 100 / 100

每回合基础氧气消耗 1
深度系数 = 1 + depth / 50  （30m 时为 1.6，100m 时为 3.0）
深度氮气增量 = depth / 30   （30m 时每回合 +1，100m 时每回合 +3.3）
理智衰减
  0–30m:   0
  30–60m:  0.2/回合
  60–100m: 0.5/回合
  100m+:   1.0/回合 起步，深度每 +50m 翻倍

氮气阶段：
  0–40:    安全
  40–60:   需 1 次减压停留
  60–80:   需 2 次减压停留
  80–95:   需 3 次减压停留，氮醉每回合 -0.5 理智
  95+:     氮醉每回合 -1.5 理智，强制减压
```

所有数值在 M4 内测前都视为占位，按测试反馈大幅调整。
