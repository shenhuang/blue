# 深海回响 · ch1 主线「柱迁移」+ 文案提案

> 状态：**提案·待作者拍**。不落实现——等并发经济 session 落定后再动手（见 docs/playtest-findings.md D-2）。
> 机制地基已就绪并验绿（`tier.story → columnStory` + firing + reach 分离 + `check-mainline-reachable`）；本文只定**形状 + 文案**。
> 文案语调过 `protagonist-voice`（冷静寡言·克制·禁直白哭/泪）。新写的每条标 `[占位·待过稿]`。

---

## A. 迁移形状（build-gated 主线链）

**核心一句**：四个主线 beat 的**文案原样复用**（见 B-1·不重写），改的只是「怎么解锁」——从「日志点亮即可下潜」改成「**先建好该区前哨 → 该区主线坐标才下得去**」。

链条（`reveal` = 日志 `marksPois` 拿到即**早见**；`reach` = 建好 host 前哨才**下得去**；`build 解锁` = 上一 beat 完成的 flag）：

| 顺 | beat（复用事件） | host 灯塔 | reach 门（建什么） | 建造由谁解锁 | 完成置 flag |
|---|---|---|---|---|---|
| ① 珊瑚丛 | `ch1.anchor_reef` | `lighthouse.home`（恒在） | **免费**·教学完即可下 | —（入口） | `story.ch1.anchor.reef` |
| ② 残骸 | `ch1.anchor_wreck` | `ch1_wreck_outpost` | 建**残骸前哨** | `anchor.reef` | `story.ch1.anchor.wreck` |
| ③ 中层 | `ch1.anchor_midwater` | `ch1_midwater_outpost` | 建**中层浮标** | `anchor.wreck` | `story.ch1.anchor.midwater` |
| ④ 热液→观测站 | `ch1.anchor_vent` → `ch1.ending_station` | `ch1_vent_outpost` | 建**热液井台** | `anchor.midwater` | `story.ch1.ending.fulfilled` |

→ 走法变成：教学 → 潜①(免费) → **建残骸前哨** → 潜② → **建中层浮标** → 潜③ → **建热液井台** → 潜④ →「观测站」结局。前哨脊柱从此**主线必经**（配 `check-economy-reachability` 保证沿途料够·不 grind）。

### 三个待你拍的细节（附建议）

1. **reef beat 放哪**：建议**保持免费入口**——家灯塔恒在、教学完即可下①，不给它挂前哨门。②③④ 才 build-gated。（否则开局无前哨可建 → 死锁。）
2. **beat 坐落的深度**：现有柱 `tier` 深度与 beat 原深度有出入（如 reef beat 10–22m vs `col.home` t1 30–40m；vent beat ~85m vs `col.vent` t1 25–75m）。**建议给每根柱加一个专门的 `story` tier 承载 beat**（落在该区 beat 的原深度·与刷怪 tier 并存·`noPoi` 之外用新 `story` 字段），而不是把 beat 硬塞进现有刷怪 tier——既保 beat 深度手感、又不动现有 farm 平衡。
   - ⚠️ `check-dive-refs` 要求 tier 从 1 连续单调——加 story tier 要么排进序列（占一个 tier 号、`noPoi` 不出刷点 POI 但出 story POI），要么单独建模。实现时按这条收口。
3. **「升级 host 前哨 → 解锁更深 beat」**（你提的那条）：ch1 **先只做上面 4-beat 链**；更深 beat 作为**机制已支持的扩展点**留 St-later / 二章（与 `deep_game_vision` 的 d_reveal 冻结一致·别进 ch1）。B-3 给一个**纯演示**样板看长相。

---

## B. 文案

### B-1　复用（原样保留·**不重写**）

这 5 段已是成稿质量、且承载 canon（四鱼四群落 / 四低调 tells / 圆满「当时为真」）——迁移**只改触发位置（锚点 POI → 柱 story tier），文本一字不动**：

- `ch1.anchor_reef`「漆号珊瑚丛」——萤光背礁鳚 ·「天黑了才找得见」。
- `ch1.anchor_wreck`「舷窗后的注视」——锈甲沉船鲷 ·「群落比坐标重要」。
- `ch1.anchor_midwater`「中层的光」——幻光流帆鱼 · 送行队形那处幻觉。
- `ch1.anchor_vent`「黑烟与灯」——热液岩鳞鱼 · 带路的扫样地光 → 触发结局。
- `ch1.ending_station`「观测站」——四 tells（不摘面罩 / 站太干净 / 队友只在反光 / 台词全出自日志）· 顺手 loot 破损饰品 → St2 留白结局接口。

（`ch1.ending_blank` 留白结局同样原样保留。）

### B-2　新增占位（build-gating 引入的「连接节拍」· 每区一句 · `[占位·待过稿]`）

新模型里你**先建前哨**、那一区的主线坐标才从「灰（dim·看得到去不了）」转「亮（lit·下得去）」。给这一刻各加一句短连接（挂在该前哨「通电点亮」最后一阶的 narrative 尾，或作 reach 翻转时的一行系统/日志）：

- **残骸前哨建成**：「井架亮起来的那一刻，海图上残骸那点从灰转成亮。日志第二条坐标——你现在下得去了。」`[占位·待过稿]`
- **中层浮标建成**：「浮标的灯一亮，没有底的那片水里，第一次有了一个你够得着的标记。第三条坐标，亮了。」`[占位·待过稿]`
- **热液井台建成**：「井台通了电，海沟底那点光在海图上稳住了。最后一条坐标——你一直知道它在哪，现在你建得起下去的那条路。」`[占位·待过稿]`

（语气克制·不抒情过头·留给你按手感收。reef 是免费入口·无此连接句。）

### B-3　样板 · 「升级 host 前哨 → 更深 beat」（**纯演示机制长相 · 非 ch1 内容 · 非 canon · 别采用**）

只为让你看清**那种**内容的形状（一个深 beat ＝ 带 `story` 的更深 tier·reach 由 host 前哨**升级**而非建成解锁）。文本是占位假货：

```
id: sample.beat_deeper          ← 演示用·非真事件
host: <某前哨>·tier N+1（需把该前哨 probe 升到 N+1 才 reach）
title: 〈占位标题〉
body: 「升过一级，灯才够探到这一截。声呐说下面有路，灯说下面什么都没有。
       这一页日志上没有——它标到上一层就停了。」[占位·演示·非 canon]
options:
  - 〈往更深穿〉   → [占位] 检定/后果由你定
  - 〈贴回波摸边〉 → [占位]
```

→ 看点只有一个：**深 beat 的解锁＝host 前哨升级**（`depthTierRevealState`：tier ≤ 已建级才 lit）。内容你来填、且按 canon「ch1/ch2 对失联真相零泄漏」收。

---

## C. 待你拍后我做什么（等并发 session 落定）

1. 按 A 的形状把 4 beat 从 `chart_pois` 锚点迁到柱 `story` tier（含 A-2 的 story-tier 建模）。
2. 触点②（`outpostUnlocked` 翻「上一步」门）+ 触点③（结局读链）+ 同步**重写两个 Mac-only playthrough**（`playthrough-story §5` 的「恰 4 story POI」断言、`playthrough-outpost §0` 的 `requiresAnchor` 断言）。
3. B-2 连接句落地（标记保留到你过稿）。
4. Mac/nightly 全量 `npm run regress` 绿 → 收尾提交。
