# Nightly verify + publish — 2026-06-06

**Result: ✅ GREEN — published 3 backlogged commits to `origin/main`.**

## Tree state on entry
- Working tree effectively **CLEAN**: only the always-ignored `CLAUDE.md` (untracked) and pre-existing ` M .deploy-token.example` were present — neither is ours to commit.
- Local `main` was **ahead of `origin/main` by 3 commits** that a prior session committed but never pushed (the Mac-side push is blocked by proxy/credential issues; unattended publishing via `.deploy-token` is how this content ships).
- `origin/main` was at `cd21eab` (the 2026-06-05 nightly report). `cd21eab` is a strict ancestor of local `HEAD` `4049a7d`, so publishing was a clean fast-forward (no rebase, no merge, no force).

## Unpushed commits published this run
| sha | summary |
|-----|---------|
| `e50e53b` | #78 声呐与房间 S2 不可信扫描 — `spoofsSonar` 假信标(节点版 mimic) / `evadesSonar` 无回波 / 低 san 伪接触 + 读数乱码 + 深 band `sonarDeception` 非单调失真 |
| `ae45291` | #79 深水区 Phase 2b 续 — 材料中转·寄存 + 寄存丢失 + 深渊前哨 `abyssal_deep` + 平衡 pass |
| `4049a7d` | #80 声呐与房间 续 — S3 威胁定位廉价版 + §6.5 宏观灯塔扫描 + 声呐范围升级轴 |

## Build / typecheck / regression suite
`npm run regress` → **GREEN, 25/25 通过, 墙钟 6.0s** (node v22.22.0, node_modules already present so no `npm ci`).

Per-task (all ✓): `verify-tutorial`, `build` (production vite build to fresh temp dir), `typecheck` (`tsc --noEmit`), `smoke-chart-ui`, and playthroughs `bands`, `bluecaves`, `chart`, `combat`, `combat-scenarios`, `corpse`, `decay`, `economy`, `lighthouse`, `lighthouse-scenarios`, `mapgen-scenarios`, `mimic`, `outpost`, `save`, `scenarios`, `sensors`, `sonar`, `stealth`, `upgrades`, `wreckyard`, `playthrough`.

## Exploratory / hands-on testing
Throwaway probe at `/tmp/probe_blue.ts` (never in repo), importing the engine modules directly and driving them — targeted at the surface area of the 3 unpushed commits. **336,764 invariant assertions passed, 0 failures.**

- **A — per-node sonar/clarity fuzz on real generated deep maps** (5 deep bands × 40 seeds = 200 maps, 2,790 nodes, across a sanity × turn matrix). For every node, `nodeSonarView` / `sonarReturn` / `clarityForNode` / `signature` never threw and obeyed their contracts: `evadesSonar` → `noEcho` + `deceptive`; `spoofsSonar` → disguised as `ascent_point` + `deceptive`; plain nodes → truthful kind, never deceptive; `displayKind` always a valid `NodeKind`; clarity always a valid tier (`full`/`sonar`/`none`); `signature` always finite ≥ 0. Exercised 3,200 spoof-views, 4,860 evade-views, 9,835 garbled (low-san) views.
- **A2 — S2/S3 helpers.** `sonarPhantoms`: empty at high sanity, only ever anchored to genuinely-scanned nodes, count ≤ memory size, finite coords. `threatContact`: `null` below the alert warn line, otherwise well-formed (`angle` finite, `proximity` ∈ [0,1], `range` ∈ {far,mid,near}, `imminent`/`garbled` boolean) with no NaN path. `sonarScanRange` always an integer in [2,4] across upgrade bonuses 0…99 (double cap holds).
- **A3 — mapgen determinism + deception is non-structural.** Same (seed, range, deception) → byte-identical maps. Crucially, generating with `sonarDeception` vs `0` (same seed) produced **identical** node kinds/depths/layers/adjacency — deception only annotates `spoofsSonar`/`evadesSonar`, never restructures (91 deceptive nodes across 30 hadal seeds; **dec=0 → exactly 0 deception = backward compatibility confirmed**). Deceived nodes were never the start node and never `ascent_point`/`corpse` (landmark exemption holds).
- **B — outpost depot/decay economy (#79).** Across 4 outposts × elapsed sweeps: `outpostDecayLevel`/`depotDecayLevel` are integers in [0,4] and monotonic non-decreasing; `effectiveStored` never produced negative/zero-qty entries, never exceeded raw, was monotonic non-increasing, and exactly equalled `max(0, raw − decayLevel×LOSS)`; **decay-0 was byte-identical to raw** (the regression guarantee); current/water-flow outpost rusts ≥ static at equal elapsed.
- **C — save round-trip.** Persisted `profile.outpostState` (incl. `stored`/`storedRun`/`maintainedRun`) survived `serialize → deserialize` exactly, and serialize was idempotent. A state carrying a run with the new `sonarDeception`/`scanMemory`/`sensorTuning.sonarScanRange` fields round-tripped without throwing. No SAVE_VERSION regression.
- **D — chartConditions (#80).** Pure and deterministic for a given profile; returns a serializable object; never threw.

**Findings:** No game defects. The probe's only red was a probe-side wrong field name (it asserted `ThreatContact.bearing`; the real field is `angle`) — corrected in the throwaway script, after which it was fully green. This was an observation about my own test, not the code under test.

## Fixes made
None to the game/repo — nothing was red. (The one correction was inside the `/tmp` throwaway probe and is not part of the repository.)

## Commit + push
- No new project changes to commit (tree was clean apart from ignored files), so no feature commit was created this run.
- Pushed the 3 existing local commits to `origin/main` via `.deploy-token` (fast-forward, no `--force`):
  `cd21eab..4049a7d  HEAD -> main`, push exit 0.
- **Verified:** `ls-remote refs/heads/main` = `4049a7d4fe709378bea414bd9c0c1357005558c6` = local `HEAD`. The GitHub Actions deploy runs automatically on push.
- Token never echoed; `CLAUDE.md` / `.deploy-token` / `.deploy-token.example` never staged.

## State on exit
`main` in sync with `origin/main` at `4049a7d`. Working tree clean except the expected always-ignored `CLAUDE.md` (untracked) and pre-existing ` M .deploy-token.example`. No leftover `.git/*.lock` or `tmp_obj_*`.
