# Nightly verify + publish — 2026-06-09

**Result: ✅ GREEN — published 2 backlogged commits (声呐渲染重做 #104·有机洞穴) to `origin/main`.**

## Tree state on entry
- Working tree effectively **CLEAN**: only the always-ignored `CLAUDE.md` and a pre-existing untracked `docs/archive/nightly/REPORT-2026-06-07.md` (a prior quiet-night report, left local per policy) — neither is ours to commit.
- Local `main` was **ahead of `origin/main` by 2 commits** that a prior author session committed (author-accepted v3 of the organic-cave sonar render) but never pushed — exactly the verify-then-publish handoff this task exists for.
- `origin/main` was at `72bff3d` (the weekend-content publish). It is a strict ancestor of local `HEAD` `9f4f15d`, so the rebase in step 2 was a no-op and publishing is a clean fast-forward (no merge, no force).

## Unpushed commits published this run
| sha | summary |
|-----|---------|
| `82b4368` | feat(声呐渲染重做·#98·④ 有机洞穴): `SonarScanPanel` SDF reworked from "graph-skeleton straight capsules" into a real side-profile cave — domain-warped smin union of routed tunnels + room blobs (author acceptance v3). +217/−46 in `src/ui/SonarScanPanel.tsx`. |
| `9f4f15d` | docs(#104): CHANGELOG #104 + QUIRKS #102 + STATUS top blockquote + SPEC §6④/§9 decision log for the above. |

(Plus this nightly report, committed alongside since a deploy is happening anyway.)

## Build / typecheck / regression suite
- `npx tsc -b` → **exit 0**.
- `npx vite build` (to `/tmp/nightly-dist`) → **exit 0**, 109 modules, built in 1.34s.
- All **22** `scripts/playthrough*.ts` → **GREEN** (22/22). node v22.22.0, node_modules already present (no `npm ci`).

> One false-positive note: the FAIL filter (`✗` substring) flagged `playthrough-mimic.ts`, but it exits 0 and ends "回归通过". The `✗` is a decorative boolean-false marker in a display line (`isPoiLit ✓ / isPoiExplainedByLighthouse ✗（宏观 tell）✓`), not an assertion failure. Verified by inspecting the line in context. Real pass count is 22/22.

## Exploratory / hands-on testing
Three throwaway probes under `/tmp` only (never in repo), importing the engine/UI modules directly via the `@/*` alias (`tsx --tsconfig ./tsconfig.json`). Targeted at tonight's surface area — the reworked cave-render code in `SonarScanPanel.tsx`. **0 failures across ~760k checks.**

- **Probe 1 — cave-render pure-function fuzz (`/tmp/probe1_cave.ts`).** The five functions tonight's commit reworked (`caveWarp`, `caveSdf`, `buildCaveGeometry`, `poiOffset`, `voidTrack`) are the canvas's geometry source; a `NaN`/`Infinity` here would silently corrupt the baked cave image (NaN clamps to 0 in `ImageData`) and neither typecheck nor the regression suite would catch it. **260,000 direct checks** over random + extreme inputs (incl. ±1e9, ±0, sub-epsilon) → all finite; empty-geometry `caveSdf([],[])` → `+Inf` not `NaN`. **120 real generated maps** (`zone.blue_caves`, depth ranges 30m→270m incl. deepest bands) → `deriveMapLayout` → `buildCaveGeometry` with randomized progressive-reveal memory: **5,669 geometry elements** all finite with radius > 0; per-node marker `voidTrack(poiOffset)` finite. **495,000 canvas-bake samples** — the exact half-res `caveSdf` grid loop the panel runs in `useEffect` — **zero NaN**. Edge cases (nothing/single/all revealed, dangling memory id not in layout) → no throw.
- **Probe 2 — broad invariant fuzz (`/tmp/probe2_invariants.ts`).** 220 randomized full dives (zones east_reef / blue_caves / old_lighthouse_reef × depth ranges to 270m), 2,271 steps, randomized ping/move/explore/event choices. Every step asserted: no throw; `stats.{oxygen,nitrogen,sanity,stamina}` finite & sane; `currentDepth` finite ≥ 0; `power` ∈ [0, POWER_MAX]; `alert` ∈ [0, ALERT_MAX]; `turn` finite, non-negative, **monotonic non-decreasing**; no soft-lock (**0/220 hit the 80-step cap** — all reached a natural terminal: 127 ascent, 92 funeral, 1 combat). **235 mid-dive save round-trips** (`serialize→deserialize`) preserved `currentNodeId` and `SAVE_VERSION === 4`.
- **Probe 3 — `SonarScanPanel` SSR render (`/tmp/probe3_ssr.tsx`).** Rendered the actual React component via `react-dom/server` across **60 real run states** (`NODE_PATH` pointed at project `node_modules` so bare `react` specifiers resolve from `/tmp`). The canvas `useEffect` is SSR-skipped, but the full SVG-overlay render path executes (adjacency markers, `voidTrack`/`poiOffset` placement, stalker / threat / phantom branches, empty-scan early return). **60/60 rendered** (5 empty-scan, 26 with markers), no throw, **no `NaN`/`undefined` leaked into the SVG**.

**Findings:** No game defects. Nothing red anywhere. The `SonarScanPanel` rework is numerically robust and renders cleanly across the fuzzed state space.

## Fixes made
None — nothing was red. (No code touched beyond writing this report.)

## Commit + push
- No uncommitted project changes to stage; the real changes were the 2 pre-existing local commits. Wrote + committed this report (`nightly:` message), then pushed `72bff3d..HEAD` to `origin/main` via `.deploy-token` (fast-forward, no `--force`). GitHub Actions `deploy.yml` redeploys automatically to https://shenhuang.github.io/blue/.
