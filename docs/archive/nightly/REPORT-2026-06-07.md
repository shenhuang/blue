# Nightly Verify + Publish — 2026-06-07

**Result: GREEN — published.** Weekend content + an in-game changelog feature shipped to `main` and are live-deploying via GitHub Actions.

| | |
|---|---|
| Tree state on entry | **DIRTY** — weekend content uncommitted (10 modified, 80 new) |
| Synced base (origin/main before) | `e6ce537` (feat dev-tools #97) |
| Local was | 8 commits ahead, 0 behind (clean fast-forward; merge-base = origin HEAD) |
| Build + typecheck | ✓ green (vite build 2.6s, tsc --noEmit 5.6s) |
| Regression suite | ✓ **27/27** · 墙钟 7.1s |
| Exploratory | ✓ no defects (3366 scenario runs + integrity/save/changelog probes) |
| Fixes made | none (green on arrival) |
| Commit | `72bff3d` |
| Push | ✓ `e6ce537..72bff3d HEAD -> main` (ls-remote confirms origin = `72bff3d`) |

## What shipped

A large weekend content drop plus a player-facing changelog feature:

- **~2.3k lines of new events** across four zones: `reef` (+899), `wreck_graveyard` (+513), `abyssal` (+465), `blue_caves` (+399).
- **76 new scenario baselines** under `scenarios/` (honoring the "加内容必配 baseline" convention).
- **New in-game ChangelogModal** (player-facing update log): `src/data/changelog.ts`, `src/types/changelog.ts`, `src/ui/ChangelogModal.tsx`, plus `src/App.tsx` + `src/styles.css` wiring and `src/types/index.ts` re-export. This is a UI feature beyond pure content; it is UI-only (no engine dependency — `check-boundaries` green), passed typecheck/build/smoke, and is thematically tied to the content drop, so it was shipped per the "publish green uncommitted changes" policy.
- **Docs**: `docs/QUIRKS.md`, `docs/STATUS.md`, `docs/archive/CHANGELOG.md` updated; the prior `docs/archive/nightly/REPORT-2026-06-06-publish.md` was also committed (it was left untracked by the previous run).

Staged via explicit paths only (`git add src scenarios docs`); `CLAUDE.md` and `.deploy-token` were verified **not** staged.

## Build / typecheck / regression — per task

`npm run regress` → **全绿 ✓ · 27/27 通过 · 墙钟 7.1s** (exit 0).

| task | result | task | result |
|---|---|---|---|
| check-boundaries | ✓ | playthrough-lighthouse | ✓ |
| verify-tutorial | ✓ | playthrough-lighthouse-scenarios | ✓ |
| build | ✓ | playthrough-mimic | ✓ |
| typecheck | ✓ | playthrough-mapgen-scenarios | ✓ |
| smoke-chart-ui | ✓ | playthrough-outpost | ✓ |
| playthrough | ✓ | playthrough-save | ✓ |
| playthrough-bands | ✓ | playthrough-sensors | ✓ |
| playthrough-bluecaves | ✓ | playthrough-scenarios | ✓ |
| playthrough-chart | ✓ | playthrough-sonar | ✓ |
| playthrough-combat | ✓ | playthrough-stalker | ✓ |
| playthrough-combat-scenarios | ✓ | playthrough-stealth | ✓ |
| playthrough-corpse | ✓ | playthrough-upgrades | ✓ |
| playthrough-decay | ✓ | playthrough-wreckyard | ✓ |
| playthrough-economy | ✓ | | |

## Exploratory testing

Throwaway probes in `/tmp` (never in repo), importing the real engine/data modules. Targets chosen from the uncommitted weekend content (the new events/scenarios across reef/abyssal/blue_caves/wreck) and the new ChangelogModal, plus gaps the fixed scripts don't cover (broad randomized fuzz, static reference integrity, save corruption paths).

**Probe A — runtime fuzz, every event × every option × 6 seeds** (`seeds 1,2,3,7,42,1337`). 207 events → **3366 scenario runs, 3414 steps executed**. Asserted each step: no throw, no NaN/Inf stat deltas, no dangling `continueEvent` target; per run: `survived` is boolean, `finalPhase` valid, summary deltas finite. Outcomes observed: 42 combat triggers, 36 forceAscend, 0 unexpected deaths. 48 "option not visible" results were correctly classified as **by-design flag gating** (multi-stage `lighthouse.outpost_*` build steps requiring prior-stage flags) — not defects.

**Probe B — static reference integrity over ALL branches** (incl. check onSuccess/onFailure, which RNG may not hit). 726 outcome branches validated: no dangling `triggerEventId`, loot `qty`/`chance` well-formed and in `[0,1]`, all deltas finite, no unknown `endDive`, every event has body + ≥1 option + valid `depthRange`/`weight`, no duplicate option ids.

**Combat references** — the 7 combat ids referenced by events (`combat.blind_eel_solo`, `combat.cave_octopus_solo`, `combat.drowned_lantern_solo`, `combat.reef_barracuda_solo`, `combat.reef_grouper_solo`, `combat.tutorial_shark`, `combat.wreck_spider_crabs_pair`) all resolve via `getEncounter` — no dangling combat refs (a dangling one would soft-lock at runtime).

**Probe C — save round-trip + corruption** — serialize→deserialize is lossless (re-serialize byte-identical; `Set` fields `activeFlags`/`loreEntries` restored; gold/lore preserved). Garbage JSON → `null`; wrong `version` → `null` (confirms the #99 "no migration, reject incompatible" behavior).

**Probe D — changelog data** — 6 entries, all valid ISO `YYYY-MM-DD` dates, newest-first ordering (the modal relies on `slice(0,N)`), every change line non-empty.

**Zone-tag coverage** (confirms the new content is present and exercised): cave 49, wreck 47, reef 42, abyssal 22, shallow 16, hadal 11, subhadal 10, nameless 9, midnight 6, tutorial 6, twilight 5.

**Findings:** No clear reproducible defects; no actionable notes. Nothing to fix.

## Fixes made

None — everything was green on arrival.

## Infra note (for future sessions)

A stale `.git/index.lock` (0 bytes, dated 08:34, left by an earlier process — no git was actually running) blocked the first `git add`. Cleared per the sandbox procedure (`mv` into `.git/.sandbox-junk/`, since the mount cannot `unlink`), then staging succeeded. Post-commit/push, swept ~101 `tmp_obj_*` plus `index.lock`/`REBASE_HEAD.lock`/`packed-refs.lock`/`HEAD.lock` into `.git/.sandbox-junk/`. Final tree CLEAN (only `CLAUDE.md` + `.deploy-token` untracked, as always). The rebase onto `origin/main` was a no-op ("Current branch main is up to date") since origin was an ancestor.

## Publish confirmation

`git ls-remote …/blue.git refs/heads/main` → `72bff3d9780a1b60d8856acf67202fc3fa858dcc` == local HEAD. Actions deploy to https://shenhuang.github.io/blue/ runs automatically on push (not opened from here).

_This report is left uncommitted this run (written post-push, per the established pattern — the next run commits it)._
