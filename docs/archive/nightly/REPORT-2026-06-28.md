# Nightly verify + publish — 2026-06-28

**Result: ✅ GREEN — published 2 unpushed interactive-session commits to `origin/main` behind a fully green gate (regress 74/74 exit 0 + env-recovered prod build exit 0): `f652915` (#235 ch1 economy material-theming — build/upgrade bills bio→mineral/salvage + material `role` single-source + cross-region material gates + new `check-build-material-theming`) and `4532331` (#236 chart fix — 漆号珊瑚丛 dive-POI mapX 0.04→0.11 off the coastline + new `playthrough-chart §0c` offshore invariant), plus one player-changelog draft entry + this report. No weekend-branch merge needed (0 pending). Exploratory fuzzing: 0 defects over 336 generated dive runs + 48 save round-trips.**

## Tree state on entry
- Main tree on `main` = `4532331`, **2 commits ahead** of `origin/main` (`fc8411f`), 0 behind — `origin/main` is an ancestor (verified `merge-base --is-ancestor` → clean fast-forward).
- **Uncommitted icon WIP** in the working tree (modified `scripts/regress.mjs`, `src/engine/poiEvents.ts`, `src/ui/{CorpseView,EquipmentDoll,ItemCell,SeaChartView}.tsx`; new `scripts/check-material-icons.mjs`, `src/engine/poiMaterials.ts`, `src/ui/{itemIcons,materialIcons}.tsx`) — author's in-flight work; per the #236 STATUS note「不裹同树图标 WIP」, left untouched and **not** shipped.
- A concurrent session (`elegant-gracious-mendel`) shares this repo (its stale `/tmp/regress.log` surfaced) — re-verified branch/HEAD immediately before commit and re-fetched before push to avoid a race.
- psm feature worktrees present (`.worktrees/{cave-multimouth,cave-temp-wire,chart-deps,poi-roaming,weekend}`) + `/tmp` verify worktrees — not touched (sandbox prune would delete them, quirk #138).

## Weekend merge — none pending
`main..auto/weekend` = **0** (`auto/weekend` is 166 commits behind main). `.worktrees/weekend` left as-is.

## Verification strategy — isolated clean worktree
The main tree carries uncommitted icon WIP, *including a modified `scripts/regress.mjs` that adds an unfinished `check-material-icons` gate*. Running the gate on the dirty tree would test WIP, not the code being shipped. So the gate ran on the **exact committed code** in a detached worktree at `4532331` (`/tmp/nv-0628`, no WIP). The player-changelog edit was applied + typechecked there first (byte-identical to the main-tree edit) before committing to main.

### Regression gate — GREEN (74/74)
`npm run regress` (in the clean worktree) exit 0, wall **7.2s**, **74/74 ✓** — typecheck (6.3s); ~30 static gates incl. `check-boundaries`, `check-build-material-theming` (#235), `check-mainline-reachable`, `check-economy-reachability`, `check-data-schema`, `check-terminology`, `check-lunar-reach`, `check-append-only-docs`, `check-status-fresh`, `check-doc-links`; `verify-tutorial`; 9 SSR smokes; all 23 playthroughs incl. `playthrough-chart` (the #236 §0c offshore gate) and the flake-prone `playthrough` (passed first try). Re-ran `--only typecheck` after adding the player-changelog draft: still green (3.6s).

### Prod build — GREEN (env-recovered)
`regress` auto-skips the build in-sandbox (its guard looks for a `rollup-linux-arm64-gnu` that vite 8 no longer uses). Ran it manually since the Pages deploy runs `vite build` on push. Mounted `node_modules` holds **darwin-arm64** natives on a **linux-arm64** host (environment mismatch, not a code RED). Installed under `/tmp` and wired via `ESBUILD_BINARY_PATH` + `NODE_PATH` (author `node_modules` untouched): `@esbuild/linux-arm64@0.28.1`, `@rolldown/binding-linux-arm64-gnu@1.1.3`, `lightningcss-linux-arm64-gnu@1.32.0`. `vite build --outDir /tmp/nightly-dist` → **exit 0**, `✓ built in 179ms`, hashed chunks emitted (only a chunk-size advisory warning). Note: vite 8 bundles via **rolldown** (not rollup) + **lightningcss** — both Rust natives required; the docs' "rollup-linux fix" recovery name is stale (same finding as the 2026-06-27 report).

## Exploratory testing
Throwaway probe in `/tmp` only (`zzz_probe.ts`, in the discarded worktree; never staged), engine imported the way `scripts/playthrough*.ts` do, seeded LCG via `tools/playtest-sim`.
- **Random-seed dive fuzz** — 336 runs across 12 `(zone, band)` cells × 2 risk margins × 14 seeds via `playDive`. **0 engine throws, 0 invariant violations** (`maxDepth`/`minO2`/`minSanity`/`turns`/`goldEarned` all finite & in-bounds). 323 survived / 13 deaths (plausible in danger bands) / 1 step-cap hit (not a systemic soft-lock).
- **Save round-trips** — 48 `createNewRun` states across all 6 zones → `serialize → deserialize → re-serialize` byte-stable, 0 failures.
- **Economy (#235)** — `computeMaterialStats()` runs clean (34 demand totals, 0 NaN).
- **#236** offshore invariant covered by the passing `playthrough-chart §0c` gate.
- No defects. Note only: 1/336 runs hit the sim's 600-step cap; not reproducible as a defect.

## Shipped
Fast-forward push to `origin/main`: `f652915` (#235) + `4532331` (#236) + one `nightly:` commit (player-changelog draft entry for 2026-06-28 + this report). The GitHub Pages Actions deploy runs automatically on push.

## Not shipped
- Uncommitted icon WIP — left in the working tree for the author.
