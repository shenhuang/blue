# Nightly verify + publish — 2026-06-25 (second run)

**Result: ✅ GREEN — published 15 unpushed interactive-session commits (temperature wiring #199, cave roadmap T3a/T3b #196/#201, captain_revisit fix #200, pre-dive pack-step skip #202, item-pickup modal) plus one player-changelog edit to `origin/main` behind a green gate (53/53 regress + env-recovered prod build). No weekend branch merge needed (0 pending). Supersedes nothing — earlier run `759754d` (REPORT-2026-06-25.md) shipped #179–#185; this run ships the work that landed after it.**

## Tree state on entry
- Main tree on **`main`** = `85766c1`, **15 commits ahead** of `origin/main`, **0 behind** (origin is an ancestor → clean fast-forward, verified via `merge-base --is-ancestor`).
- Clean tree (no uncommitted tracked changes; CLAUDE.md / .deploy-token always-ignored).
- psm feature worktrees present (.worktrees/{cave-bmap,cave-multimouth,cave-temp-wire,chart-deps,poi-roaming,weekend}) — separate branches, not on main, not touched.

## Weekend merge — none pending
`main..auto/weekend` = **0** (auto/weekend is 121 commits behind main; its content already landed via earlier merges). `.worktrees/weekend` left as-is (sandbox prune would delete it — quirk #138).

## Regression gate — GREEN (53/53)
`npm run regress` exit 0, wall 7.0s, 53/53 tasks ✓ (typecheck 4.3s, all checks/smokes/playthroughs incl. playthrough-tutorial-e2e / playthrough-temperature / check-cave-bindings / check-story-open-events, scenario baselines). Build auto-skipped in sandbox (#147).
ENV recovery (node_modules held darwin-arm64 native binaries on a linux-arm64 host): installed `@esbuild/linux-arm64@0.28.1`, `@rolldown/binding-linux-arm64-gnu@1.1.3`, `lightningcss-linux-arm64-gnu@1.32.0` under /tmp; `ESBUILD_BINARY_PATH` + `NODE_PATH`. Prod build verified separately: `vite build` exit 0, all chunks emitted, built in 216ms (chunk-size warnings only).

## Exploratory testing
- **Engine invariant probe** (`/tmp/probe.ts` + corrected `/tmp/probe2.ts`): temperature model sweep (intensity×insulation grid + 200-turn stress accumulation — no NaN), chart generation fuzz over 40 seeds with extra flags unlocked (no throw, finite POI coords), save round-trip over 12 rich states at `SAVE_VERSION 11` (bankedGold/flags preserved, serialize idempotent, corrupt/version-skew → null no-throw), depth-band range sanity, cave registry sanity. **0 real failures** (initial 43 "failures" were probe field-name mistakes — bands use `depthRange:[min,max]`, caves use `caveId`/`entranceDepths`; corrected probe 67/67).
- **T3b multi-mouth cave invariant** (focus of #201): all 4 traversal-flagged caves declare ≥2 entrance depths — serpentine_deep [42,72,56], mirror_maze [33,50,62], flooded_gallery [60,76], blue_throat [36,52]. Holds (also enforced by `check-cave-bindings` in regress).
- No defects found. Probes kept in /tmp only.

## Fixes
None needed — gate green on entry.

## Shipped
- Player changelog: edited the existing `{ date: '2026-06-25' }` entry in `src/data/changelog.ts` to add two newly-shipped player-facing items not covered by the earlier draft — item-pickup popup, and the skippable pre-dive pack step (#202). Draft for author to polish; no spoilers/dev jargon.
- Committed `src/data/changelog.ts` + this report as a `nightly:` commit on top of the 15 interactive-session commits, fast-forwarded `origin/main`.
