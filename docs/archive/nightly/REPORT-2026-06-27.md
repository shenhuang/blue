# Nightly verify + publish — 2026-06-27

**Result: ✅ GREEN — published 1 unpushed interactive-session commit (`73cb6fa` · east-reef qualification-zone revisit full-fix #220: `forbiddenEventIds` single-source-of-truth gate #189 + free-ascend redundant-ascent-point removal #3 + short-dive-to-wreck layerCount 5→3) plus one player-changelog draft entry to `origin/main` behind a fully green gate (64/64 regress incl. all behavior tests + env-recovered prod build, exit 0). No weekend branch merge needed (0 pending). Exploratory probing of the #220 change surface: 0 defects over 2,610 generated maps.**

## Tree state on entry
- Main tree on **`main`** = `73cb6fa`, **1 commit ahead** of `origin/main` (`8fe1269`), **0 behind** — `origin/main` is an ancestor (verified `merge-base --is-ancestor` → clean fast-forward). The `[ahead 1]` was **real**, not a phantom ref (confirmed via `ls-remote`: remote head = `8fe1269`).
- Clean tree (no uncommitted tracked changes; CLAUDE.md / .deploy-token always-ignored).
- psm feature worktrees present (`.worktrees/{cave-multimouth,cave-temp-wire,chart-deps,poi-roaming,weekend}`) — separate branches, not on main, not touched (sandbox prune would delete them — quirk #138).

## Weekend merge — none pending
`main..auto/weekend` = **0** (already merged flat; `auto/weekend` is 145 commits behind main). `.worktrees/weekend` left as-is.

## Regression gate — GREEN (64/64) + prod build (exit 0)
`npm run regress` exit 0, wall **8.3s**, **64/64 ✓** — typecheck (4.8s), all ~20 static gates (check-boundaries / check-data-schema / check-lunar-reach / check-story-open-events / check-affected-edges / …), verify-tutorial, all 7 SSR smokes, all 23 playthroughs **including the three scripts `73cb6fa` modified** (`playthrough-story`, `playthrough-tutorial-e2e`, `playthrough-wreckyard`). Re-ran the full gate after adding the player-changelog draft: still 64/64 (7.8s).

ENV recovery (mounted node_modules holds **darwin-arm64** native binaries on a **linux-arm64** host — environment mismatch, not a code RED): installed `@esbuild/linux-arm64@0.28.1` (reused existing `/tmp/esbuild-linux`), `@rolldown/binding-linux-arm64-gnu@1.1.3`, `lightningcss-linux-arm64-gnu@1.32.0` under `/tmp`; wired via `ESBUILD_BINARY_PATH` + `NODE_PATH` (author node_modules untouched). With these the **prod `vite build` succeeds — exit 0**, real hashed chunks emitted to a temp dir (warnings only: deprecation notices + chunk-size advisory). Note: vite 8 now bundles via **rolldown** (not rollup) — the docs' "rollup-linux fix" recovery name is stale; the rolldown + lightningcss linux bindings are what's needed now.

## Exploratory testing
Throwaway probes in `/tmp` only (`probe-220.ts`), targeting exactly what #220 changed; engine modules imported the way `scripts/playthrough*.ts` do, driven with seeded LCG.
- **A — free-ascend mid-layer ascent_point removal (#3):** generated **1,250 maps** across the 5 free-ascend layered zones (`east_reef, old_lighthouse_reef, open_midwater, wreck_graveyard, whalefall`), 250 seeds each. **Mid-layer `ascent_point` count = 0** (the fix holds); every map has a reachable deepest-layer ascent_point (no soft-lock); all depths finite, no dangling edges.
- **B — east_reef revisit short-dive (B):** 250 seeds, post-tutorial state. Deepest layer ≤ 2 and deepest depth == **30m** every time → revisit reaches the wreck in ≤3 layers as intended.
- **C — `forbiddenEventIds` revisit gate mutual exclusivity (#189):** replicated the `startDiveFromPoi` storyOpenEvents predicate against real event data. `captain_revisit` (forbids `captain_quarters`) and `captain_revisit_empty` (requires `captain_quarters`) are **provably mutually exclusive** on `event_seen:tutorial.captain_quarters` — exactly one eligible per progress state once the hook flag is set, never both (ambiguous) and never neither (soft-lock).
- **D — broad crash/finite/connectivity sweep:** 40 seeds × all 35 zones = **1,360 maps** — no throws, all depths/layers finite, no dangling edges, start node connected.
- **One triaged non-defect:** an initial assertion flagged 21 "deepest ascent_point unreachable" cases; inspection of `old_lighthouse_reef` (which has `nodesPerLayer [2,3]`) showed the deepest layer holds *two* ascent_points — one on the descended path, one an alternate branch — so the correct invariant "≥1 deepest exit reachable" holds. This is pre-existing map topology (#220 never touched edge generation), not a regression. Corrected assertion → **0 failures** total.

## Fixes
None needed — gate green on entry and after the changelog edit.

## Shipped
- **`73cb6fa`** (interactive-session commit, #220) — the real game change being published.
- **Player changelog:** prepended one `{ date: '2026-06-26' }` draft entry to `src/data/changelog.ts` (felt navigation polish: shorter/more-direct revisit dives, no redundant ascent points in free-ascend shallows, revisit beats no longer silently breaking). Player wording, no spoilers/dev jargon — draft for the author to polish.
- This report + the changelog edit committed as a `nightly:` commit on top of `73cb6fa`, then fast-forwarded onto `origin/main` via token push.
