# Nightly Verify + Publish — 2026-06-05 (Fri)

**Result: VERIFIED GREEN · PUBLISH SKIPPED (local not ahead of origin).** No code changed.

## Tree state — DIRTY (pre-existing, not made this run)
- Modified (tracked): `.deploy-token.example` — a **trailing-newline-only** diff (last line lost its final `\n`). Not weekend content work, not made by this run. Left **untouched** per policy.
- Untracked (ignored by policy): `CLAUDE.md`, `docs/nightly/` (report dir).
- No other working-tree changes. Because a tracked change I did not make is present, the tree is treated as **DIRTY**: no rebase, no stash/discard, no fix-commit; verification ran against the current tree.

## Sync
- `git fetch origin main` OK.
- Local HEAD == origin/main == `4ac45b5` ("部署修复：删冗余 static.yml … + .deploy-token 无人值守推送机制"). Branch already up to date — **no rebase needed** (and would have been skipped anyway given the dirty tree). Ahead of origin: **0 commits**.

## Build + typecheck — GREEN
- `npx tsc -b` → exit **0** (no type errors).
- `npx vite build` → **90 modules transformed, built in ~0.9s**, output present (index.html + assets/index-*.js 340 kB / *.css 15.7 kB).
  - Note: the first attempt with `--outDir /tmp/nightly-dist --emptyOutDir` failed with `EACCES: rmdir '/tmp/nightly-dist/assets'` — the sandbox could not delete a **stale dir from a prior run**, not a code error (transform had already succeeded). Re-ran to a fresh unique `/tmp/nightly-dist-<ts>` dir → clean success. No repo `./dist` was touched.

## Regression suite — 20/20 PASS
Every `scripts/playthrough*.ts` exited 0 with its "✓ … 通过/完成" line:

| script | result | script | result |
|---|---|---|---|
| playthrough.ts | ✓ exit 0 | playthrough-lighthouse-scenarios.ts | ✓ exit 0 |
| playthrough-bands.ts | ✓ exit 0 | playthrough-lighthouse.ts | ✓ exit 0 |
| playthrough-bluecaves.ts | ✓ exit 0 | playthrough-mapgen-scenarios.ts | ✓ exit 0 (seed scan 60/60) |
| playthrough-chart.ts | ✓ exit 0 | playthrough-mimic.ts | ✓ exit 0 (see note) |
| playthrough-combat-scenarios.ts | ✓ exit 0 (9/9) | playthrough-outpost.ts | ✓ exit 0 |
| playthrough-combat.ts | ✓ exit 0 | playthrough-save.ts | ✓ exit 0 |
| playthrough-corpse.ts | ✓ exit 0 | playthrough-scenarios.ts | ✓ exit 0 (110/110) |
| playthrough-decay.ts | ✓ exit 0 | playthrough-sensors.ts | ✓ exit 0 |
| playthrough-economy.ts | ✓ exit 0 | playthrough-stealth.ts | ✓ exit 0 |
| playthrough-upgrades.ts | ✓ exit 0 | playthrough-wreckyard.ts | ✓ exit 0 |

**Marker note (not a failure):** the `✗`/`断言失败` grep tripped once, in `playthrough-mimic.ts`, on the descriptive line `isPoiLit ✓ / isPoiExplainedByLighthouse ✗（宏观 tell）✓`. The `✗` depicts the mimic's macro *tell* (a lit chart POI that no lighthouse explains); the trailing `✓` and exit 0 confirm the assertion passed. No real assertion failure anywhere in the suite.

## Exploratory testing
Recent history is the deep-water arc (Phase 0–3: bands, sensors/clarity, alert/stealth, outposts, mimic capstone, hadal/abyssal/trench content) — all already committed; the working tree had no new weekend content this Friday. So I targeted **cross-cutting invariants** the fixed scripts assert only along scripted paths: randomized full dives, exhaustive event-outcome resolution, and save round-trips of the newer deep-water run fields. One throwaway script, `/tmp/probe_main.ts` (never in repo), driving the real engine/data modules. **13,375 checks, 0 failures.**

- **Probe A — full-dive invariant fuzz (125 runs = 5 bands × 25 seeds, 1,441 steps).** Seeded `Math.random` (LCG) per run for reproducibility; drove each dive via the real `startDive → enterNodeSelection → moveToNode / resolveOption → planAscent/executeAscent` loop, randomly toggling light/sonar and biasing toward the deepest node to push the low-sanity/deep paths. Asserted **every step**: stats finite (no NaN/undefined); `oxygen∈[0,max]`, `stamina∈[0,max]`, `sanity∈[0,100]`, `nitrogen∈[0,100]`, `power∈[0,powerMax]`, `alert∈[0,100]`, `depth≥0`; `clarity()∈{full,sonar,none}`; valid `next.kind`; and **no soft-lock** (any `nodeSelect` must carry ≥1 choice — `enterNodeSelection` auto-routes to ascent at 0). Outcome: 23 surfaced, 102 reached an ambush/combat (expected at high alert; combat is covered by its own scripts), 0 deaths, 0 invariant violations, 0 throws, all walks terminated.
- **Probe B — every event × option resolves cleanly (100 events, 1,245 event×option×depth resolutions).** All 9 event files (incl. recent trench/abyssal/hadal/mimic), each option resolved at depths {10,40,75,120,160}, skipping `visibleIf`-gated-off options. Asserted: registered in EVENT_DB; ≥1 option; `narrative` is `string[]`; valid `next.kind`; `continueEvent` targets an existing event; post-outcome stats finite. No throws, no broken links.
- **Probe C — save round-trip on deep-band states (30).** Built mid-dive states (drained power=7, alert=55, sanity=18, oxygen=31, sonar=ping, upgraded powerMax/lampRange) and `serialize → deserialize`. Asserted power/alert/powerMax/sanity/`sensors.sonarUnlocked`/`sensorTuning.lampDepthReach` survive, restored invariants hold, and re-serialization is byte-stable. Confirms the newer deep-water fields persist correctly (the "未发布不迁移" design relies on createNewRun defaults + deserialize fallback).

**Findings:** no clear defects, no notes worth flagging. Engine is robust across randomized seeds, the full event catalog, and deep-water save persistence.

## Fixes
None. No defect surfaced; tree was dirty (and not ahead), so no code was changed.

## Push result
**SKIPPED — local not ahead of origin** (HEAD == origin/main == `4ac45b5`, 0 commits ahead → nothing to publish). Independently, the working tree carries a change not made this run (`.deploy-token.example`), which also forbids publishing. `.deploy-token` is present and non-empty (unattended publishing IS configured) — it simply wasn't needed tonight. No commit, no push, no force. The live site is already at the latest committed state.

---
*Verification only; no repository or GitHub state modified. Exploratory script lived in `/tmp` and was not committed.*
