# Nightly verify + publish — 2026-06-20

**Result: ✅ GREEN — shipped 8 unpushed equipment commits (#144/#145) + integrated 3 weekend POIs into `main` behind a green gate (42/42), added a player-changelog draft, and published to `origin/main`.**

## Tree state on entry
- Main tree on **`main`**, working tree clean except one untracked WIP scaffold `docs/spec/lore_ledger.md` (left for the author — not staged) and the always-ignored `CLAUDE.md` / `.deploy-token`.
- `main` = `bd74ddc`, **8 commits ahead** of `origin/main` = `d0a938a` — all real, player-visible changes from the #144/#145 equipment sessions (sandbox interactive sessions can't push; publishing them is the nightly's job). `[ahead 8]` is real, not a quirk-#103 phantom (verified via fetch + `ls-remote`).
- Weekend worktree `.worktrees/weekend` on `auto/weekend` = `3af3a80` (1 commit ahead of the branch point), marked "prunable" — the known relative-pointer mis-detection; **NOT pruned** (pruning would delete the worktree, per the method-B rollout note).

## Weekend merge — integrated at file level (sandbox rebase is unsafe)
Pending `main..auto/weekend` = **1 commit** (`3af3a80`): 3 roaming POIs in `chart_pois.json` (东礁海草床 / 海沟死烟囱 / 渗热缝, placeholder coords).

Two complications:
1. **`auto/weekend` was 15 commits behind `main`** (the equipment work landed after the branch point) → no fast-forward; it needed a rebase first.
2. **CHANGELOG numbering collision** — the weekend task wrote a `#143` CHANGELOG.md entry *on its branch* (against quirk #130: append-only docs should only be written at main-integration), colliding with main's own `#143` (鲸落).

**A `git rebase` in the sandbox wedged**: the mount forbids `unlink`, and git's sequencer unlinks its own `*.lock` files mid-rebase → stuck with stale `index.lock` / `REBASE_HEAD.lock` / `rebase-merge/`. I recovered the worktree by moving the stale locks + rebase state aside (rename *is* allowed) and re-pointing its `HEAD` back to `auto/weekend`. Both branch refs stayed intact throughout; the main tree was never touched.

Since normal commits use *rename* (allowed) while rebase/merge/cherry-pick use *unlink* (blocked), I integrated the weekend content **at the file level**:
- `git show auto/weekend:src/data/chart_pois.json > src/data/chart_pois.json` — main's `chart_pois.json` was byte-identical to the merge-base (`3f6da60`), so this is a clean **+3 POIs** (verified: diff = +60 lines, exactly `roam.east_seagrass_bed` / `roam.vent_dead_chimneys` / `roam.vent_heat_seam`, valid JSON).
- Re-added the weekend's CHANGELOG entry as **`#146`** at the top of main's CHANGELOG (renumbered to resolve the collision; integration note inline).
- After committing, **reconciled `auto/weekend`** forward to main's new tip so the content is not re-merged next run.

## Regression gate — 42/42 GREEN
Sandbox is aarch64 Linux with macOS-native `node_modules`, so the dual-esbuild pattern: tsx tasks via esbuild 0.28.0, `vite build` via esbuild 0.21.5 + `@rollup/rollup-linux-arm64-gnu` 4.60.4 (installed under the outputs mount; `ESBUILD_BINARY_PATH` + `NODE_PATH`).

- **41/41 non-build ✓** — typecheck (3.6s) + verify-tutorial + check-branch + check-boundaries + check-event-dc + check-enemy-refs + check-dive-refs + 4 SSR smokes (chart / map-editor / story-editor / equipment) + 30 playthroughs. Wall **5.3s**, zero flakes.
- **prod build ✓** (0.9s).
- Run on `main` against the exact shippable tree (equipment + 3 POIs + player-changelog edit).

## Exploratory / hands-on testing
Throwaway tsx probe under /tmp (never in repo), targeting the newest/riskiest code the fixed baselines don't fuzz — the #144/#145 equipment system + save v7. ~330 randomized assertions:

- **Sonar craft (#145):** rejected without materials, allowed with; slot fills, `hasSonarEquipped` flips true, gold (200−120=80) + materials deducted correctly; a second craft into the occupied slot is a no-op. ✓
- **Equipment stats:** sonar maxLevel = 5; all stat fields finite at every level incl. an over-max (lv999) clamp; `unlockedAccessorySlots` = 1 placeholder. ✓
- **300 random loadouts:** every `getRunBonuses` + `getEquipmentStats` field finite & non-negative. ✓
- **Chart gen:** `generateChart` does not throw with the 3 new POIs present in the data. ✓
- **Save v7:** round-trip preserves equipment; a v6 save → `null` (no migration); a corrupt save → `null`, no throw; `hydrateGameState` idempotent. ✓

No defects found.

## Fixes
None — no RED at any point.

## Shipped
- Pushed `main` → `origin/main`: the 8 equipment commits (`1569d2e` … `bd74ddc`) **plus** the nightly integration commit (chart_pois +3 POIs, CHANGELOG `#146`, player-changelog draft, this report). GitHub Actions auto-deploys on push.
- `SAVE_VERSION` = 7 (from #145) — old local saves are wiped on next launch (unreleased game, quirk #99).
- `auto/weekend` reconciled to main's new tip.

## Notes for the author
- **Player changelog:** added a draft entry only for the 3 weekend POIs. I did **not** add one for the #144/#145 equipment system, even though it's the biggest player-visible change shipping tonight — the interactive sessions that built it left the player changelog untouched (segment 1+2; the locker→slot swap flow is incomplete and numbers are placeholders), so I deferred its player-facing announcement to you. Add one when the equipment feature is player-complete.
- **`docs/spec/lore_ledger.md`** is an untracked WIP lore-tracking scaffold (empty placeholders) from an interactive session — not committed.
- **The weekend task writes `CHANGELOG.md` on `auto/weekend`** (quirk #130 violation) → that's what caused the `#143` collision. Worth tightening the weekend skill to leave append-only docs (CHANGELOG/QUIRKS) for main-integration only, which would let future nightlies fast-forward cleanly instead of doing a file-level integration.
