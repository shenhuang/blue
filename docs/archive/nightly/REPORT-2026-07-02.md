# Nightly verify + publish — 2026-07-02

**Result: ✅ GREEN — published 14 unpushed interactive-session commits to `origin/main` behind a fully green gate (regress 81/81 exit 0 + env-recovered prod build exit 0), spanning `#240`→`#247`: unified item icons, B-2 outpost final-stage narrative, `check-upgrade-refs` gate + dead-`unlockShopItem` removal, general NPC trust system (Phase 1 mechanism + Phase 2 special merchant), flee/scare-off no longer drops animal materials, combat-UI overhaul (enemy grid cards / avatar + action icons / fixed-scroll log / mobile ring HP), and the treasure-trade Slice 1+2 (special merchant silas→Sela rename+retheme, Corin keepsake questline coin→badge, deeper Sela re-encounters, Mira generic-chart tiering). Plus one player-changelog draft entry + this report. No weekend-branch merge needed (0 pending). Exploratory fuzzing: 0 defects over 194,251 generated assertions (trust invariants + save round-trips + new DialogEffects).**

## Tree state on entry
- Main tree on `main`, HEAD = `c50d9c0`, **14 commits ahead** of `origin/main` (`804aad5`), 0 behind — `origin/main` is an ancestor (verified `rev-list --count main..origin/main` = 0 → clean fast-forward).
- The stale nightly-report blockquote that `npm run handoff` echoes still said "2 commits ahead"; live `git rev-list` is the truth (14). These 14 are real game-code/content commits accumulated by interactive sessions since the last publish (#240 on 06-29 → #247 on 07-02); several STATUS notes flag them "commit 待 Mac/nightly" — this run is that nightly.
- Working tree carried 4 untracked throwaway `scripts/_debug_silas*.ts` scripts (an interactive session's leftover probes) — **not** staged, **not** shipped.
- A concurrent session (`elegant-gracious-mendel`) shares this repo — re-verified branch/HEAD immediately before commit and re-fetched before push to avoid a race.
- psm feature worktrees present (`.worktrees/{cave-multimouth,cave-temp-wire,chart-deps,poi-roaming,weekend}`) — not touched (sandbox prune would delete them, quirk #138).

## Weekend merge — none pending
`main..auto/weekend` = **0** (`auto/weekend` is 187 commits behind main). `.worktrees/weekend` left as-is.

## Regression gate — GREEN (81/81)
`npm run regress` exit 0, wall **10.3s**, **81/81 ✓** — typecheck; ~35 static gates incl. `check-boundaries` (engine↛ui + rule-7 trust single-writer), `check-npc-trust`, `check-collectibles`, `check-upgrade-refs`, `check-material-icons`, `check-economy-reachability`/`-roles`/`-graph`, `check-mainline-reachable`, `check-terminology`, `check-lunar-reach`, `check-append-only-docs`, `check-status-fresh`, `check-doc-links`; `verify-tutorial`; all SSR smokes (chart/map-editor/story-editor/equipment/economy-panel/combat-panel/chart-editor/event-combat-arc/poi-events); all 23 playthroughs incl. `playthrough-trust` (Sela + trust tiers) and the flake-prone `playthrough` (passed first try). Re-ran the full gate after adding the player-changelog draft: still **81/81 ✓** (11.8s) — the exact committed tree is green.

In-sandbox, `ESBUILD_BINARY_PATH` was set to a Linux esbuild so all tsx behavior tests/smokes actually ran (without it, `regress.mjs` auto-defers them and prints a non-publishable "SUBSET ✓").

## Prod build — GREEN (env-recovered)
`regress` auto-skips the build in-sandbox (its guard still looks for `@rollup/rollup-linux-arm64-gnu`, which **vite 8 no longer uses**). Ran it manually since the Pages deploy runs `vite build` on push. Mounted `node_modules` holds **darwin-arm64** natives on a **linux-arm64** host (environment mismatch, not a code RED). Installed under `/tmp` and wired via `ESBUILD_BINARY_PATH` + `NODE_PATH` (author `node_modules` untouched): `@esbuild/linux-arm64@0.28.1`, `@rolldown/binding-linux-arm64-gnu@1.1.3`, `lightningcss-linux-arm64-gnu@1.32.0`. `vite build --outDir /tmp/nightly-dist` → **exit 0**, `✓ built in 333ms`, 181 modules transformed, hashed chunks + `index.html` + `favicon.svg` emitted (only a chunk-size advisory warning). Note for future runs: vite 8 bundles via **rolldown** (not rollup) + **lightningcss** — both Rust natives required in addition to esbuild; the task file's "rollup-linux fix" recovery name is stale (this is the third native, lightningcss, that the older toolchain didn't need).

## Exploratory testing
Throwaway probe in `/tmp` only (`probe-nightly.ts`; never staged), engine imported the way `scripts/playthrough*.ts` do. Targeted the newest, least browser-verified code (the trust + treasure-trade work of #243/#244/#246/#247).
- **Trust invariants (fuzz)** — 6,000 random gain/lose sequences across 3 NPCs (negative + oversized amounts): value always finite and clamped to `[TRUST_MIN,TRUST_MAX]`, `gainTrust` never decreases / `loseTrust` never increases, `change.delta` == actual net, and the input profile is never mutated (purity).
- **Tier derivation** — swept every value in `[MIN-20, MAX+20]` for Sela + Aldo: `trustTier` monotonic non-decreasing, within `[0, thresholds.length]`. Sela's data-driven thresholds `[10,30,60]` verified at exact boundaries (9→0, 10→1, 29→1, 30→2, 59→2, 60→3, 400→3). Default ladder unchanged `[25,75,150,300]`; negative/zero amounts are no-ops.
- **Save round-trip / additive-field compat (quirk #99)** — a state carrying the new `profile.trust` + new `story.ch1.corin_returned` / `flag.sela.met_deep` flags survives `serialize → deserialize` (flags come back as a `Set`, trust value preserved); an **old save missing the `trust` field still loads** and defaults to 0; a version-bumped save and corrupt JSON both return `null` (no throw).
- **New DialogEffects `gainTrust` / `takeItem` (#246)** — a huge `gainTrust` inside `applyDialogEffects` is correctly clamped to `TRUST_MAX` (proving it routes through the `trust.ts` single writer, not a raw field write); `takeItem` on an absent item is a safe no-op; `undefined` effects pass through unchanged; the input state is not mutated.
- **Total: 194,251 assertions, 0 failures.** No defects.

## Fixes made
None — nothing red. No code changes beyond the player-changelog draft entry.

## What shipped
Pushed `main` → `origin/main` (fast-forward). Contents: the 14 pre-existing commits `#240`→`#247` (titles above), plus one new `nightly:` commit adding the player-facing changelog draft entry (2026-07-02: combat-UI redesign / special merchant + treasure maps / item icons / flee-no-materials) and this report. The GitHub Actions Pages deploy runs automatically on push (`.github/workflows/deploy.yml`).
