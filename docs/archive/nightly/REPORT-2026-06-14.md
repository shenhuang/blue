# Nightly verify + publish — 2026-06-14

**Result: ✅ GREEN — merged weekend branch `auto/weekend` (3 commits) into `main` behind a green gate, added a player-changelog draft, and published to `origin/main`.**

## Tree state on entry
- HEAD was parked on **`auto/weekend`** (the weekend content task parks the working tree there). Working tree **CLEAN** (only the always-ignored `CLAUDE.md` / `.deploy-token`).
- `main` == `origin/main` == `9e83b8c` (the #131 探深§10 收尾 commit). `auto/weekend` == `b8b4bb2`, **3 commits ahead** of main, and `main` was a strict ancestor of `auto/weekend` (clean fast-forward, no rebase needed — `auto/weekend..main` count = 0).

## Weekend merge
Pending `main..auto/weekend` = **3 commits**, all real game content:

| sha | summary |
|-----|---------|
| `c0889ba` | weekend-poi (#133): 中层/热液场 +3 roaming 机会点（温跃断层 / 暗流斜带 / 冷泉洼·占位坐标）— `src/data/chart_pois.json` |
| `ab24af1` | weekend (#134): 10 deep-tier events (midwater 90–140m ×4, vent 125–165m ×3, trench/midnight 108–175m ×3) + 20 scenario baselines |
| `b8b4bb2` | weekend (#134): STATUS + CHANGELOG #134 docs |

Gate was run **on `auto/weekend`** (see below); GREEN → fast-forwarded `main` by ref move (`git branch -f main auto/weekend && git checkout main` — `main` confirmed ancestor, tree untouched). No force, no merge commit.

## Regression gate
Run on `auto/weekend` (then re-run on `main` after the changelog edit). Sandbox is aarch64 Linux with macOS-native `node_modules`, so per the dual-esbuild pattern the gate ran as two invocations: `regress --skip build` (tsx tasks via esbuild 0.28.0) + `vite build` separately (esbuild 0.21.5 + rollup linux-arm64-gnu, installed under /tmp).

- **34/34 non-build tasks ✓** — typecheck (5.1s) + verify-tutorial + check-boundaries + check-event-dc + check-enemy-refs + check-dive-refs + smoke-chart-ui + 25 playthroughs. Wall ~6.7s, zero flakes.
- **prod build ✓** — `tsc -b` clean, `vite build` clean (only the pre-existing >500 kB chunk-size advisory, not an error).
- Re-ran after the player-changelog edit: **34/34 + build still GREEN** (wall 4.9s).

## Exploratory / hands-on testing
Throwaway tsx probes under /tmp (never in repo), driving `runEventScenario` / `listAllEvents` the way `scripts/playthrough*.ts` do. Focus: the weekend content (events + POIs), since the fixed baselines only replay specific seed/choice paths.

- **New-event fuzz (#134):** all 10 new events × every option × 40 seeds = **1,200 full-state resolutions**. Every option resolved; all per-step + summary stat deltas finite; all check success-rates in [0,1]; `vent.mineral_column` correctly has no skill-checks (3 plain outcomes). Zero throws, zero scenario errors.
- **Edge-state sweep:** same events × every option × 25 seeds at `sanity=3, oxygen=5, stamina=5, nitrogen=95` = **750 runs** — this forces the failure branches (high-drain `onFailure`, e.g. −9/−11 sanity). All finite, none wild, zero errors/throws.
- **Global corpus sweep:** every one of the **196** events run (first option, seed 7) — zero throws, zero errors, all deltas finite. Confirms the merge didn't break anything corpus-wide.
- **New roaming POIs (#133):** all 3 present and well-formed — coords in [0,1], modifier `depthRange` nested within the host zone's range (thermocline [60,84]⊆midwater[55,85]; cold-seep [92,116]⊆vent[85,118]; drift-band = current-only), all gated by `flag.tutorial_complete`.
- **Reachability:** new events surface under their intended zoneTag×depth filters — midwater under `[midwater]`, vent under `[vent]`, trench under **`[midnight]`** (intentional depth-tone tag per #134), pools non-empty at 110–160m.

**Findings: no defects.** Two initial probe "failures" were both bugs in my probe, not the game — `roamingTemplates` keys on `templateId` not `id`, and trench events tag `[midnight]` not `[trench]`. Both reconciled and re-verified green.

## Fixes made
None — nothing was red.

## Player changelog
Shipping content is player-noticeable (new chart points + deeper-tier encounters), so prepended one draft entry (`2026-06-14 · 更深的水 · 新去处与新遭遇`) to the top of `src/data/changelog.ts` — player voice, no dev jargon, no deception spoilers (matches the file's conventions + deep-game-vision north star). Draft for the author to polish.

## Shipped
- `main` fast-forwarded onto `auto/weekend` (3 weekend commits) + 1 nightly commit (player changelog draft + this report).
- Pushed `origin/main`. GitHub Actions deploy runs automatically on push (footer build stamp advances because the game actually changed). Tracking ref refreshed via `git fetch` to avoid a phantom `[ahead]` (quirk #103).
