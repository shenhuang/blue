# Nightly verify + publish — 2026-06-25

**Result: ✅ GREEN — published 18 unpushed interactive-session commits (weekend deep-water content + #181–#185) plus one uncommitted player-facing tutorial event to `origin/main` behind a green gate (48/48 regress + env-recovered prod build), and prepended a player-changelog draft. No weekend branch merge needed (0 pending).**

## Tree state on entry
- Main tree on **`main`** = `e164386`, **18 commits ahead** of `origin/main`, **0 behind** (origin is an ancestor → clean fast-forward, verified via `merge-base --is-ancestor`).
- Uncommitted, tracked: `src/data/events/tutorial.json` — a new player-facing event `tutorial.captain_revisit_empty` (revisit the old wreck, go down, find it undisturbed) left by an interactive session. **Shipped** this run.
- Untracked WIP left for the author, **not staged**: `scripts/_test_split_debug.ts` (plus always-ignored `CLAUDE.md` / `.deploy-token`).

## Weekend merge — none pending
`main..auto/weekend` = **0** (auto/weekend is 76 commits behind main; its content already landed via earlier merges). Worktree `.worktrees/weekend` marked "prunable" — known relative-pointer mis-detection; **NOT pruned** (sandbox prune would delete it).

## Regression gate — GREEN (48/48)
`npm run regress` exit 0, wall 6.8s, 48/48 tasks ✓ (typecheck, all checks/smokes/playthroughs, scenario baselines). Prod build auto-skipped in sandbox (missing `@rollup/rollup-linux-arm64-gnu`, #147); verified separately with env recovery (installed `@rollup/rollup-linux-arm64-gnu@4.60.4` + `@esbuild/linux-arm64@0.21.5` under /tmp, `ESBUILD_BINARY_PATH` + `NODE_PATH`): `vite build` exit 0, 179 modules, built in ~965ms.

## Exploratory testing
- **POI fixed-resource depletion (#185)** — throwaway probe (`/tmp/probe-poi-depletion.ts`, 12 assertions, all pass): deterministic node ids for same `seedKey`; depletion preserves map topology (no nodes added/removed); geode-vein event nodes emptied when `item.bluecave_geode` harvested; generation idempotent under depletion; `SAVE_VERSION 9` round-trip of `profile.harvestedResources` / `run.harvestedNodes` (`Map<poiId, Set>`) survives serialize→deserialize; `addToPoiSetMap` is immutable, copy-on-write, and idempotent.
- **Map-generation invariant sweep** (`/tmp/probe-randmaps.ts`, 1404 assertions): 468 maps (39 zones × 12 seeds) — non-empty, all node depths finite ≥ 0, all edges reference existing nodes, `analyzeMap` never throws.
- No defects found. Probes kept in /tmp only.

## Fixes
None needed — gate green on entry.

## Shipped
- Prepended player-changelog draft `{ date: '2026-06-25' }` (resource depletion, location-specific encounters, revisitable locations, reworked HUD icon bar) to `src/data/changelog.ts`.
- Committed `src/data/events/tutorial.json` (new event) + `src/data/changelog.ts` + this report, fast-forwarded `origin/main`.
