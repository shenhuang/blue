# 深海回响 · 变更日志

> 从 STATUS.md 迁出的完整会话历史 + 历次内容/实装 pass 摘要。当前状态见 [../STATUS.md](../STATUS.md)。

## 会话历史（session log）

> 截至 2026-05-29，本 session 做了**洞穴"迷路" mapgen 重写** + **打捞行会 Lv.1/Lv.2**（Lv.1 选点预知尸体、Lv.2 出海前选目标尸体）+ **气穴/扎营节点化**（air_pocket/camp NodeKind）。
> （上一波：港口海图 POI 选点；再上一波：沉船墓园。）
> 收尾做了一次**全项目体检 + 清理 pass**：修了一个真 bug（`getEvent` 只装教学事件 → UI 渲染非教学事件"未找到"，quirk #38），并落地存档序列化层 / 共享 LCG / 注册守卫 / 若干去重（quirk #39–#40）。
> 最后把**海图升级成 2D 地图视图**（标记点 + 选中看信息，quirk #41）+ **D-reveal 程生姓名故障化**（quirk #42）。
>
> **2026-05-30 周末内容 pass（两次）**：上午补旧灯塔礁灯塔线 5 reef 事件 + brass_fitting；下午补**深水段 45-60m 共 4 事件**（`wreck.silted_hold`/`cave.halocline`/`wreck.porthole`/`cave.blue_floor`，realistic×2 + uncanny + cosmic）+ 5 baseline + `lore.deep_water.*`，把旧灯塔礁 60m 事件池从 1 填到 5（详见 quirk #44）。事件 38→42。
> **2026-05-30 第三个周末 pass**：给 **reef（旧灯塔礁）补首个原生战斗**——梭鱼（玻璃大炮：hp16/evasion4/dmg[5,9]/predatory 不退）+ `combat.reef_barracuda_solo` + `item.barracuda_jaw` + 触发事件 `reef.barracuda`（只挂 `[reef]`，隔离 26-44m）；另补蓝洞群 **12-25m 浅段 2 事件**（`bluecaves.squeeze` realistic check + `bluecaves.other_bubbles` uncanny lore，quirk #20）。事件 42→45，敌人 3→4，事件 baseline 21→24，战斗 baseline 5→6（详见 quirk #45）。
> **2026-05-31 周日内容 pass（第一次）**：补**沉船墓园 4 个 dive 事件**（cosmic 原生 dive 从 1〔engine_room_hum〕补到 3 + uncanny 加厚）——`cold_stores`（uncanny·26-42m·stamina check·canned_food/old_fishing_net 人造 loot）/ `hull_handprints`（uncanny·24-40m·oncePerSave·sanity check·lore.handprints，玻璃里侧的手印）/ `the_knocking`（cosmic·30-48m·oncePerRun·sanity check + 回敲违禁分支·lore.the_knocking，是 dive_slate『不要回敲』的正面付现）/ `the_open_door`（cosmic·40-50m·oncePerRun·sanity check·门槛/吸力意象·lore.the_door）。全挂 `[wreck]`、loot 只人造物（quirk #44/#47）。事件 48→52，事件 baseline 29→34（含 the_knocking 听/回敲双 baseline）。详见 quirk #47。
> **2026-05-31 周日内容 pass（敌人）**：给**沉船墓园补第二只敌人＝沉灯水母**（`enemy.drowned_lantern`，**项目首只 cosmic-tier 敌人 + 首只 sanity-主导敌人**：hp24/armor1/evasion1/predatory，主攻「脉光」纯 sanity——『理智消耗战』，slow/tanky 反盲鳗·反章鱼）+ 触发事件 `wreck_graveyard.drifting_light`（cosmic·34-50m·拔刀/关灯避战 sanity check/退开）+ `item.lantern_gland`→Mira + combat/event baseline。另补 `reef.lighthouse_lens`（uncanny·30-44m·灯室的镜·填 reef 26-44m 中段 uncanny 缺口）+ `bluecaves.the_narrowing`（cosmic·14-25m·回头的路·填蓝洞 12-25m 浅段 cosmic 缺口）。事件 56→59，敌人 5→6，事件 baseline 39→43，战斗 baseline 7→8。**三个长线缺口（墓园敌人 / reef 中段 uncanny / 蓝洞浅段 cosmic）本 pass 全部补上**。详见 quirk #48。
> **2026-05-31 周日内容 pass（第四次 · realistic 探索密度）**：轮换离开近几 pass 的 cosmic/uncanny/敌人侧重，回到 **realistic 探索质感**，跨 reef/wreck/cave 三 zone 补 **4 个 realistic dive 事件、无新敌人**（守『敌人别太多·优先事件』）——`reef.shelf_break`（realistic·30-44m·礁壁断口·stamina vs12·coral_shard，**填 reef 26-44m realistic 缺口**：此前该段只有 barracuda 战斗触发 + lobster_hole 尾）/ `reef.urchin_barren`（realistic·16-30m·海胆滩·无 check·coral_shard+sanity-1，补 reef 浅中段 realistic 密度）/ `wreck_graveyard.galley`（realistic·20-34m·伙房·stamina vs13·canned_food/old_fishing_net 人造 loot·守 quirk #44/#47）/ `bluecaves.breakdown_pile`（realistic·16-26m·塌石堆·无 check 资源取舍·稀释 entrance_light 过曝 quirk #20）。全 realistic、全只挂单 zone tag（quirk #19）、无 lore/无 d_reveal。事件 59→63，事件 baseline 43→49（2 个 stamina-success + 4 个 no-check 路径）。详见 quirk #49。
> **2026-06-01 基建地图 revamp · Phase A（材料经济）实装**：把"建设值（每潜按 depth/node 计分）买升级"**整体换成"材料 ＋ 金币 买升级"**。`UpgradeDef.cost: number → UpgradeCost{ materials: MaterialCost[]; gold }`（5 个升级账单见 `data/upgrades.json`）；每个 material 加 `tier 1–4`（深度分档，门控高阶升级要更深的料）；`canPurchase`/`purchaseUpgrade` 改双资源（材料缺口 + 金币）；**`buildingPoints` 整体移除**（types/engine/UI/存档迁移 v1→v2 全删，`computeRawBuildingPoints`/`computeBuildingPoints` 删除）；**Mira 加回购侧**（T1/T2 可买，买价=卖价×2，`shopStock` 限量+回港补满；T3/T4 只卖不买）。SAVE_VERSION 1→2。回归全绿（`playthrough-upgrades`/`-economy`/`-save` 改写 + `smoke-chart-ui` 加 UpgradePanel/MiraShopView 两节）。详见 quirk #50 + `docs/深海回响_基建地图_SPEC.md`（Phase A 已打勾）。提交 `4612c0c`。
> **2026-06-01 基建地图 revamp · Phase B（灯塔数据模型）实装**：把"单一岸边港口"在**数据层**扩成**多座灯塔基地**。新 `Lighthouse` 类型（`types/lighthouse.ts`：id/name/mapX/mapY/level/`builtUpgrades:Set` + inert `integrity?`/`region?` 留 Phase D）+ `profile.lighthouses: Lighthouse[]`；现有港口重构成 **home 灯塔**（`lighthouse.home`，`createHomeLighthouse()` 单一来源，createInitialProfile 种入 + 迁移补种）；SAVE_VERSION **2→3**（`case 2` 给旧档种 home）。新 `engine/lighthouses.ts`（与全局 `upgrades.ts` 平行、互不污染）：`canBuildAt`/`buildAtLighthouse`（每灯塔升级轨，账单复用 Phase A 双资源）/`getLighthouseBonuses`（聚合 lightRadiusBonus/reachReduction）/`nearestLighthouse`（最近灯塔距离工具）+ `data/lighthouse_upgrades.json`（信标轨占位）。**灯塔此刻 inert**——reveal（点亮揭示）/ reach（最近灯塔算 distance）是 **Phase C**，本阶段没接进 chart/dive/UI，游戏行为不变。`dockyard` 仍全局（归属决策留 Phase C）。回归全绿（新 `playthrough-lighthouse.ts` + `-save` v2→v3 + verify-tutorial 加账单材料校验）。详见 quirk #51 + SPEC（Phase B 已打勾）。
> **2026-06-01 基建地图 revamp · Phase C（海图集成 + 修复循环）实装**：把 Phase B 的 inert 灯塔接进游戏，**revamp 三支柱（材料/灯塔/海图）闭环**。① **reveal**（`chart.ts`）：POI 落在某座已拥有灯塔的点亮半径内才可见——`isPoiLit`/`revealRadius`（home L1 半径 `BASE_LIGHT_RADIUS=0.72` 覆盖现有 4 锚点 + 近端 roaming，两个远端 roaming 落半径外、留给前哨）折进 `isPoiVisible` + generateChart roaming 过滤；② **reach**（`effectiveDistance`）：出海 distance 按最近的已拥有灯塔的归一化距离换算（`REACH_NORM_PER_TIER=0.3`，使 4 锚点从 home 算的档位＝写死 0/1/1/2 不破手感）减 reachReduction，无坐标/无灯塔退回写死 distance；`dive.ts::startDiveFromPoi` 消费；③ **修复废弃灯塔**：`data/events/lighthouse.json` 的 `lighthouse.ruin_north`（repair 选项 outcome 带新字段 `restoreRuinId` → `applyOutcome` 调 `engine/lighthouses.ts::restoreLighthouse` 权威校验 profile 银行账单 → push 新灯塔 + 置 `flag.lighthouse_restored.<id>` 把事件门控掉）；`data/lighthouse_upgrades.json` 加 `ruins[]`；④ **dockyard 迁灯塔**：`dockyard` 从全局升级迁成 home 灯塔「船坞」设施（`lighthouse.dockyard.lv1`，新 `lhtrack.dockyard` homeOnly 轨），其 `extraConsumableSlot` 经新 `getRunBonuses`（全局升级 ＋ 家灯塔船坞）桥回 run 加成；旧灯塔礁等远海 POI 的抵达门从 `requiresUpgrade` 改成新 `requiresLighthouseUpgrade`（读 home.builtUpgrades）；SAVE_VERSION **3→4**（`case 3`：已购 dockyard 搬进 home.builtUpgrades）；⑤ **UI**：`SeaChartView` 渲染灯塔节点 + 点亮范围圈，新 `LighthouseBuildPanel`（海图上建设施，"灯塔设施"按钮唤出）。回归全绿（新 `playthrough-lighthouse-scenarios.ts` + `scenarios/lighthouse/` + 改 `playthrough-chart`/`-lighthouse`/`-upgrades`/`-economy`/`-save`/`smoke-chart-ui`/`verify-tutorial`）。详见 quirk #52 + SPEC（Phase C 已打勾）。
> **2026-06-02 内容 pass（reef 浅段 fresh-wrongness + reef 第二只敌人「石斑鱼」+ 深段 realistic 密度）**：revamp 三支柱闭环后回到内容，一次补齐 [Weekend Content Log] 记的三个长线缺口。① **reef 浅段 uncanny/cosmic（作者选「全新浅水错位」母题，刻意不续灯塔『下面的光』线）**：`reef.silversides`（uncanny·10-24m·一墙银鱼把你扣在球心、围的却是一片空沙·sanity vs46+lore.reef_shallows.the_gap）/ `reef.sun_net`（**cosmic·14-25m·oncePerRun·沙上一格钉死不动的太阳光网·项目首个浅段 cosmic**·sanity vs50+lore.the_still_square）/ `reef.warm_seam`（uncanny·12-24m·一道从礁底缝里上来的血温暖水·no-check+lore.the_warm_crack）——全 `[shallow,reef]`（zones.json 里只灯塔礁 0-25m 吃 shallow，east_reef=tutorial、蓝洞=cave，故天然隔离），loot coral/loot-free（quirk #44/#19），新 lore 命名空间 `lore.reef_shallows.*`，三者都轻触『下面』暗线但**刻意不触发 d_reveal**。灯塔礁 12m 事件池 4→6（终于有浅段 cosmic）。② **reef 第二只敌人＝石斑鱼**（`enemy.reef_grouper`，territorial『礁檐守卫』原型：hp30/armor2/**evasion1 最低几乎必中**/threat4 不追/observer，gulp[6,10] 全场最高单击+buffet[3,6]）——与梭鱼(glass cannon 速杀)/章鱼(cave aggressor bruiser)/盲鳗(fast flanker)/沉灯水母(cosmic caster)全互补：**低闪避·厚甲·最重单击的重装墙，但 opt-in**（触发事件 `reef.coral_overhang` 给 sneak 避战取 loot / leave 无代价退两个非战斗出口，与梭鱼 predatory『总要付代价』相反）。4-turn 消耗战，掉新 T2 天然物 `item.grouper_maw`（石斑鱼鳔，organic→Mira）。③ **深段 realistic 密度**：`cave.sump_pool`（46-60m·`[cave]`·回水潭潜越·coral）/ `wreck.chain_locker`（44-60m·`[wreck]`·锚链舱打捞·brass/canned，quirk #44/#47）——填 60m 事件池（5→7），同时加厚蓝洞群/沉船墓园深段。事件 64→70、敌人 6→7、combat 7→8、item 21→22、event baseline 49→57、combat baseline 8→9，全回归绿。**敌人分布现各 zone 2 只（守『敌人别太多』，下个 content session 不建议再加敌人）**。详见 quirk #53。
> **2026-06-02 内容 pass（续二 · 墓园浅段 fresh-wrongness，首次作 deep-game vision 的「伏笔层」）**：接 reef 浅段那次的对称缺口——沉船墓园浅段 18-25m 此前**无 cosmic**、uncanny 仅 dive_slate/handprints 擦边（22-24m 起）。补 3 个 `[wreck]` 浅事件，全是墓园版「不信任自己的眼睛」，且**刻意是深水欺骗（见自动记忆 deep-game-vision）的浅水伏笔、叙述永不交底**：`the_other`（uncanny·18-25m·两船间一个跟你同步、隔着固定距离的潜水员·sanity vs48+lore——**伏笔 corpse-wearer**）/ `all_facing`（**cosmic·18-25m·oncePerRun·从上方看整片沉船的船首全转向塌口/深水·sanity vs50+lore·墓园首个浅段 cosmic·伏笔『深处的拉力』**）/ `full_nets`（uncanny·18-25m·拖网船的网被底下某物往深处拽得绷紧·stamina vs13 割上半截→old_fishing_net+canned 人造 loot / follow_down→lore·**伏笔深处的『渔夫』**）。全 `[wreck]`（quirk #47 跨灯塔礁 25m+，故 loot 仅人造物守 #44）、loot 仅 full_nets、新 `lore.wreck_graveyard.{the_other,all_facing,full_nets}`、**不触发 d_reveal**、**无新敌人（守 2/zone）**。事件 70→73，event baseline 57→62。详见 quirk #54。
> **2026-06-02 内容 pass（续三 · 深水伏笔 mid 层 25-44m，承 quirk #54「叙述永不交底」铁律 + 自动记忆 [[deep-game-vision]] 北极星）**：接 reef/墓园浅段 fresh-wrongness（#53/#54）的对称缺口，把『越深越欺骗』伏笔从浅段推进到三个 zone 的中段，**一 zone 一事件、一事件一母题、各填真缺口、无新敌人（守 2/zone）**：`reef.no_bottom`（cosmic·32-44m·oncePerRun·`[reef]`·断口外空蓝把你『往下要』＝『深处的拉力』，reef 首个非灯塔线 cosmic-mid，与 realistic `shelf_break` 同地标〈断口〉配成『诚实危险＋错读』）/ `bluecaves.the_glow`（uncanny·30-44m·`[cave]`·黑里一点无法溯源、你一拐它就移到别处的光＝『无灯之光』，伪装成灯塔的 mimic 假信标预告，加厚蓝洞『有人在下面』暗线）/ `wreck_graveyard.no_bubbles`（uncanny·26-42m·`[wreck]`·背对你干活却不冒一个泡的潜水员＝corpse-wearer 伏笔，并埋可读 tell〈不呼吸〉，承浅段 `the_other`）。全 loot-free、单 zone tag（#19）、**不触发 d_reveal**、无 combat。新 `lore.reef_deep.*`（首用）+ 复用 `lore.bluecaves.*`/`lore.wreck_graveyard.*`。事件 73→76，event baseline 62→68。详见 quirk #55。
> **2026-06-02 内容 pass（续四 · realistic 探索密度收尾，作者选「内容收尾·realistic 密度」）**：浅/中两层「越深越欺骗」伏笔成型后（#53/#54/#55），轮换回 realistic 探索质感（同 #49 逻辑），把各 zone 最薄的 **realistic** 段补厚——**一 zone 一事件、各填该 zone 真缺口、无新敌人（守 2/zone）、无 lore/cosmic/不触发 d_reveal、无新 item**。recon 用 `event-runner --list --zone-tag` 按 tone 数各 zone realistic 覆盖（lighthouse-reef 池报告浅/深段已被 #53 填平、不再是信号）：`wreck_graveyard.deck_cargo`（realistic·18-26m·`[wreck]`·后甲板捆死的整船货+垮货网·cut_lashings stamina vs13→canned_food/old_fishing_net 人造 loot / pick_spillage→brass_fitting·**填墓园浅段 realistic 缺口**——#54 把 18-25m 堆成 uncanny/cosmic 后补无错位打捞质感平衡 tone）/ `bluecaves.lobster_crack`（realistic·26-44m·`[cave]`·侧壁横缝里够礁虾·reach_in stamina vs13→coral_shard+lobster·**填蓝洞中段 realistic 缺口**——此前中段 realistic 仅 3 导航地标、无觅食 beat·天然 loot 守 #44）/ `reef.sand_channel`（realistic·34-44m·`[reef]`·礁脊间平行沙沟+来回涌·work_groove stamina vs12→coral_shard+lobster·**填 reef-only 深中段 realistic 缺口**——此前仅 shelf_break 触 44m，与之同地标家族〈断口/沙沟〉机制不同〈横涌 vs 垂壁〉）。全单 zone tag（#19）、loot 按 zone（#44/#47：wreck 人造·cave/reef 天然）、stamina-check 只锁 success baseline（#49）。事件 76→79，event baseline 68→71，敌人 7/combat 8/item 22 不变。详见 quirk #56。
> **2026-06-02 内容 pass（续五 · 深水伏笔深段 45-60m，承 quirk #54/#55「叙述永不交底」+ [[deep-game-vision]] 北极星，续「ok next」自动续做）**：浅（#53/#54）→ 中（#55）「越深越欺骗」伏笔成型、realistic 密度收尾（#56）后，把两条 apex 母题推进到**最深层（45-60m）、做成最强的 mimic/corpse-wearer 预告，但仍不触发 d_reveal**。**关键约束：reef zone 45m+ 段 tag=`[wreck,cave]`（#47），`[reef]` 事件在 45m+ 不出现 → 深段只有 cave/wreck 两 tag 可用**（reef『拉力』母题已在中段 no_bottom 封顶）。一 tag 一母题、全 cosmic·oncePerRun·loot-free·无新敌人（守 2/zone）：`cave.false_beacon`（46-60m·`[cave]`·超出自家灯塔光照边界却有一点暖得正是岸上灯塔该有颜色的光稳稳悬着『像有人替你点着』＝**伪装成灯塔的安康鱼 mimic 假信标的直接深段预告**·承中段 the_glow〈无灯之光〉、接 deep_water『下面的光』暗线 cold_light/the_window·新 lore.deep_water.the_false_beacon·account_for_it sanity vs55 双分支 + swim_for_it『缺氧也照游过去』代价）/ `wreck_graveyard.the_wearer`（44-56m·`[wreck]`·旧式铜盔潜水服、无灯无泡却知道你在哪、招手引你＝**穿尸体引诱的 corpse-wearer 直接深段预告**·承浅段 the_other / 中段 no_bubbles、埋可读 tell〈无灯/无泡/老古董装备/招手机械重复〉、go_to_him 反用本作对死者的温柔·新 lore.wreck_graveyard.the_wearer·read_him sanity vs55 双分支·无 combat）。全单 zone tag（#19）、不触发 d_reveal、叙述永不交底。事件 79→81，event baseline 71→75（两事件各 success+fail），敌人 7/combat 8/item 22 不变。**浅/中/深三层伏笔全部成型。** 详见 quirk #57。
> **2026-06-02 深水区 vision design（续「lets pin it down」，纯设计、零代码/零回归改动）**：内容层饱和后转入 [[deep-game-vision]] 正式开建的 design 阶段。作者四点拍板——① clarity＝双层 + 双传感器（宏观海图灯塔网 + 微观下潜**近距灯/远距声呐 ping**，**关灯关声呐＝致盲但降 signature 让捕食者更难发现你**＝主动感知双刃）；② 递归纵深大地图（既有 zone 更深 band → 终端 zone → 超深海沟 → 深渊，**永远有比最深更深的**）；③ mimic 首次＝海图假 POI（无灯之光）引诱 → 入潜兑现（both）；④ tell↔sanity＝模糊 + 检定更难（both）。出 **`docs/深海回响_深水区_SPEC.md`（v0.1）**：§3 架构（双层 clarity / 双传感器 / 探测双刃 / 可扩展深度轴 / mimic capstone）+ §4 现有代码接点 + §5 四 phase（0 双传感器 clarity+探测 → 1 深度轴 → 2 跨 run 供给前哨 → 3 mimic+d_reveal）+ §6 数据类型草案 + §7 待作者复核子决策 + §8 tunables + §9 守则。**下一步 Phase 0，开建前作者过 SPEC §7。** 自动记忆 [[deep-game-vision]] 已按拍板更新（clarity 从单纯涌现细化成双传感器+探测双刃）。
> **2026-06-03 深水区 Phase 0a 实装（微观双传感器 clarity + 不可信声呐 + 电池 + 低 san 腐蚀，深水区第一笔代码）**：把 `visibility:dark` 盲航泛化成统一 micro-clarity 三态——**灯（近·真相·暴露高·清水近免费）/ 声呐 ping（远·不可信回波·耗电·后期解锁）/ 摸黑（盲·最隐蔽）**。新 `engine/clarity.ts`（`clarity(run)` 预览档 + `sonarReturn` 不可信表象 + `lampPreview` 真相/幻觉 + `signature` + `lampPowerDrain`，tunables 集中文件顶）。run 加 `sensors{light,sonar,sonarUnlocked}` + `power`/`powerMax`；`enterNodeSelection` 按档把 preview 烤成 真相/声呐表象/盲（引擎侧门控、便于回归）；`dive.ts` 加 `setLight`/`pingSonar`（移动后 ping 消散、power 归零强制摸黑）；`tickTurns` 灯耗电（清水因子 0、黑水/微浊才耗）。低 san 腐蚀：声呐<60 注入假回波、灯<25 也幻觉（确定性哈希、叙述永不交底 #54）。**声呐后期解锁**（作者定）：`upgrade.sonar.lv1`（深料 lantern_gland+eel_skin+beak）→ `unlockSonar` → run.sensors.sonarUnlocked，早期＝仅有灯·黑水天然受限。**未发布暂不做存档迁移**（作者 2026-06-03）：不 bump SAVE_VERSION、run 新字段靠 createNewRun 种默认 + 反序列化兜底。UI：NodeSelectView 成纯渲染器（按 choice.clarity）+ 灯/声呐控制 + 电量 pill。新 `playthrough-sensors.ts`（10 节）+ smoke E 改写。**全绿 + prod build。0b（探测/隐身、消费 signature）留下一 session。** 详见 quirk #58 + SPEC §11（0a 已勾）。提交见 [[sandbox-git-commit]]。
> **2026-06-03 深水区 Phase 0b 实装（探测/隐身：警觉积累→接近/伏击，同日续，消费 0a 的 signature）**：作者拍板探测＝「警觉积累→接近/伏击」（§3.3 最完整形态）。**关键：今天战斗 100% 事件选项触发、零自动遭遇 → 0b 是新增的自动遭遇路径**。新 run 级 `alert`（点灯/ping 在深水逐回合抬、摸黑/浅水消退；`tickTurns` 累加 clamp 0–100；`clarity.ts::alertDelta` = signature 超基线 × 深度因子 × GAIN − DECAY，深度因子浅水 0＝§7.5）。`dive.ts::moveToNode` 进节点时 `predatorApproaches`（alert≥60 + 够深）+ 非地标节点 + 该 zone 有 `ambushEncounters` → `startCombat` 触发接近遭遇（复用 zone 现有 solo 敌）、触发后 alert 落回 0。`ZoneDef.ambushEncounters` 三深水 zone 各配 2 敌、浅/教学不配（§7.5 兜底）。**可生存无脚本死**：NodeSelectView 预警（WARN 35 黄/THRESHOLD 60 红，提示熄灯）给读 tell 窗口 + 摸黑能甩 + 遭遇可打可逃；地标（上浮口/气穴/扎营）不被伏击＝总留出路。新 `playthrough-stealth.ts`（6 节）+ smoke E2。**全绿 + prod build。Phase 0（0a 感知+0b 探测）完成。下一步 Phase 1 深度轴 或 Phase 0 升级轨。** 详见 quirk #59 + SPEC §11（0b 已勾）。
> **2026-06-03 深水区 Phase 0 升级轨实装（灯/声呐 效果·耗能·抗欺骗 + 电池容量 随材料经济成长，§11 最后一项 → Phase 0 全闭）**：作者选方向 B（升级轨）+ 四旋钮全选（powerMax / 能耗效率 / 抗欺骗 / 隐蔽）。新 `run.sensorTuning`（出海前 `deriveSensorTuning` 一次性烤、下潜内不变，clarity.ts 纯函数读它 + 缺省回退基线常量＝未升级/旧档逐字节复现 0a/0b）。链路沿用 #58 声呐解锁模板：6 新 `UpgradeEffect` → `getUpgradeBonuses`(sum) → `getRunBonuses`(透传) → `createNewRun` 烤进 `run.sensorTuning`/`powerMax`。数据＝复用 `line.sonar_rig` 续 `sonar.lv2`（ping 省电 + 声呐抗欺骗）+ 新 `line.dive_kit`（电池/聚光灯具〔灯效率+隐蔽〕/抗扰灯罩〔灯抗欺骗+电池〕），深度分层账单（升级线 3→4）。**范围/分辨留 Phase 1**（需节点级 clarity）。地板/上限守北极星——抗欺骗永不归零（无完全可信传感器）+ 隐蔽有上限·点灯 signature 永 > 摸黑（读真相必自曝）。DRY：`RunStartBonuses` 是 createNewRun bonuses 超集，dive/dialog 直接整个传。未发布不 bump SAVE_VERSION（sensorTuning 普通对象 JSON 自动 round-trip + 兜底）。回归：`-sensors` §11 / `-upgrades` §8 / `-save` round-trip / smoke J4 + 全绿 + prod build。**Phase 0 完整闭环（感知+探测+成长）；下一步 Phase 1 深度轴或内容打磨。** 详见 quirk #60 + SPEC §10/§11。
> **2026-06-03 深水区 Phase 1 plumbing 实装（可扩展纵向深度轴：band 阶梯 + 蛙跳下潜）**：开建前 AskUserQuestion pin 四点。作者拍板——① band＝**新全局 `data/depth_bands.json`**（跨 zone 共享深度阶梯，band 引用 zone 提供内容、用绝对 depthRange 覆盖 zone）；② 解锁＝**软门控**（band 不带硬 flag，可达性由装备〔声呐/电池/升级，吃深料，#60〕+ 后续强敌决定——Phase 0 升级轨即是门）；③ 蛙跳 `startDiveFromOutpost`＝**最小版**（home 灯塔 stand-in，真前哨留 Phase 2）；④ 成本＝**不加深度耗电税**（深 band 更暗→灯打不透→被迫用更耗电声呐+重 ping→电量间接压力，复用现有 visibility→forced-sonar→power 回路）。实装：`types/bands.ts` + `data/depth_bands.json`（reef_deep murky / trench_mouth·throat dark·**破 60m 准硬上限**·借蓝洞内容占位）+ `engine/bands.ts` + `mapgen GenOpts.depthRange` 覆盖 + `dive.ts::startDiveFromOutpost`（镜像 startDiveFromPoi、走 getRunBonuses＝升级直通）+ `clarity.ts` 写死 60→`ALERT_DEPTH_FULL` + SeaChartView 蛙跳列表。新 `playthrough-bands.ts`（7 节）+ smoke A2，全绿 + prod build。**内容（trench 专属 zone/事件/tag）+ 范围分辨（节点级 clarity）+ 真前哨蛙跳（Phase 2）留后续。** 详见 quirk #61 + SPEC §10/§12。
> **2026-06-04 深水区 Phase 1 续·节点级 clarity 实装（深度分档 + 范围/分辨升级轨，填 #60 deferred 钩子）**：把 run 级二元 clarity 升成节点级。`clarity(run)` 留作"天花板"（灯 full/声呐 sonar/摸黑 none，UI header 用），新 `clarityForNode(run,node)` 在其上**按节点深度差降档**——浅水（≤25m `CLARITY_FULL_DEPTH`）豁免＝所见为真；深水里灯只照得到近处（≤`LAMP_DEPTH_REACH`6m），灯够不到的陡降没声呐就黑、有声呐补中段（≤`SONAR_DEPTH_REACH`14m）、太深连声呐都没回波；横/上行不降档（深度只藏"下面"）。`enterNodeSelection` 改 per-choice 烤档（`NodeChoice.clarity` + UI `clar-<档>` 早就 per-choice、**UI 零改**）。**范围/分辨做成升级轨**（填 #60 明确 deferred 的那块）：`SensorTuning` 加 lampDepthReach/sonarDepthReach、2 新 `UpgradeEffect`（lampRangeBonus/sonarRangeBonus）沿用 #58/#60 桥（effect→getUpgradeBonuses→getRunBonuses→createNewRun→deriveSensorTuning）、`data/upgrades.json` 加 `dive_kit.lv4`〔远摄灯组 灯 reach+4〕+ `sonar.lv3`〔声呐 reach+8〕（升级线仍 4、升级 9→11）、reach 有上限（`LAMP_DEPTH_REACH_MAX`14/`SONAR_DEPTH_REACH_MAX`26＜最深陡降＝守"最深处灯也买不穿、必须自己摸黑/声呐下去"）。**尸体定位（打捞行会 Lv.1）不被深度藏住**（地图知识、灯有效就认得出，守 quirk #36——避免节点级 clarity 误伤既有功能）。`playthrough-sensors` 加 §12（9 节）+ `-upgrades`§8 续 lv4/lv3 + `-save` reach round-trip + `smoke` J5。全绿（typecheck / 18 playthrough / scenarios 75 / combat 9 / mapgen 4+60 / lighthouse / verify-tutorial / smoke）+ prod build。未发布不 bump SAVE_VERSION（sensorTuning 普通对象 JSON round-trip + 兜底）。详见 quirk #62 + SPEC §10/§12。
> **2026-06-04 深水区内容 pass · trench 专属事件「回波对不上」+ band.tags 覆盖（承 [[deep-game-vision]] 北极星；作者选「深段/trench 内容」→「回波对不上」母题，AskUserQuestion）**：Phase 1 占位的 trench band（60-108m，此前借蓝洞内容、深 cave 事件 ≤60m 故事件池**空**）终于有专属内容。**工程＝band 级 tag 池（填 SPEC §12 Deferred「band 级 tags 覆盖 zoneTagsByDepth」）**：`DepthBand.tags?: ZoneTag[]`——**用已存在但闲置的 `twilight`/`midnight` ZoneTag、零类型改动**——经 `startDiveFromOutpost → startDive(opts.bandTags) → mapgen GenOpts.bandTags → 节点 zoneTag 抽取 + buildEventPool(tagsOverride)` 覆盖 zoneTagsByDepth；trench_mouth=`[cave,twilight]`、trench_throat=`[cave,midnight]`（附加 cave 保留回退池）＝trench 专属事件池、与借来的蓝洞内容隔离。**内容＝新 `data/events/trench.json`（6 事件）**，母题「回波对不上」（声呐返回 ≠ 点灯凑近真相，付现 throat blurb + Phase 0 不可信声呐 #58 + 节点级 clarity 陡降变黑 #62）——系统覆盖一次 ping 能骗你的全部方式：假底 `the_return`(cosmic) / 假人 `second_diver`(uncanny·corpse-wearer 声呐侧回声) / 距离不对 `the_sounding`(uncanny) / 假墙 `the_wall`(cosmic·直接付现 throat blurb) / 被应答 `the_answer`(cosmic·轻触 §3.7) / 没有回波 `no_echo`(uncanny·「看不清才往下走」)。全 loot-free / 无敌人（守 2/zone，mimic·corpse-wearer 是 Phase 3 apex 例外）/ 不触发 d_reveal / 叙述永不交底（#54），每个都留「关声呐·摸黑」的诚实但盲的出口（感知双刃 §3.2-3.3）。新 `lore.trench.*`。事件 81→87、event baseline 75→87（+12 trench scenarios），敌人 7/combat 8/item 22 不变。全回归绿 + prod build。详见 quirk #63。
> **2026-06-04 深水区 C · band 级探测压力倍率（「全做」session 第一笔，填 #61/§12 Deferred「band 级 alert 倍率」）**：`alertDepthFactor` 在 60m(`ALERT_DEPTH_FULL`) 饱和、故 trench_mouth/throat 探测压力此前相同——本次加 `DepthBand.alertFactor?`（缺省 1）让更深 band「越深越凶」：`startDiveFromOutpost` 落 `run.bandAlertFactor` → `clarity.ts::alertDelta` **只乘进暴露增益、不动 DECAY**（摸黑/浅水净消退不变＝逃生阀门倍率买不断、守无脚本死）。data：reef_deep 缺省 1 / trench_mouth 1.3 / trench_throat 1.6 / 深渊 2.0。`playthrough-bands.ts` §9 + 全绿 + prod build。未发布不 bump SAVE_VERSION（派生·round-trip+兜底）、UI 零改。详见 quirk #64 + SPEC §10/§12。
> **2026-06-04 深水区内容 pass · 深渊 band（>108m）+ abyssal 专属事件「永远有比最深更深的」（「全做」session 第二笔，续 #63 band.tags 机制开更深一层）**：trench band（60-108m）之下再开 `band.abyssal`（108-140m·dark·alertFactor 2.0·`tags:[cave,abyssal]`〔既有闲置 ZoneTag、零类型改动〕·借蓝洞 mapgen·递归更深不硬编码地板）。新 `data/events/abyssal.json`（5 事件 import 进 zones.ts）：母题从「回波对不上」（声呐骗你）推进到「『底』本身在退/拽/从更深处招你」——没有底 `no_floor` / 从下面上来的东西 `the_rising`（apex 伏笔）/ 更深处的假灯 `deeper_light`（mimic 伏笔·勾连 `lore.deep_water.cold_light`）/ 不该活着却活着 `the_permission` / 身体停了沉没停 `still_falling`（reef.no_bottom 拉力最深兑现）。全 loot-free / 无敌人（守 2/zone·mimic·corpse-wearer 仍 Phase 3 apex）/ 不触发 d_reveal / 叙述永不交底，每事件留诚实但盲/退的逃生出口。新 `lore.abyssal.*`。11 scenarios + `playthrough-bands.ts` §10。事件 87→92、baseline 87→98。全绿 + prod build。详见 quirk #65 + SPEC §10/§12。
> **2026-06-04 深水区 Phase 2a · 跨 run 分阶段前哨 + 真蛙跳出潜点（「全做」session 第三笔；作者 pin 脊柱优先 / 3 阶段 / 未发布不迁移，AskUserQuestion）**：把 Phase C 一次性 ruin 修复（#52）扩成**多阶段、跨 run 持久**的前哨脊柱（能源经济/衰减留 Phase 2b）。**进度＝flag、零存档形状改动**（`outpostStageFlag`=`flag.<id>.s1..3` 持久、半亮扛过死亡、SAVE_VERSION 仍 4）。新 `OutpostDef`（lighthouse_upgrades.json `outposts[]`，3 stage·T1→T2→T3 深料分层）+ `advanceOutpost`（按阶段校验账单·扣料·置 flag·**建满 push 一座 Lighthouse 复用 Phase C reveal/reach**）+ outcome 新 `advanceOutpostId`。**真蛙跳**：`startDiveFromOutpost` 从 `deepestOutpostLaunch`（半亮 ≥USABLE2 且更浅的最深前哨）起跳、预耗氧按「目标顶−前哨底」（trench_mouth：home 3→深槽前哨 1 回合）。3 阶段建造事件 `lighthouse.outpost_reef_deep`（visibleIf flag 门控·一阶/潜·forbiddenFlags 点亮后门掉）。新 `playthrough-outpost.ts`（6 节）。事件 92→93。全绿（19 playthrough / scenarios 98 / verify-tutorial 93 / smoke）+ prod build。详见 quirk #66 + SPEC §5/§10。
> **2026-06-04 深水区 Phase 2b（A）· 前哨能源经济 + 水下衰减 + 海图前哨 UI + 多前哨链（「全做 2」session 第一笔；作者纠正：未发布不迁移≠不持久化，衰减照常存为 additive 字段不 bump）**：接 Phase 2a 脊柱（#66）。**① 能源**＝派生（不碰存档形状）：新 4 个 `LighthouseEffect`（energyGen/energyDraw/rechargeBonus/oxygenSupply）+ 新 `engine/outposts.ts::outpostEnergy`（容量＝base 1 + 水力〔仅 current 前哨〕− 衰减；按 draw 累加判在线，超容量设施掉线）。静水前哨 base 1 只够 1 个补给设施；水流前哨建水力 → 容量足 → 更多在线（§3.6 选址权衡）。**② 衰减**＝新 additive `profile.outpostState{maintainedRun}`（shopStock 套路·seed `{}`+`?? {}` 兜底·**SAVE_VERSION 仍 4**）：`outpostDecayLevel`＝(runsCompleted−maintainedRun)×速率（水流 ×2）、封顶 4；后果 = 容量缩（补给掉线＝变暗）+ `effectiveOutpostStage` 回退（重度衰减 < USABLE → 蛙跳失效）；`maintainOutpost`（re-ferry 小账单重置计时）；`advanceOutpost` 每阶顺手重置衰减。补给设施在线时经 `effectiveOutpostBonuses` 桥进 `startDiveFromOutpost`（充电→powerMax / 充氧→oxygenMax）。**边界**：衰减/能源只在蛙跳出潜层兑现，**不**碰 chart.ts reveal（真·reveal dimming 留后续、护住 reveal 回归）。**③ UI**：`SeaChartView` 新 `OutpostPanel`（建造/维护/能源/衰减/半亮状态，advanceOutpost·maintainOutpost 直通）+ `LighthouseBuildPanel` 加 outpostOnly/currentOnly 轨门控（充电/制氧只在前哨、水力只在水流前哨；家/废墟灯塔隐藏）+ smoke N/M2-M4。**④ 多前哨链**：新 `outpost.trench_deep`（band.trench_throat·submerged+current·深料账单）服务 abyssal/hadal 蛙跳（#66 模板：OutpostDef + 建造事件 `lighthouse.outpost_trench_deep`〔midnight tag 隔离〕）。`playthrough-outpost` §6-10。详见 quirk #67 + SPEC §3.6/§5。
> **2026-06-04 深水区内容 pass · 超渊 band（B）>140m + hadal 专属事件「连更深/上下都不再是连续的线」（「全做 2」session 第二笔，续 #65 递归更深）**：深渊（>108m·#65）之下再开 `band.hadal`（140-180m·dark·alertFactor 2.5·`tags:[cave,hadal]`·**新增 ZoneTag `hadal`**〔twilight/midnight/abyssal 已用尽〕·借蓝洞 mapgen·**架构不硬编码地板**）。新 `data/events/hadal.json`（4 事件 import 进 zones.ts）：母题从深渊「『底』在退/拽/招」推进到「连『更深』『上』『下』都不再是一条连续的线」——水里的缝 `the_seam`（缝那边是另一套对不上的数）/ 没有上的上 `no_up`（每条上浮路都通向更深）/ 对不上的读数 `the_reading`（表报熟悉的浅水数·骗你以为快到家）/ 软的底 `the_soft_floor`（会呼吸·回看你的『底』＝apex/mimic/corpse-wearer 伏笔，不交底）。全 loot-free/无敌人（守 2/zone·apex 仍 Phase 3 例外）/不触发 d_reveal/永不交底，每事件留诚实但盲/退出口。新 `lore.hadal.*`。7 scenarios（#43 实跑抄）+ `playthrough-bands` §11。事件 93→98、baseline 98→105。详见 quirk #68 + SPEC §12。
> **2026-06-04 深水区 Phase 3 mimic capstone（C）· 海图假 POI 引诱 → 入潜兑现 → d_reveal（「全做 2」session 第三笔；作者在场逐拍 AskUserQuestion 三 pin：d_reveal 活下来即触发·保持暧昧 / 回报 lore+d_reveal+unlock·无 loot / 『另一个世界』先留钩子不展开）**：北极星 capstone 落地，回收浅/中/深 + trench/abyssal 全部伏笔。**关键设计：两只 apex（mimic / corpse-wearer）做成 EVENT 而非战斗敌人**——deception 的恐怖不靠 slugfest、且守『敌人别太多』（spoofsSonar/evadesSonar 节点钩子留给未来节点版）。**① 引诱（宏观）**：`ChartPoi.mimic?` + `chart.ts` 注入「无灯之光」假 POI（`isPoiLit` 恒真＝诱饵 / 新 `isPoiExplainedByLighthouse` 恒假＝宏观 tell「不在你网里」）；软门控 `shouldLureMimic`（任一水下前哨 ≥ 半亮才被引诱、不带硬 flag、可复诱）。**② 兑现（入潜）**：`startDiveFromPoi(mimic)` 强制把开场设成兑现事件（weight 0、不入池、不可错过、事件自身 forceAscend 收尾＝一次性 capstone）。**③ 事件**：新 `data/events/mimic.json`——`mimic.false_beacon`（读穿 tell〔光不落在任何东西上/不反光/扰流不对〕理智 vs 62·**低 san 更难读＝tell↔sanity 耦合**；读穿+够强活着离开 → 新 outcome `setProfileFlags`[flag.d_reveal,flag.mimic.false_beacon.survived] + lore.deep_water.the_false_beacon + forceAscend；读错/盲信/拒看 → 活着但惨重·**不置 d_reveal·保持暧昧**·无脚本死）+ `mimic.the_wearer_apex`（corpse-wearer 姊妹 apex·深渊 organic·回收 the_other→no_bubbles→the_wearer·读穿给 lore·**不置 d_reveal**〔只由 mimic 触发，作者定〕）。**④ d_reveal 机制**：新 `Outcome.setProfileFlags`（持久写 profile.flags、≠ applyFlags 的 run 局部）→ FuneralView/CorpseView 既有 renderDiverName 翻死者名为「你」（#42 钩子终于接上、保持暧昧）。**⑤ UI**：SeaChartView mimic POI 宏观 tell。全 loot-free / 无战斗 / 叙述永不交底（#54）。新 `playthrough-mimic.ts`（6 节）+ 5 scenarios。事件 98→100、baseline 105→110。详见 quirk #69 + SPEC §3.5/§5/§10。**『另一个世界』(§3.7) 仍留专门 session 逐拍；spoofsSonar/evadesSonar 节点版 mimic 留后续。**
> **2026-06-04（post-session UX 修·玩家反馈）· 单向下潜预告**：玩家在开阔水域过了上浮口后发现回不了头、被打措手不及。**确认逻辑（两套机制正交，types/dive.ts 已注明）**：拓扑由 `ZoneDef.mapShape` 决定——层状（reef/wreck/东礁，`connectsTo` 只向下）＝**单向下潜不可回头**（走过的节点不再是选项·非「被挡」是「不在选项里」）/ 迷路图（蓝洞群 + 借它的 trench/abyssal/hadal band，`mapShape:'maze'`·connectsTo 对称）＝双向可回头；上浮语义由 `canFreeAscend` 单独控制（开阔水域随处可「此处上浮」/ 蓝洞仅 2 个上浮口）。`enterNodeSelection` 从不过滤选项（visited 节点也照列，只是 flagged）——单向纯因层状图无回边。**机制保留**（单向＝press-your-luck 张力·避免冗长回溯·dive 长度有界），**补预告消除惊吓**：新 `engine/zones.ts::zoneAllowsBacktrack(zoneId)`（`mapShape==='maze'`）+ `NodeSelectView` 层状 zone 持久提示「这一带的水路只往下通——走过的地方，回不去了」（迷路图不提示·免在能回头的 zone 误导）+ smoke E3（层状给/迷路不给）。全绿。**留意：洞穴（canFreeAscend false）里 NodeSelectView 仍无条件渲染「此处上浮」按钮、点了走到 AscentView 被 isAscentBlocked 挡——是个独立的小 UX wart（展示了点了没用的按钮），未在本次修，留后续。**
> **2026-06-05 声呐探索 + 多事件房间 S0（新功能·`docs/深海回响_声呐与房间_SPEC.md` §7 S0；作者方向「A→C→D 全做」第一笔，quirk #71）**：把下潜内声呐从「逐选项预览」升成探索性的洞穴声呐扫描。**只读真图、不改 DiveNode 模型**（欺骗 spoofs/evades 留 S2）。① **抽公共布局推导** `ui/mapLayout.ts::deriveMapLayout`（节点图→2D 坐标单一来源，`MapDevPanel` 改用、观感不变——节点没存坐标，此前面板内联铺点 #SPEC§4）；② 新 `engine/sonar.ts`：`revealSonarScan`（从当前节点无向 BFS 揭示有限程内真实节点）+ `scanFreshness`（余像按 turn 渐隐）+ `sonarScanRange`（基线 2 跳·**范围是主升级轴**·留后续；双上限 <最深 <全洞）；③ 新 `run.scanMemory`（nodeId→扫到时 turn·累积·run 级·`?? {}` 兜底·**不 bump SAVE_VERSION**）；④ `pingSonar` 写 scanMemory + **当场抬 alert**（`clarity.ts::sonarPingAlertDelta`：浅水免压×`alertDepthFactor`·深 band ×`bandAlertFactor`·clamp）+ **1 scan/停留 guard**（已 ping 未移动→no-op，移动后 sonar 归 off 才能再扫）；⑤ 新 `ui/SonarScanPanel`：起手全黑、随 ping 一块块点亮、**缩放只看身边一小片** + **残图小地图**给方位感、固定亮度按 turn 渐隐余像（**重复 ping 不更亮**）、地标(↑出口/气穴/扎营)标形；**仅声呐解锁后出现**（软门控 #58）。新 `playthrough-sonar`(9 节) + smoke E4。全绿（typecheck/21 playthrough/119 scenarios/verify/smoke）+ prod build。提交 `f57b17e`。**S1（多事件房间 `DiveNode.features?`）/ S2（不可信·填 spoofs/evades + 低 san 伪接触/乱码）/ S3（威胁定位 blip→stalker）+ §6.5 宏观灯塔扫描揭示动画 留后续。**
> **2026-06-05 深水区内容·渊外 band（>180m）+ subhadal 专属事件（方向 C；续 #63/#65/#68 递归更深机制开最深一层，quirk #72）**：超渊（hadal 140-180·#68）之下再开 `band.subhadal`（180-230·dark·`alertFactor 3.0`·`tags:[cave,subhadal]`·借蓝洞 mapgen），**新 ZoneTag `subhadal`**（twilight/midnight/abyssal/hadal 已用尽，types/events.ts 加）。母题从 hadal『上下不是连续的线』推进到**『过了最后一个有名字的深度，它不再骗你——只给你下去的理由』**（深处的诱饵）。`data/events/subhadal.json` 4 事件（import 进 zones.ts EVENT_DB）：`the_offer`（在你想放弃那刻递来恰好需要的落脚/暖水＝诱饵）/ `further_down`（底总退一点点·『就一点』递归骗你下去＝『永远有比最深更深的』最紧表达）/ `the_low_light`（底下一盏像家/像你灯的暖光＝**mimic 最深伏笔**·承 deeper_light/false_beacon）/ `no_answer`（ping 不再有任何回波·连谎都不给＝**S0/S2 声呐欺骗收束**）。守则同深段：全 loot-free/无敌人（mimic·corpse-wearer 仍 Phase 3 apex 例外·本层只埋）/**不触发 d_reveal**(#42)/**永不交底**(#54)/每事件留关灯关声呐的诚实但盲/退出口；新 `lore.subhadal.*`。经 `DepthBand.tags` 覆盖 zoneTagsByDepth 隔离事件池、从 trench_deep 前哨蛙跳软门控可达（无需新前哨）。9 scenarios（event-runner --out json 实跑抄 #43）+ `playthrough-bands` §12（130 抽出/0 泄漏·hadal/cave 双隔离）。events 100→104、scenarios 110→119、敌人 7/combat 8 不变。全绿 + prod build。提交 `c4df28b`。
> **2026-06-05 深水区 Phase 2b·超渊前哨（hadal_deep）——前哨脊柱延到 band.hadal、让渊外蛙跳可达（方向 D·#66/#67 模板，quirk #73）**：`lighthouse_upgrades.json::outposts[]` 加 `outpost.hadal_deep`（`band.hadal`·submerged **静水**〔无 current → 靠 base 能源 1·最深的基地养不起几盏补给〕·3 阶段深料账单 beak/eel/brass/lantern_gland 升序更贵·result『超渊前哨』）+ `lighthouse.json` 建造事件 `lighthouse.outpost_hadal_deep`（`zoneTags:[hadal]` 专属·visibleIf 三阶 flag 门控·`forbiddenFlags:[s3]`·`advanceOutpostId`）。**引擎全通用零改**（advanceOutpost/deepestOutpostLaunch/outposts 能源衰减）：进度走 profile.flags、点亮 push 灯塔、**SAVE_VERSION 仍 4**。脊柱 **home→reef_deep→trench_deep→hadal_deep**：hadal_deep 半亮把 C 开的渊外(subhadal>180m)蛙跳预耗氧从 home **9 回合降到 1**，软门控（料即门）、不服务更浅 band（前哨须更浅·deepestOutpostLaunch）。`playthrough-outpost` §11（三前哨选最深起跳）+ smoke N1（超渊前哨）。events 104→105。全绿 + prod build。提交 `d8b1e72`。**Phase 2b 仍可续：真 reveal dimming（衰减接 chart.ts 半径缩·需 reveal 回归一起改）/ 寄存材料设施 + 丢失后果 / 平衡 pass（tunables 在 engine/outposts.ts 顶 + lighthouse_upgrades.json）。**
> **2026-06-05「A+C+D 全做·打磨到高完成度」session A·声呐与房间 S1 多事件房间（声呐与房间 SPEC §6/§7 S1·#74）**：把下潜「一节点一事件」升成「房间可含多个事件点」。**向后兼容·零存档形状改动**：`DiveNode.features?: NodeFeature[]`（缺省＝走旧 `eventId` 单事件路径＝旧图逐字节不变）；多 feature 房间到达**不自动触发**——`moveToNode` 路由到 `enterNodeSelection`，把房内**未探** feature ＋ 出口并列摆出（新 `FeatureChoice` + `subPhase.features`），`exploreFeature` 选一处＝付氧(`FEATURE_EXPLORE_TURNS`=1)＋触发其事件＋标记已探（`run.activeFlags` 的 `feat:<nid>:<fid>`、run 级不入存档形状、不 bump SAVE_VERSION），一房可连探付氧、选出口走人，探完只剩出口。mapgen `maybeMultiFeatureRoom`（**仅 `maxRoomFeatures>1` 才进＝缺省零额外 rng＝旧 mapgen 快照不破**）按 `rollExtraFeatures` 偶尔把事件房间升级成 2–3 feature「大房间」（大房间稀有·`excludeIds` 同房去重·守 #19 单 tag/#44/#47 loot 隔离）；门控＝`DepthBand.maxRoomFeatures`（深 band trench/throat=2·abyssal/hadal/subhadal=3·reef_deep 等不设＝1），透传 `startDiveFromOutpost→startDive→mapgen`。`SonarScanPanel` 把多 feature 房间画成大轮廓(is-room)＋里头几颗 feature blip（仍只读真图）。回归：`playthrough-sonar` §10（房间菜单/连探付氧/已探过滤/进房路由/向后兼容/探完清空）＋ `playthrough-mapgen-scenarios` 多事件房间不变量扫（60 seed·192 大房间/全 2–3/同房不重复/确定性）＋ smoke E5。全绿。**S2（填 `spoofsSonar`/`evadesSonar` 不可信扫描·与节点版 mimic 合流）/ S3（威胁定位）/ §6.5（宏观灯塔扫描揭示动画）仍留后续。**
> **同 session C·深段多 feature 房间内容（#75）**：给深 band 补料喂 S1 大房间——abyssal/hadal/subhadal 各 **+2 事件**（5→7 / 4→6 / 4→6），守各 band 欺骗母题、全 loot-free/无敌人/不触发 d_reveal/永不交底/单 band tag/每事件留关灯关声呐的诚实盲出口、3 选项（给入·理智 check·盲退）：abyssal `the_company`〈同行的下沉影子〉＋`lower_still`〈探在虚空上的假底檐·『永远有比最深更深的』〉/ hadal `the_fold`〈直线绕回原点更深·空间折叠〉＋`the_return_path`〈自己的记号正确地把你往深领〉/ subhadal `the_current`〈舒服的下降暖流〉＋`the_quiet_yes`〈它不必骗你·你自己同意·『诱饵』收束最深一拍〉。新 `lore.{abyssal,hadal,subhadal}.*`；12 scenarios（`event-runner --out json` 实跑抄 #43·success+fail 各档）。内容事件 **105→111**、scenarios **119→131**；敌人仍 7。
> **同 session D·Phase 2b 收尾·真 reveal dimming（深水区 SPEC §3.6·#76）**：补上 #67 明确 deferred 的「衰减不碰 chart.ts reveal」缺口——前哨灯塔的海图点亮半径随衰减线性收缩（新 `engine/outposts.ts::effectiveRevealRadius` ＝ `revealRadius × (1 − decay/MAX × OUTPOST_REVEAL_DECAY_SHRINK[0.5])`，满衰减缩到半径一半·**永不归零**；home/废墟/水上灯塔无衰减＝原样）。`chart.ts::isLit` 与 `SeaChartView` 光圈都改用它＝久不维护的前哨在海图上「变暗」、它点亮的远海机会点重新隐没（须 re-ferry 维护补回＝闭合衰减↔海图回路）。**decay-0 逐字节不变＝既有 reveal 回归全绿**。回归：`playthrough-outpost` §11（零衰减=原始/满衰减缩半/单调/home 不受影响/远点 `isPoiVisible` 重新隐没）＋ `playthrough-chart`/`-lighthouse`/`-lighthouse-scenarios`/smoke 全绿。**Phase 2b 仍可续：寄存材料设施+丢失后果 / 平衡 pass。** 提交见 [[sandbox-git-commit]]。
> **2026-06-05 声呐与房间 S2·不可信扫描（声呐与房间 SPEC §7 S2·方向 A「然后确保这次完成它」·#78）**：把声呐扫描从 S0「只读真图」升成**会撒谎的扫描**，欺骗逻辑收在 `clarity.ts` 一处、`SonarScanPanel` 纯渲染（别在面板加分支，§7/§10）。三种不可信改写（你分辨不了是哪个、且不该一眼看穿＝§5「要 subtle」）：① **spoofsSonar→假信标**（节点版 mimic「无灯之光」·`clarity.ts::nodeSonarView.displayKind` 把节点在声呐图画成一道朝上的出口/上浮口·与 #69 海图 mimic 合流·**不触发 d_reveal**只由 mimic 兑现事件触发 #42）；② **evadesSonar→无回波**（`noEcho`·声呐图不画该 blip＝捕食者躲过你的 ping·留一处空缺）；③ **低 san→读数乱码（`is-garbled`·深度等仪表字损坏成不可读·约三成·按 `run.turn` 变＝你的脑子实时崩）＋伪接触（`clarity.ts::sonarPhantoms`·与真接触一模一样的幻影 blip·锚在真实接触附近随其余像渐隐·下一记 ping 换一批）**。**深 band 更易骗**＝新 `DepthBand.sonarDeception?`（0..1·落 `run.sonarDeception`）抬高 `clarity.ts::effectiveFalseEchoSanity`（base + dec×`SONAR_BAND_DISTORTION_SCALE`50·封顶 `SONAR_FALSE_ECHO_SANITY_BAND_MAX`88＝高 san 仍留一线可信）·**非单调**：throat 0.2→abyssal 0.28→hadal 0.32 越深越骗、到 subhadal **回落 0.06**＝兑现『那些骗你的把戏都停了』（越深越欺骗的梯度在渊外反转成诱饵·reef_deep/trench_mouth 不设＝浅段相对老实）。**节点版 mimic 落点**＝mapgen 新 `applySonarDeception`（**确定性 FNV 哈希·零 rng**·绝不移动任何 seed 的生成顺序＝旧图/深 band 快照 rng 流逐字节不变·只多挂派生字段；仅 `sonarDeception>0` 才进＝缺省零改动；地标/起点/尸体豁免守 #36；放在 layered/maze 分流之后两拓扑共用）给部分内部节点钉 spoofs/evades。**缺省（浅水/POI/旧档·`run.sonarDeception` undefined）→ effectiveFalseEchoSanity 恰好回退升级基线＝零行为变化**（守 sensors 回归·`sonarReturn` 既有 spoof/evade/低 san 文本分支不破）。plumb 沿 #64 `bandAlertFactor` 模板：depth_bands.json → `DepthBand.sonarDeception` → `startDiveFromOutpost` 落 run + 透传 `startDive→mapgen GenOpts`。**未发布不 bump SAVE_VERSION**（run.sonarDeception 派生·JSON round-trip+`?? 0` 兜底）。回归：`playthrough-sonar` §11（spoof/evade 表象·失真阈值封顶/回落·低 san 伪接触/乱码·mapgen 欺骗 pass 门控/豁免/确定性）+ `playthrough-mapgen-scenarios` 不可信声呐失真不变量扫（60 seed·185 欺骗节点·只挂内部节点·门控缺省零改动·确定性·不破迷路不变量·**192 大房间数不变＝零 rng 印证**）+ smoke E6（spoof 假信标 is-spoof+↑/evade 无回波/低 san 乱码+伪接触/高 san 控制组）。全绿（25/25·5.9s）+ prod build。**S3 威胁定位（alert→接触 blip→定位 stalker）+ §6.5 宏观灯塔扫描揭示动画 + 声呐范围升级轴/房间 feature 数升级 仍留后续；节点版 mimic 已与 #69 合流。** 提交见 [[sandbox-git-commit]]。
> **2026-06-05 周末内容 pass · trench 加 4 事件「回波对不上」续（续 #63；周末内容引擎自动无人值守 session）**：trench band（60-108m·twilight/midnight 各 maxRoomFeatures=2）是**最薄的深 band 子池**（此前各 3 事件、易被多 feature 大房间〔#74〕抽干），且上次内容 pass（#75）只补 abyssal/hadal/subhadal、跳过 trench → 本次专补 trench，twilight/midnight 各 +2、tone 均衡（twilight c2/u3、midnight c3/u2）。母题守『回波对不上』（声呐返回 ≠ 点灯/伸手的真相），4 个都是此前 6 个〔the_return/second_diver/the_sounding/the_wall/the_answer/no_echo〕未覆盖的**全新 ping-欺骗种类**、互不重复：`trench.the_moving_floor`（cosmic·twilight·oncePerRun·声呐扫出的『海底』其实是一层灯笼鱼＝**DSL / 海军当年的『幻影海底』**，声呐把活物读成实底；灵感＝deep scattering layer / phantom bottom）/ `trench.the_old_ping`（uncanny·twilight·给放弃了的一记声呐过很久才回、且变老变糊＝**时间/距离不对**；灵感＝SOFAR 声道低频绕远路过很久才漏回来）/ `trench.the_remembered`（cosmic·midnight·oncePerRun·一路 ping 攒出的图是另一条更宽更多岔的竖井＝**累积扫描的图本身是错的**，勾连 S0 声呐扫描余像 scanMemory #71）/ `trench.the_opening`（uncanny·midnight·声呐在石壁上让出一道灯里并不存在的下行口＝**the_wall〔假墙〕的反面**：声呐减一道障、引你钻进石头）。全 `[twilight]`/`[midnight]` 单 band tag（#19·**绝不挂 cave 免泄漏进普通蓝洞池**·playthrough-bands §8 已验 trench 抽出且 0 泄漏）、loot-free、**无新敌人**（守 2/zone·mimic·corpse-wearer 仍 Phase 3 apex）、**不触发 d_reveal**（#42）、**叙述永不交底**（#54：理性解释〔声学/生物学〕与错读始终叠着、游戏拒绝裁决）、每事件留一条『关声呐/摸黑』诚实但盲的出口、3 选项（信声呐给入 / 理智 check 读穿 / 关声呐摸黑），check 沿 trench 惯例（cosmic 理智 vs55 / uncanny 理智 vs50）、deltas/oxygenTurnCost 对照现有 trench 事件（success san -4~-5·give-in san -6·cosmic fail san -13·oxygenTurnCost 2/3/5）。新 `lore.trench.{the_moving_floor,the_long_water,the_remembered,the_opening}`。10 新 `scenarios/trench_*.json`（`event-runner --out json` 实跑抄 #43：每事件 success/give-in，两个 cosmic 旗舰另加 fail〔sanity=22→rate0.05·seed1 确定性失败〕）。事件 111→115、event baseline 131→141（trench scenarios 12→22）、敌人 7/combat 8 不变。全绿（`npm run regress` 25/25·7s）+ prod build。**不 commit**（无人值守·改动留用户审阅）。详见 quirk #77。
> **2026-06-05 深水区 Phase 2b 续·彻底完成 D（材料中转/寄存 + 寄存丢失 + 深渊前哨 abyssal_deep + 平衡 pass · 方向 D「回归文化有 schedule 在跑·这次把 D 彻底完成」·#79）**：作者定向 D、要求三项全做。① **材料中转/寄存设施（SPEC §3.6「材料中转/寄存」+「寄存材料丢失」终于落地，补 #67/#76 deferred 的最后一块）**——深水前哨建「材料中转站」(`lhtrack.depot`·outpostOnly·新 `LighthouseEffect.storageCapacity`·lv1 容量 6/lv2 +6=12·**不耗能源**＝被动库房恒在线、避免「能源不够→取不到存料」双罚) 后可把材料**就近寄存在前哨**：维护（`maintainOutpost`）从寄存就近取料则**免去 ferry 金费**（前哨上有料·不必雇船运下来＝把料前置到深处的回报·home 没钱没料也维护得起）。寄存随前哨荒废**锈蚀流失**（`effectiveStored`＝raw − `depotDecayLevel`×`DEPOT_LOSS_PER_LEVEL`1·**独立 `storedRun` 计时**与结构 `maintainedRun` 解耦＝建造一阶只重置结构、存/取/维护才「打理」寄存·激流前哨锈得更快·满 4 级最多 4 单位·**非永久全损**可重存补回守 §3.6），损耗 **derive-only**（同 `effectiveRevealRadius` 风格·提交只在玩家动作时·烤入损耗后不复活）。存档：`outpostState` 条目扩成 `{ maintainedRun, stored?: MaterialCost[], storedRun? }`（additive·JSON-native 无 Set·`?? 默认` 兜底·**不 bump SAVE_VERSION**·`playthrough-save` round-trip）。**架构守单向依赖**（`outposts.ts→lighthouses.ts` 不可循环）：寄存逻辑全在 `outposts.ts`，`advanceOutpost`（在 lighthouses.ts）只**保留** stored/storedRun 字段（spread 既有条目·不丢寄存）＝**无寄存活动的前哨仍写 `{ maintainedRun }` 单字段·既有 -outpost/-save 回归逐字节不变**。UI＝`SeaChartView::OutpostPanel` 加寄存区（建了中转站才显示·有效库存/容量/锈蚀·存/取按钮·`depositables`＝维护材料∪下一阶段材料）+ `styles.css` 两排按钮排版 + smoke M5/N3/N4。② **深渊前哨 `abyssal_deep`（补脊柱 abyssal 缺口·#66/#73 模板·引擎零改）**——`lighthouse_upgrades.json::outposts[]` 加 `outpost.abyssal_deep`(`band.abyssal`·submerged **静水**·3 阶深料账单 110/180/290 金·料介于 trench_deep 与 hadal_deep) + 建造事件 `lighthouse.outpost_abyssal_deep`(`zoneTags:[abyssal]`·三阶 flag 门控·`forbiddenFlags:[s3]`·`advanceOutpostId`)。脊柱补全 **home→reef_deep→trench_deep→abyssal_deep→hadal_deep**：abyssal_deep 半亮把 hadal 蛙跳预耗氧从 home **7→1 回合**、也缩短 subhadal、不服务更浅 band（蛙跳点须更浅）。事件 115→116。③ **平衡 pass**——`engine/outposts.ts` 顶加「平衡基准」复核块（前哨建造账单单调阶梯 reef Σ280<trench Σ480<**abyssal Σ580**<hadal Σ660·衰减节奏·寄存经济）；钉新寄存/abyssal 值进阶梯，**复核**既有衰减/能源/reveal-dimming(#67/#76)+S2 声呐失真梯度(#78·非单调)+mapgen rollExtraFeatures(#74)均自洽**未改**（无实测不擅动作者已调值）。回归 `playthrough-outpost` §0 脊柱自洽/§12 寄存（存/取/容量/损耗派生·提交不复活/维护免金费/空站走岸上金费/建造保留寄存）/§13 abyssal 蛙跳 + `playthrough-save` outpostState round-trip + smoke M5/N1/N3/N4。全绿（`npm run regress` 25/25·6.5s）+ prod build。**Phase 2b 至此三齿（变暗/阶段回退/寄存丢失）全＝§3.6 闭环；仍可续：寄存丢失的更狠后果（现锈蚀封顶 4 单位）/ reef 第二前哨（侧向覆盖）/ 维护账单随前哨深度分级（现 flat）。** 提交见 [[sandbox-git-commit]]。
> **2026-06-06「彻底完成 A」session·声呐与房间 续（S3 威胁定位 + §6.5 宏观灯塔扫描 + 声呐范围升级轴 · 方向 A·#80）**：作者定向 A、三项全做。① **声呐范围升级轴（声呐与房间 §8.1·S0 留的「主升级轴」终于接上）**——新 `sonarScanRangeBonus` 经 `UpgradeEffect→getUpgradeBonuses→getRunBonuses→createNewRun→deriveSensorTuning→SensorTuning.sonarScanRange`，`sonar.ts::sonarScanRange(run)` 从读基线常量改成读 `run.sensorTuning?.sonarScanRange ?? SONAR_SCAN_RANGE`（基线仍 2·缺省回退＝S0 行为逐字节不变）；新 `SONAR_SCAN_RANGE_MAX=4`＝**双上限**（< 全洞 + < 最深·deriveSensorTuning 夹紧·守北极星「再升也扫不穿整洞/照不到最深处·最深仍得自己摸黑」）；data 加 `line.sonar_rig` 续 `sonar.lv4`（宽频阵列·+1 跳·eel×3·beak×3·240 金）/`lv5`（相控阵·+1 跳→满 4·lantern×2·beak×3·320 金，金阶梯 120→160→200→240→320 单调）；UpgradePanel 加「声呐扫得更广」效果标签。② **S3 威胁定位（廉价版·§7 S3 上半·`clarity.ts::threatContact(run)` 单一来源·面板纯渲染）**——把 Phase 0b 一直抽象的 `run.alert` 做成声呐图上一处**近似接触**：alert<预警线(ALERT_WARN 35)→null；越线→`{angle(按 turn 漂移·定不住),proximity(随 alert 涨·blip 越逼近你越靠中心),range:远/中/近,imminent(≥ALERT_THRESHOLD 60),garbled(低 san 距离读不出)}`，廉价版**不锚到节点**（确定性 FNV·不耗 RNG·SSR 安全）。`SonarScanPanel` 画琥珀 `.sonar-threat` blip（imminent→`.is-near` 偏红脉动）+ 粗距标（远/中/近·garbled→`?`）。**与低 san 伪接触(`sonarPhantoms`)分两轴**：威胁＝alert 驱动（真危险·高 san 也在·琥珀）vs 伪接触＝san 驱动（你脑子·san 回满即消·cyan），守北极星「世界坏了还是你疯了」。**定位 stalker（图上占位·逐回合逼近·可 evadesSonar 躲扫描）是更大改动·§8.7 留作者拍板**（本次只做廉价 blip）。③ **§6.5 宏观灯塔扫描**——(a) **海况派生**：新 `chart.ts::chartConditions(profile)`→`SeaChart.conditions{tide:涨/退潮,weather:晴/薄雾/浓雾}`（确定性 FNV on runsCompleted·**不入存档**·不碰 roaming LCG·分布晴~55/雾~27/浓雾~18）；**浓雾遮蔽一处 roaming 机会点**（确定性挑·随 runsCompleted 变＝下次回港潮退又回来·**锚点/mimic 永不遮**＝守进度安全、海面相对可信、欺骗留微观#6.5/§10）；SeaChartView 表头渲染海况一行。(b) **测绘扫描揭示动画（纯 UI·engine reveal 集合不变）**：灯塔 `.chart-survey-sweep` 慢暖 sweep（3.2s·ease-out·key 绑 `builtUpgrades.size` → 建造/点亮时重挂＝重播＝点亮回报演出·≠ 旋转探照灯）+ POI `.chart-poi-arrive` 错峰淡入（按序号 delay＝随波逐个浮现/「尚未扫到」·新 run 新机会点重挂重播·锚点不重播）。回归：`playthrough-sonar` §2 扩（范围升级路径·+1/升满夹上限/揭示更多节点）+ 新 §12（threatContact 预警线门/逼近度/imminent/低 san garbled/确定性）；`playthrough-chart` §3 改从 `chartConditions` 派生期望 roaming 数（seed 无关·robust）+ 新 §7（海况确定性/随回合变/浓雾遮一处/锚点 4 不受影响）；`playthrough-upgrades` §8 扩买 sonar lv4/lv5（断言 `sonarScanRangeBonus=2`·run.sensorTuning.sonarScanRange=上限）；`playthrough-save` bonuses 加 `sonarScanRangeBonus`；smoke E7（威胁 blip·高 alert→is-near/低 alert→无）+ J6（sonar lv4/lv5 标签）+ L（survey-sweep/poi-arrive/海况）。全绿（`npm run regress` 25/25·8s）+ prod build。**SAVE_VERSION 仍 4**（SensorTuning 加字段·只 deriveSensorTuning 构造·`?? 基线` 兜底；SeaChart 不入存档）。**A 三件落地；仍留：S3 定位 stalker（§8.7 作者拍板）/ §6.5 即时新 POI 浮现（现新 POI 下个 run 才进·避免 roaming 重洗）/ 定向 ping（§5·暴露按方向计·新机制）/ 房间 feature 数升级。** 提交见 [[sandbox-git-commit]]。
> **2026-06-06 周末内容 pass · 最深三 band（abyssal/hadal/subhadal）各 +4 事件「越深越离奇」（周末内容引擎自动无人值守 session·续 #65/#68/#72/#75·#81）**：按『越深越优先』专补**最深也最薄**的三 band（abyssal 7/hadal 6/subhadal 6），**跳过刚补过的 trench（#77）免母题撞档**，各 **+4**、全 12 个为各 band 现有未覆盖的全新欺骗/诱饵种类。灵感＝WebSearch 三口新井：① xenophyophore（巨型单细胞在 hadal 底拿沉积垒『屋子』）② HPNS 高压神经综合征（>200m 自发震颤/幻觉/认知崩＝深段『是世界坏了还是你疯了』的**真实病理锚**）③ 失温反常脱衣＋终末钻穴（发烫的暖与正好你大小的庇护＝送命的诱饵）。**abyssal『永远有更深』**：`the_houses`〈单细胞垒的屋子成行往深处挤·口朝你·uncanny〉/`the_guideline`〈绷着往下的导引绳·『顺线走』训练反被利用·uncanny〉/`the_fall`〈鲸落：连这么大的死都还在往下、成下一样东西下潜的理由·cosmic〉/`the_settling`〈海雪密到落定·停久了被盖成底·cosmic〉。**hadal『上下/连续断裂』**：`two_readings`〈主备表读数差一半·挑哪块往上·uncanny〉/`the_still_water`〈绝对静水无参照·上下前都不作数·uncanny〉/`the_long_minute`〈潜水表与耗气对不上·时间也断了·cosmic〉/`the_shaking`〈HPNS 手自抖·身体连续性也断·cosmic〉。**subhadal『诱饵·不再骗你』**：`the_sound_of_home`〈从下面传来港口钟/海鸥/船响·no_answer 反面·uncanny〉/`the_warmth`〈失温的暖诱你解封·身子最后一个谎·cosmic〉/`the_small_room`〈正好你大小的龛诱你蜷进去歇下·终末钻穴·uncanny〉/`the_kept_air`〈气压针不掉了·像深处替你免单·拿掉唯一送你上去的限·cosmic〉。守则全同深段（#54 永不交底〔每 check-success 给平淡解释「道理都对」、give-in/fail 落错读·两读始终叠着〕·#19 单 band tag·#42 不触发 d_reveal·loot-free·无新敌人〔守 2/zone·mimic·corpse-wearer 仍 Phase 3 apex 例外·只埋伏笔不登场〕·3 选项〔信/给入 → 理智 check 读穿〔cosmic vs60·uncanny vs58〕→ 灭灯关声呐盲退·留最低 signature 诚实出口·无脚本死〕）。新 `lore.{abyssal,hadal,subhadal}.*` 各 4 条。36 新 scenarios（每事件 give-in+check success+check fail·`event-runner --out json` 实跑抄 #43·全 seed 1·fail 用 `--sanity 22`→rate 0.05 clamp 确定性失败）。事件 116→128〔event-runner --list〕、event baseline 141→177、敌人 7/combat 8/item 22 不变。`npm run regress` **25/25 全绿（6.3s）+ prod build**。**未 commit**（无人值守·改动留用户审阅）。**下一步候选：trench 仍可再补 / reef·蓝洞·墓园浅中段 realistic 密度轮换 / S3 定位 stalker（§8.7 作者拍板）；别加第三只常规敌人、别触发 d_reveal。**
> **2026-06-06 周末内容 pass · 40-60m 深段「灯的边缘」质感补完（周末内容引擎自动无人值守 session·轮换离开连补四 band·#82）**：近三次内容 pass（#75/#77/#81）连补最深四 band（trench/abyssal/hadal/subhadal 现各 10-11 事件、已饱和），而 established 深段（`cave.*`/`wreck.*` 40-60m）自 2026-06-02（#57）起 **4 天未动** → 本次按「每次换 zone/深度/tone 侧重」**轮换回 established 深段**，并守「越深越优先」专挑其**最深、最薄**的 40-60m 段（不与刚补的深 band 撞档）。**recon**（`event-runner --list --zone-tag wreck/cave` 数 40-60m tone）：**[wreck] 40-60m 深池最薄（仅 5 内容事件·2C/2R/1U）、[cave] 40-60m uncanny 也薄（2U）**——两深池都偏 cosmic-heavy、缺 realistic/uncanny（cosmic 强度已由深 band 充分供给，故本批**精确补 R/U**）。**10 新事件**（append 进 `reef.json`·全 `wreck.*`/`cave.*` 既有跨 zone 前缀·**零 registrar 改动**）：**wreck 深段 5**〔`boiler_room`(R·锅炉舱轮机打捞)/`shaft_alley`(R·推进轴窄隧)/`trapped_air`(U·**Harrison Okene 沉船气穴**·oncePerRun·`breathe_it` 吸陈气 **+6 O2/−5 San** 拿氧换理智机制)/`wardroom`(U·四十年没散的一桌饭)/`careened`(U·侧翻船·上下成谜的迷向)〕+ **cave 深段 5**〔`silt_out`(U·**silt-out/percolation** 零能见·touch-contact 摸壁生还)/`brine_pool`(C·**深海卤水池/Jacuzzi of Despair**『洞底的湖』有岸有浪·oncePerRun·**本批唯一 cosmic**)/`restriction`(R·**no-mount restriction** 卸瓶过卡口)/`the_arrows`(U·永久导引绳上对不上的方向箭头)/`flowstone`(R·干洞期长成、如今淹在 50m 的石幔厅)〕。**tone 4R/5U/1C**（精确填 40-60m 实缺口·非凑等分；深 band 已供足 cosmic）。灵感＝WebSearch **三口新井**（cave-diving silt-out/percolation·Harrison Okene 沉船气穴·Jacuzzi of Despair 深海卤水池）。守则：loot 按 #44/#47（`[wreck]`→brass_fitting/canned_food 人造·`[cave]`→coral_shard/lobster 天然·深 cosmic/uncanny loot-free）、单 zone tag（#19）、**无新敌人**（守 2/zone·mimic/corpse-wearer 仍 Phase 3 apex 例外）、**不触发 d_reveal**（#42）、uncanny/cosmic 守**叙述永不交底**（#54：平淡解释＋错读始终叠着不裁决）、stamina-check 只锁 success baseline（#49）。**15 新 scenarios**（success+fail/lore·`event-runner --out json` 实跑抄 #43·含 4 sanity fail〔uncanny vs50 起步 san20·cosmic vs55 起步 san22〕+ `trapped_air/breathe_it` 起步 oxygen40 验**正 oxygen delta**+5）。事件 **128→138**〔`event-runner --list`〕、event baseline **177→192**、敌人 7/combat 8/item 22 不变。`npm run regress` **25/25 全绿（6.2s）+ prod build**。**未 commit**（无人值守·改动留用户审阅）。详见 quirk #82。**下一步候选：[wreck] 深段仍可补 1 realistic 平衡 tone / reef·蓝洞·墓园浅中段（≤44m）realistic 密度轮换 / trench 再补；别加第三只常规敌人、别触发 d_reveal。**
> **2026-06-06 即时新 POI 浮现（声呐与房间 §6.5 尾巴·方向 A「声呐与房间收尾」之一·#83）**：建灯 / 点亮前哨后**新进入范围的 POI 当场浮现**，不必等下个 run（补 #80 明确留的尾巴）。两处：① `SeaChartView` 的 chart memo 依赖从只 `runsCompleted` 加上**灯塔覆盖签名**（lighthouses 坐标 + 设施 + `effectiveRevealRadius` + flags）→ 中途建灯/升级当场重算 → 新进范围的锚点立刻浮现（复用既有 `.chart-survey-sweep`/`.chart-poi-arrive` 动画·新元素 key 稳定故只新 POI 入场动画）；② `chart.ts` roaming 选取从「顺序加权抽」（依赖池子组成·池变大会**整组重洗**）改成 **pool-independent 确定性键**（Efraimidis–Spirakis A-Res：`key = u^(1/weight)`·u 由 (runsCompleted, templateId) FNV 哈希·保留加权语义）+ roaming id 改**模板键** `poi.roam.<run>.<templateId>`＝同 runsCompleted 重算稳定、池变大也不重洗已显示机会点（至多被更高优先的新点亮挤掉一个·确定性最小变化）。删 `generateChart` 的 `rng` 入参（roaming 现恒确定性·只改 2 处 test 调用）。`playthrough-chart` §8（远端 roaming 即时点亮 + 模板键 id 确定性）+ §3 跨 run 6 种组合 + §7 浓雾遮蔽不变。全绿 26/26。**
> **2026-06-06 猎手（声呐图上的捕食者）SPEC + Stalker Phase 1 spine（方向 A 之二·作者三问拍板·#84）**：声呐与房间 §8.7 此前留作者拍板的「定位 stalker」开题。作者三问定调 → 新 `docs/深海回响_猎手_SPEC.md` v0.1 捕捉完整愿景（感官模态 光/声/双感·感知分层 灯＝知道在接近/声呐＝知道位置+距离·切信号行为 停原地/移到上次信号点·升级规避 T1 吸声 T2 主动迷彩·大型生物狭小空间避难·执着等待者耗资源·decoy 道具〔战斗中也能逃〕·全深度小概率浅弱深难）+ §7 分阶段。**Phase 1 spine 实装**＝把抽象「警觉」(#59) 做成**有位置、会逼近、按你用哪种感官显示不同保真度**的猎手：`run.stalker?`/`run.huntEnabled?`（run 级·派生·`?? ` 兜底·**不 bump SAVE_VERSION**）+ 新 `engine/stalker.ts`（spawn 量程外现身〔非当场伏击〕/ advance 沿图逼近〔无向 BFS〕/ scan **被 ping 扫到才更新位置** §8.7 / evadesSonar 深 band 躲扫描·确定性不耗 RNG）+ `dive.ts` moveToNode 分流（`run.huntEnabled` → 有位置猎手；否则旧 `alert→maybeApproachEncounter` 瞬时伏击·**逐字节守 playthrough-stealth §4-§6**）+ pingSonar 定位 + `SonarScanPanel` 精确深红 `✕` blip（压住同一只猎手的模糊威胁接触 sonar-threat）+ `NodeSelectView` 切信号后 searching 提示 + `DepthBand.hunts`（trench+ 五个深 band 开·`data/depth_bands.json`·缺省 off）。**感知分层**＝灯「有东西在接近」（既有 alert-warning）/ 声呐「它在哪 + 多远」（新 blip·会过时·§8.7）·**同一只猎手两种读数**。接触 → **复用 zone 现有 `ambushEncounters`**（不加新敌·守「敌人别太多」）。逃脱阀门＝切信号（摸黑/停 ping）→ alert 消退 → searching → 跟丢 despawn（北极星：摸黑是逃生阀门·无脚本死）。新 `playthrough-stalker`（spawn 非瞬时 / 逼近接触触发现有伏击 / 切信号搜→跟丢 / 声呐定位+深 band evade / **additive 控制组**非 huntEnabled 走旧瞬时 / 存档 round-trip）+ smoke E8（声呐定位精确 blip + 压住模糊接触 / 未定位回落）。全绿 **26/26**（playthrough-stalker 自注册 +1）+ prod build。Phase 2+ deferred（升级规避 T1/T2·decoy·大型生物狭小空间·执着等待者·浅水小概率弱变体·完整模态分类落数据·已 SPEC 捕捉）。**
> **2026-06-06 周末内容 pass · established 浅中段（≤44m）质感补完「所见为真，但有一处不对」（周末内容引擎自动无人值守 session·轮换离开连补五 band/深段·#85）**：近五次内容 pass（#75/#77/#81/#82）连补最深四 band + established 40-60m 深段（现已饱和），而 established **浅/中段（≤44m）** 自 #57（2026-06-02 深段）起多日未动 → 本次按「每次换 zone/深度/tone 侧重」**轮换回浅中段**（恰是 #81/#82 两次都点名的下一步「reef·蓝洞·墓园浅中段 realistic 密度轮换」）。**recon**（脚本按 zone×深度段×tone 数实装）：**[cave] 26-44m realistic 仅 1 / cosmic 0 / uncanny 3、[reef] 26-44m uncanny 仅 1、[wreck] 26-44m 严格段 realistic 0**——三处真缺口。**11 新事件**（append 进 `blue_caves.json`×4 / `reef.json`×4 / `wreck_graveyard.json`×3·全既有 zone 前缀·**零 registrar 改动**）：**蓝洞中段 4**〔`hells_bells`(U·**El Zapote 钙质空心石钟 Hells Bells**·盐线长出口朝下的活石钟·叩薄壳却无声)/`sulfur_cloud`(C·**Cenote Angelita 硫化氢云层**·悬浮 H2S 层『落进别人的天空』·**本批唯一 cosmic·填 cave 26-44 cosmic=0**)/`gour_pools`(R·**rimstone gour pools/cave pearls** 钙质水梯田+洞穴白虾·填 cave mid realistic)/`dry_bones`(U·**cenote 陆生巨兽/古人类化石 Hoyo Negro**·石头里朝出口的食肉大颌·地质深时方向恐惧)〕+ **旧灯塔礁中段 4**〔`coral_spawn`(U·oncePerRun·**同步珊瑚产卵『往上落的雪』**·月夜·填 reef mid uncanny〈此前仅 1〉)/`cleaning_station`(R·隆头鱼清洁站·石斑鱼张口受洗·学它沉住气下手)/`dusk_chorus`(U·**礁噪/枪虾合唱『一刀切』的静**·等下一声却听见远处不属于这里的低响)/`sea_fan_thicket`(R·柳珊瑚海扇梳横流+缠扇骨的海蛇尾)〕+ **沉船墓园中段 3**〔`ghost_net`(R·**幽灵渔网仍在持续捕杀**·填墓园 26-44m realistic〈严带内此前 0〉)/`the_cargo`(R·**Thistlegorm 摩托/板条箱货舱锈成一团**)/`rusticles`(U·**Titanic 锈瘤/嗜铁菌 Halomonas titanicae 活体吃船**·一碰即塌·钢只剩薄薄透亮一层)〕。**tone 5R/5U/1C**（精确填浅中段实缺口·非凑等分；浅中段守『所见为真』故 lean R/U·cosmic 由深 band 供足、本批仅 sulfur_cloud 一个真补缺）。灵感＝WebSearch **四口新井**（Hells Bells 钙质石钟·同步珊瑚产卵·幽灵渔网 ghost net·Cenote Angelita 硫化氢云）。守则：loot 按 #44/#47（`[reef]`/`[cave]`→coral_shard/lobster 天然·`[wreck]`→brass_fitting/canned_food/old_fishing_net 人造·uncanny/cosmic 伏笔层 loot-free）、单 zone tag（#19）、**无新敌人**（守 2/zone·mimic/corpse-wearer 仍 Phase 3 apex 例外）、**不触发 d_reveal**（#42）、uncanny/cosmic 守**叙述永不交底**（#54：平淡解释＋错读始终叠着不裁决·如『活的钟壁』『托着雾的东西』『朝出口等着的颌』『不属于这里的低响』『被吃空的船还立着只因没人碰』）、stamina-check 只锁 success baseline（#49·dc 12-13 浅中段不可强制 fail〔rate 公式 stat 不可负到 dc-30〕）。新 `lore.bluecaves.{hells_bells,sulfur_cloud,dry_bones}` + `lore.reef.{coral_spawn,dusk_chorus}` + `lore.wreck_graveyard.rusticles`（6 条）。**17 新 scenarios**（覆盖全 11 事件·`event-runner --out json` 实跑抄 #43·5 个 check-success 验 `checkPassed`·6 条 lore 路径·余为 sanity/中性诚实出口分支）。事件 **138→149**〔`event-runner --list`〕、event baseline **192→209**、敌人 7/combat 8/item 22 不变。`npm run regress` **26/26 全绿（8.2s）+ prod build**。**未 commit**（无人值守·改动留用户审阅）。详见 quirk #85。**下一步候选：reef 浅段（≤25m）uncanny/cosmic 仍可轮（守 fresh-wrongness 伏笔层）/ 蓝洞浅段 realistic 加厚 / trench 再补；别加第三只常规敌人、别触发 d_reveal。**
> **2026-06-06 定向 ping · 方向扇区（声呐与房间 §5·方向 A「确保真的完成」之一·#86）**：把声呐从全向一律升成可朝一个扇区（朝深处/侧向/来路·按 `node.layer` 差分·与布局 x∝layer 一致）聚焦——那方向探更远、别处更短（`sonar.ts::revealSonarScanDirectional`·近场仍全向一小圈·波束只沿聚焦扇区扩＝连贯无孤立远 blip·封顶 `SONAR_DIR_RANGE_MAX`），且**暴露按方向计**：窄波束整体更安静（`clarity.ts::sonarPingAlertDelta(run,dir)`×`SONAR_PING_DIR_MULT`0.55），但正对**听得见声音的猎手**扇区则尖峰（×`SONAR_PING_TOWARD_MULT`1.7＝净>全向＝你照亮它了·「别朝声感猎手方向 ping」·`pingAimsAtSoundStalker` 用猎手**真实**位置算暴露·UI 警示 `seenStalkerSector` 用**会过时**位置＝你按旧图瞄）。扇区分类 `nodeSector` 住 sonar.ts（引擎不碰 ui/mapLayout·无环·clarity→sonar 既有）。作者 2026-06-06 拍板「方向扇区」（口述「朝深处/侧向/朝来路」即此三向）。`pingSonar(state,dir?)` 透传定向揭示+定向暴露+记 `sensors.sonarDir`（移动后随 sonar 清·不 bump SAVE_VERSION）；NodeSelectView 三向聚焦按钮（朝猎手向带 `aims-threat ⚠`）+ omni 标「全向」·SonarScanPanel 聚焦标注。**omni（dir 缺省）逐字节不变**＝守 sensors/sonar 既有回归。`playthrough-sonar` §13 + smoke E4b·全绿 26/26·提交 `6bfdc76`。
> **2026-06-06 房间 feature 数升级 · 大房间出现率轴（声呐与房间 §6/§8.3 续·方向 A 之二·收尾 #74 尾巴·#87）**：新升级旋钮 `roomFeatureChanceBonus` 沿 #80 同款传感器桥（`UpgradeEffect→getUpgradeBonuses→UpgradeBonuses→getRunBonuses→createNewRun→deriveSensorTuning`·编译期 7 处全补＝护栏）·夹到 `ROOM_FEATURE_CHANCE_MAX`(0.3)；`startDive` 读 `run.sensorTuning.roomFeatureChanceBonus` 透传 mapgen `GenOpts`→`rollExtraFeatures` 把『单 feature』门槛往下压＝深处「大房间」更常出现。**band `maxRoomFeatures` 仍是房间尺寸天花板**（升级只让大房间更常见、不破深 band 上限）。缺省 0（POI/教学/未升级）→ `rollExtraFeatures` 阈值/rng 消耗不变＝mapgen 输出逐字节复现旧图（向后兼容护栏）。data `salvage_guild` 续 lv4「深潜打捞队」（`eel_skin`×4+`lantern_gland`×2+220 金·深料账单·effect 0.18）。`playthrough-upgrades` §9 + `-mapgen-scenarios` 出现率不变量（bonus0 逐字节·bonus0.3 抬率·确定性）+ `-save` round-trip + smoke J7·全绿 26/26·提交 `953622a`。
> 给"未来的自己"和"下一个接手 session 的 Claude"看。
> **2026-06-06 周末内容 pass · 无名渊 band（>230m·最深一层）+ nameless 专属事件「界限没了·往下的那个已经是你」（周末内容引擎自动无人值守 session·续 #63/#65/#68/#72 递归更深机制开最底一层·#88）**：recon 实数（`event-runner --list` 矩阵 + 近五 pass #75/#77/#81/#82/#85 已把最深四 band + established 40-60m + ≤44m 全填饱和）确认唯一未触的 fresh 方向＝**再开一层更深的 band**（『永远有比最深更深的』北极星）。渊外（subhadal 180-230·#72）之下开 `band.nameless`（230-290·dark·`alertFactor 3.5`〔续 #64 升序、> 渊外 3.0〕·`sonarDeception 0.04`〔续 #78 在渊外 0.06 的反转再低一档·『它扫回来的就是你自己·谎不必撒』〕·`tags:[cave,nameless]`·借蓝洞 mapgen·maxRoomFeatures 3·hunts true），**新 ZoneTag `nameless`**（twilight/midnight/abyssal/hadal/subhadal 已用尽，types/events.ts 加）。**母题刻意不重渊外的『外部诱饵』家族**（暖/檐/家的光/钟——subhadal 9 事件已铺满），推进到最后一层：**过了诱饵，连『你』和『它』的界限也没了——声呐唯一回波是你的形状、你的声音从下面慢半拍答你、你的记号在你没去过的前头、影子不肯跟你转身上去、下一个决定在你伸手前就替你做好**＝[[deep-game-vision]]『一直是同一人』D-reveal + corpse-wearer『穿你』的**最深最强伏笔，但仍不触发 d_reveal**（#42·留作者逐拍）。`data/events/nameless.json` **9 事件**（import 进 zones.ts EVENT_DB）：`the_return`(U·声呐回波是你·SONAR) / `your_own_voice`(U·你声音从下面答你·SOFAR 声道) / `the_closing`(C·oncePerRun·分不清你在沉还是它升来迎你) / `already_marked`(U·你的记号在没去过的前头) / `the_outline`(C·oncePerRun·上浮时影子不跟着转、仍朝下) / `no_one_deciding`(C·oncePerRun·下一步在你伸手前已被先你一步的你做好·escalate subhadal.the_quiet_yes 从『同意』到『没有在做决定的那个你』) / `the_pale_floor`(U·假底＝你呼出的气＋海雪·真物理打底的 grounding 事件) / `the_cold_that_knows`(U·冷径直找你装具每一处只有你知道的旧伤) / `nothing_wrong`(C·oncePerRun·apex·什么都没有不对·你习惯了该死的深度＝最坏的信号·呼应 subhadal.the_warmth 逻辑)。tone **5U/4C**（最深 band cosmic-lean·含 1 个真物理 grounding 守不全飘）、4 oncePerRun 是四个大 beat。守则全同深 band：**loot-free / 无新敌人**（守 2/zone·mimic·corpse-wearer 仍 Phase 3 apex 例外·本层只埋不登场）/ **不触发 d_reveal**(#42) / **叙述永不交底**(#54·每事件平淡解释〔解离/自窥 heautoscopy/SOFAR 折返/失温/narcosis〕与『往下的那个是你』错读始终叠着、游戏拒绝裁决) / 单 zone tag(#19) / 每事件留『关声呐·别看那个分身·夺回手·现在就走』的诚实但盲/退逃生口（感知双刃·无脚本死 §9）。新 `lore.nameless.*`。灵感＝WebSearch 三口新井（**Third Man factor**〔极限求生大脑分裂出陪你的另一个我〕/ **heautoscopy 自窥幻视**〔看见分身·自我与分身边界模糊·说不清『你』在哪〕/ **SOFAR 深海声道**〔声音被困折射、传几百里延迟拐回〕——正好充当『平淡解释』半边）。经 `DepthBand.tags` 覆盖 zoneTagsByDepth 隔离事件池、从 hadal_deep 前哨蛙跳软门控可达（无需新前哨·同 subhadal）。22 scenarios（`event-runner --out json` 实跑抄 #43·9 事件 read_it success+fail 各档 + 4 oncePerRun 中枢 give-in lore 路径）+ `playthrough-bands` §13（130 抽出/0 泄漏·subhadal/cave 双隔离·alertFactor 3.5 > 3.0 升序）。事件 149→158、event baseline 209→231、敌人 7/combat 8/item 22/band 6→7。全回归绿（`npm run regress` 26/26 + typecheck + 16 playthrough + scenarios 231 + combat 9 + mapgen 4+60 + verify-tutorial + smoke + prod build）。**未 commit**（无人值守·留审阅）。详见 §6 anchor #88。

## 历次 pass 摘要（原 STATUS §6 中的会话型条目，保留原编号 #N）

45. **reef zone 首个原生战斗 encounter＝梭鱼（玻璃大炮原型）**：`enemy.reef_barracuda` hp 16（全场最低）/ armor 0 / evasion 4（最高，命中 0.95−4×0.04＝0.79）/ damage [5,9]（单击最高）/ hostility **predatory**（不触发 territorial 低血撤退，打到死）。knife_slash(8-14) **2 刀即杀**（hp 太薄）；但首回合梭鱼多半会先咬一口（dart [5,9]），所以"大炮"是真威胁，只是速杀下只来得及咬一两口。baseline `reef_barracuda_solo__normal_kill`（knife_slash×3、seed 1）= **2 turns / stamina −19（含一次 7 点突咬）/ oxygen −2** / 掉 barracuda_jaw；**stamina Δ 随 seed 在 −17~−19 浮动（咬不咬、咬多少由 RNG 定），故 baseline 只锁单一 seed**。触发事件 `reef.barracuda` 只挂 `[reef]` tag → 隔离在旧灯塔礁 26-44m（不跨 zone）。**加敌人五件套**：enemies/*.json（含 `combatEncounters` + 敌人自带 `loot.guaranteed`）→ combat.ts 三处注册（import + ENEMY_DEFS + COMBAT_ENCOUNTERS）→ 新 loot item 进 items.json → 触发事件挂对应 zone tag → ≥1 combat baseline + ≥1 event baseline。**敌人 schema 坑**：用 `hp`（非 maxHp）、encounters 放 `combatEncounters`（非 encounters）、loot 在敌人 def 的 `loot.guaranteed`（非 encounter）。

46. **蓝洞群深段敌人＝洞穴章鱼（physical 攻坚「深处闸门」原型 / 反梭鱼）**：`enemy.cave_octopus` hp 26（仅次教学暗礁鲨 32，全场非教学最厚）/ armor 1 / evasion 3 / threat 6 / hostility **territorial**（hp≤30%≈7 时 50% 撤退，能被 scare/flee 收）/ aiPattern aggressor / 三攻击（缠臂 [3,5] w3 + 角喙 [5,8] w1 + 喷墨 0 物理 + sanityDamage [2,4] w1）。与梭鱼（hp16 glass cannon 速杀）正好相反：**厚血 + 甲 → 拖成 3-4 turn 消耗战**，每回合都在烧氧/体力，在深段（40-55m）氧本就紧，是真正的"打还是绕"决策。knife_slash(8-14) 减 1 甲 = 7-13 有效，约 3 刀杀。baseline `cave_octopus_solo__normal_kill`（knife_slash×4、seed 1）= **3 turns / stamina −28 / oxygen −3 / 无喷墨命中故 sanity 0** / 掉 cave_octopus_beak；**stamina −20~−30、sanity 0~−3 随 seed 浮动（咬几下/喷不喷墨由 RNG 定），故只锁 seed 1**（同梭鱼套路）。触发事件 `bluecaves.octopus_den` 挂 `[cave]` tag → 跨 zone 共享到旧灯塔礁 cave 层（与盲鳗同模式，不同于梭鱼的 `[reef]` 隔离）；三选 = 拔刀(hasEquipment tool)/压灯慢退(stamina vs 13 避战)/绕开，参照 reef.barracuda + blind_eel_lair。**意义**：蓝洞战斗 1→2（盲鳗 uncanny sanity flanker + 章鱼 realistic physical bruiser），且是**蓝洞首个 realistic-tone 战斗**。掉天然物（章鱼喙）守 quirk #44。加敌人仍走 quirk #45 五件套。

48. **沉船墓园第二只敌人＝沉灯水母（cosmic-tier「理智消耗战」原型 / 反盲鳗·反章鱼）+ 2026-05-31 周日敌人 pass 三长线缺口全补**：`enemy.drowned_lantern` hp 24 / armor 1 / evasion 1（命中 0.91，易打中）/ speed 4 / threat 6 / **tier cosmic（项目首只）** / hostility **predatory**（不触发 territorial 低血撤退，打到死）/ aiPattern caster（**注意：`aiPattern` 是纯 metadata，引擎不 branch——`combat.ts` 的 runEnemyTurn/enemyAttackPlayer 只按 attack `weight` 随机选招，只有 dev 面板显示它**，所以随便填合法枚举值都行）/ 两攻击（脉光 damageType `sanity`·damage `[0,0]`·sanityDamage `[4,7]` w3 主攻〔纯 sanity 母攻，照 `blind_eel.eel.hum` 套路：damageType sanity + damage [0,0] + sanityDamage〕+ 曳丝 physical `[2,4]` + sanity `[1,2]` w2）。设计＝**「理智消耗战」**，与梭鱼(hp16 glass cannon 速杀)/章鱼(hp26 纯物理 bruiser)/盲鳗(hp18 evasion4 物理主导·sanity 点缀的快速 flanker)全互补：**slow/tanky + 主攻烧 sanity → 拖得越久脑子越空**，是墓园『打还是躲』里"打"的理智代价。knife_slash(8-14)−armor1=7-13，~3 刀杀；baseline `drowned_lantern_solo__normal_kill`（knife_slash×4、seed 1）= **3 turns / stamina -20 / oxygen -3 / sanity -10**（**项目首个 sanity Δ 非零的战斗 baseline**）；**sanity -8~-14 / stamina -20~-29 随 seed 浮动（脉不脉、扫不扫、个别 seed 4 turns 由 RNG），故只锁 seed 1**（同 quirk #45/#46）。掉 `lantern_gland`（天然身体部位→ Mira，符合 quirk #44）。触发事件 `wreck_graveyard.drifting_light` 挂 `[wreck]`（按 quirk #47 跨 zone 共享到灯塔礁 25m+，但「漂着的冷光」在礁底沉船间不出戏，呼应 reef.lantern_glow / lore.deep_water.cold_light『下面的光』暗线——**只呼应不解释、不触发 d_reveal**，否则像 the_knocking 一旦被『解谜』就泄了气）。加敌人仍走 quirk #45 五件套（enemies/*.json → combat.ts 三处注册 import+ENEMY_DEFS+COMBAT_ENCOUNTERS → loot item 进 items.json → 触发事件挂 zone tag → ≥1 combat + ≥1 event baseline；verify-tutorial 的注册守卫会拦未 import 的 enemy 文件）。**同 pass 另两个事件**（补 §5 点名的另两长线缺口，均 ≥1 baseline）：`reef.lighthouse_lens`（uncanny·30-44m·`[reef]` 隔离·sanity vs 48·loot brass_fitting / sight_along → lore.old_lighthouse.the_lens·填 reef 26-44m 中段 uncanny，此前最深 reef-only uncanny 是 fog_bell 到 38m）+ `bluecaves.the_narrowing`（cosmic·14-25m·`[cave]`·oncePerRun·loot-free·lore.bluecaves.the_way_out·填蓝洞 12-25m 浅段 cosmic，此前最浅 cosmic 是 32m falling_up——把『感知/方向错乱』母题下放到还看得见真出口的浅段，反而更不安）。

50. **材料经济（基建地图 Phase A，2026-06-01）—— 升级双资源账单 / `buildingPoints` 整体移除 / Mira 回购 + shopStock / 存档 v1→v2**：
   - **升级 `cost` 从 `number`（建设值）变 `UpgradeCost{ materials: MaterialCost[]; gold }`（`types/upgrades.ts`）**。`canPurchase` 顺序是**材料先于金币**：逐条 `countInInventory(profile.inventory, itemId) >= qty`，缺 → `{reason:'notEnoughMaterials', shortfall}`（shortfall 列每种还差几个，供 UI 高亮"还差 X×N"）；材料齐了才查 `bankedGold >= gold`，缺 → `{reason:'notEnoughGold', goldShort}`。**推论：满金空仓买升级落 notEnoughMaterials（不是 ok / 不是 notEnoughGold）——"金币买不了升级"靠这个顺序保证**。`purchaseUpgrade` 逐条 `removeFromInventory` + `bankedGold -= gold`，不可购买时 no-op（不偷扣）。改账单数值就改 `data/upgrades.json`，引擎零改动；`describeUpgradeCost` 是 log+UI 共用的账单格式化（别在 UI 再写一份）。
   - **material `tier 1–4`（`items.json` 每个 material 标）= 深度分档**（T1 浅~T4 cosmic，见 §3 items.json）。tier 是"难度标签"：驱动①升级账单稀有度（高阶升级点深料，强制下深）②Mira 回购门控（仅 T1/T2 可买回）。**加新 material 记得标 tier，否则 `tierOf` 返回 undefined → 不可回购、且不会被任何按 tier 的逻辑算进**。
   - **`buildingPoints` 整体移除**：types（PlayerProfile + RunOutcome.buildingPointsEarned）/ engine（`death.ts::computeRawBuildingPoints` + `ascent.ts::computeBuildingPoints` 删除，executeDeath/executeAscent 不再发点）/ 全部 UI（PortView/SeaChartView/ResolutionView/CorpseView/UpgradePanel）/ 脚本。**死亡/上浮不再发任何元进度点数——进度＝带回的材料本身**。
   - **存档 `SAVE_VERSION 1→2`**（quirk #39 流程）：`migrateSave` 的 `while` 加 `case 0/1`（fall-through）删 `prof.buildingPoints`，`v=2`。旧点数**直接丢弃不折算**（SPEC §6/§10 决策）。灯塔字段留 Phase B 再迁。回归在 `playthrough-save.ts` step 6（注入 v1 档→断言迁移后无 buildingPoints + version 2）。
   - **Mira 回购（出售侧，`port.ts`）**：T1/T2 材料可买回，**买价 = 卖价(`miraOfferFor`)× `MIRA_BUY_MARKUP`(2)，恒 > 卖价**；T3/T4 `miraBuyPriceFor` 返回 0（只卖不买，保住"深度=进度"门控）。**`shopStock` 软限量**：`SHOP_STOCK_BY_TIER`（T1=8/T2=4）是每次回港的备货上限，存在 `profile.shopStock?: Record<itemId,number>`（**可选 + 普通对象**——旧档缺它无妨，`getShopStock` 缺项懒默认成满货，JSON 原生 round-trip 无需特殊迁移）；`buyFromMira` 买 `min(qty, 余货, 金币能买的)` 后递减 stock，`handleReturnToPort` 把 `shopStock={}` 清空＝补满。**这些都是 tunable（SPEC §9）：markup / 各档上限 / T3-T4 是否开放回购，集中在 `port.ts` 顶部三个常量**。
   - **UI**：UpgradePanel 用 `CostLine` 渲染"材料×需求量（不足→红 + 已有数）＋ N 金（不足→红）"，按钮三态（修缮/材料不足/金币不足（还差 N））；面板**渲染全部升级线**——某行可买不代表别行也可买（smoke 别断言"满足时整面板无不足态"）。MiraShopView 加回购区 `listMiraBuyables(profile)`（遍历物品库非背包，缺料也能补），按钮三态（买 1/钱不够/售罄）。
   - **作用域**：本 Phase **只动经济**，没碰灯塔数据模型 / 海图 reveal / nearest-lighthouse distance（那是 Phase B/C，见 `docs/NEXT_SESSION_PROMPT.md`）。**新增 UI smoke**：`smoke-chart-ui.tsx` J（UpgradePanel 三态+缺口）+ K（MiraShopView 回购 T1/T2 不含 T3/T4 + 买/钱不够/售罄）——承 quirk #38 教训，UI 数据路径要补 SSR 渲染断言。

51. **灯塔基地数据模型（基建地图 Phase B，2026-06-01）—— 多灯塔数据层 + 引擎脚手架，灯塔此刻 inert**：
   - **`Lighthouse` 类型在 `types/lighthouse.ts`**（不是 state.ts——所有灯塔类型集中一处：实体 + LighthouseUpgradeDef/Effect/Bonuses/Track/File）；`PlayerProfile.lighthouses: Lighthouse[]` 在 state.ts 引入它。`types/index.ts` 已 `export * from './lighthouse'`。
   - **home 灯塔单一来源 `state.ts::createHomeLighthouse()`**（id `lighthouse.home`，`HOME_LIGHTHOUSE_ID`）：createInitialProfile 种入 + migrateSave `case 2` 补种，**别在两处各写一份字面量**（改坐标/名字只改工厂）。坐标 mapX 0.06/mapY 0.5（海图最左港口位，POI 在 0.18+）。**name 暂沿用 SPEC 锁定的「旧灯塔」**——与出海 zone「旧灯塔礁」同源 lore 但不同地点，潜在歧义；name 是 content/tunable，Phase C 灯塔上海图可见时再由作者定（已记 NEXT_SESSION）。
   - **存档 SAVE_VERSION 2→3**（接 Phase A 的 1→2）：`migrateSave` `case 2` 给缺 `lighthouses` 的旧档种 home。**关键：migrateSave 在 `JSON.parse(reviver)` 之后跑，所以此处 Set 已是真 Set——种 home 时直接 `new Set()`（不是 `{__set:[]}`）**；同理 `lighthouse.builtUpgrades` 这种**嵌套在数组对象里的 Set 也走现有 replacer/reviver 自动 round-trip**（已验，`playthrough-save.ts`）。
   - **`engine/lighthouses.ts` 与 `engine/upgrades.ts` 平行、互不污染**：随身装备升级＝全局（`profile.unlockedUpgrades` + `getUpgradeBonuses`）；灯塔设施＝每灯塔（`lighthouse.builtUpgrades` + `getLighthouseBonuses`）。账单**复用** Phase A 的 `materialShortfall`/`describeUpgradeCost`（别再写一份）。`buildAtLighthouse` 不可变更新时**只 map 替换目标那一座灯塔**（`i===idx ? {...l, builtUpgrades:newSet} : l`），别整盘替换污染别座（回归专门断言前哨不被污染）。`canBuildAt` 比全局多一个 `needsLighthouseLevel`（灯塔 level 门槛）。
   - **灯塔 inert**：本 Phase **没有**把灯塔接进 chart.ts（reveal POI 可见性）/ dive.ts（nearest-lighthouse 算 distance）/ 任何 UI（建造界面）。`engine/lighthouses.ts` 的函数有 `playthrough-lighthouse.ts` 单测但游戏流程不调用。**Phase C 才消费 `getLighthouseBonuses`（lightRadiusBonus→揭示半径、reachReduction→出海拉近）+ 把 `nearestLighthouse` 接进 distance + `lighthouse_ruin` 修复事件 + SeaChartView 渲染灯塔 + 建造 UI**。
   - **`dockyard` 归属**：SPEC §3.3 建议 dockyard→home 灯塔升级，但 Phase B **没动**（dockyard 仍全局，仍用 `hasUpgrade` 门控旧灯塔礁 POI + 进 getUpgradeBonuses；迁灯塔会牵动海图门控+加成聚合，是更大改动）。留 Phase C 评估。
   - **`lighthouse_upgrades.json` 是单文件**（非目录）→ 不触发 verify-tutorial 的目录注册守卫（quirk #39e 只扫 events/enemies/npcs 子目录）；但 verify-tutorial **§4b 新增账单材料校验**（全局 upgrades + 灯塔 upgrades 的 `cost.materials.itemId` 都必须是真 item，catch 拼错）。加新灯塔设施轨改这个 JSON 即可，引擎零改动。

52. **海图集成 + 修复循环（基建地图 Phase C，2026-06-01）—— 灯塔从 inert 到"做事"，revamp 三支柱闭环**：
   - **reveal（`chart.ts`）**：新 `isPoiLit(profile, poi)`/内部 `isLit(profile, x?, y?)`——POI 落在**任一**已拥有灯塔的 `revealRadius(lh)` 内才点亮（遍历所有灯塔，不只看最近）。`revealRadius = BASE_LIGHT_RADIUS(0.72) + (level-1)*PER_LEVEL(0.12) + lightRadiusBonus*PER_BONUS(0.12)`（常数在 `lighthouses.ts` 顶部，tunable SPEC §9）。`isPoiVisible = flagsSatisfied && isPoiLit`；generateChart 的 roaming 过滤也加 `isLit`。**home L1 半径 0.72 故意只覆盖现有 4 锚点（最远沉船墓园 ≈0.662）+ 近端 3 roaming，两个远端 roaming（蓝洞暗河口 0.791 / 塌口北缘 0.802）落半径外** → 修复前哨灯塔才点亮（决策：作者选 uniform radius、非祖父化豁免；SPEC §10.8）。**无坐标的 POI 默认点亮**（不因缺坐标隐藏）。
   - **reach（`effectiveDistance(profile, poi)`，`chart.ts`）**：distance 档 = `round(nearestLighthouse 距离 / REACH_NORM_PER_TIER(0.3)) - reachReduction`，clamp ≥0；**无坐标 / 无灯塔退回写死 `poi.distance`（fallback）**。0.3 是**刻意校准**：使 4 锚点从 home(0.06,0.5) 算出的档位＝写死 0/1/1/2（不破手感，`playthrough-chart` §2b(d) 锁死）；roaming 按几何略偏（本就"潮位常变"）。`dive.ts::startDiveFromPoi` 用它替代写死 `poi.distance`（预耗氧 + turn）。
   - **修复废弃灯塔**：下潜里能**持久写 profile** 的 outcome 之前只有 `loreEntry`——新增 `restoreRuinId` 同类（`applyOutcome` 在 loreEntry 之后处理）。事件 `lighthouse.ruin_north`（`data/events/lighthouse.json`，注册进 `zones.ts`）repair 选项 `outcome:{restoreRuinId}` → `restoreLighthouse(state, id)` **权威校验**（`canRestoreRuin` 按 **profile 银行**材料＋金币，**不是 run.inventory**——下潜带不进银行料、run.inventory 起步空）：成功扣 profile 料＋金 + push `ruin.result` 灯塔（builtUpgrades 空 Set）+ 置 `flag.lighthouse_restored.<ruinId>`（事件 `forbiddenFlags` 据此不再出）；不够/已修则只叙事不改档（幂等）。账单/结果灯塔在 `lighthouse_upgrades.json::ruins[]`，verify-tutorial §4c 校验 cost＋result，walkOutcome 校验 restoreRuinId 引用真 ruin。
   - **dockyard 迁灯塔**：dockyard 从 `upgrades.json` 全局线删，迁成 `lighthouse_upgrades.json` 的 `lhtrack.dockyard`（**新 `LighthouseTrack.homeOnly:true`**——建造 UI 对前哨隐藏此轨）。它唯一真效果是 `extraConsumableSlot`（旧 `unlockZone` 是死的——`bonuses.unlockedZones` 没被任何 chart/dive 消费，只被旧测试断言，已删该断言）。**桥**：新 `LighthouseEffect.extraConsumableSlot` + `LighthouseBonuses.extraConsumableSlot`，`getRunBonuses(profile)` = 全局 `getUpgradeBonuses` ＋ **家灯塔**（仅 home）的 `extraConsumableSlot`；`dialog.ts::startDive` 和 `dive.ts::startDiveFromPoi` 两个出海口都改用它。3 个远海 POI 的抵达门从 `requiresUpgrade`（查 unlockedUpgrades）改成新 `ChartPoi.requiresLighthouseUpgrade`（`poiLockReason` 查 **home** 的 builtUpgrades）；**锁定串保持「需要「船坞 Lv.1」」**（facility name 沿用，smoke A/B 串不变）。**SAVE_VERSION 3→4**（`case 3`：已购 `upgrade.dockyard.lv1` 从 unlockedUpgrades 删 + `lighthouse.dockyard.lv1` 加进 home.builtUpgrades；没买过的档不塞）。
   - **UI**：`SeaChartView` 在 chart-map 里渲染每座灯塔的 `.chart-lighthouse`（aria-label「灯塔：<名>」）+ `.chart-light-radius`（归一化半径圈，pointer-events:none 不挡 POI 点击、置 POI 之下）；底部「灯塔设施」按钮 toggle 新 `LighthouseBuildPanel`（镜像 UpgradePanel，用 `getLighthouseTracks`/`canBuildAt`/`buildAtLighthouse`，按 `track.homeOnly` 对非 home 隐藏船坞轨）。**半径圈 % 宽高、容器非正方时是椭圆**——v1 接受，纯视觉（tunable）。
   - **回归**：新 `playthrough-lighthouse-scenarios.ts`（引擎直调 resolveOption 跑修复成功/失败/幂等 + reveal/reach 前后 + 存档 round-trip——**因 runEventScenario 的 inventory 落 run 不落 profile、且不暴露 final lighthouses，成功路径只能引擎直调**）+ `scenarios/lighthouse/`（leave / 身无分文 restore 两条 harness 路径）。改 `playthrough-chart`（§2 门控 + 新 §2b reveal/reach）/`-lighthouse`（新 §6 半径+船坞桥）/`-upgrades`（全局线 3→2，dockyard 全迁）/`-economy`（"金币买不了升级"改用 tankhouse）/`-save`（v3→v4 + 没买不塞）/`smoke-chart-ui`（B 用 home 船坞、J 用 salvage 账单、新 L/M）/`verify-tutorial`（requiresLighthouseUpgrade + ruin 校验）。

53. **内容 pass（2026-06-02）—— reef 浅段 fresh-wrongness 母题 / reef 第二敌人「石斑鱼」(territorial 重装) / 深段 realistic 密度 / 敌人分布达 2-per-zone**：revamp 三支柱闭环后，本 session 回到 [Weekend Content Log] 记的三个长线内容缺口，一次补齐。
   - **priority 1 · reef 浅段 fresh shallow-wrongness（作者明确选「全新浅水错位」，刻意不续灯塔『下面的光』线）**：旧灯塔礁浅段（10-25m）此前几乎全 realistic、**无浅段 cosmic**。补 3 个 `[shallow,reef]` 事件，主题是**晒亮/安全的浅水被悄悄证伪**（三条不同感官通道）：`reef.silversides`（uncanny·动物行为·银鱼群让出的球把你扣在心、围的却是一片空沙）/ `reef.sun_net`（**cosmic·光/物理·沙上一格太阳光网钉死不动，失败分支『真正的底蒙了层薄东西』·项目首个浅段 cosmic**）/ `reef.warm_seam`（uncanny·温度·一道从礁底缝上来的血温暖水，『像谁在底下慢慢呼吸』）。新 lore 命名空间 `lore.reef_shallows.*`（the_gap/the_still_square/the_warm_crack），三者都轻触『下面』暗线但**刻意不触发 d_reveal**（同 quirk #44/#48 的克制——揭示是存档级不可逆决定，留给在场用户）。**为什么 `[shallow,reef]` 安全**：zones.json 里只有 `old_lighthouse_reef` 的 0m 段挂 `shallow`，`east_reef`=`[tutorial]`、蓝洞=`[cave]`、墓园=`[wreck]`，故 `[shallow,reef]` 事件天然只在灯塔礁 0-25m 出现（quirk #19 反例的正面确认），coral_shard 天然 loot 不出戏；sun_net cosmic loot-free。灯塔礁 12m 事件池 4→6。
   - **priority 2 · reef 第二只敌人＝石斑鱼（`enemy.reef_grouper`，territorial『礁檐守卫』原型）**：hp30（全场非教学最厚）/ armor2（最高）/ **evasion1（最低——hit 0.91 几乎必中）** / speed6 / threat4（低）/ hostility territorial（hp≤30% 撤退，可被 scare/flee 收）/ aiPattern observer（纯 metadata，quirk #48）/ 2 纯 physical 攻击（gulp 吞口 [6,10] **全场最高单击** w2 + buffet 侧撞 [3,6] w3）。**生态位与现有 5 敌全异**：梭鱼=fragile-fast-burst predatory、章鱼=cave aggressor 厚血 bruiser(+ink sanity)、盲鳗=fast sanity flanker、沉灯水母=cosmic sanity caster、教学鲨=脚本。石斑＝**「低闪避·必中·厚甲·最重单击·但 opt-in」的重装墙**——threat 低不追，触发事件 `reef.coral_overhang`（realistic·20-38m·`[reef]`）给 `sneak_larder`（stamina vs13 避战取洞底 loot）/ `leave_ledge`（无代价退）两个**非战斗出口**，与梭鱼 predatory『拔不拔刀都付代价』正相反——这是 territorial 的玩法签名。combat baseline `reef_grouper_solo__normal_kill`（knife×5、seed 1）= 4 turns / stamina-32 / oxygen-4 / 掉 grouper_maw（seed 1：grouper 只 buffet×2 无 gulp、turn3 territorial 撤退、turn4 补刀；stamina 随 seed 浮动故只锁 seed 1，同 quirk #45/#46/#48）。新 loot `item.grouper_maw`（石斑鱼鳔，**material T2 organic**·sellPrice15·天然身体部位→ Mira，符合 quirk #44；organic 与 lobster/eel_skin 同档=得趁鲜卖）。加敌人走 quirk #45 五件套（enemy json→combat.ts 三处 import+ENEMY_DEFS+COMBAT_ENCOUNTERS→item→触发事件→combat+event baseline，verify-tutorial 注册守卫拦未 import）。
   - **priority 3 · 深段 realistic 密度**：verify-tutorial『旧灯塔礁事件池』报告是薄段信号——补浅段后最薄是 60m 段（5）。补 `cave.sump_pool`（realistic·46-60m·`[cave]`·回水潭潜越·coral 天然）+ `wreck.chain_locker`（realistic·44-60m·`[wreck]`·锚链舱打捞·brass/canned 人造，quirk #44/#47），**depthRange 都摸到 60** 故填 60m 段（5→7），同时跨 zone 加厚蓝洞群（cave 46-55）/ 沉船墓园（wreck 44-50）深段。低 dc stamina-check 只锁 success baseline（quirk #49）。
   - **敌人分布现 2-per-zone**（reef 梭鱼+石斑 / cave 盲鳗+章鱼 / wreck 蛛蟹+沉灯水母）——**『敌人别太多』已到位，下一个 content session 不建议再加敌人**，优先事件 / 终局 lore 触发（`flag.d_reveal` 仍无触发器，quirk #42——或可由 `lore.reef_shallows.*`／『下面』暗线收口）。
   - 计数：事件 64→70、敌人 6→7、combat 7→8、item 21→22、event baseline 49→57、combat baseline 8→9。全回归绿（含 -economy/-upgrades，新 T2 grouper_maw 进 Mira 回购池但没破账单/回购断言）。

54. **内容 pass（2026-06-02 续二）—— 墓园浅段 fresh-wrongness 3 事件 / 内容首次作 deep-game vision 的「伏笔层」 / 「叙述永不交底」定为深水写法铁律 / 无新敌人**：接 reef 浅段（quirk #53）的对称缺口，补沉船墓园浅段 18-25m（此前无 cosmic、uncanny 仅 dive_slate/handprints 擦边 22-24m 起）。3 个 `[wreck]` 事件。
   - **与之前内容 pass 的关键不同：这次每个事件都刻意是「深水越深越欺骗 + apex mimic/corpse-wearer」北极星（见自动记忆 deep-game-vision）的浅水伏笔**——`the_other`（两船间跟你同步、隔固定距离的潜水员 → 伏笔 corpse-wearer「穿尸体引诱」）/ `all_facing`（从上方看沉船船首/舱口/尸体全转向塌口深水 → 伏笔『深处的拉力』，**墓园首个浅段 cosmic**）/ `full_nets`（拖网船的网被底下某物往深处拽得绷紧、还在被拉 → 伏笔深处的『渔夫/安康鱼』）。把浅段「晒亮处的不安」做成深水欺骗的预告，与 reef 浅段（sun_net/silversides/warm_seam，quirk #53）对称。
   - **写法铁律（源自 deep-game vision 的「是世界坏了还是你疯了——通常两者皆是」）：深水/伏笔事件的叙述永不交底**——既给一个平淡解释（倒影 / 几十年的洋流 / 还没烂的网），又留一个错的读法，两种始终叠着，不确认也不否认。范例：the_other『像你的光弹回来的影』、all_facing『告诉自己是洋流……信了一半』、full_nets `follow_down`『底下还在拉，慢慢的匀匀的』。reef.sun_net『你告诉自己是云』是同写法的先例。**以后写深水/cosmic 内容沿用此铁律。**
   - **loot（quirk #44/#47）**：仅 `full_nets` 掉人造物（old_fishing_net + canned，`[wreck]` 跨灯塔礁 25m+ 不出戏）；the_other / all_facing loot-free（纯不安）。全不触发 d_reveal。**无新敌人**（各 zone 已 2 敌，守『敌人别太多』）。
   - 新 `lore.wreck_graveyard.{the_other,all_facing,full_nets}`。事件 70→73，event baseline 57→62（the_other signal_success / all_facing fix_bearing success+fail〔cosmic sanity 双分支，fail 起步 sanity 20→8〕/ full_nets cut_upper_success〔stamina 仅 success，quirk #49〕+ follow_down〔no-check+lore〕）。statsDelta 全 `event-runner --out json` 实跑抄出（quirk #43）。全回归绿（typecheck / playthrough / -wreckyard / -combat / -corpse / -bluecaves / scenarios 62 / combat 9 / mapgen / -save / verify-tutorial / smoke-chart-ui）。

55. **内容 pass（2026-06-02 续三）—— 深水伏笔 mid 层（25-44m）3 事件 / 一 zone 一母题 / 「叙述永不交底」铁律续用 / 无新敌人**：承 quirk #53（reef 浅段）/ #54（墓园浅段）的 fresh-wrongness，把自动记忆 deep-game-vision 的『越深越欺骗』信任梯度从**浅段伏笔**推进到三个 zone 的**中段（25-44m）**。设计原则：**一 zone 一事件、一事件一条 vision 母题、各填该 zone 的真缺口、不加敌人（各 zone 已 2 只，守『敌人别太多·优先事件』）**。recon 用 `event-runner --list` 按 zone-tag 数中段 tone 覆盖：reef 26-44m 唯一 cosmic 是灯塔线 lantern_glow（→补非灯塔 cosmic）；cave/wreck 中段 uncanny 已密、但 cave 无『假光』、wreck 无『corpse-wearer 中段付现』（→各补一条）。
    - **reef · 『深处的拉力』**：`reef.no_bottom`（cosmic·32-44m·oncePerRun·`[reef]`）——游出断口悬在开阔水里，脚下空蓝没有底，深度表自己往下走『是它在把你往下要』。**reef 首个非灯塔线 cosmic-mid**，与 realistic `reef.shelf_break`（同是『礁壁断口』地标）配成『诚实危险＋错读』姊妹事件。`look_down` sanity vs50：成功给平淡解释（下降流／潜水服深处压扁丢浮力『道理都对。你还是没敢把那片蓝多看第二眼』）+ `lore.reef_deep.no_bottom`（**新命名空间**，parallel `lore.reef_shallows.*`）；失败＝错读吞人（空蓝像『慢慢张开又合上的口子』、不记得自己沉下来过）。loot-free。把浅段 wreck.all_facing（船首朝深水）的『拉力』母题带到 reef。
    - **cave · 『无灯之光』（mimic 假信标伏笔）**：`bluecaves.the_glow`（uncanny·30-44m·`[cave]`）——黑水道尽头一点你盼着出口该有的颜色的光，朝它游它不变大、一拐石壁就移到别处亮起『在等你换个方向』。直接预告 deep-game vision 里**伪装成灯塔的安康鱼 mimic**（海图上『无灯之光』的假 POI），借蓝洞黑暗做画布，加厚蓝洞『有人在下面/先来者』暗线（other_bubbles/left_behind_gear/sounding_line）。`go_toward`（no-check）→`lore.bluecaves.the_glow`；`douse_lamp` sanity vs48 成功＝关灯看真样子给平淡解释（『会发光的虫子，你对自己说』）。loot-free。
    - **wreck · corpse-wearer 伏笔 + 可读 tell**：`wreck_graveyard.no_bubbles`（uncanny·26-42m·`[wreck]`，按 #47 也跨灯塔礁 25m+，loot-free 不犯 #44）——两船间一个背对你伏在舷窗上『干活』的潜水员，头顶却没有一个气泡。把浅段 `the_other`（同步的潜水员）往深里推一格＝deep-game vision 的**『穿尸体的东西』corpse-wearer**，并刻意埋一个**可读 tell（不冒泡＝不呼吸）**——呼应 vision『够强+读出 tell 才能活』、为日后 mimic 机制留 tell 的雏形。`watch` sanity vs48 成功给两个平淡解释（空潜水服挂断缆随涌浪摆／闭路呼吸器不冒泡『挑了你更想信的那个』）；`rap_tank`（no-check 敲气瓶）→`lore.wreck_graveyard.no_bubbles`。**无 combat**（伏笔不是敌人，守 2/zone）。
    - **写法**：全部死守 quirk #54 铁律——既给平淡解释又留错的读法、两种叠着、不确认不否认、**全不触发 d_reveal**（揭示留给 vision 正式开建/在场作者）。全单 zone tag（#19）。baseline statsDelta 全 `event-runner --out json` 实跑抄（#43）；cosmic check 做双分支（no_bottom fail 起步 sanity20→8 不触 0 底）、uncanny sanity check 只锁 success + 配一个 no-check lore baseline（确定性）。
    - 计数：事件 73→76，event baseline 62→68（+6：no_bottom look_down success+fail / the_glow go_toward+douse_lamp_success / no_bubbles rap_tank+watch_success），敌人 7（不变）、combat 8（不变）、item 22（不变，全 loot-free 无新材料）。全回归绿（typecheck / 全部 playthrough / scenarios 68 / combat 9 / mapgen 4+60 / verify-tutorial / smoke-chart-ui）+ prod build 通过。**三 zone 中段深水伏笔层成型；下一步＝继续 realistic 密度收尾，或 deep-game-vision 正式开建（opacity→供给→mimic，需作者在场，见 NEXT_SESSION_PROMPT.md）。**

56. **内容 pass（2026-06-02 续四）—— realistic 探索密度收尾 / 一 zone 一事件填各 zone 真缺口 / 无新敌人 / 作者选「内容收尾·realistic 密度」**：浅段（#53/#54）+ 中段（#55）「越深越欺骗」伏笔层成型、「明显 tone 缺口」收尾后，本 pass 轮换回 realistic 探索质感（同 #49 的轮换逻辑），把三个 zone 各自最薄的 **realistic** 段补厚。
    - **recon 方法（承 onboarding）**：verify-tutorial 的「旧灯塔礁事件池」报告（现 12m=6 / 25m=21 / 38m=21 / 50m=20 / 60m=7）已被 #53 把浅/深段填平、不再是薄段信号；改用 `event-runner --list --zone-tag {reef,cave,wreck}` **按 tone 逐 zone 数 realistic 覆盖**找真缺口。三个真缺口：① 墓园浅段 18-25m realistic 仅 2（cabin_entrance/current_drag），且 #54 刚把该段堆成 uncanny/cosmic → tone 失衡；② 蓝洞中段 25-44m realistic 仅 3、全是导航地标（forked_passage/makeshift_ledge/stalactite_hall）、无觅食/资源 beat；③ reef-only 深中段 36-44m realistic 仅 shelf_break 触到 44m（lobster_hole→35 / urchin_barren→30）。
    - **三事件（一 zone 一个，各填上述缺口）**：`wreck_graveyard.deck_cargo`（18-26m·`[wreck]`·后甲板捆死的整船货+垮货网·`cut_lashings` stamina vs13 撬木箱→canned_food/old_fishing_net / `pick_spillage` 散货里捡→brass_fitting / `leave_cargo`·**人造 loot** 守 #44/#47）/ `bluecaves.lobster_crack`（26-44m·`[cave]`·侧壁横缝里够礁虾·`reach_in` stamina vs13→coral_shard+lobster chance / `snap_coral` 只掰珊瑚 / `leave_crack`·**天然 loot** 守 #44·给蓝洞中段补首个觅食 beat）/ `reef.sand_channel`（34-44m·`[reef]`·礁脊间平行沙沟+来回涌·`work_groove` stamina vs12 顶涌摸沟底→coral_shard+lobster chance / `near_ledge` 沟口礁檐摸一把 / `cross_over` 沟脊上方横过·**天然 loot**·与 shelf_break 同『断口/沙沟』地标家族但机制不同：横向涌 vs 垂直壁）。
    - **守则**：全 realistic（无 lore / 无 cosmic / 无 oncePerRun / **不触发 d_reveal**）、全单 zone tag（#19）、loot 按 zone（#44/#47：`[wreck]`→人造跨灯塔礁 25m+ 不出戏 / `[cave]`+`[reef]`→天然）、**无新敌人**（各 zone 已 2 只，守『敌人别太多·优先事件』）、**无新 item**（全用现有 T1/T2 材料 coral_shard/lobster/canned_food/old_fishing_net/brass_fitting，故 item 仍 22）。stamina-check 只锁 success baseline（#49：满 stamina→0.95 clamp、seed 1 必过；fail 分支 deltas 写时手验未进 baseline）；statsDelta 全 `event-runner --out json` 实跑抄（#43，三者 success 均 `{oxygen:-2}`）。事件无 oncePerRun/oncePerSave，故每个都有可重复触发的常规探索价值。
    - 计数：事件 76→79，event baseline 68→71（+3：三个 success 分支 cut_lashings/reach_in/work_groove，命名/format 同 #43/#49，文件在 `scenarios/` 根）。敌人 7 / combat 8 / item 22 不变。全回归绿（typecheck / 全部 playthrough / scenarios 71 / combat 9 / mapgen 4+60 / verify-tutorial / smoke-chart-ui）+ prod build 通过。**realistic 密度此轮收尾（三 zone 各段 realistic/uncanny/cosmic + 浅/中/深 tone 已齐）；下一步＝深水伏笔深段（45-60m）续铺〈承 #54/#55，可加直指 mimic/corpse-wearer 的更强伏笔但仍不触发 d_reveal〉，或 deep-game-vision 正式开建（opacity→供给→mimic，需作者在场，见 NEXT_SESSION_PROMPT.md）。**

57. **内容 pass（2026-06-02 续五）—— 深水伏笔深段 45-60m / 两条 apex 母题（mimic 假信标 + corpse-wearer）推进到最深层、最强预告但仍不触发 d_reveal / 无新敌人 / 续「ok next」自动续做**：承 #53/#54（浅段）+ #55（中段）的『越深越欺骗』伏笔层 + #56 realistic 密度收尾，把两条直指 [[deep-game-vision]] apex 威胁的母题铺到深段（45-60m），封顶浅/中/深三级弧线。
    - **深段 recon（与 #56 同法）**：`event-runner --list --zone-tag {cave,wreck}` 数 44-60m tone。**关键约束：reef zone 45m+ 段 tag=`[wreck,cave]`（quirk #47），`[reef]` 事件在 45m+ 不出现——所以深段只有 cave / wreck 两个 tag 可用**（reef『拉力』母题已在中段 no_bottom 封顶、无法下探）。两个真缺口：① cave 深段无『假光』（the_glow 封顶 44m）；② wreck 深段无『corpse-wearer』（no_bubbles 封顶 42m）——正好是 mimic / corpse-wearer 各自的深段空位。
    - **两事件（一 tag 一母题，全 cosmic·oncePerRun·loot-free·sanity vs55 双分支）**：`cave.false_beacon`（46-60m·`[cave]`·**写在 reef.json**，紧邻 cave.blue_floor 这个既有深段 cave cosmic）——超出自家灯塔光照边界、却有一点暖得正是岸上灯塔该有颜色的光稳稳悬着『像有人替你点着』＝**伪装成灯塔的安康鱼 mimic 假信标的直接深段预告**（海图『无灯之光』假 POI 的 in-dive 雏形），承中段 the_glow、接 deep_water『下面的光』暗线（cold_light/the_window）·新 `lore.deep_water.the_false_beacon`；三选 account_for_it（sanity vs55，成功给平淡 debunk〈自己的头灯散在盐雾/缺氧把冷蓝看成暖色〉+ lore；失败＝光把人往深里领）/ swim_for_it（no-check，付『缺氧绝望照样游过去』代价 sanity-7+往深处领）/ put_it_behind。`wreck_graveyard.the_wearer`（44-56m·`[wreck]`·**写在 wreck_graveyard.json**，承 the_other→no_bubbles 弧）——旧式铜盔潜水服、无灯无泡却知道你在哪、招手引你过去＝**穿尸体引诱的 corpse-wearer 直接深段预告**，埋可读 tell〈无灯/无泡/几十年没人穿的老装备/招手机械重复〉呼应 vision『够强+读出 tell 才能活』·新 `lore.wreck_graveyard.the_wearer`；三选 read_him（sanity vs55，成功＝数出 tell 退开 + lore；失败＝读不出、被引诱滑近）/ go_to_him（no-check，**反用本作对死者的温柔**『你没法把一个人丢在这么深的地方』·代价 sanity-8）/ keep_hull_between（埋 tell：它招手对着你刚才站过的位置、不是现在的你）。**无 combat**（伏笔非敌人，守 2/zone）。
    - **守则**：全 cosmic（深段 deception 层）、oncePerRun、loot-free、单 zone tag（#19）、**不触发 d_reveal**（揭示留 vision 正式开建/在场作者，#42/#44 沿用）、叙述永不交底（#54：两事件成功路都给平淡解释 +『你挑不出哪个更真』）。baseline 双分支：success 默认 sanity 100→rate 0.95 必过 + 断言 `loreAdded`；fail 设 `stats.sanity 22`（≤dc-30=25→clamp 0.05 必败、起步 22 减 12/13 落 10/9 不触 0 底，对照 reef.no_bottom 的 sanity20）。statsDelta 全 `event-runner --out json` 实跑抄（#43）：false_beacon success `{oxygen-2,sanity-5}` / fail `{oxygen-5,sanity-13}`；the_wearer success `{oxygen-2,sanity-5}` / fail `{oxygen-4,sanity-12}`。
    - 计数：事件 79→81，event baseline 71→75（+4：两事件各 account_for_it/read_him 的 success+fail）。敌人 7 / combat 8 / item 22 不变。全回归绿（typecheck / 全部 playthrough / scenarios 75 / combat 9 / mapgen 4+60 / verify-tutorial / smoke-chart-ui）+ prod build 通过。**浅/中/深三层『越深越欺骗』伏笔层全部成型（mimic 假信标 the_glow→false_beacon、corpse-wearer the_other→no_bubbles→the_wearer 各有浅中深级预告）；下一步＝低强度可再补深段不同感官变体，或 [[deep-game-vision]] 正式开建（opacity→跨 run 供给→mimic+d_reveal，需作者在场，见 NEXT_SESSION_PROMPT.md B 路）。**

58. **深水区 Phase 0a：微观双传感器 clarity + 不可信声呐 + 电池 + 低 san 腐蚀（2026-06-03，深水区第一笔代码）**——把 `visibility:dark` 盲航泛化成统一 micro-clarity。SPEC `docs/深海回响_深水区_SPEC.md` §11 0a（已勾）是源真；§10 决策日志记拍板与偏差。
    - **模型（三态权衡，非谁压谁）**：灯（近·地面真相·暴露 signature 最高·清水近免费/黑水耗电）/ 声呐 ping（远·**不可信返回**·耗电大·**后期才解锁**）/ 摸黑（盲·最隐蔽·省电）。"没有完全可信的传感器"：san 越低声呐先失真、san 足够低连灯也幻觉（灯最稳、最后崩）。
    - **新 `engine/clarity.ts`（单一来源 + tunables 集中文件顶）**：`clarity(run)`→`'full'|'sonar'|'none'`（`lampEffective` = 灯开+有电+水非 dark；`sonarActive` = 解锁+ping+有电）；`sonarReturn(run,node)` 不可信表象（① `node.evadesSonar` 没回波 / ② `node.spoofsSonar` 喂假回波〔mimic 钩子，默认 unset〕/ ③ san<60 注入假回波 / 否则粗糙 plausible 表象，**全 ≠ 真 preview**）；`lampPreview(run,node)`（真相；san<25 幻觉）；`signature(run)`（灯>声呐>摸黑，0b 消费）；`lampPowerDrain`（清水因子 0→浅水近免费 / murky 0.5 / dark 1）。**确定性哈希挑文案（不消耗 Math.random）**——不扰动 `withSeededRandom` 场景回归、playthrough-sensors 可稳定断言。
    - **run 状态**：`sensors:{ light; sonar:'off'|'ping'; sonarUnlocked }` + `power`/`powerMax`（电池，类比 oxygen）。`createNewRun` 种默认（灯开/声呐 off/能力派生/电满）；两个出海口（dialog.ts startDive + dive.ts startDiveFromPoi）经 `getRunBonuses().sonarUnlocked` 传入。
    - **引擎门控**：`enterNodeSelection` 一次算 `tier=clarity(run)`，按档把每个 choice 的 preview 烤成 真相/`sonarReturn`/盲 + 标 `choice.clarity`（**门控移到引擎侧、UI 成纯渲染器**——承 quirk #38「别只测引擎」的教训，逻辑可被 playthrough 测）。**地标（上浮口/气穴/扎营）盲航仍显示真相**（结构性，沿用 #37）。尸体提示只在 full+Lv.1（#36）。`tickTurns` 灯耗电。`dive.ts::setLight`/`pingSonar`（`refreshSelection` 重算选点；移动后 ping 归 off=脉冲瞬时；power 归 0→强制 none）。
    - **声呐后期解锁（作者 2026-06-02 加的关键约束）**：`upgrade.sonar.lv1`（新 `line.sonar_rig`，深料账单 lantern_gland T4 + eel_skin/cave_octopus_beak T3 + 120 金）→ `unlockSonar` effect（types/upgrades.ts + getUpgradeBonuses OR 聚合）→ `sonarUnlocked`。**早期＝仅有灯，黑水天然探索受限（"先经历黑暗中无声呐"），分级解锁；即使有灯仍有受限处。** dark POI 出海日志按 sonarUnlocked 给不同提示。
    - **存档（不做迁移，作者 2026-06-03：未发布）**：**不 bump SAVE_VERSION（留 4）、不加 migrateSave 步**；run 新字段靠 createNewRun 种默认 + 反序列化读取处 `?? 默认` 兜底（clarity.ts 全防御性读）。`playthrough-save` 仍校验 sensors/power 的**序列化 round-trip**（序列化≠迁移，仍需保证存读不丢字段）。发布前再按 #39 流程统一补迁移。
    - **UI**：`NodeSelectView` 按 `choice.clarity` 渲染（`.clar-sonar` 暖色斜体不可信 / `.clar-none` 更暗）+ 灯开关 / 声呐 ping 按钮（门控 sonarUnlocked + 电量足）；`StatusBar` 加电量 pill（tint-amber）。`smoke-chart-ui` E 改写为 clarity 渲染 + 电量 + 传感器 + 声呐门控断言。
    - **三处偏差**（作者在场敲定，SPEC §10/§11）：① `clarity(run)` 不带 node（node 级细分留 Phase 1）；② `DiveModifier.visibility` **并入而非删**（作 clarity 输入：dark→none、murky 不挡灯但耗电+理智压力照旧）；③ 声呐解锁轨在 0a 做（效果/耗能/电量档位留 Phase 2）。低 san 阈值 60/25 是 §8 tunable。
    - **回归**：新 `playthrough-sensors.ts`（10 节）。改 `playthrough-upgrades`（线 2→3 加声呐组件）、`playthrough-save`（加 sensors/power **序列化 round-trip** 断言；不做迁移）、`smoke-chart-ui`（E）。全绿（typecheck / 12 playthrough / scenarios 75 / combat 9 / mapgen 4+60 / verify-tutorial / smoke）+ prod build + dev tree-shake 干净。
    - **下一步 = 0b（探测/隐身，碰 combat，消费 `signature(run)`）**：高 signature→捕食者接近/伏击/提高遭遇；摸黑滑过。见 SPEC §11 0b + NEXT_SESSION_PROMPT.md。**别预先触发 d_reveal（#42）；别加第三只常规敌人（mimic/corpse-wearer 是 Phase 3 apex 例外）。**

59. **深水区 Phase 0b：探测/隐身——警觉积累 → 接近/伏击（2026-06-03，同 0a 当日续，消费 0a 的 signature）**——主动感知的「被探测」侧落地。SPEC §11 0b（已勾）源真、§10 决策日志记拍板。
    - **作者拍板（AskUserQuestion）**：探测做成「警觉积累→接近/伏击」（§3.3 最完整形态，非轻量入节点 roll、非只调现有战斗）。**关键发现（子 agent 勘察）：今天战斗 100% 由事件选项 `outcome.triggerCombatId` 触发、零自动/随机遭遇**——所以 0b 是**项目首个自动遭遇路径**。
    - **警觉模型（run 级，非 node 级）**：`run.alert`（0–100，createNewRun 种 0）。实装为 run 级＝「捕食者对**你**的注意、随你移动」，比 §3.3 字面的 per-node 更贴。`clarity.ts::alertDelta(run,turns)` = `(signature 超静默基线 × alertDepthFactor × ALERT_GAIN) − ALERT_DECAY`；`tickTurns` 每回合累加 clamp 0–100。深度因子 `alertDepthFactor`：≤25m 返 0（**§7.5 浅水免探测压力**）、25m 起线性、60m 满（更深 band 斜坡留 Phase 1）。**点灯/ping 在深水抬、摸黑/浅水降**——摸黑是逃出生天的阀门。
    - **接近触发（`dive.ts::moveToNode`）**：过渡 tick 后、节点 kind 分发前，`maybeApproachEncounter`：`predatorApproaches(run)`（alert≥`ALERT_THRESHOLD`60 + 够深）+ **进的是非地标节点（event/corpse）** + 该 zone 有 `ambushEncounters` → 确定性选一个（`pool[visitedNodeIds.length % len]`，不消耗 RNG）→ `startCombat` 触发接近遭遇、alert 落回 `ALERT_AFTER_TRIGGER`(0)。**地标（上浮口/气穴/扎营）不被伏击＝总留「摸黑奔向出口」的出路。**
    - **数据（`ZoneDef.ambushEncounters?: string[]` + zones.json）**：reef/cave/wreck 三深水 zone 各配 2 个**现有 solo encounter**（reef=barracuda/grouper、cave=blind_eel/octopus、wreck=spider_crab/drowned_lantern）；**教学/浅水 zone 不配 → §7.5 数据兜底**（无池即便满警觉也不触发，playthrough-stealth §6 锁）。
    - **可生存无脚本死（守则）**：`NodeSelectView` 高警觉预警（`ALERT_WARN`35 黄 / THRESHOLD 60 红「熄灯，现在」）给「读 tell → 主动降暴露」窗口；摸黑约 8 回合甩回预警线下；触发的遭遇是普通战斗、可打可逃。
    - **没碰既有战斗**：startCombat / CombatState / 事件触发战斗一律没动；0b 只新增「从 moveToNode 进 combat」这一条入口。`dive.ts` 新 `import { startCombat } from './combat'`（无循环：combat 不 import dive）。
    - **存档**：同 0a，**未发布不迁移**——`run.alert` 靠 createNewRun 种 0 + 反序列化 `?? 0` 兜底，不 bump SAVE_VERSION（留 4）。
    - **tunables（§8，作者可调）**：GAIN 1.5 / DECAY 3 / THRESHOLD 60 / WARN 35 / MIN_DEPTH 25 / AFTER_TRIGGER 0。校准：60m 满因子约 10 回合到阈值、声呐 ping（exposure 3）远慢于举灯（exposure 6）。
    - **回归**：新 `playthrough-stealth.ts`（6 节：抬升/摸黑消退/浅水免压/越线触发遭遇/摸黑滑过/无池不触发）+ `smoke-chart-ui` E2（高/低警觉预警渲染）。全绿（typecheck / 13 playthrough / scenarios 75 / combat 9 / mapgen 4+60 / verify-tutorial / smoke）+ prod build。
    - **Phase 0（0a 感知 + 0b 探测）至此完成。下一步＝Phase 1 深度轴（banded 蛙跳，SPEC §5/§11）或 Phase 0 升级轨（灯/声呐效果·电量档位）；见 NEXT_SESSION_PROMPT.md。**

60. **深水区 Phase 0 升级轨：灯/声呐 效果·耗能·抗欺骗 + 电池容量 随材料经济成长（2026-06-03，§11 最后一项 → Phase 0 全闭）**——让 0a/0b 造的传感器随港口升级成长。SPEC §11「升级」（已勾）源真、§10 决策日志记拍板。
    - **作者拍板（AskUserQuestion）**：方向 B（升级轨）；**四旋钮全选**——powerMax（电池容量）/ 能耗效率（灯每回合耗电 + 声呐 ping 耗电）/ 抗欺骗（灯·声呐 各自的低-san 失真阈值）/ 隐蔽（signature 减免）。轨道结构「你觉得怎么做最合适」→ 我定**复用 `line.sonar_rig` 续 lv2 + 新 `line.dive_kit` 两条线、深度分层账单**。
    - **关键设计取舍：范围/分辨不做**——0a 把 `clarity(run)` 定为 **run 级二元**（full/sonar/none、不带 node），「范围/分辨」需节点级 clarity ＝ Phase 1（0a 偏差 #2 已声明 deferred），故本期没机械钩子、只做四个现在能接的旋钮。开建前用 AskUserQuestion 跟作者把这点讲清。
    - **新 `run.sensorTuning`（types/state.ts）**：本次下潜的"有效传感器参数"（`pingCost` / `lampDrainMult` / `sonarFalseEchoSanity` / `lampHallucinationSanity` / `signatureReduction`）。出海前由 `clarity.ts::deriveSensorTuning` **一次性烤**、下潜内不变（同 powerMax / sonarUnlocked 快照模式）。`clarity.ts` 纯函数读 `run.sensorTuning?.X ?? 文件顶基线常量`——**未升级 / 旧档 / 脚本部分 run 逐字节复现 0a/0b 基线**。
    - **桥（沿用 #58 声呐解锁的同一条模板）**：6 新 `UpgradeEffect`（powerMaxBonus / sonarPingCostReduction / lampEfficiency / sonarRobustness / lampRobustness / signatureReduction）→ `getUpgradeBonuses`（sum 聚合）→ `getRunBonuses`（透传，前哨灯塔暂不贡献这些）→ `createNewRun` 烤进 `run.powerMax`（= `POWER_MAX` + bonus）+ `run.sensorTuning`。
    - **地板/上限守北极星（集中 `deriveSensorTuning`）**：① 抗欺骗有地板——`SONAR_FALSE_ECHO_SANITY_MIN`30 / `LAMP_HALLUCINATION_SANITY_MIN`10，声呐/灯升满仍会在够低 san 崩＝**无完全可信传感器**（§3.2）；② 隐蔽有上限 `SIGNATURE_REDUCTION_MAX`3 + `SIGNATURE_MIN_ACTIVE`2 地板——点灯/ping 的 signature 永 > 摸黑＝**读真相必自曝**结构张力不被升级买断（§3.2/§3.3，playthrough-sensors §11f 锁）；③ ping 耗电地板 3 / 灯耗电乘子地板 0.25。
    - **数据（`line.dive_kit`「潜水装备」+ `sonar.lv2`，升级线 3→4）**：dk lv1「加大电池组」powerMax+20（浅料 coral×4/lobster×3 +30 金）/ dk lv2「聚光灯具」灯耗电 ×0.5 + 隐蔽 3（中料 brass×3/chitin×2 +70 金）/ dk lv3「抗扰灯罩」灯抗欺骗 25→15 + powerMax+20（深料 lantern_gland T4/beak T3 +150 金）；sonar lv2「声呐组件 Lv.2」ping −2 + 声呐抗欺骗 60→40（深料 lantern_gland/eel_skin +160 金）。账单深度分层＝沿用 quirk #50「下深拿料门控高阶」。
    - **DRY**：`RunStartBonuses` 现是 `createNewRun` bonuses 的超集 → `dive.ts::startDiveFromPoi` / `dialog.ts::startDive` 改成直接 `createNewRun({ zoneId, bonuses })` 整个传，删掉两处逐字段抄（再加旋钮时不会漏抄）。
    - **UI（quirk #38）**：`UpgradePanel::renderEffect` 加 6 个效果标签——**穷尽 switch、漏一个 case typecheck 就报「lacks ending return」**（这是好护栏：加 effect kind 必补 UI 标签）。`NodeSelectView` ping 按钮/门控读 `sonarPingCost(run)`（缺省回退常量）。新升级线全数据驱动、UpgradePanel 自动渲染所有 level（locked 显「需要前一级」），无新组件。
    - **存档**：同 0a/0b **未发布不迁移**——`run.sensorTuning` 是普通对象、`serializeGameState` 整体 `JSON.stringify` 自动 round-trip；旧档缺该字段 → clarity 回退基线。不 bump SAVE_VERSION（留 4）。`playthrough-save` 用非默认 bonuses 校验 round-trip。
    - **回归**：`playthrough-sensors` §11（powerMax / ping / 灯效率 / 抗欺骗双向 / 隐蔽 + 各自地板上限 + 未升级=基线 + createNewRun 端到端）/ `playthrough-upgrades` §8（dk lv1-3 + sonar lv1-2 购买 → bonuses 聚合 → getRunBonuses 透传 → createNewRun 烤进 run + needsPrev 门控）/ `playthrough-save`（sensorTuning/powerMax 非默认值 round-trip）/ `smoke-chart-ui` J4（新线 + 声呐 lv2 + 隐蔽/抗欺骗 效果标签渲染）。全绿（typecheck / 14 playthrough / scenarios 75 / combat 9 / mapgen 4+60 / lighthouse / verify-tutorial / smoke）+ prod build。
    - **Phase 0 完整闭环（0a 感知 + 0b 探测 + 升级轨成长）。下一步＝Phase 1 深度轴（开建前 AskUserQuestion pin band 表 / 解锁门 / 蛙跳 / 深 band 成本曲线）或内容打磨；见 NEXT_SESSION_PROMPT.md。**

61. **深水区 Phase 1 plumbing：可扩展纵向深度轴（band 阶梯 + 蛙跳下潜，2026-06-03）**——「很大的图」的脊柱第一段。SPEC §5 Phase 1 + §12 清单（已勾 plumbing）源真、§10 决策日志记拍板。
    - **作者拍板（AskUserQuestion，开建前 pin）**：① band 结构＝**新全局 `data/depth_bands.json`**（跨 zone 共享深度阶梯，非扩 zones.json——匹配 SPEC §6 草案 + 递归无界「终端 zone→海沟→深渊」愿景）；② 解锁＝**软门控**（原话「准备做成软控，首先材料装备限制探索，然后后面会有一些强力敌人做战斗力检测」）；③ 蛙跳＝**最小版**（home 灯塔 stand-in）；④ 成本＝**不加深度耗电税**（原话「不会增加耗电，但是更深的地方需要用更耗电的声呐，以及更频繁的使用探测设备，变相增加耗电」）。
    - **band 模型（`types/bands.ts` + `data/depth_bands.json` + `engine/bands.ts`）**：`DepthBand{id,name,zoneId,depthRange[绝对],order,visibility?,current?,blurb,danger?}`。**band 引用 zone 提供内容**（mapgen 形状/事件池/zoneTagsByDepth/ambushEncounters），**用自己的绝对 depthRange 覆盖 zone.depthRange**（band 决定下到多深、zone 决定那里有什么）。`getBands` 按 order 升序（越深越后、可续写、不硬编码地板）。3 band：reef_deep（灯塔礁 45-60 murky）/ trench_mouth（蓝洞 60-82 **dark·破 60m**）/ trench_throat（蓝洞 82-108 **dark·递归更深**）；trench 暂借蓝洞内容＝占位（内容期填专属 zone）。
    - **关键发现：60m「准硬上限」纯是数据**——`mapgen::generateDiveMap` 早就 `depthOffset` 平移 zone.depthRange、无硬 clamp；耗氧/减压本就连续随深度（tickTurns/planAscent）。唯一写死的深度是 `alertDepthFactor` 的 `60`。所以 Phase 1 = band 数据模型 + `GenOpts.depthRange` 覆盖（band 绝对窗口、缺省回退 zone）+ 抽 `ALERT_DEPTH_FULL`（深 band 饱和=1、不报错），**没有"拆硬上限"的大重构**。
    - **软门控（不写硬 flag）**：band 无 `unlockedBy`、UI 全列。深 band = 黑水 → `lampEffective` false → **没声呐（声呐是深料升级 #58）就 `clarity none`（瞎着下）**；电池/抗欺骗/隐蔽（#60）都吃深料 → **Phase 0 升级轨即是事实门槛**（装备越强越下得去）。「强力敌人战力检测」＝Phase 3 apex / zone ambushEncounters（0b），本期没加敌人（守 2/zone）。
    - **成本＝间接（作者）**：**不动 lightDrainFactor**（不加深度耗电项）。深 band 的 visibility=dark 复用现有 `visibility→clarity→forced-sonar→power` 回路：灯打不透 → 关灯省电 + 改 ping 声呐（SONAR_PING_COST/次）+ 移动后 ping 消散 → 每路口重 ping → 电量压力**涌现自环境**，不是深度税。这是把作者「更深→被迫用更耗电声呐」直译成机制。
    - **蛙跳 `dive.ts::startDiveFromOutpost(state, bandId)`**：镜像 `startDiveFromPoi`——home 灯塔 stand-in outpost、band 绝对窗口透传 `startDive({depthRange})`、`bandDiveModifier` 落 run.diveModifier、蛙跳预耗氧（band 顶深/20 档）、**走 `getRunBonuses`**（Phase 0 升级轨 sensorTuning/powerMax 直通＝软门控钥匙）。出潜能见度叙事抽 `appendVisibilityLog`（与 startDiveFromPoi 共用、避免文案漂移）。UI：SeaChartView 蛙跳列表（home 在则列全部 band、`danger` 进 title）。
    - **存档**：band 派生数据、不入存档；run 没加新字段（复用 diveModifier/depthRange 透传）→ 不 bump SAVE_VERSION（留 4）。
    - **回归**：新 `playthrough-bands.ts`（7 节：band 表+order/破 60m+黑水/depthRange 覆盖 vs 回退/startDiveFromOutpost 落 run/软门控〔无声呐瞎、买声呐解锁〕/升级轨直通蛙跳/alertDepthFactor 深 band 饱和不报错）+ `smoke-chart-ui` A2（蛙跳列表渲染）。全绿（typecheck / 15 playthrough / scenarios 75 / combat 9 / mapgen 4+60 / lighthouse / verify-tutorial / smoke）+ prod build。
    - **Deferred（明确留后续，见 SPEC §12）**：~~trench 专属事件/band 级 tag 池~~（**已实装 #63·band.tags + trench.json 6 事件「回波对不上」**；专属 trench/abyss *zone* 仍可后续再拆）；~~范围/分辨节点级 clarity~~（**已实装 #62**）；真前哨蛙跳（Phase 2 持久前哨建成后换掉 home stand-in）；band 级 alert 倍率/不饱和成本曲线。**下一步＝Phase 2 跨 run 前哨/能源、或深段内容、或 Phase 1 续（band 级 alert 曲线）。**

62. **深水区 Phase 1 续·节点级 clarity：深度分档 + 范围/分辨升级轨（2026-06-04，填 #60/§12 明确 deferred 的"范围/分辨"钩子）**——把 0a 定的 run 级二元 clarity 升成节点级。SPEC §12「范围/分辨」（本次勾）+ §10 决策日志（2026-06-04 条）源真。
    - **模型（天花板 + 深度降档）**：`clarity(run)` 留作 run 级**天花板**（灯 full/声呐 sonar/摸黑 none，NodeSelectView header 仍用它）；新 `clarity.ts::clarityForNode(run,node)` 在天花板之上按节点**深度差**（`node.depth − currentDepth`，只算"比你深"的陡降）降档：
      - 浅水 `currentDepth ≤ CLARITY_FULL_DEPTH`(25，与 ALERT_MIN_DEPTH 同一条§7.5浅水线)→ 不降档、给天花板＝**所见为真**；
      - 深水灯档：dd ≤ `LAMP_DEPTH_REACH`(6m) → full（灯照得到）；dd 超灯 reach 但 `sonarActive` 且 dd ≤ `SONAR_DEPTH_REACH`(14m) → sonar（声呐补）；再深 → none（灯声呐都够不到＝黑，**没 ping 的陡降也是黑**）；
      - 深水声呐档（灯无效/dark band）：dd ≤ sonarReach → sonar、否则 none；
      - **横/上行（dd≤0）不降档**——深度只藏"下面"。
    - **为什么 UI 几乎零改**：`NodeChoice.clarity` 字段 + `NodeSelectView` 的 `clar-<档>` per-choice 渲染**早在 0a 就位**（#58），此前只是所有选项恰好同档。`enterNodeSelection` 把 `const tier = clarity(run)`（一次）换成 per-choice `clarityForNode(run,n)`，choice 各带各档——UI 直接显示混合档，没动渲染层。
    - **范围/分辨＝升级轨（填 #60 缺口）**：#60 明确把"范围/分辨"defer 到 Phase 1（run 级二元没节点钩子）；本次补上。`SensorTuning` 加 `lampDepthReach`/`sonarDepthReach`，2 新 `UpgradeEffect`（`lampRangeBonus`/`sonarRangeBonus`）**沿用 #58/#60 同一条桥**（effect → `getUpgradeBonuses`(sum) → `RunStartBonuses`/`getRunBonuses` 透传 → `createNewRun` → `deriveSensorTuning` 烤进 run.sensorTuning）。数据＝`data/upgrades.json` 加 `upgrade.dive_kit.lv4`〔远摄灯组·灯 reach +4→10·中深料 beak/chitin〕+ `upgrade.sonar.lv3`〔声呐 reach +8→22·深料 eel_skin/beak〕（升级线仍 4、升级 9→11）。`UpgradePanel::renderEffect` 穷尽 switch 补 2 标签（quirk #38 护栏）。
    - **地板/上限守北极星**：reach 有上限 `LAMP_DEPTH_REACH_MAX`(14) / `SONAR_DEPTH_REACH_MAX`(26)，**均 < 深图最深陡降**＝灯/声呐升满也照/扫不穿最深的直坠口、最深处必须自己摸黑或委身声呐下去（呼应"永远有比最深更深的"/"读真相要付代价"）。缺省回退基线常量＝未升级/旧档逐字节复现 0a 行为。
    - **护栏：尸体定位（打捞行会 Lv.1）不被深度藏住**——`hasCorpseHint`/corpse preview 此前 gated on run 级 tier；节点级降档会让"深处陡降的尸体"在有 Lv.1 时也不再被标记（误伤 quirk #36 既有功能）。修：`enterNodeSelection` 里 **Lv.1 标记的尸体 + 灯有效 → 豁免深度降档（强制 full）**（与地标同列），因为尸体定位是**地图知识**、不靠灯的物理 reach。无 Lv.1 的深处尸体仍按深度变黑（一致）。
    - **存档**：未发布不迁移——`sensorTuning` 加两字段是普通对象、`JSON.stringify` 自动 round-trip + clarity.ts `?? 基线` 兜底，不 bump SAVE_VERSION（留 4）。
    - **回归**：`playthrough-sensors` 新 §12（9 节：浅水豁免/仅灯陡降黑/灯近+声呐补中段/黑水全声呐/灯 reach 升级/声呐 reach 升级/未升级=基线+上限/横上行不降档/Lv.1 尸体豁免）+ `-upgrades`§8 续买 dk lv4+sonar lv3 断言 reach 聚合直通 + `-save` reach round-trip + `smoke` J5（新 effect 标签渲染）。全绿（typecheck / 18 playthrough / scenarios 75 / combat 9 / mapgen 4+60 / lighthouse / verify-tutorial / smoke）+ prod build。
    - **Deferred（续 SPEC §12）**：band 级 alert 倍率 / 越深越狠不饱和成本曲线（现 `ALERT_DEPTH_FULL` 60 饱和）；~~trench 专属内容（Phase 1 占位）~~（**已实装 #63·band.tags + trench.json 6 事件**，可再加密/续更深 band）；真前哨蛙跳（Phase 2）。**下一步＝Phase 2 跨 run 前哨/能源、或深段内容、或 band 级 alert 曲线。别预先触发 d_reveal（#42）、别加第三只常规敌人。**

63. **深水区内容 pass · trench 专属事件「回波对不上」+ band.tags 覆盖（2026-06-04）**——Phase 1 占位 trench band（60-108m，此前借蓝洞、深 cave 事件 ≤60m 故池空）终于有专属内容。作者选「深段/trench 内容」→ 母题「回波对不上」（AskUserQuestion）。SPEC §12 Deferred「band 级 tag 池」+「trench 内容」本次（部分）勾、§10 决策日志记。
    - **工程＝band 级 tag 池（最小、可扩展 plumbing，填 §12 Deferred）**：`DepthBand.tags?: ZoneTag[]`（types/bands.ts）覆盖 zoneTagsByDepth。**关键省事：`twilight`/`midnight`/`abyssal` 早在 `ZoneTag` 联合里闲置**（types/events.ts）→ 用 twilight(竖井·口)/midnight(竖井·喉) **零类型改动、无穷尽 switch 风险**（abyssal 留 >108m）。链路：`startDiveFromOutpost` 传 `band.tags` → `startDive(opts.bandTags)` → `mapgen GenOpts.bandTags` → ① 节点 zoneTag 抽取 `opts.bandTags ?? tagsForDepth` ② `buildEventPool(tagsOverride)`（两处一致＝图与池同 tag）。depth_bands.json：trench_mouth=`[cave,twilight]`、trench_throat=`[cave,midnight]`——**附加而非纯替换**（带上 cave 保留 zone 回退池、避免深 band 事件稀时退化成空水道；实测 cave 在 60-108m 无事件 → 池纯 trench，但 forward-compat 保留）。
    - **内容＝`data/events/trench.json`（新文件，6 事件；import 进 `zones.ts` EVENT_DB，verify-tutorial 注册守卫自动纳入）**：母题「回波对不上」——声呐返回 ≠ 点灯凑近真相，把 Phase 0 不可信声呐（#58）+ 节点级 clarity 陡降变黑（#62）做成事件付现（你看不清·只能 ping·ping 在骗你·还得往下）。**系统覆盖一次 ping 能骗你的全部方式**：假底 `the_return`(cosmic·oncePerRun) / 假人 `second_diver`(uncanny·corpse-wearer 声呐侧回声、不推进 Phase 3) / 距离不对 `the_sounding`(uncanny) / 假墙 `the_wall`(cosmic·oncePerRun·直接付现 throat blurb) / 被应答 `the_answer`(cosmic·oncePerRun·轻触 §3.7「另一个世界」) / 没有回波 `no_echo`(uncanny·「看不清才往下走」)。
    - **守则**：全 `[twilight]`/`[midnight]` 单 tag（#19）、**loot-free**（深 cosmic 伏笔惯例，与 #44 无冲突）、**无新敌人**（守 2/zone，mimic·corpse-wearer 是 Phase 3 apex 例外）、**不触发 d_reveal**（#42）、**叙述永不交底**（#54：理性解释与错读始终叠着，`the_wall` fail 明写「是灯骗你还是声呐骗你还是你自己，你分不出来」）。每事件留**「关声呐/摸黑」诚实但盲的出口**＝最低 signature（感知双刃 §3.2-3.3），可生还有代价、无脚本死（`no_echo` 可直接 turn back）。新 `lore.trench.*`（the_return/the_company/the_sounding/the_wall/the_answer/the_quiet）。
    - **回归**：12 新 `scenarios/trench_*.json`（6 事件 × success/fail 或 outcome，statsDelta `event-runner --out json` 实跑抄 #43）+ `playthrough-bands.ts` §8（band.tags 端到端：带 tags→132 trench 事件命中 / 不带→0 泄漏，兼证旧占位是空水道）。事件 81→87、event baseline 75→87、敌人 7/combat 8/item 22 不变。全绿（typecheck / 18 playthrough / scenarios 87 / combat 9 / mapgen 4+60 / lighthouse / verify-tutorial / smoke）+ prod build。未发布不 bump SAVE_VERSION（band.tags 派生、不入存档）。
    - **下一步＝Phase 2 跨 run 前哨/能源（深度脊柱真地基）、或继续铺 trench/更深 band 内容（abyssal 留 >108m）、或 Phase 1 续 band 级 alert 倍率。别预先触发 d_reveal、别加第三只常规敌人。**

64. **深水区 C · band 级探测压力倍率（2026-06-04，填 #61/§12 Deferred「成本曲线·band 级 alert 倍率」）**——`alertDepthFactor` 在 `ALERT_DEPTH_FULL`(60m) 饱和，故 trench_mouth/throat 此前探测压力相同（都因子 1）。本次让更深 band「越深越凶」。
    - **工程**：`DepthBand.alertFactor?: number`（types/bands.ts，缺省 1）→ `startDiveFromOutpost` 落 `run.bandAlertFactor`（types/state.ts，可选；跟 diveModifier 同走 `{...run}` 传递、startDive 的 `...state.run` 不剥离）→ `clarity.ts::alertDelta` 把它**只乘进暴露增益**（`gain = exposure × alertDepthFactor × ALERT_GAIN × (run.bandAlertFactor ?? 1)`）、**不动 DECAY**。data：reef_deep 缺省（=1·45-60m 深度因子本就未饱和）/ trench_mouth 1.3 / trench_throat 1.6 / 深渊 2.0（#65）。
    - **守北极星**：倍率只乘增益不乘消退 → 摸黑/浅水的净消退与倍率无关＝**逃生阀门倍率买不断**（守无脚本死 §9）。缺省回退 1＝POI 下潜/reef_deep 逐字节复现旧 alert 行为。
    - **回归**：`playthrough-bands.ts` §9（数据升序 / 落 run 端到端 / alertDelta 真随倍率放大 6→8.7→11.4 / 摸黑净消退 −3 不被放大）。未发布不 bump SAVE_VERSION（bandAlertFactor 可选数字、派生自 band、JSON round-trip + `?? 1` 兜底）。**UI 零改**（alert 预警读 run.alert 阈值、形状未变）。SPEC §12 Deferred「成本曲线」本次勾、§10 决策日志记。

65. **深水区内容 pass · 深渊 band（>108m）+ abyssal 专属事件「永远有比最深更深的」（2026-06-04，续 #63 band.tags 机制开更深一层）**——trench band（60-108m，#63）之下再开一级 `band.abyssal`（108-140m），用 #63 的 band.tags 机制（既有闲置 ZoneTag `abyssal`、零类型改动）挂专属事件池。整套『越深越欺骗』伏笔最深的一层。
    - **band**：`depth_bands.json` 加 `band.abyssal`（order 4·dark·`alertFactor 2.0`〔续 #64 升序、> throat 1.6〕·`tags:[cave,abyssal]`·借蓝洞 mapgen），**递归更深、不硬编码地板**（架构本就支持，#61）。留更深 band（>140m）续写空间。
    - **内容＝`data/events/abyssal.json`（新文件 5 事件；import 进 zones.ts EVENT_DB）**：母题从 trench「回波对不上」（声呐骗你）推进到「『底』本身在退/在拽/从更深处招你」——`no_floor`(cosmic·oncePerRun·没有底) / `the_rising`(uncanny·从下面上来的东西·apex 伏笔) / `deeper_light`(cosmic·oncePerRun·更深处替你点的假灯·mimic 伏笔·douse 勾连既有 `lore.deep_water.cold_light`) / `the_permission`(uncanny·不该活着却活着·生存铁律恐怖侧) / `still_falling`(cosmic·oncePerRun·身体停了沉没停·reef.no_bottom 拉力最深兑现)。
    - **守则**（同 #63）：全 `[abyssal]` 单 tag（#19）、loot-free、**无新敌人**（守 2/zone，mimic·corpse-wearer 仍 Phase 3 apex 例外·本层只埋伏笔不登场）、**不触发 d_reveal**（#42）、叙述永不交底（#54）、每事件留「关灯关声呐/往上爬/抓住别动」诚实但盲/退的逃生出口（感知双刃 §3.2-3.3）。新 `lore.abyssal.*`。
    - **回归**：11 新 `scenarios/abyssal_*.json`（statsDelta `event-runner --out json` 实跑抄 #43）+ `playthrough-bands.ts` §10（深渊 >108m·dark·倍率 2.0·order 末位 / band.tags 端到端抽出 abyssal 事件 / abyssal 不漏进 cave/trench 池、trench 不漏进深渊·深度+tag 双隔离）。事件 87→92、event baseline 87→98（+11 scenarios），敌人 7/combat 8/item 22 不变。全绿（typecheck / 18 playthrough / scenarios 98 / combat 9 / mapgen / lighthouse / verify-tutorial 92 events / smoke）+ prod build。未发布不 bump SAVE_VERSION（band/事件派生、不入存档）。

66. **深水区 Phase 2a · 跨 run 分阶段前哨 + 真蛙跳出潜点（2026-06-04，「全做」session 第三笔；作者 pin：脊柱优先 / 3 阶段 / 未发布不迁移，AskUserQuestion）**——把 Phase C 一次性 ruin 修复（#52）扩成**多阶段、跨 run 持久**的前哨脊柱。能源经济/衰减留 Phase 2b（作者选脊柱优先）。
    - **进度＝flag（零存档形状改动）**：作者选「未发布不迁移」→ 前哨建造进度不入 profile 新形状，用 `profile.flags` 的阶段标记 `outpostStageFlag(id,s)`=`flag.<id>.s<1..3>` 持久（半亮扛过死亡、Set 自动 round-trip、**SAVE_VERSION 仍 4**）。`outpostStage(profile,id)` 读最高阶段 flag、`isOutpostLit` 判满。
    - **静态 def + promote 复用灯塔网**：新 `OutpostDef`（types/lighthouse.ts；data/lighthouse_upgrades.json `outposts[]`）=`{id,name,bandId,stages[3],result}`，每 stage 一份双资源账单（**T1→T2→T3 深料分层 gate 点亮**：珊瑚→黄铜+蟹甲→章鱼角喙）。`advanceOutpost(state,id)`（engine/lighthouses.ts）按当前阶段权威校验账单（profile 银行）、扣料、置 flag；**建满→push 一座 Lighthouse**（result，幂等防重复）＝复用 Phase C reveal/reach（宏观网络下延 free）。outcome 新 `advanceOutpostId`（types/events.ts）→ applyOutcome 调（与 `restoreRuinId` 平行）。
    - **真蛙跳出潜点（换掉 home stand-in）**：`startDiveFromOutpost`（dive.ts）从 `deepestOutpostLaunch`＝**已半亮（≥ `OUTPOST_USABLE_STAGE`=2）、且 band.order < 目标**的最深前哨起跳，预耗氧按「目标 band 顶 − 前哨 band 底」算（前哨越深越省）。half-lit（stage 2）即可起跳＝作者「半亮给部分收益」。trench_mouth：home 3 回合 → 深槽前哨 1 回合；同层/更浅 band 不受益（前哨须更浅）。
    - **3 阶段建造事件**：`lighthouse.outpost_reef_deep`（lighthouse.json·`[wreck]`·45-60m·oncePerRun·`forbiddenFlags:[s3]`）三个 `visibleIf` flag 门控的建造选项（勘察/运件/通电），每阶段只露当前阶段、点亮后整事件门掉＝一阶/潜（蛙跳节奏）。
    - **回归**：新 `playthrough-outpost.ts`（6 节：三阶推进+扣料+promote / no-op 幂等+断料不退 / 蛙跳缩短预耗氧 / 事件阶段门控 `isOptionVisible` / 进度 round-trip）。事件 92→93（建造事件，无 stat-delta 故无 scenario）。全绿（typecheck / 19 playthrough / scenarios 98 / combat 9 / mapgen / lighthouse / verify-tutorial 93 / smoke）+ prod build。未 bump SAVE_VERSION。
    - **Phase 2b 留**：能源经济（base 层跑设施/充电/充氧）+ 水下衰减（水流更快·可水力发电）+ 设施轨 + SeaChartView 前哨/蛙跳出潜点 UI surfacing（本期建造走 dive 事件、蛙跳收益自动透明、**无 UI 数据路径改动故无 smoke 改**）。多前哨链（trench→abyssal 出潜点）按同模板续（加 OutpostDef + 建造事件即可）。

> 注：quirk #67–#76 的详情记在**顶部滚动条目**里（近十个 session 的惯例：新条目即 quirk 正文，§6 编号列表只补锚点）。下面 #77/#82/#85 同此办（#78–#81/#83–#84 详情亦见顶部对应条目），详情见顶部对应条目。

77. **trench 内容续 pass · 4 个全新 ping-欺骗事件（2026-06-05，周末内容引擎自动无人值守 session·续 #63）**——trench band（60-108m）是最薄的深 band 子池（twilight/midnight 各 3 事件·maxRoomFeatures=2·#74 大房间易抽干），#75 又跳过它 → 本次专补 twilight/midnight 各 +2。母题守『回波对不上』，4 个均为现有 6 个未覆盖的全新欺骗种类：`the_moving_floor`（声呐把灯笼鱼层读成实底＝DSL 幻影海底·cosmic）/ `the_old_ping`（给放弃的 ping 过久才回且变老＝时间/距离不对·SOFAR 声道·uncanny）/ `the_remembered`（一路 ping 攒出的图是另一条竖井＝累积扫描图本身错·勾连 S0 scanMemory·cosmic）/ `the_opening`（声呐让出灯里没有的下行口＝the_wall 的反面·uncanny）。守则全同深段（#54/#19/#42/loot-free/无敌人/留摸黑出口/3 选项/check cosmic vs55·uncanny vs50）；新 `lore.trench.*` 4 条；10 新 scenarios（#43 实跑抄·含 2 cosmic fail）。事件 111→115、event baseline 131→141、敌人 7 不变。`npm run regress` 25/25 全绿 + prod build。**未 commit**（无人值守·留审阅）。详见顶部 2026-06-05 条目。

82. **40-60m 深段「灯的边缘」质感补完（2026-06-06，周末内容引擎自动无人值守 session·轮换离开连补四 band #75/#77/#81）**——established 深段（`cave.*`/`wreck.*` 40-60m）自 #57（2026-06-02）起 4 天未动，深 band 已饱和 → 轮换回来专补其最深、最薄的 40-60m 段。recon 确认 [wreck] 40-60m 仅 5 内容事件（2C/2R/1U）、[cave] 40-60m uncanny 仅 2，两深池都 cosmic-heavy 缺 R/U（cosmic 由深 band 供足）。10 新事件 append 进 `reef.json`（`wreck.{boiler_room,shaft_alley,trapped_air,wardroom,careened}` + `cave.{silt_out,brine_pool,restriction,the_arrows,flowstone}`·零 registrar 改动），tone **4R/5U/1C**（精确填缺口）。灵感＝WebSearch 三口新井：cave-diving silt-out/percolation、Harrison Okene 沉船气穴（`trapped_air` 的 `breathe_it` 拿氧换理智 **+6 O2/−5 San**·oncePerRun·首个正 oxygen-delta 事件）、Jacuzzi of Despair 深海卤水池（`brine_pool`『洞底的湖』·本批唯一 cosmic）。守则全同既有深段：loot 按 #44/#47（wreck→人造·cave→天然·深 cosmic/uncanny loot-free）、单 tag #19、无新敌人（守 2/zone·mimic/corpse-wearer 仍 Phase 3 apex）、不触发 d_reveal #42、uncanny/cosmic 守叙述永不交底 #54、stamina-check 只锁 success #49。15 新 scenarios（#43 实跑抄·含 4 sanity fail + `breathe_it` 起步 oxygen40 验正 delta）。事件 128→138〔`--list`〕、event baseline 177→192、敌人 7/combat 8/item 22 不变。`npm run regress` 25/25 全绿（6.2s）+ prod build。**未 commit**（无人值守·留审阅）。详见顶部 2026-06-06「灯的边缘」条目。

85. **established 浅中段（≤44m）质感补完「所见为真，但有一处不对」（2026-06-06，周末内容引擎自动无人值守 session·轮换离开连补五 band/深段 #75/#77/#81/#82）**——连五次 pass 都在最深 band + 40-60m 深段、已饱和 → 轮换回多日未动的 established 浅/中段（≤44m·#81/#82 两次点名的下一步）。recon 实数确认三缺口：[cave] 26-44m realistic 1/cosmic 0、[reef] 26-44m uncanny 1、[wreck] 26-44m 严带 realistic 0。11 新事件（`blue_caves.json`×4 / `reef.json`×4 / `wreck_graveyard.json`×3·零 registrar 改动）：`bluecaves.{hells_bells(U·Hells Bells 钙质石钟),sulfur_cloud(C·Cenote Angelita H2S 云·本批唯一 cosmic·填 cave cosmic=0),gour_pools(R·gour pools/cave pearls),dry_bones(U·cenote 化石)}` + `reef.{coral_spawn(U·oncePerRun·同步珊瑚产卵·填 reef mid uncanny),cleaning_station(R·清洁站),dusk_chorus(U·礁噪一刀切的静),sea_fan_thicket(R·海扇梳流)}` + `wreck_graveyard.{ghost_net(R·幽灵渔网·填墓园 mid realistic),the_cargo(R·Thistlegorm 货舱),rusticles(U·Titanic 嗜铁菌锈瘤)}`。tone **5R/5U/1C**（浅中段守『所见为真』lean R/U·cosmic 由深 band 供足）。灵感＝WebSearch 四口新井（Hells Bells·同步珊瑚产卵·幽灵渔网·Angelita 硫化氢云）。守则全同既有：loot 按 #44/#47（reef/cave→天然·wreck→人造·uncanny/cosmic 伏笔 loot-free）、单 tag #19、无新敌人（守 2/zone·mimic/corpse-wearer 仍 Phase 3 apex）、不触发 d_reveal #42、uncanny/cosmic 守叙述永不交底 #54、stamina-check 只锁 success #49（dc 12-13 浅中段公式上不可强制 fail）。新 lore 6 条（`lore.bluecaves.*`×3 + `lore.reef.*`×2 + `lore.wreck_graveyard.rusticles`）。17 新 scenarios（#43 实跑抄·覆盖全 11 事件·5 check-success 验 checkPassed·6 lore 路径）。事件 138→149〔`--list`〕、event baseline 192→209、敌人 7/combat 8/item 22 不变。`npm run regress` 26/26 全绿（8.2s）+ prod build。**未 commit**（无人值守·留审阅）。详见顶部 2026-06-06「所见为真」条目。

83. **即时新 POI 浮现（2026-06-06·方向 A「声呐与房间收尾」之一）**——建灯 / 点亮前哨后新进范围的 POI 当场浮现（不必等下个 run·补 #80 留的尾巴）：`SeaChartView` chart memo 加**灯塔覆盖签名**（坐标+设施+`effectiveRevealRadius`+flags）→ 建灯当场重算 + `chart.ts` roaming 改 **pool-independent 确定性键**（Efraimidis–Spirakis A-Res·模板键 id `poi.roam.<run>.<templateId>`）＝重算不重洗已显示机会点。删 `generateChart` 的 `rng` 入参。`playthrough-chart` §8。详见顶部 2026-06-06「即时新 POI 浮现」条目。

84. **猎手 SPEC + Stalker Phase 1 spine（2026-06-06·方向 A 之二·作者三问拍板）**——声呐与房间 §8.7「定位 stalker」开题：新 `docs/深海回响_猎手_SPEC.md` 捕捉完整愿景（感官模态 光/声/双·感知分层 灯=接近/声呐=位置·切信号行为 停/移到上次信号点·升级规避 T1 吸声 T2 迷彩·大型生物狭小避难·执着等待者·decoy 道具·全深度浅弱深难）。Phase 1 spine 实装＝**有位置、会逼近、按感官显示不同保真度**的猎手（`run.stalker?`/`run.huntEnabled?`·新 `engine/stalker.ts`·`dive.ts` moveToNode 分流·`SonarScanPanel` 精确 blip·`DepthBand.hunts` trench+）：灯=接近/声呐=位置**同一只猎手**·接触复用 `ambushEncounters`（不加新敌）·摸黑切信号=逃生阀门·**additive 守 playthrough-stealth §4-§6·不 bump SAVE_VERSION**。新 `playthrough-stalker` + smoke E8。Phase 2+ deferred（已 SPEC 捕捉）。详见顶部 2026-06-06「猎手 … Stalker Phase 1」条目。

86. **定向 ping · 方向扇区（2026-06-06·方向 A「确保真的完成」之一）**——声呐从全向一律升成可朝一个扇区（朝深处/侧向/来路·`node.layer` 差分）聚焦：那方向探更远、别处更短（`revealSonarScanDirectional`·近场仍全向·波束只沿聚焦扇区扩=连贯·封顶 `SONAR_DIR_RANGE_MAX`），暴露按方向计（`sonarPingAlertDelta(run,dir)`：窄波束更安静×0.55·正对声/双感猎手扇区尖峰×1.7＝照亮它·`pingAimsAtSoundStalker` 真实位置/`seenStalkerSector` 会过时位置）。扇区分类 `nodeSector` 住 sonar.ts（引擎不碰 ui/mapLayout）。`sensors.sonarDir` 记聚焦（移动清·不 bump SAVE_VERSION）。omni 缺省逐字节不变。`playthrough-sonar` §13 + smoke E4b·提交 `6bfdc76`。详见顶部对应条目。

87. **房间 feature 数升级 · 大房间出现率轴（2026-06-06·方向 A 之二·收尾 #74 尾巴）**——`roomFeatureChanceBonus` 沿 #80 传感器桥（7 处全补=护栏·夹 `ROOM_FEATURE_CHANCE_MAX`0.3）→ `startDive` 读 `run.sensorTuning` 透传 mapgen `rollExtraFeatures` 抬大房间率（**band `maxRoomFeatures` 仍是尺寸天花板**·缺省 0 逐字节复现旧图）。data `salvage_guild` lv4「深潜打捞队」effect 0.18。`playthrough-upgrades` §9 + `-mapgen-scenarios` 出现率不变量 + `-save` round-trip + smoke J7·提交 `953622a`。详见顶部对应条目。

88. **无名渊 band（>230m·最深一层）+ nameless 专属事件「界限没了·往下的那个是你」（2026-06-06·周末内容引擎无人值守·续 #72 递归更深）**——渊外（subhadal 180-230·#72）之下开 `band.nameless`（230-290·dark·alertFactor 3.5·sonarDeception 0.04·`tags:[cave,nameless]`·新 ZoneTag·借蓝洞 mapgen·hunts），母题刻意不重渊外『外部诱饵』而推进到『你和它界限没了』（声呐回波是你/你声音从下面答你/记号在没去过的前头/影子不跟你上浮/决定先你一步做好）＝[[deep-game-vision]] D-reveal+corpse-wearer 最深伏笔·**不触发 d_reveal**(#42)。`data/events/nameless.json` 9 事件（5U/4C·4 oncePerRun·含 1 真物理 grounding `the_pale_floor`）·全 loot-free/无新敌人（守 2/zone）/单 tag #19/永不交底 #54/每事件留盲退逃生口。灵感 Third Man factor / heautoscopy 自窥 / SOFAR 声道（充当『平淡解释』半边）。22 scenarios（#43 实跑抄·9 事件 success+fail + 4 give-in）+ `playthrough-bands` §13（130 抽出/0 双隔离/升序）。事件 149→158·event baseline 209→231·band 6→7·敌人 7/combat 8/item 22 不变。`npm run regress` 26/26 + prod build 全绿。提交 `c249644`（本 session 起手收编周末内容·见 [[sandbox-git-commit]]）。详见顶部 2026-06-06「无名渊」条目。

89. **猎手 Phase 2+ · §3 升级规避 T1 吸声 / T2 主动迷彩（2026-06-06·方向 E·猎手 SPEC §3·#86 天然延续）**——玩家侧规避，**对称**猎手 `stalkerEvadesScan`：T1 `soundAbsorbBonus` 规避**声感**猎手 / T2 `camoBonus` 规避**光感**（双感取 min＝两者都有才甩得动·耦合 `Stalker.sensesBy`）。新 `engine/stalker.ts::playerEvadesStalker`（感官匹配·封顶 `STALKER_PLAYER_EVADE_MAX`0.6·深 band〔≥108m〕打折 `×0.5`·确定性不耗 RNG）→ `advanceStalker` alert 越线但被规避的那一回合**当作信号切断**（→searching·你真甩得动它）；**守地板**＝规避永不到 1·最深/最凶仍找得到你（对称 `SIGNATURE_MIN_ACTIVE`）。两旋钮沿 #80/#87 传感器升级桥（7 触点全补·编译期护栏·夹 `STEALTH_BONUS_MAX`0.6）；data 新 `line.evasion_rig`（吸声涂层 lv1 / 主动迷彩 lv2·深料软门控）+ UpgradePanel 标签。**缺省 0 → playerEvadesStalker 恒 false → advanceStalker 逐字节不变**（additive/gated 守 playthrough-stealth·不 bump SAVE_VERSION）。`playthrough-stalker` §8（感官匹配/双感取短板/深 band 打折守地板/被规避转 searching/无升级逐字节）+ `-upgrades` §10（桥 round-trip+夹上限+前置门控）+ `-save` round-trip + smoke J8·全绿 26/26·提交 `574ae4a`。详见顶部对应条目。

90. **声呐与房间 SPEC 收尾 · 三 residual 清零 · SPEC 关闭（2026-06-06·方向「释放·closeout」·作者『做完能释放掉文件/流程的先做、直到全部完成』→ 全部 3 项）**——把声呐与房间 SPEC 仅剩三项 residual 全做完、**SPEC 彻底完成关闭**（仅余的猎手 Phase 2+ 在猎手 SPEC §7·与本 SPEC 无关）。① **聚焦扇区可视化**：`SonarScanPanel` 据 `run.sensors.sonarDir` 画聚焦扇区楔形 `sonar-focus-wedge`（`focusWedgePath` 纯几何·deeper 朝右/back 朝左/lateral 上下两瓣·略超量程环＝那一向探更远·暖色 warn 与冷色真图一眼可分）·纯渲染·smoke E4b。② **各方向 reach 各自升级（§5）**：新 `UpgradeEffect{kind:'sonarDirReachBonus',dir,value}`（带 `dir` 判别·**首个非标量旋钮**·聚合进 `Record<SonarDir,number>`）沿同款传感器桥（`getUpgradeBonuses→getRunBonuses→createNewRun→deriveSensorTuning`·逐向夹 `[0,SONAR_DIR_REACH_MAX]`2）落 `SensorTuning.sonarDirReach`；`revealSonarScanDirectional` 加第 5 参 `dirReach`（聚焦焦距 `min(SONAR_DIR_RANGE_MAX+reach, base+FOCUS+reach)`·近场/别向不延长＝守北极星「再聚焦也扫不穿整洞」）·`sonarDirReach(run,dir)` 读·`pingSonar` 透传；data `sonar_rig` lv6/7/8 三向各 +1（**定向仍免费·reach 才是 power-up**）。**缺省全 0 → 定向揭示逐字节不变·不 bump SAVE_VERSION**（record 纯对象 round-trip）。③ **§5 later 接触带大小 + 开放水域扫描**：`Stalker.large?`（spawn `depth≥STALKER_LARGE_DEPTH`108m·缺省小 blip）→ `stalkerSonarBlip` 透传 → `SonarScanPanel` 画弥散质量 `sonar-stalker-mass` 一大团；开放水域（层状 zone·`!zoneAllowsBacktrack`）声呐图不画洞壁通道边（`sonar-edge`）、只显接触读数（`is-open-water`）·迷路洞穴仍画通道。新 quirk #91（`.sonar-stalker circle` 通用规则压过嵌套 mass 的 CSS 特异性陷阱）。`playthrough-sonar` §13(i/j) + `-upgrades` §11 + `-stalker` §2b + `-save` round-trip + smoke E4b/E4c/E8/J9·全绿 26/26 + prod build·提交 `e55187d`。详见声呐与房间 SPEC §7（关闭块）/§11。

92. **声呐图垂直化＝真实深度·「位置即深度」系统不变量（2026-06-06·作者已定定向任务·#92）**——声呐图 / MapDevPanel 纵轴此前无深度含义（`deriveMapLayout` 把 depth 横排 x∝layer、y 只是同层 id 堆叠序）→ 改成 **y∝node.depth（上＝浅 / 下＝深·真实米数·固定 px/米·取 > blip 直径防纵向重叠）**，x 改同深度分散（按 depth 分箱居中·纯避重叠·无方向语义），单一来源 `ui/mapLayout.ts` 共用 SonarScanPanel + MapDevPanel。① **P1 渲染**：SonarScanPanel viewBox 转 portrait·`focusWedgePath` 重映（deeper↓/back↑/lateral←→·SVG y 朝下）·量程环/楔形/接触半径改 `min(VIEW_W,VIEW_H)`·残图小地图竖盒·`.sonar-scan` 限高 300px；MapDevPanel 零改自动跟随（全由 layout 驱动）。② **P2 扇区耦合**：`engine/sonar.ts::nodeSector` 从 `layer` 差分改 **`node.depth` 差分**（新 `SECTOR_DEPTH_EPS`1.5 容差带）——否则「朝深」楔形指下、定向 ping 却扩 layer-deeper（迷路图里可能 depth 更浅、渲染在上方）＝自相矛盾；`revealSonarScanDirectional`/`stalkerSector`/`seenStalkerSector`/`pingAimsAtSoundStalker` 全随之按真实深度。`playthrough-sonar` §13 fixture 解耦 layer↔depth（layer 故意相反）真测 depth-based（layer 实现会三条全错）。③ **P3 系统不变量**：审计确认 mapgen 主下行 depth 早已单调-from-start（层状 `round(d0+step·L)` 逐层非减+同层相等·迷路树距+jitter 起点钉 d0、最深钉 d1）→ 只加 `playthrough-mapgen-scenarios`「位置即深度」垂直性不变量（迷路 60 + 层状 30 seed）兜·**不改生成·不破快照**（区别 #90 全 additive）。**视觉验证**（绿 ≠ 画对·quirk #91 教训）＝SSR→SVG 坐标核对：上浅下深 monotonic·楔形朝下·min 间距 ≥ blip 直径无重叠·回边 chord 可读。新 quirk #93（位置即深度系统不变量）。未发布不 bump SAVE_VERSION（布局纯渲染派生·EPS 常量）。`npm run regress` 26/26 + prod build 全绿·提交 `1829d14`（feat）+ 本 docs commit。详见深水区 SPEC §13 + §10 决策日志 + QUIRKS #93。

94. **基建治理 session · 把解耦/定位约定做成机制（非散文）+ 两个 Tier-2 设计提案（2026-06-06·作者已定元层任务·非游戏功能开发）**——上一轮架构评审发现的耦合/膨胀按分层安全收拾；统一原则（已写进 `CLAUDE.md` 顶部 + 起手约定）：想长期守住的解耦/约定落成**机制**（regress 门/脚本/gitignore/类型/目录边界）而非散文——散文随 session churn 丢失、机制不会。**Tier 1（实装·机制化）**：① `engine ↛ ui` 边界做成会红的门——新 `scripts/check-boundaries.mjs`（扫 `src/engine/**` 命中 `react`/`ui` import 即报 `file:line` 退 1·兼容多行 import）注册进 `scripts/regress.mjs`·全量任务 **26→27**·现 0 违例＝绿（quirk #95；负路径实测能正确报红、`./build-utils`/`@/ui-utils` 不误判）。② 交接定位从 git ground truth 再生——新 `scripts/handoff.mjs` + `npm run handoff`（纯只读·git 一律 `--no-optional-locks` 不留锁·打印 git log/status + 最新 nightly REPORT 头 + STATUS 顶 blockquote + CHANGELOG 尾），交接 prompt 只留人写的方向/下一步、根治「夜间漂移」手抄失真（quirk #96）。提交 `bbd08d3`（feat）+ 本 docs commit。**Tier 2（仅方案·不实装·见 `docs/infra/`）**：`dive_ts_split_proposal.md`（`dive.ts` 826 行/14 import 的纯搬移式拆分·barrel 保公共 API 零 import churn·**defer 到并发隔离就位后再做**——它是三写手最易撞车的文件、现在大改最大化冲突·与降耦合目标相反）+ `concurrency_isolation_proposal.md`（周末/夜间/交互三写手共用裸 `main` → 方案 A：周末引擎独立 `auto/weekend` 分支 + 夜间合并·与沙箱「不能 unlink」共存·**触及两定时任务 SKILL·待作者确认才动**）。**Tier 3（仅标记给作者）**：内容天花板——ZoneTag 已用尽、nameless 是叙事地板（230-290m·#88）、周末引擎再加事件边际递减；建议周末引擎从「更多事件」转向**变体密度 / 更少更大批**，增长轴转交互 session 的新机制（猎手 Phase 2+ / 深水区 Phase 3）。**不碰**游戏功能 / SPEC / `SAVE_VERSION` / 定时任务 SKILL。`npm run regress` **27/27** 全绿（含新 check-boundaries·墙钟 6.6s）+ prod build。详见 `docs/infra/*` + QUIRKS #95/#96。

97. **发布版带 dev 面板·URL `?dev` 门控（2026-06-06·作者要求线上可测·元层·非游戏功能）**——`App.tsx` dev 面板（Shift+D/C/M = 事件/战斗/地图调试器）门控从编译期 `import.meta.env.DEV` 改成运行时 `DEV_TOOLS = import.meta.env.DEV || URLSearchParams(location.search).has('dev')`。**每个发布版都带 dev 能力、默认隐藏**——仅 `https://shenhuang.github.io/blue/?dev` 启用（藏在 Shift 快捷键后·普通访客看不到·地图调试器会揭示整张图破坏迷雾/声呐设计故门控）。运行时值 → Rollup 不再 dead-code 消除三元 → `EventDevPanel`/`CombatDevPanel`/`MapDevPanel` 三 lazy chunk 进 bundle，仅 `?dev` 打开某面板时才下载（普通访客零额外负载·主包仅留 import() 引用 + ?dev 判定）。`vite build` 产物 `assets/` 验三 `*DevPanel-*.js` 在。全绿 27/27 + prod build。提交 `e6ce537`（feat·已 push origin `caba35b→e6ce537`·一并带上未推的 #94·触发 GitHub Pages 部署）。**别改回 `import.meta.env.DEV`（会去掉线上 dev·作者就没法在线上测）——见 QUIRKS #97。**

98. **reef 浅段 `[shallow,reef]` 变体密度 · 10 新浅水事件（2026-06-07·周末内容引擎自动无人值守 session·轮换离开连六补深 band/深段/established ≤44m）**——近六 pass（#75/#77/#81/#82/#85/#88）填满最深四 band + established 40-60m + ≤44m + 新开 nameless（最深一层）后，按 #94 Tier-3「内容天花板·转变体密度」+ #88 点名「established 浅段 reef≤25m 伏笔层轮换」，轮换回**最薄也最常被看见的段**：recon（verify-tutorial 事件池）确认旧灯塔礁 **12m `[shallow,reef]` 浅池仅 6 个事件**（4R/2U·sun_net 14m 起不入 12m 池），而 25/38/50m 段各 26–33——浅池是全 zone 最薄、又每次出海必经（直接对抗「重复 2–3 次就认脸」的原始瓶颈）。**10 新事件 append 进 `reef.json`**（全既有 zone 前缀·**零 registrar 改动**），tone **4R/4U/2C**（浅段守『所见为真』lean R/U·cosmic 仅 2 个 subtle 伏笔）：realistic `fish_trap`〈鬼笼·徒手 stamina 抓虾 / 刀挑放生+割网黄铜〉/ `mooring_block`〈系泊水泥沉块·拧黄铜卸扣 stamina〉/ `staghorn`〈鹿角珊瑚林·挪进林心觅食〉/ `seagrass_flat`〈海草滩·掏根垫·克隆草「比灯塔老」grounded 一笔〉；uncanny 伏笔 `garden_eels`〈花园鳗一齐缩洞，正中几根慢半拍/顺着你转·sanity vs47〉/ `upside_down_jellies`〈倒立水母 Cassiopea 朝上的口+蜇水·等着接从上面掉下来的你·vs47〉/ `cuttle_passing`〈乌贼 passing-cloud 催眠·看下去 lost-time 多耗氧〉/ `star_sand`〈有孔虫星砂·脚下全是小骨头·伏笔下面〉；cosmic 伏笔 `your_shadow`〈影子不跟你·朝深水走·伏笔 nameless.the_outline·oncePerRun vs50〉/ `the_low_note`〈低到耳听不见的一下一下·始终在你正下方配着你·成功＝自己心跳的平淡解释·oncePerRun vs50〉。灵感＝WebSearch 三口新井（garden eels 同步缩进黏液管穴 / Cassiopea 倒立水母「stinging water」cassiosome 黏液刺胞 / cuttlefish passing-cloud 移动条纹压垮猎物运动检测）。守则全同既有浅段：loot 按 #44/#47（realistic→天然 coral_shard/lobster + 灯塔渔人造 brass_fitting/old_fishing_net·`[shallow,reef]` 只在灯塔礁出现天然隔离不漏·uncanny/cosmic 伏笔 loot-free，star_sand 仅 coral chance）、单 tag #19、**无新敌人**（守 2/zone·mimic/corpse-wearer 仍 Phase 3 apex 例外）、**不触发 d_reveal** #42、uncanny/cosmic 守**叙述永不交底** #54（success 给平淡解释「道理都对」/「是你自己的心跳」·fail 落不交底）、stamina-check 只锁 success #49（dc 13 浅段公式上撞不到 0.05 fail clamp），cosmic 双分支 fail 起步 sanity 20→8 不触底（#43）。新 `lore.reef_shallows.*` 6 条（the_garden / the_open_mouths / the_passing / the_star_sand / your_shadow / the_low_note·延续既有 the_gap/the_still_square/the_warm_crack 浅水 fresh-wrongness 命名空间）。**13 新 scenarios**（`event-runner --out json` 实跑抄 #43·覆盖全 10 事件·4 check-success 验 checkPassed·6 lore 路径·2 cosmic 双分支 success+fail·1 tool-gated `cut_it`〔默认 loadout 带潜水刀〕）。事件 **158→168**〔`--list`〕、event baseline **231→244**、敌人 7/combat 8/item 22 不变；旧灯塔礁 12m 浅池 6→11。`npm run regress` **27/27** 全绿（6.7s·含 prod build·check-boundaries / typecheck / 全 playthrough / scenarios 244 / combat 9 / mapgen 4+60 / verify-tutorial 168 / smoke）。**未 commit**（无人值守·留审阅）。

99. **沉船墓园浅段 `[wreck]` 变体密度 · 10 新浅水事件（2026-06-07，周末内容引擎自动无人值守 session·续同日 #98 reef 浅段变体密度·按 #94 Tier-3 换 zone 轮换）**——#98（同日早些档）补完 reef 12m 浅池后，按 #94 Tier-3「内容天花板·转变体密度 / 最常被看见的池」轮换**换 zone**：reef→沉船墓园。recon（`event-runner --list` × 深度 × tone）确认近七 pass（#75/#77/#81/#82/#85/#88/#98）已填满最深四 band + nameless + established 40-60m + ≤44m + reef 浅段 → **唯一仍薄的『每潜必经』池＝墓园 18-25m 浅段**（严格浅段仅 ~2R/2U/1C：deck_cargo/cabin_entrance + the_other/full_nets + all_facing），又是每次墓园出海必经（直接对抗「重复 2–3 次就认脸」的原始瓶颈）。**10 新事件 append 进 `wreck_graveyard.json`**（全既有 zone 前缀·**零 registrar 改动**），tone **5R/4U/1C**（浅段守『所见为真』lean R/U·cosmic 仅 1 个 subtle 伏笔·深层 cosmic 强度已由深 band 供足）。灵感＝WebSearch **四口全新井**（刻意避开 #85 已用的 Thistlegorm/Titanic/幽灵网 + corpse-wearer〈the_other/no_bubbles/the_wearer〉/ 敲击〈the_knocking/dive_slate〉线）：① **船钟认尸船**（钟＝船的名字/心脏·谁找到钟谁替无名船报名）② **沉船成生物栖地**（康吉鳗占机舱/通风筒当白天的窝·『成了礁』）③ **保存完好的内部**（煤还堆着+铲子摆着没人收·住舱铺位毯子靴子原样·全船钟停在沉没那一分钟）④ **Antikythera 机械 + 古陶罐货**（过精巧的青铜齿轮团·钢船底下更老的封口陶罐）。realistic：`ship_bell`〈撬钟认名·brass_fitting+lore.the_bell / 只刮名记下〉/`the_lodger`〈康吉鳗占窝·绕过它够旧渔具 stamina·**非战斗** wreck-as-habitat〉/`coal_bunker`〈扒煤够工具箱·黑雾 silt 质感·brass/canned〉/`the_reef_now`〈沉船整个长成礁·**depthRange [18,24] 隔离不漏进灯塔礁 wreck 层（reef wreck tag 从 25m·#47）→ 故可破例用天然 coral/lobster loot**·rest 给少见的 +1 sanity〉/`amphora_hold`〈钢船底下更老的封口陶罐·起罐取青铜封箍·『比灯塔还老』深时 grounded·lore.the_older_ship〉；uncanny（全**新鲜错位·刻意不碰 corpse-wearer**）：`the_quarters`〈太齐整的住舱·缸把手全朝一向·oncePerRun·success 平淡解释〈淤泥定住〉/fail『有人在收拾』·lore.the_kept_quarters·loot-free·拿东西分支用本作对死者的温柔『有些屋子不该拿走东西』〉/`the_gear_lump`〈Antikythera 式数不停的齿轮·**撬走 stamina→死废铜 grounded／拨动 sanity→uncanny 数不停**两路·永不交底不说它数什么·lore.the_count〉/`the_fish_that_wait`〈鱼不惊散反聚拢齐齐等你·学会的期待『等接下来该来的那一下』·≠blind_school·lore.the_waiting_fish〉/`the_same_minute`〈全船钟停在她沉的同一分钟·水停钟真实现象+时间冻结 dread·≠pocket_watch_log/trench.the_long_minute·lore.the_same_minute〉；cosmic：`swept`〈甲板有一臂宽是有人扫到一半的干净·栏杆铜擦亮、烂缆盘成洋流盘不出的圈·『等会儿回来接着扫』＝有东西在打理沉船·oncePerRun·**环境无人影**·subtle 深水欺骗最浅伏笔·与 corpse-wearer 同主题但非其〈无 figure〉·lore.the_swept_deck〉。守则全同既有：loot 按 #44/#47（realistic→wreck 人造 brass_fitting/canned_food/old_fishing_net·the_reef_now 隔离破例天然·uncanny/cosmic 伏笔 loot-free）、单 tag #19、**无新敌人**（守 2/zone·mimic/corpse-wearer 仍 Phase 3 apex 例外）、**不触发 d_reveal** #42、uncanny/cosmic 守**叙述永不交底** #54（success 给平淡解释「道理都对」/fail 落不交底）、stamina-check 只锁 success #49（dc 13 浅段公式上撞不到 0.05 fail clamp），cosmic/uncanny 双分支 fail 起步 sanity 20→8/9 不触底（#43）。新 `lore.wreck_graveyard.*` 7 条（the_bell/the_older_ship/the_kept_quarters/the_count/the_waiting_fish/the_same_minute/the_swept_deck）。**17 新 scenarios**（`event-runner --out json` 实跑抄 #43·覆盖全 10 事件·6 realistic stamina/no-check·the_quarters/the_gear_lump/swept 三 sanity 事件含 fail 分支·the_reef_now rest 验罕见 +1 sanity·全 7 lore 路径）。事件 **168→178**〔`--list`〕、event baseline **244→261**、墓园池 24→34、敌人 7/combat 8/item 22 不变。`npm run regress` **27/27** 全绿（6.4s·含 prod build·check-boundaries / typecheck / 全 playthrough / scenarios 261 / combat 9 / mapgen 4+60 / verify-tutorial 178 / smoke）。**未 commit**（无人值守·留审阅）。

100. **声呐渲染重做 ②③④ 实装 session（2026-06-07·交互 session·接 #98 `59ee10d` 洞穴一致性之上·作者逐拍 demo 拍板的源真＝`docs/spec/深海回响_声呐渲染重做_SPEC.md`）**——把下潜内声呐图从「示意 node graph（圆点+连线）」重做成**有机洞穴垂直剖面 + 雷达扫描**，并把猎手从「节点快照」升成「可处通道中段 + 扫描门控显示」。按 SPEC §6 引擎优先→渲染最后、每步独立全绿分提：**② 猎手 mid-edge + 追击（`5eedc11`·`stalker.ts`）**＝位置 `nodeId`→`{nodeId,edgeTo?,edgeProg?}`（additive 不 bump）+ `STALKER_HSPEED`(0.8) 速度阀（<1＝可甩·靠原地回合贴近·不变量 `1−HSPEED≤CONTACT_DIST`0.5＝一跳之差当回合必贴上）+ 三态 + 对穿（`dive` 传 `fromNodeId`）/贴近接触 + 扫描门控快照红点（`seenEdgeTo/seenEdgeProg`·只波前扫到那刻刷新）+ 停下迎战（`standAndFight`·复用 encounterId + `startCombat` 注入玩家 `ambushing` 先手·`startCombat` 加可选 `initialPlayerStatuses`）。**③ 声呐开/关窗口（`ccef8b0`·`clarity.ts`/`dive.ts`/UI）**＝`SensorState.sonarOn`(本回合·缺省开)/`sonarNext`(下回合预承诺) + scan-on-open（`autoScanOnArrival` 到站自动扫·无尖峰·电不够哑火）+ 暴露按状态（持续开则透传期计 signature）+ 本回合反悔（`pingSonar`）+ NodeSelectView 开/关切换；**仅 `sonarUnlocked` 才落新字段 + 持续发射＝未解锁逐字节不变**（`sonarActive` 仍 keyed on `sonar==='ping'`·最小改动保 stealth）。**④ canvas 洞穴渲染（`3ce44e3`·`SonarScanPanel` SVG schematic→canvas）**＝有机洞穴剖面（导出纯函数 `caveSdf`：SDF capsule 隧道+blob 洞室+值噪声不规则发光岩壁·半分辨率）+ 雷达扫描（rAF ~1.2s·亮前缘+淡化拖尾+波前 clip 门控揭示）+ 节点显隐（只相邻可去 `choices` 画可点标记·点击=move·防剧透）+ mid-edge 红点插值（呼吸点+外圈·青你/红猎手·不要 X）+ 旧图保留（去渐隐·完成 ③ §4 retention 渲染面）+ 非洞穴黑+占位；`deriveMapLayout` 传小 `pxPerMeter`(13)+byLayer x 铺开（并入本就未提交的 `mapLayout.ts`）；面板 CSS 走 `document.head` 注入（避脏 styles.css + 不让 class 名进 SSR 文本污染 smoke 子串断言）。回归：`playthrough-stalker` §1-3 改判 mid-edge + 新 §9·`playthrough-sonar` §7/§13h 改判 scan-on-open + 新 §14·`playthrough-sensors` §9·`smoke-chart-ui` E4-E8 重写为新结构。每步 `npm run regress` **27/27** 全绿。新 quirk #100（洞穴一致性）/#101（渲染重做约定与 gotcha）。**canvas 画对仍待作者 dev-server 肉眼**（绿≠画对·SSR 只断 class 串·quirk #91/#93·调参旋钮见 SPEC §9 末）。详见声呐渲染重做 SPEC §6/§9 + QUIRKS #100/#101 + [[deep-game-vision]]。

101. **蓝洞群浅段 `[cave]` 变体密度 · 10 新浅水事件（2026-06-07·周末内容引擎自动无人值守 session·续同日 #98 reef / #99 墓园浅段·按 #94 Tier-3 换 zone 轮换 reef→墓园→蓝洞群）**——#98/#99 补完 reef 12m 与墓园 18-25m 浅池后，按 #94 Tier-3「内容天花板·转变体密度 / 最常被看见的池」轮换**换 zone**到蓝洞群。recon（`event-runner --list --zone-tag cave` × 深度 × tone）确认近七 pass（#75/#77/#81/#82/#85/#88/#98/#99）已填满最深四 band + nameless + established 40-60m + ≤44m + reef/墓园浅段 → **唯一仍薄的『每潜必经』池＝蓝洞 12-25m 浅段**（4R/4U/1C·且 quirk #20 早记 entrance_light 在该段过曝、几乎每潜必见）。**10 新事件 append 进 `blue_caves.json`**（全既有 `bluecaves.*` 前缀·**零 registrar 改动**），tone **4R/4U/2C**（浅段守『所见为真』lean R/U·cosmic 仅 2 个 subtle 伏笔）。灵感＝WebSearch **四口全新井**（刻意避开 #85 已用的 Hells Bells / 硫化氢云 / cenote 化石）：① **cenote 垂下的树根**（顶上垂进水里的树根·缠绕/扯面罩 entanglement hazard）② **盲毒 remipede（Xibalbanus）**（无眼·唯一有毒甲壳类·靠化学感觉的长须探猎物＝『看不见你但知道你在』）③ **relict 钟乳石＝淹掉的干洞**（钟乳石只在空气里长·水下的＝这洞曾是干的、海后来漫上来）④ **halocline 假水面**（盐淡水交界一层平镜·像水面/像出口·底下全在化开）。R：`root_tangle`〈洞口树根·`thread_through` stamina vs13 钻越→龙虾 forage / 绕远〉/`percolation`〈气泡拱松顶泥下雨·压低慢过 / 屏气冲〉/`shell_drift`〈一代代空壳堆·翻找 coral / 不动〉/`surge_passage`〈洞口外涌还够得着·数拍子 stamina vs13 借力 / 等它平〉；U（伏笔·叙述永不交底·success 给平淡解释 / fail 落不交底）：`the_feelers`〈盲毒虫的须靠嗅觉一齐朝你探·sanity vs47·success＝是嗅不是看〉/`the_mirror`〈半水里一层假水面·sanity vs47·success＝盐线 halocline·**假出口**〉/`the_draft`〈进不去人的小洞往外匀匀呼凉气·give-in 贴脸 / `block_it` 堵口 sanity vs47 success＝潮汐挤压·承 someone_below 暗线〉/`fresh_silt`〈细灰上朝里去、不是你的新鲜鳍沟/掌印·`press_beside` 自比 sanity vs47 success＝静水存印·先来者 someone_below〉；C（oncePerRun·loot-free·sanity vs50 双分支·fail 起步 sanity 20→8 不触底 #43）：`deeper_blue`〈竖井暖蓝越深越暖诱你下去·success＝氮醉+光学·**深处诱饵最浅一拍**·承 no_bottom/subhadal 诱饵线〉/`the_same_passage`〈一直直走却被绕回认得的原地·success＝洞穴失向·**空间欺骗最浅一拍**·承 hadal.the_fold〉。守则全同既有浅段：loot 按 #44/#47（realistic→天然 coral_shard/lobster·`[cave]` 12-25m 与灯塔 cave-tag 45m+ 不重叠＝天然隔离仅蓝洞·U/C 伏笔 loot-free）、单 tag #19、**无新敌人**（守 2/zone·remipede/盲虫只作环境 fauna 非 combat·mimic/corpse-wearer 仍 Phase 3 apex）、**不触发 d_reveal** #42、U/C 守**叙述永不交底** #54、stamina-check 只锁 success #49（dc 13 浅段公式上撞不到 0.05 fail clamp）。新 `lore.bluecaves.*` 6 条（the_feelers / the_false_surface / the_breath / fresh_silt / the_deep_blue / the_same_passage）。**15 新 scenarios**（`event-runner --out json` 实跑抄 #43·覆盖全 10 事件·6 check-success 验 checkPassed·6 lore 路径·2 cosmic 双分支 success+fail）。事件 **178→188**〔`--list`〕、event baseline **261→276**、蓝洞事件 27→37、敌人 7/combat 9/item 22 不变；蓝洞 12-25m 浅池 ~9→19（稀释 entrance_light 过曝·quirk #20）。`npm run regress` **27/27** 全绿（8.7s·含 prod build·check-boundaries / typecheck / 全 playthrough / scenarios 276 / combat 9 / mapgen 4+60 / verify-tutorial 188 / smoke）。**未 commit**（无人值守·留审阅）。

102. **旧灯塔礁中段 `[reef]` 变体密度 · 10 新「猎奇」海洋生物事件（2026-06-07·周末内容引擎自动无人值守 session·续同日 #98 reef 浅 / #99 墓园浅 / #101 蓝洞浅·三浅池当日补完后轮换深度 shallow→mid）**——#98/#99/#101 当日把三个 zone 的 12-25m 浅池（每潜必经）依次补满后，按 #94 Tier-3「变体密度·最常被看见的池」**轮换深度**到次常见的 mid 段（26-44m）。recon（`event-runner --list` × zone×深度×tone 实数矩阵）确认 mid 段三池里 **reef 26-44m 最薄且 tone 最偏（6R/4U/2C·realistic-heavy）**，且 `[reef]` tag 只在旧灯塔礁出现（不跨 zone 泄漏·quirk #19）＝最低风险池。**10 新事件 append 进 `reef.json`**（全既有 `reef.*` 前缀·**零 registrar 改动**·loreEntry 自由串无需注册），tone **2R/6U/2C**（专补 realistic-heavy 缺口·把 reef mid 拉向 8R/10U/4C）。灵感＝WebSearch **四口全新井（真实海洋生物「猎奇」素材·扭进冷克制 uncanny 语气·科学只作跳板·授权夸张但守红线）**：① **Cymothoa exigua 噬舌虫**（钻鱼嘴吃掉舌头、自己当宿主舌头使·动物界唯一替宿主长零件的寄生虫）② **博比特虫/sand striker**（3m 多毛虫埋沙伏击·五根触须探振动·一摆尾斩鱼·啃白的鱼骨铺满沙沟）③ **锯齿矛䲁拟态清洁鱼**（学清洁鱼条纹+招客舞靠近·一口咬下鱼鳍·被咬的照旧回来排队）④ **棘龙虾单行迁徙**（触角搭尾排成几十只的队·昼夜行军·地磁导航）；另接既验真实素材：鹦嘴鱼黏液茧（吐袋盖气味防夜捕）·虾蛄柄眼分头追+空蚀重拳·珊瑚夜里伸扫帚触手蜇邻居争地盘·介形虫黄昏齐拍发光「串珠」·桶海绵长寿 deep-time。R：`lobster_train`〈龙虾单行队·stamina vs13 薅 lobster / 记方向 / 让过〉/`barrel_sponges`〈桶海绵『比灯塔老』grounding·stamina vs13 掏 coral+lobster〉；U（叙述永不交底·success 平淡解释「道理都对」/ fail 落不交底）：`borrowed_tongue`〈噬舌虫·小黑眼睛朝上看你·vs48·fail 不停用舌尖顶上颚确认还连着〉/`false_cleaner`〈拟态清洁鱼·vs48·『连被咬的也照旧排队』·拟态母题但不碰 mimic apex/不触发 d_reveal〉/`the_cocoon`〈鹦嘴鱼黏液睡袋·no-check 碰茧 lore『把自己吐进袋子好叫死亡闻不见』〉/`the_watcher`〈虾蛄柄眼·stamina vs13 越眼够 coral + sanity vs48 对峙·双 check〉/`the_sweepers`〈珊瑚夜战·stamina vs13 挤过够 coral + no-check 看慢战 lore『踩着彼此的死往大里长』〉/`shadow_above`〈巨影掠过全礁僵住·no-check 跟僵 lore + sanity vs48 硬干活拿 coral·『这么浅的水什么能那么大·始终没看清』〉；C（oncePerRun·loot-free·sanity vs50 双分支·fail 起步 sanity 20→8 不触底 #43）：`the_strike`〈博比特虫沙伏·34-44m·贴壁绕开 / 丢石诱发再过 / 绕远路·fail 被悄悄拽深一截〉/`blue_hour`〈介形虫黄昏齐拍发光·38-44m·success＝求偶齐拍·『书上没说它们应和的拍子从下面什么来』·承 the_low_note 低频伏笔·灭灯诚实盲退出口〉。守则全同既有：loot 按 #44（reef→天然 coral_shard/lobster·U/C 伏笔 loot-free）、单 tag #19、**无新敌人**（守 2/zone·所有生物作 fauna 非 combat·虾蛄/博比特虫的危险用 check/规避演而非加敌·mimic/corpse-wearer 仍 Phase 3 apex）、**不触发 d_reveal** #42、U/C 守**叙述永不交底** #54、stamina-check 只锁 success #49（dc 13 撞不到 0.05 fail clamp）。新 `lore.reef.{borrowed_tongue,false_cleaner,the_cocoon,the_watcher,the_sweepers}` + `lore.reef_deep.{shadow_above,the_strike,blue_hour}` 共 8 条。**17 新 scenarios**（`event-runner --out json` 实跑抄 #43·覆盖全 10 事件·6 check-success 验 checkPassed+lore·2 cosmic 双分支 fail〔sanity 20→rate 0.05/0.08·seed 1〕·4 stamina/loot success·3 no-check lore）。事件 **188→198**〔`--list`/verify-tutorial〕、event baseline **276→293**、reef 事件 32→42、reef mid 26-44m 池 12→22、敌人 7/combat 9/item 22 不变。`npm run regress` **27/27** 全绿（6.8s·含 prod build·check-boundaries / typecheck / 全 playthrough / scenarios 293 / combat 9 / mapgen 4+60 / verify-tutorial 198 / smoke）。**未 commit**（无人值守·留审阅）。**无新 quirk**（沿用 #19/#43/#44/#49/#54）。**下一批候选**：reef mid 可补 cosmic 平衡 / 蓝洞·墓园 mid 同法变体密度 / [wreck] 40-60m 补 1R / trench 续；#94 Tier-3 建议优先变体密度而非再开 >290m 新 band；别加第三只常规敌人、别触发 d_reveal。

103. **深渊 `[abyssal]` 变体密度 ·『前所未见之物』9 新真实深海地貌/群落事件（2026-06-07·周末内容引擎自动无人值守 session·续同日 #98/#99/#101/#102 四档 established-zone 浅/中段变体密度·按本任务「越深越优先、越深越离奇」轮换到深度 band）**——recon（`event-runner --list` 全量 zone×band×tone）确认五个深 band（trench 10/abyssal 11/hadal 10/subhadal 10/nameless 9）皆已饱和、各自母题被深掘（hadal=上下不是线/仪表骗你·subhadal=诱饵·nameless=你和它界限没了）；唯 **abyssal（108-140m）的 `_doc` 母题本就把『真实深海地貌/群落拧进 底在退/拽/招』**（已含鲸落 the_fall/海雪 the_settling/xenophyophore 屋 the_houses/导引绳 the_guideline），且深渊＝生态最稠密层（teeming abyssal plain）＝最该塞满前所未见之物（越深越离奇），故是吸纳全新具体素材最自然、最不重复、最低风险（append 进已注册的 `abyssal.json`·**零 registrar**·loreEntry 自由串无需注册）的深 band；越深的 hadal/subhadal/nameless 反而该保持稀疏抽象（生态变薄·叙事更心理），故 abyssal 做成最稠密 band 是生态 + 叙事双正当。**9 新事件**，tone **4C/5U**，全 `[abyssal]` 单 tag（#19·band.tags 隔离不跨蓝洞），全 **loot-free**、**无新敌人**（守 2/zone·生物全作 fauna 非 combat·mimic/corpse-wearer 仍 Phase 3 apex 例外）、**不触发 d_reveal**（#42）、**叙述永不交底**（#54：read 成功给平淡科学解释「道理都对」/ give-in/read 失败落不交底·游戏拒绝裁决），每事件 3 选项＝give-in（往深/接受）／理智 check 读穿（success 平淡解释·fail 错读被拽深）／关灯绕开认准上的诚实但盲逃生口（最低 signature·守无脚本死 §9）。灵感＝WebSearch **四口全新井**（brine pool「绝望热浴缸」halocline 看得见的面/腌住几十年不烂的尸体/甲烷贻贝岸 · Atolla wyvillei 窃贼警报旋转蓝光招更大捕食者来收拾攻击者 · Apolemia 管水母 47m 最长动物/活漂网 + Macropinna 桶眼鱼透明头壳内可转的管状眼·朝上找影也朝前盯食 · Chapopote 冷沥青火山/陷住路过动物/硬底）。**C·oncePerRun**：`brine_pool`〈底下的盐卤湖·有岸有面·岸上躺着的不是睡是泡·vs60〉/`the_alarm`〈碰一下炸成转着的蓝火轮·把你位置播给底下更大的·vs60·逃生口交叉勾连 `lore.deep_water.cold_light`〉/`the_tar`〈冷沥青火山黑壳·留住每样在它上头歇脚的·vs60〉；**C**：`needs_nothing`〈氧最少层什么都不要的小黑·你还要气＝你还活着·vs60〉；**U（vs58）**：`the_tubeworms`〈红口管虫林齐齐朝坡下斜·指的是饭不是路〉/`the_herd`〈海参成群下坡·队列有你大小的空当〉/`the_long_line`〈管水母假导引绳·会朝你缩·区别 the_guideline 真绳〉/`the_upturned_face`〈桶眼鱼把绿眼睛从朝上转到朝下·你跟着望进黑〉/`the_glass`〈玻璃海绵笼·里头一对幼时钻进长大出不来的虾『至死不分』·羡慕出不来的安稳〉。新 `lore.abyssal.{brine_pool,the_tubeworms,the_herd,the_alarm,the_long_line,the_upturned_face,the_glass,the_tar,needs_nothing}` 9 条。**14 新 scenarios**（`event-runner --out json` 实跑抄 #43·覆盖全 9 事件·6 give-in + 4 check-success〔验 checkPassed+lore·满 san seed1 rate0.95〕 + 3 check-fail〔san22→rate0.05·seed1·sanity 22→7 不触底·无 lore〕·含 brine_pool/the_alarm/needs_nothing 三个 cosmic fail）。事件 **198→207**〔`--list`/verify-tutorial〕、event baseline **293→307**、abyssal 事件 11→20（深渊＝最稠密 band·maxRoomFeatures 3 易抽干·补料正当）、敌人 7/combat 9/item 22 不变。`npm run regress` **27/27** 全绿（6.8s·含 prod build·check-boundaries / typecheck / 全 playthrough / scenarios 307 / combat 9 / mapgen 4+60 / verify-tutorial 207 / smoke）。**未 commit**（无人值守·留审阅）。**无新 quirk**（沿用 #19/#42/#44/#54）。**下一批候选**：abyssal 仍可续『前所未见之物』（冷泉甲烷气泡场/热泉口管虫群/雪人蟹/盲虾/鲸骨 Osedax 骨虫/玻璃章鱼）但已 20 事件、别再大涨；hadal/subhadal/nameless 各 9-10 但 maxRoomFeatures 2-3 易抽干、可各 +1~2 守各自母题；trench twilight/midnight 各 5、最浅深 band、可补；或回 established zone 蓝洞/墓园 mid 同 #102 法变体密度。别加第三只常规敌人、别碰 mimic/corpse-wearer/d_reveal。

104. **声呐有机洞穴渲染 ④ port · 从「图骨架直胶囊」改成真实侧剖洞穴（2026-06-09·交互 session·接 #100 ④ canvas 之上·作者逐图验收 v3·feat `82b4368`）**——#100 把声呐图重做成 canvas 洞穴剖面后，④ 的「canvas 画对」一项仍待作者肉眼（绿≠画对·#91/#93）。本 session 起 headless 离屏渲染（真 `generateDiveMap`+`deriveMapLayout`+**真导出的 `caveSdf`**·PPM→PNG）给作者看图验收。作者反馈原 ④「现在像 node-link 图（每节点一个圆+每边一根直线）·要真实侧剖洞穴：随机房间形状 + 蜿蜒隧道〔可直〕 + 节点只是洞里的 POI（可多 POI 同室·可贴边）·能关联剧情更好」→ **把 `SonarScanPanel` 的洞穴 SDF 从「每节点圆 blob + 每边直胶囊」重做成有机侧剖洞穴**（节点图退为**隐藏骨架**·连通/深度/gameplay 全不变·纯渲染）：**域扭曲**（`caveWarp` domain warp·把直胶囊弯成蜿蜒水道+不规则岩壁）+ **smin 平滑并集**（相邻房间熔成一间大洞＝多 POI 同室·`SMIN_K` 越小越分明）+ **弯折路由隧道**（1-2 控制点垂偏 `CTRL_OFF`·随边浮动半宽 `CH_BASE/CH_VAR`）+ **房间散瓣 + 偶发死路壁龛**（`buildCaveGeometry`·确定性按 node/edge id 派生·守洞穴一致性 #100）+ **POI 偏心 + 语义落点**（`poiOffset`：ascent/air 偏顶·rest/camp 偏底·event 贴壁·作者「点不必正中可贴边」）+ **POI/猎手/你 标记 `voidTrack(−warp)`** 跟随扭曲后的洞（**不浮在岩里·关键坑**）。`caveSdf`/`buildCaveGeometry`/`poiOffset`/`voidTrack`/`caveWarp` 纯导出（便于验证·已用真导出函数离屏渲染比对作者验收的 v3 still·**MAD 0.7/255 一致＝port 忠实**）。调参旋钮集中在 `SonarScanPanel` 顶部（`CH_BASE`/`CH_VAR`/`ROOM_BASE`/`ROOM_VAR`/`WARP_AMP`/`WARP_FREQ`/`SMIN_K`/`CTRL_OFF`/`WALL_LO`/`WALL_HI`）。**不入存档·不 bump SAVE_VERSION**（几何派生纯渲染）·`engine↛ui` 不破（全住 ui·#95）·SSR smoke E4-E8 结构/class 串不变（偏移纯视觉·点击仍触发同一条 move）。`npm run regress` **27/27** 全绿（14.1s·含 prod build）。新 quirk #102（有机洞穴＝隐藏骨架的渲染皮·标记必 voidTrack）。**canvas 画对仍待作者线上 `?dev` 肉眼**（sweep 动画/点击/对齐·SSR 测不到·#91/#93）。详见 SPEC §6④/§9 + QUIRKS #102 + [[sonar-render-redo]]。 **（2026-06-09 同日续·dev 验收工具·feat 见提交）**：为接上本条「canvas 画对仍待作者肉眼」，把声呐**有机洞穴渲染搬上 `MapDevPanel`**（Shift+M·**直接替换原节点图**·开阔水域无洞才回退节点图·无 toggle）——整图全揭、与游戏内**同一套**渲染铺出洞穴剖面，任意 zone/seed/ΔDEPTH 即时翻看（seed+1/🎲），免进游戏一路下潜才看到。**单一来源**＝把逐像素烤洞抽成导出纯函数 `bakeCaveRGBA(cave,rect,outW,outH)`（水道蓝绿/岩壁发光青/岩石透明·deepK 按 rect 纵向），玩家取景窗（220×300）与 dev 全图概览共用＝两处洞穴长一样、不会 churn 成两套着色（守洞穴一致性 #100）；MapDevPanel 用导出的 `SONAR_PX_PER_M/COL_W` 同款布局比例 + `buildCaveGeometry` 全揭示铺满整图 + 节点深度/类型字覆盖层 + 开阔水域提示。**dev 概览 ≠ 游戏内**：概览整图全揭、无雾无扫（判几何 / 对 demo 用）·游戏内只见绕你一小片雾化窗（~2 跳·渐进揭示）——故『游戏里像 node-link/schematic』多半是揭示窗 / 缩放、非几何坏，这工具一眼分得清。**确定性**＝同 seed 同图同洞·revisit 不变·不同 seed 不同洞（probe 实测：同 seed 烤洞 checksum 逐字节一致·5 seed 5 不同洞）。重构**像素逐位不变**（probe：`bakeCaveRGBA` 分支选择＝`caveSdf` vs `WALL_LO/WALL_HI`·0 失配·玩家视图零行为变化）。`MapDevPanel` 仍 DEV-only lazy chunk（不进玩家初始 bundle·`?dev` 门控不破）·ui-only（`engine↛ui` #95 不破）·不入存档·不 bump SAVE_VERSION。`npm run regress` 全绿 + prod build + 离屏 probe 全过。

105. **效率体检 + 自动化管线修缮（2026-06-10·交互 session·全项目 review）**——作者要求审全项目（结构 / docs / 工作流 / 管线 / skill / 定时任务）找效率改进。**审计结论**：HEAD `9ef539e` 已推已部署（GitHub Actions build+deploy 双绿·以 GitHub API 核实）、树净、27/27 全绿；**「幻影 [ahead N]」确诊**——`.deploy-token` 发布走显式 URL push、不更新 `refs/remotes/origin/main` → `npm run handoff` 持续误报 ahead（本 session 的交接 prompt 就被误导去催一个其实已发布的 push）。**修缮（约定落成机制·CLAUDE.md 顶部原则）**：① **夜间 verify/publish SKILL 验证段重写**——原手写串行 playthrough 循环 + 「✗」子串判红（曾误报 playthrough-mimic·见 REPORT-2026-06-09）→ 改 `npm run regress` 单命令门（并行 ~11s·按退出码判·且补上此前夜间从不跑的 verify-tutorial / check-boundaries / smoke 三关）；REPORT 路径修正 `docs/nightly/`→`docs/archive/nightly/`（docs 重组后一直没改）；push 确认后追加 `git fetch origin main` 刷新 tracking ref＝根治幻影 ahead（quirk #103）。② **周末内容引擎 SKILL 对齐现行文档约定**——onboarding 改 `npm run handoff` + `docs/QUIRKS.md`（原指「STATUS §6 quirks」·早已迁出）；收尾改「进度按编号追加 CHANGELOG·STATUS 顶 blockquote 只留当前+~2」（**STATUS 膨胀根因**：旧 prompt 让引擎把进度堆进 STATUS 头部·06-07 一日堆 5 条）；去掉写死的「26/26」（现 27·改以 `--list` 为准）；基线体检改 `npm run regress:quick`。③ **STATUS.md 顶 blockquote 9 条→3 条**（滚出的 #92/#94/#98/#99/#100/#101/#102 全在本 CHANGELOG·纯删重）。④ **`regress.mjs` 收尾自动清扫**——沙箱里 vite build 每次在仓库根漏一个 `vite.config.js.timestamp-*.mjs`（mount 不能 unlink·quirk #1·已积 38 个）→ regress 跑完 best-effort `renameSync` 进 `.git/.sandbox-junk/`（try/catch 包裹·绝不影响退出码；Mac 上 vite 自删、通常扫不到东西）；顺手清掉积存 38 个 + 移除一次性脚本 `scripts/explore-bluecaves.ts`（5 月蓝洞密度统计用·不在 regress 任务列·使命完成）。⑤ **并发隔离方案 A 实装（作者同 session 批准·quirk #104）**——`auto/weekend` 分支已建（自 `9ef539e`）；周末引擎 SKILL 改为「所有 commit 只落 auto/weekend·不碰 main·不 push·收尾停在分支」；夜间 SKILL 加 merge 步（pending>0 → 上分支〔main→分支只加不删·可行〕→ 分叉先 rebase〔冲突即停·报告人工〕→ 在分支跑 regress 门 → 绿则 **`git branch -f main auto/weekend` 移 ref 完成 ff**〔先验 is-ancestor 只前进·含新增文件的分支 checkout 回 main 会被 unlink 挡·故移 ref 不动树〕→ 照常 ship）；`handoff.mjs` 加「当前分支 + auto/weekend 待合并数 + 幻影 ahead 提示」段；根 CLAUDE.md 加并发隔离节。提案 `docs/infra/concurrency_isolation_proposal.md` 状态头记两点实装偏差（移 ref 代替 checkout+merge·档尾停分支）。**dive.ts（现 930 行）拆分的前置就此满足**——只待作者在场的安全窗口。**定时任务现状**：4 个全 disabled（作者确认刻意保持关闭；隔离机制在重开时自动生效）。`npm run regress` **27/27** 全绿（10.7s·含 prod build·新清扫已实跑生效）。新 quirk #103。skill prompt 改动在 `~/Claude/Scheduled/`（仓库外·不随本 repo 提交）。**遗留待办**：④ 有机洞穴仍待作者线上 `?dev` 肉眼验收（最高优先·通过即可关声呐渲染重做 SPEC）；下一个 gameplay 方向由作者拍板（猎手 Phase 2+ / §3.7 另一个世界 / poiOffset↔剧情）。

106. **品味评审 + 收尾打包：积压推送 / 树面机制化 / dive.ts 拆分实装（2026-06-10 晚·交互 session）**——作者要求以「品味」审项目，再按 NEXT_SESSION_PROMPT 把能收的全收。**品味评审结论**（按价值排序·前三为后续机制化候选）：① 事件选项 label 手写「（理智 vs 60）」与 `check.dc` **双写**（全库 70+ 处·EventView 完全不读 check 渲染·retune 一次 DC 文案即说谎）→ 候选：label 回归纯 fiction + UI 从数据渲染 check 徽章；保守版＝regress 加 lint 校验括号数字==dc；② ui/ **12 处手搓 `phase: { kind: ... }` 字面量转移**（`{ kind:'ascent', targetDepth:0 }` 在 EventView/RestView/CombatView/NodeSelectView 各拼一份）→ 候选：engine 出具名转移（beginAscent/returnToPort）+ check-boundaries 加第二规则「src/ui 禁 phase 字面量」；③ #99 的 `?? 默认` 兜底**漫进引擎读点**（dive 拆分前一文件 27 处 `?.`·连类型必填的 sensors 也在防御）→ 候选：deserialize 后单点 hydrate 补默认、类型转必填（非迁移链·#99 精神不变）；④ dive.ts god file（本 session 已拆·见下）。刻意不动：scenarios/ 328 平铺（前缀即分组·grep 友好）、styles.css 单文件（规模未到）、注释 spec 编号密度（是机制非噪音）。**收尾执行**：(a) **积压全推**——92e8b0a/b8d592d 真 ahead 2（`ls-remote` 核实非幻影）+ 树面 chore `217367b`（**/CLAUDE.md 进 .gitignore**＝夜报散文 "always-ignored" 落成机制、「别 git add」常驻告诫作废；**收编安静夜报告 REPORT-2026-06-07** 进 archive 消常驻 untracked caveat）一并推上 origin（`9ef539e..217367b`）+ post-push fetch 刷新 tracking ref（quirk #103 照做·[ahead] 真归零）；(b) **dive.ts 纯搬移拆分实装**（`264e02f`·按 docs/infra/dive_ts_split_proposal.md §3–§6·实装门全满足：隔离 A 在位 + 定时任务全停＝零并发窗口）——930 行 wiring 中枢降为 **barrel + 6 子系统兄弟文件**（dive-start/-select/-sensors/-move/-stalker/-actions），公共 API 名字/签名/import 路径零改（ui/ scripts/ 及 dialog.ts 的 './dive' 全不动），函数体 sed 按行域逐字节搬运·**diff 机器证明纯搬移**（仅 4 处 module-private helper 提 export：featureDoneFlag / autoScanOnArrival / maybeApproachEncounter / stalkerStep），提案表按真实调用图修正三处（refreshSelection 唯一调用方在 sensors→随 sensors 留私有；FEATURE_EXPLORE_TURNS→随 actions；assertNever 单用户→dive-move 私有·免设 dive-internal），调用图核准无环（start→select·sensors→select·move→sensors/stalker/select·actions→select）；(c) **NEXT_SESSION_PROMPT 按 quirk #96 瘦身重写**（砍与 CLAUDE.md/QUIRKS 复读的 onboarding/约束段·只留人写的方向与待办）。`npm run regress` **27/27** 全绿（5.5s·含 prod build·mapgen 确定性 baseline·SSR smoke）。新 quirk #105（dive barrel 约定）。**遗留：④ 有机洞穴作者今晚线上 `?dev` 验收（最高优先·五点检查单已交作者）**；品味评审 ①②③ 机制化候选待作者拍板。

107. **品味评审三候选机制化落地：phase 转移收口 / 存档 hydrate 单点 / check-DC 双写 lint（2026-06-10·交互 session·作者拍板「2/3/4 不需人工审核的一直做、收尾优先」）**——#106 品味评审的 ①②③ 全部从「散文候选」做成**会在 `npm run regress` 红的机制**（CLAUDE.md 顶部原则），regress **27→28 任务**。**(②→feat `phase 转移收口`)**：新 `engine/transitions.ts` 六个具名纯转移（`toPort`〔纯导航·刻意不叫 returnToPort 避免与 port.ts::handleReturnToPort 结算语义撞名〕/`beginAscent`/`toShop`/`toChart`/`toDiveEvent`/`toGameOver`），ui/ 12 处手搓 `phase:{kind:...}` 字面量全部替换（App/NodeSelect/Combat/Event×3/PortEvent/SeaChart/Rest/Port×2/MiraShop）；`check-boundaries.mjs` 加**规则二**「src/ui/**+App.tsx 禁 `phase:{`/`subPhase:{` 对象字面量构造」（读 phase 不限·只禁构造·负路径实测会红）。engine 内部构造 phase 不受限＝它是 owner。**(③→feat `存档 hydrate 收口`)**：`?? 默认` 兜底从全引擎读点收口到 `engine/state.ts::hydrateGameState`（deserialize 后**单点**补 canonical 默认·非迁移链·#99 精神不变：改坏形状仍 bump 弃档）——run 五字段（`sensorTuning`/`scanMemory`/`bandAlertFactor`/`sonarDeception`/`huntEnabled`）+ profile 两容器（`shopStock`/`outpostState`）**类型转必填**，createNewRun/createInitialProfile 种默认、band 数据缺省在 startDiveFromOutpost 落点消化（`?? 1/0/false`·DepthBand 字段仍可选）；真条件字段（`diveModifier`/`stalker`/`sensors.sonarOn/sonarNext/sonarDir`）**不补**＝缺席即语义；引擎/UI ~45 处防御读（`?.`/`?? 基线`）改直读（clarity/sonar/dive-sensors/dive-move/dive-start/events/stalker/port/lighthouses/outposts + NodeSelectView/SonarScanPanel·顺手清掉同病的 `run.turn ?? 0`/`run.stats?.sanity ?? 100`）；**playthrough-save 新 §6 hydrate 门**（删字段旧档→补 canonical 默认·真条件不补·幂等·只缺 power 按档内 powerMax 补满）；修四处 fixture 显式-undefined 盖种子坑（playthrough-bands lit/dark·playthrough-sonar 三 mini-run·smoke shopState/sonarState·见 quirk #106）。**(①→feat `check-DC lint`·保守版)**：新 `scripts/check-event-dc.mjs` 入 regress（纯 node）——label「（理智/体力/氧气 vs N）」标注必须与同选项 `check.{stat,dc}` 一致 + 有标注必有 check + 未知 stat 词会红（防静默漏检）；现状 **157 标注全符**（扫 561 选项·8 个无标注 check＝隐藏判定不强制·负路径篡改 dc 实测会红）。**根治版**（label 回归纯 fiction + UI 从数据渲染 check 徽章·改 70+ label 与观感）仍留作者在场 session。不 bump SAVE_VERSION（hydrate 恰是为不 bump 的纯加字段服务）；engine↛ui 不破；SSR smoke 子串不变。`npm run regress` **28/28** 全绿（11.6s·含 prod build）。新 quirk #106（必填化字段 fixture 坑 + 三处同步约定）。**遗留：④ 有机洞穴作者线上 `?dev` 验收（最高优先·不动）**；①根治版（check 徽章）与下一个 gameplay 块（猎手 Phase 2+ §4 decoy 最顺手）待作者。 **（2026-06-10 同日续·手机验收口·feat 见提交）**：作者要在手机上做 ④ 验收——dev 面板此前只挂 Shift+D/C/M 键盘监听、触屏够不着 → `App.tsx` 加 **`?dev&panel=map|event|combat` URL 直开**（`initialDevPanel`·仍在 `?dev` 门后·DEV_TOOLS false 恒 null＝普通访客零变化·桌面快捷键照常可切换/关闭·手机关面板＝去掉 panel 参数刷新）。游戏内验收项（sweep/渐进揭示/点击对齐）本就 onClick＝触屏可点；`index.html` 有 viewport meta（styles.css 零 `@media`·窄屏排版可用但从未做响应式·挤了算已知非 bug）。`npm run regress` 28/28 全绿。 **（2026-06-10 同日续②·④ 作者验收通过·声呐渲染重做 SPEC 关闭）**：作者线上翻看（含手机 `?dev&panel=map`）确认有机洞穴「现在也很好·符合预期」——④ 关账，SPEC 状态头改**已完成·只读参考**（§6 ①②③④ 全落地·旋钮在 SonarScanPanel 顶部·约定 QUIRKS #100/#101/#102）。验收中作者问「实际地图也有歇/事标记吗」→ 确认设计分层：**dev 全图的中文 kind 字（歇/事/出/尸/气/营）是 MapDevPanel 专属**（判几何用）；游戏内 `kindGlyph` 只给三个结构性地标符号（↑出口/○气穴/⌂营地·守 #36），事件/休整/尸体不挂字＝房间内容只走 clarity 门控的 preview 文案，且符号走 `view.displayKind`（spoofsSonar 画成假 ↑＝mimic 假信标靠它成立）——游戏图标注内容会破欺骗轴，维持现状。**下一方向作者拍板：猎手 Phase 2+ §4 decoy**（最顺手·接 #89 镜像规避先例）。

108. **猎手 Phase 2+ §4 Decoy 道具 + 出发前选带（2026-06-10·作者已拍主方向·#108）**——§4 全落地：**声诱标/光诱棒**两种 decoy（`items.json` 新 `ItemDef.decoy.kind`·data-driven 非硬编码 id 表），投放→把猎手引向别处、战斗内→必成脱战。**①引擎核**（`engine/stalker.ts`）：`run.decoy?: DiveDecoy{nodeId,kind,expiresTurn}`（**真条件字段**·缺席即语义·不种不补·不 bump SAVE_VERSION·同 stalker）+ `decoyLures`（感官匹配：声↔声/光↔光/**双感任一都上钩**＝§2.2「任一锁定」的两面——难甩〔§3 取 min〕但易诱）+ `activeDecoy`（过期判定纯靠 run.turn vs expiresTurn＝零 tick 字段）+ `advanceStalker` decoy 分支**优先于「有你的信号」**（你再响也被假信号盖过——烧消耗品＝代价本身故全效，区别 §3 升级守地板；lastSignal 刷成诱饵点⇒失效后它在诱饵点附近按性格搜/等＝你借机反向拉开；接触判定仍对你做＝扑诱饵路上撞上你/你站诱饵上不走照样接触·诚实）；感官不合/没诱饵/过期→分支不触发＝**逐字节不变**（§10c/d JSON 串相等断言）。确定性零 RNG。**②投放**（`dive-stalker.ts::deployDecoy`·barrel 出口）：烧一枚、落当前节点、`expiresTurn=turn+DECOY_TURNS`(6)、不耗回合（代价＝道具）、一次一枚再投覆盖；过期由 stalkerStep 顺手清字段+「哑了」叙事；上钩叙事「转向假动静」（不交底感官·#54）。**③战斗内**（§4「接现有 flee」）：`FleeEffect.guaranteed?`（跳掷骰·北极星「decoy 永远是出路」）+ `actions.json` 两个 use_decoy 行动；**物品消耗收口到 `applyPlayerAction` 1b 步**（任何 requiresItemId+consumesItem 行动统一扣·applyUseItem 不再自扣）；`listAvailableActions` **没货的道具行动不上清单**（不再常驻灰按钮）。**④data/商店**：Mira 新「消耗品货架」`SHOP_STOCK_CONSUMABLES`（decoy×2/次回港·同一套 offer×markup 加价与限量机制·材料门控不破·med_kit 故意不上架）。**⑤出发前选带（作者拍板「不全带·死了就没」）**：`dive-start.ts::applyCarryItems`（只认 consumable·qty 夹库存·占格按 slotsRequired 超容量截断·不选＝原对象原样）+ `startDiveFromPoi/FromOutpost` 新 `opts.carryItems` + SeaChartView「行前装包」±步进面板（POI 出海与蛙跳共用）——死了随身物进尸体快照（现有回收闭环）、生还 handleReturnToPort 自动归库（**零新闭环**·全是既有管线）；NodeSelectView 投放按钮（`huntEnabled`+持有才显·水里那枚显剩余回合）。回归照 #89 配方：`playthrough-stalker` **§10**（感官匹配/引开 vs 对照/不合·过期逐字节一致/站诱饵上被扑/投放消耗/moveToNode 接线叙事/战斗必成脱战+有货才上清单）+ `-save`（decoy round-trip + hydrate 不补）+ `-economy` Phase 6（货架买价限量 + applyCarryItems 五规则 + 生还归库）+ `smoke` K1b/P（装包面板/投放按钮 huntEnabled 门控/状态行）。`npm run regress` **28/28** 全绿（12.2s）+ prod build。新 quirk #107（消耗品入 run 只此一条路 + flee「脱战成功」子串判定依赖）。SPEC §7 §4 转 ✅；剩余 deferred：§5 狭缝避难/§6 执着等待者/per-encounter sensesBy/浅水弱变体。

---

109. **猎手 Phase 2 收束（§5 窄缝避难 / §6 执着等待者 / §2.2 per-encounter+active / Q3 浅水弱变体）+ ①根治版 check 徽章 + Mira dev 测试货架（2026-06-10·交互 session·作者拍板「把之前讨论的猎手全部做完，和 decoy 一起完整验收」+「①根治」+「dev 0 元全道具」）**——猎手 SPEC 机制层至此**完工**（v0.2·Phase 1 #84 → §3 #89 → §4 #108 → 本批 #109）。**(1) §5 钻狭缝**：「容得下多大」与声呐图渲染**同源**——`hash01/roomScale01` 从 SonarScanPanel 逐字迁居 `engine/sonar.ts`（面板反向 import·渲染零变化），`nodeIsNarrow`＝标度 < `NARROW_ROOM_SCALE`(0.28·约最小 28% 房间)＝玩家看图即可读出窄缝（洞穴一致性 #100 延伸）；大型猎手（`large`）寻路 blocked-BFS 绕窄节点、现身点**占位过滤**（容不下不落点·距离仍按全图算·全图全窄 → 退化小型）、你在窄缝里＝贴邻也不接触、它走到「口外」（`largeGoalFor`）守着。**(2) §6 执着等待者**：`Stalker.patience`（缺省 `STALKER_PATIENCE`=4）只管**有信号围守**——被窄缝挡住且咬着你的信号时 `guardedTurns` 累计、超了 → 放弃离开（`gaveUp` 叙事区别「跟丢」）；丢信号仍走既有 wait/seek 计时＝零新状态机；守口耗资源＝回合本身烧氧/电、不加新税。**(3) §2.2 per-encounter sensesBy + active 探测**：`CombatEncounterDef.stalker` 档案（盲鳗=sound+**active** / 章鱼=both+patience10 / 梭鱼=light / 石斑=sound+hspeed0.6·「打标签不加敌」·未标签＝深度派生逐字节不变）；active＝searching 态每 3 回合自己发一记探测、3 跳内且未被 **T2 主动迷彩**甩掉（`playerEvadesProbe`·守地板同款）→ 重新咬上（`reacquired` tell）——摸黑不再万灵、拉距仍是出路。**(4) Q3 浅水弱变体**：`ZoneDef.weakHunts` 数据 opt-in（旧灯塔礁+蓝洞群）+ 浅水线下（<25m）`weak:${runId}:${nodeId}` 哈希 1/10 现身；**信号直读灯/声呐开关**（`weakStalkerHasSignal`——浅水 alert 不积累·§7.5 不破·关灯＝当场切断＝阀门教学）；硬性「小且弱」（0.55 慢速/wait/不 large/不 active·压过档案）；`weakStalkerStep` 旁路、未 opt-in/未现身 → 旧瞬时伏击逐字节不变；顺手把 decoy 投放按钮门控放宽到「深 band 或场上有猎手」。**(5) ①根治版**：157 处 label「（理智/体力/氧气 vs N）」标注全部剥离（label 回纯 fiction）+ EventView 从 `check.{stat,dc}` 渲染 `.check-badge`（如「体力 13」·SSR 实测）+ 新 `EventOption.hideCheck`（8 处原刻意无标注的隐藏判定保住设计权）+ `check-event-dc` lint 翻面（禁标注回潮 + hideCheck 须配 check·负路径实测红）。**(6) Mira dev 测试货架**（作者把 #108 留的 med_kit 上架问题改拍成这个）：`?dev` 门后（quirk #97 同款·`DEV_TOOLS` 迁居 `ui/devMode.ts` 单一来源）MiraShopView 折叠式「测试货架」0 元白拿全部道具（`port.ts::devGrantItem` 不动金币/备货＝真经济零触碰）。Phase 3 §3.7/poiOffset 作者拍「留专门 session」。全部 run 级可选字段·不 bump SAVE_VERSION。回归：`playthrough-stalker` 新 §11-§14（窄缝/档案/active/弱变体·§1-§10 零翻修＝additive 兑现）·`npm run regress` **28/28** 全绿 + prod build。新 quirk #108（窄缝同源/弱变体读开关/档案唯一入口）+ #109（label 纯 fiction·徽章单一来源）。详见猎手 SPEC §5/§6/§7/§8/§10。

110. **作者在线验收支援 session · UX 需求单四连（dev 测试解锁 / 行前装包格子化 / Mira 交易系统 / 购买反馈）+ wreck 双敌档案 + 浅水弱变体专属「更小敌」（2026-06-10·交互 session·作者边线上验收猎手 Phase 2 边下需求·全部「先做完晚点一起验收」）**——起手把 #109 两 commit push 上线（`725ff76..df8dfca`·验收前置·Actions 绿·页脚验证），交接动线发作者后按作者消息逐条落地。**(1) dev 测试解锁**（作者被声呐组件材料卡住验收）：UpgradePanel 每条未购升级旁 `?dev` 门后「测试解锁（0 成本）」——新 `devUnlockUpgrade`（engine/upgrades.ts·不查材料/金币/前置不扣资源·已购/未知 no-op·引擎无门门在 UI＝`devGrantItem` 同口径·quirk #110）。单独先行 push 解卡（`e78014b`）。**(2) 行前装包格子化**（作者「出发前看到背包格子有上限+储物柜格子点击放入+名称图标（图标以后生成）」）：SeaChartView 行前装包从 ± 步进列表重做成**背包格**（格数＝新 `carryCapacityFor`（dive-start.ts）＝`RUN_INVENTORY_CAPACITY`(8·state.ts 新常量·createNewRun 同源) + extraConsumableSlot＝**与 applyCarryItems 截断线同一来源**·空格虚框可见「X/Y 格」·slotsRequired 计格·满包禁点）+ **储物柜格**（×N 角标·点击放入背包一件/点背包格放回）；新通用组件 `ui/ItemCell.tsx`（图标空占位框留 `data-cat` 钩子·可点/禁点/纯陈列三形态·`cellKey` 重挂载＝重放 item-pop 动画）。**(3) Mira 交易系统**（作者验完格子续拍「上=她的东西点击买·下=我的点击卖」）：MiraShopView 重构两块同构格子——「她的货（点击买）」（T1/T2 货架·售罄禁点·钱不够 short 红显+点击红字「还差 X 金」不再静默 no-op）+「你的储物柜（点击卖）」（可卖品格上标收价点击卖 1·她不收的惰性陈列带原因〔永存/消耗品/剧情物〕·全卖按钮保留）；买/卖统一 flash（含 ±金额）+ 银行金币 `gold-figure` 跳动 + 柜格 qty 变化跳动（cellKey 重挂载）；旧 MiraSellRow/MiraBuyRow/KeeperRow 删除。smoke K/P 段断言全部更新到新合同（K1c 卖侧/K1d 她不收/P1 0/8 格+空格占位）。**(4) wreck 双敌猎手档案**（#109 待办 2a）：沉船蛛蟹 `{sound·hspeed 0.5·size:'large' 钉死·patience 8}`——**浅段（18-50m）也是大家伙**＝窄缝避难/守口外/拖刮声不用蛙跳深渊就能学（introText「比你想象的大」本就指着 large；首个浅段 large·此前 large 只在 ≥108m 深度派生）；沉灯水母 `{light·active·hspeed 0.45}`——摸黑后它**自己亮一记**找你·probe tell 按感官分流（光感「黑里亮起一盏不属于你的灯」·声感原「不属于你的脉冲」·dive-stalker.ts·#54 只描述感官所读不交底机制）。**顺手修机制坑**：`maybeSpawnStalker` 奇数槽 `waitTurns=0` 把 per-encounter `active` 变**死字段**（searching 第 1 回合 despawn·PROBE_PERIOD 永远到不了——盲鳗恰在偶数槽躲过）→ active 例外抬到 `STALKER_WAIT_TURNS`（既有四档案/教学鲨零字节变化·quirk #111 记 WAIT_TURNS≥PROBE_PERIOD 承重关系）。`playthrough-stalker` 新 §15（wreck 池序守门/档案合并/large 钉死/active 例外/摸黑亮灯咬回）。**(5) 浅水弱变体专属「更小敌」**（#109 待办 2b·作者拍「可以做更小敌」）：**梭鱼幼体**（hp7/threat2/dmg[1,4]·光感冲灯·loot 无保底珊瑚看脸＝不是 farm 点）+ **盲鳗幼体**（hp6/threat2/物理[0,3]+贴附 sanity[1,3]「小的也会这一手」·声感·掉 eel_skin×1＝声呐材料浅水小额来源）——都 **territorial（挨打会撤＝小东西不恋战）**、感官同亲代（弱变体读开关的教学与亲代深水版一致）、**写进亲代 JSON**（blind_eel.json/reef_barracuda.json·变体随本体·零 registrar 改动）；`ZoneDef.weakHuntEncounters` 专属池（蓝洞群=盲鳗幼体/旧灯塔礁=梭鱼幼体·`weakStalkerStep` 取池 `weakHuntEncounters ?? ambushEncounters` 回落＝没配池行为不变）。combat baseline ×2（seed 1 各一刀杀·9→**11/11**）+ `playthrough-stalker` 新 §16（两 zone 池守门/moveToNode 接幼体遭遇/硬性不被档案推翻/注册指向幼体）。**另**：作者并行编辑的港口修缮面板布局（金币头固定+升级线滚动栏+返回钉页底）一并入库。提交 `e78014b`（dev 解锁）/`834b987`（装包+购买反馈）/`5e4dfce`（wreck 档案）/`43ba7d9`（作者面板布局）/`eebbc2d`（交易系统）/`29e5b6d`（更小敌）+ 本 docs commit；每次 push 前全量 `npm run regress` **28/28** 全绿（含 prod build）。敌人 **7→9**·combat encounter 注册 +2。遗留：作者合并验收（猎手全家桶+decoy+三 UX+更小敌+wreck 档案）→ tunables 手感反馈；①徽章样式过目；Phase 3 §3.7 仍留专门 session（作者 pin）。

111. **内容型界面统一壳 PanelShell（机制化）+ 手机适配 + 海图椭圆根治（2026-06-10·交互 session·作者「内容型的界面都该做成这样，不然内容一多很多通用交互都会出问题」·拍板「共享组件+regress 检查」+范围「Mira/灯塔/StatusBar 钉顶」+「要手机兼容」）**——把 `43ba7d9`（港口修缮「金币头固定+内容滚+返回钉底」）从单视图 CSS 形态升级成**机制**：**(1) `ui/PanelShell.tsx` 三段壳**（head=状态固定〔sub 传带 key 节点可保跳动动画〕/ body=`.panel-shell-body` 滚动〔flex+min-height:0·overscroll contain〕/ foot=出口通栏·与各页跳转对齐）；高度预算 `--shell-outside` CSS 变量（默认 150·`under-port-header` 修饰类 290）·dvh+vh 兜底（手机地址栏）·≤480px 收紧 padding；内容不足时壳随内容收缩＝小视图不必硬迁。**(2) 三视图迁移**：UpgradePanel（`.upgrade-lines` 过渡形态收编进壳·`.upgrade-head/-title/-sub/-close/-lines` CSS 全删）/ LighthouseBuildPanel（返回从右上挪到页底对齐·整页替换 SeaChartView 故走默认预算）/ MiraShopView（银行金币+`gold-figure` 跳动动画迁驻壳头·货架格滚动·「离开柜台」钉底·孤儿类 `.mira-gold` 清掉）。**(3) check-boundaries 规则三（2→3 条）**：src/styles.css 的 `overflow(-y): auto|scroll` 只许白名单 `.panel-shell-body`/`.changelog-body`（扁平 CSS 选择器归属解析·`src/ui/dev/*.css` dev 自留地不管·负路径 `.evil-scroll` 实测红退出码 1）——「内容视图统一走壳」从散文变 regress 门。**(4) 潜水侧 StatusBar sticky 钉顶**（一条 sticky 全部潜水视图受益：选点/战斗/尸体/事件/休整/上浮的氧气电量理智不再被滚走·z-index 20 让出弹窗 1000·盒影分层）+ **手机属性条根治**：`.status-stats` 两列死宽（标签 60+数值 80+gap）在 380px 屏把条轨挤到 ~2px → ≤480px 单列+列宽收紧（条轨拿回 ~200px）。**(5) 海图灯塔范围椭圆根治（作者报 PC 上不是圆）**：范围圈/测绘 sweep 的百分比尺寸 width 按容器宽、height 按容器高解析，而引擎 reveal/reach 是归一化空间各向同性 hypot（`distanceBetween`）→ 定高 260px 的宽容器必出椭圆且与判定脱钩；改 `.chart-map` `aspect-ratio:1/1`+`max-width:460px`（手机全宽自适应）＝屏幕像素与判定度量同构、圆是圆——**刻意不走**「只把圈画圆」（谎报纵向触达·海图是诚实轴）。验证：typecheck / check-boundaries（规则三正负路径）/ smoke-chart-ui / prod build 全绿；**全量 regress 留 ship 前**（树上有并行 session 活跃 WIP：NodeSelectView / SonarScanPanel / smoke-chart-ui 非本批改动·故本批**未 commit**·留收尾流程一并）。新 quirk **#112**（壳+白名单+StatusBar/手机约定）/ **#113**（chart-map 1:1 与 hypot 同构·别改回定高）。候选后续：NodeSelectView/CombatView 内容区收壳（作者拍「验收后单独做」）；SeaChartView 整页布局格局复杂暂缓。（**06-11 补记**：本批未提交的代码已分账落库 `30d2fc6`，见 #113。）

112. **声呐图实测四修 + 猎手原地同拍（2026-06-10 晚·交互 session·作者线上实测反馈单·本条为 06-11 补账——该 session commit 了代码但未走收尾仪式）**——两笔 commit 自带密记，此处只立锚：**声呐图四修**（`8fdbdf3`·反馈 1/2/3/5）：①脚下房间永远可见+半揭示残段隧道口（buildCaveGeometry 单端 STUB_FRAC 0.38·深渊「平时全黑/偶尔一面墙」修正·防剧透轴不破）②缩放/平移（拖动/捏合/滚轮光标锚定·rAF 合并重烤·拖过阈值才捕获指针防吞 POI 点击·纯视图态不入存档）③扫描波重放可见（SWEEP_MS 1200→2600·波前外侧保旧图不黑屏·SVG 标记波到才亮）⑤两段点击（第一击选中高亮·再击才 move·状态在 NodeSelectView 单一来源）+ smoke-chart-ui 新 Q 节。**猎手原地同拍**（`f31b720`·反馈 4「等待对猎手免费」）：`passTurnsWithStalker`——rest/camp 逐回合 tickTurns(1)+advanceStalker 同拍；接触＝伏击开打 interrupted 短路（觉没睡完不补）；无猎手快路径一次性 tickTurns(N) 守 additive 逐字节；不原地凭空现身；叙事单一来源 stalkerNarration/contactAmbush + 擦肩/贴邻两拍 prox 反馈；`playthrough-stalker` §17 四断言。两笔各自 regress **28/28** 全绿。两 commit 收尾时仍未 push（沙箱凭证）。

113. **洞型谱 depthCurve（k 谱）+ 平廊 POI「横岩廊」+ 洞型情报上海图 + 分账落库（2026-06-11·交互 session·作者拍「洞穴要更多样：深的/广的/广中藏深的·以后生成地图可发散」）**——【**洞型谱**】迷路图深度赋值从固定线性升级 `depth = d0 + span·frac^k`（`generateMazeMap` 步 4·**唯一曲线求值点**·未来虹吸/双坑变种都只在此扩）：k<1 井+廊（先掉竖井深处横铺）/ k=1 匀速下行（旧）/ k>1 廊+坑（长平廊尽头突掉深坑「广中藏深」）。`ZoneDef.depthCurveRange`（蓝洞群 [0.45,2.6]）+ `GenOpts.depthCurve` 显式覆盖；实潜按 FNV(`zone::seedKey::curve`) log-uniform 派生＝**每个海图洞口固定洞型性格**（续 #98 同地点同图·零 rng·**缺省/无 seedKey → k=1 逐字节复现旧图**＝既有 sweep/baseline 全不动·护栏断言在 runner）。`analyzeMap.meanDepthFrac` 形状信号 + runner「洞型谱不变量」块（30 seed 三档单调 0.71/0.56/0.38·极端 k 守迷路+#92·缺省=k1 指纹·12 POI 性格极差 ≥0.12）+ 3 份 seed11 同构对照锚点（k045/k100/k260·N13/E14 只差剖面）+ MapDevPanel「剖面 k」控件与读数——quirk **#114**·commit `e9ad187`。**（2026-06-11 同日续·平廊+情报）**：`PoiModifier` 扩成 **GenOpts 薄投影**（`depthRange/layerCount/depthCurve` 直通 startDiveFromPoi→startDive→mapgen·与 band 同管道）+ `GenOpts.layerCount` 图规模旋钮（maze N≈2×layerCount·缺省零变化）。**第一条真·平廊洞口**＝海图新 anchor `poi.anchor.flat_gallery`「横岩廊」（蓝洞群第二口·(0.44,0.28) reach 1 档·`[16,30]` 窄 span+`layerCount 10` 长图+钉死 `k 2.4`·浑浊）——N 20-24、整程贴浅、尽头掉坑，威胁换轴成「**进来太远**」回程预算（rule of thirds 进 blurb）；事件吃既有 `[cave]` 浅段池（#101 正对口）。**洞型情报**：`chart.ts::describeCaveShape`（往下掉/斜着下/往里钻）与 mapgen **同一 k 来源**（新公共 `caveDepthCurveForPlace`+`caveShapeBucket`·图上写的＝潜下去的＝海图诚实轴 quirk #113 同理·说谎钩子留参数位）·SeaChartView 一枚 chart-tag chip（零新 CSS）。回归：`playthrough-chart` §5b（横岩廊端到端 N≥20/[16,30]/mdf≤0.55/同点同图）+ §5c（三桶话术·钉死 vs 派生·layered 无标签）+ 锚点计数 4→5 + reach 表 +1；mapgen scenario `flat_gallery_poi`（N21/E23/死路 9＝窄 span 迷路感主导）；runner schema 加 depthRange/layerCount 透传——quirk **#115**。**另**：起手分账两笔——上批 #111 尾账（PanelShell 等 6 文件）落 `30d2fc6`、洞型谱落 `e9ad187`（每笔前全量 regress 门）。mapgen scenarios 4→8。收尾全量 `npm run regress` **28/28** 全绿。遗留：作者合并验收（#109/#110/#112 + 本批横岩廊/洞型标签过目）；Phase 3 §3.7 仍留专门 session（作者 pin）。**（2026-06-11 同日续·作者线上验收反馈四件）**：①**扫描黑场修复**——SonarScanPanel 重挂载（过事件回来）`prevBakeRef` 空 + 重播上一记扫描波 ⇒ 已测绘区域从全黑重新点亮（作者「每次扫描都从黑开始」）；修＝重播且无旧图时**拿本帧烤图当旧图打底**（测绘记忆≠扫描动画·波只剩动画职责·同挂载真新扫描仍走 §4 波前换新图）。②**猎手红点出墙修复**——blip 此前用房心**直线**插值，而隧道是弯折折线路由 ⇒ 通道中段红点画进岩里；修＝抽 `routeForEdgeEntry`/`edgeRoutePts`（**渲染与 blip 单一来源**·方向无关取向）+ `stalkerRoutePoint`（按弧长落点·远端未扫截进 STUB_FRAC 残段口内·双端未扫 null 回退直线）；smoke-chart-ui 新 **R 节** 5 断言（方向无关/端点=房心/blip 永在路由上/残段双向截断/无知态回退）。③**Mira dev 货架改版**（作者「期待是格子·全商品·无限·全零元」+「被滚动条藏到没看到」）——UL 列表换 **ItemCell 格子**（与正常货架同款·`item-grid live`·×N 显示已囤·cellKey 跳动反馈·0 金∞），位置上移到「她的货」正下方不再垫底；删 devShelfOpen 开合态。④**UpgradePanel 一键升满**——dev 区头部「测试：一键升满全部（0 成本）」＝全线全级 `devUnlockUpgrade` 串一遍（引擎仍无门·quirk #110 口径不变）。另答作者两问：「蛙跳开局小环形→过节点变大」＝**同一张迷路图的渐进揭示**（开局只看得见脚下房间+残段隧道口·首次扫描一次揭开多房间·非换图——黑场修复后连续感会好很多）；「每次下潜重新测绘」＝当前设计 scanMemory 是 run 级（同洞口洞形永远一致·#98，测绘记忆跨潜持久化是已记录的候选功能）。quirk **#116**。全量 regress **28/28**。**（红点二修）**：①修后作者复测「还是刷在墙外」——剩余案＝fix **锚点本身在 scanMemory 没有的节点**（探猎手量程＞房间揭示量程·spawn 量程外现身后首次定位常如此）→ 锚点周围根本没画几何、红点浮在黑岩里；修＝**渲染侧把 fix 锚点并入 renderIds**（同 #112①「脚下房间」模式·-1 哨兵·不写 scanMemory）——「那里有水」已被回波证实，只画它那间房（边仍双端齐才整条·防剧透不破），红点从此永远站在画出来的水里；smoke R6（纯函数：并入后必有锚点房 + 组件：未扫节点猎手 → blip 与 canvas 同在）。怪异文字待作者给样例（label/事件数据机械损伤扫描零检出·疑似深 band 刻意隐晦层内容）。**（三修·作者第三轮实测四条）**：①**出墙最后一道闸 `projectIntoWater`**——别再逐个追几何错位（路由近似/voidTrack 一阶误差/**琥珀威胁接触本就是极坐标不看墙**＝「还是在墙外」的另一半真凶），红点与琥珀接触渲染前一律用**画面同一块 SDF** 裁决：caveSdf 说在岩里就螺旋采样挪到最近水点（确定性·≤42px·WALL_LO−1.2 进水线·smoke R7）；作者给过「当横截面看待」的让步，但有闸后无需让步。②**拖动 ghost 禁**——「偶尔拖走图里元素」＝原生 dragstart/文本选中在指针阈值捕获前开火；`.sonar-scan-stack` user-select:none + `* { -webkit-user-drag:none }` + 容器 onDragStart preventDefault。③**声呐头描述竖排修**——聚焦 tag+回正按钮挤进 `.sonar-panel-head` 时描述句被压成 1 字宽竖条；head `flex-wrap:wrap` + sub `flex:1 1 14em; min-width:0`（挤不下整句换行横排）。④**图上点击只选中（作者拍板·替代 06-10「再击同点＝前往」）**——map 是纯定位层，move 只走下方列表项（误触发移动/事件清零·pending 提示语与 aria-label 同步改）。regress 全量门后发布。**（四修·猎手两漏洞 + 扫描动画配平）**：①**穿过 hunter 修复**——`stalkerStep`/`weakStalkerStep` 的「仅事件/尸体节点」kind 门此前把**推进+接触判定**一起拦了：走向休整/地标/上浮口时猎手原地冻结、走进/贴近/对穿全不跑＝玩家可借「落脚点免疫」踏过它；修＝**已有猎手任何移动都推进**（接触与目的地 kind 无关），kind 门只保留给**现身**（落脚点不 jump scare·能否跑掉仍由 HSPEED<1 速度差决定＝无脚本死阀门不动）·`playthrough-stalker` **§18**。②**上浮拦截 `beginAscentFromDive`**——「近在咫尺还能上浮白嫖逃战」：贴邻/压点（stalkerNear 口径）时转身向上＝它先手扑上（contactAmbush·与原地同拍「摸到你歇着的地方」同口径）；拉开一跳以上照常上浮（逃生阀门保留）；只接 NodeSelect/Rest 主动上浮（战斗 flee/事件强制不走此口）·经 dive barrel 出口·**§19**。③**扫描动画配平**——黑场修复让波叠在全亮底图上读不出「扩散」（作者：第一次之后再也看不到动画）：重播时旧图**压暗 50%**、波前扫过才恢复亮度＝扩散可读且永不回黑（测绘记忆只是暗着等波）。regress 28/28 后发布。**（五修·扫描波续播制）**：压暗配平后作者仍「有时第一班波也看不见·只见节点淡入」——根＝波班次挂在 effect 单次执行里且 `lastSweepRef`「**起播即消费**」：同一挂载内 signature 连环变（到站自动扫→视角跟人走→renderIds 加厚）⇒ cleanup 砍掉进行中的 rAF、重跑读到「已放过」直接静态成图＝整班波被吞（SVG 节点「波到才亮」是独立 CSS 动画不受影响＝作者看到的组合症状）。修＝`sweepRef {key:lastScanTurn, t0:起播时刻}` **续播制**：effect 重跑按真实流逝时间接着画、不重置不跳过；波放完后的纯平移/缩放照旧重烤直出（不放假波）。regress 28/28。怪异文字一项按作者指示撤案（叙事框架将统一重写）。**（六修·常驻渲染循环＝波必放完的机制化收口）**：续播制后作者仍报「偶尔有偶尔没有」——结论＝凡把 rAF 班次挂在 `[signature]` effect 单次执行里的方案都治不完（churn 形态太多），改**架构**：bake effect 只产数据（离屏位图+几何参数 → `bakeRef`·新班次 → `sweepRef{key,t0}`·置 `needsRedraw`），**挂载期常驻一个 rAF 循环**逐帧读 ref 合成——effect/cleanup 从此砍不到动画；波放完且无新烤＝跳帧零开销；compose（含压暗配平/前缘环带/波前换图）整体迁入循环。声呐渲染从此三段定约：**数据（effect）→ 班次（ref）→ 合成（常驻循环）**。regress 28/28。

114. **剧情定调五批 → 剧情 SPEC 落档（2026-06-11/12·交互 design session·纯文档无代码）**——作者口述三章+番外完整故事框架，五批拍板全部入新 `docs/spec/深海回响_剧情_SPEC.md`（叙事 canon+各章一页纸+写作规约 10 条+决策记录 12 条+开放问题+实装路线图 St0–St7）。内核：**深海还你你带下去的东西**（一章执念寻师/二章他者恐惧禁岛/番外愧疚放手/三章主动入幻），与机制北极星缝合为「欺骗=环境×心理·角色可下结论『是氮醉』·游戏永不交底」+「环境越空，脑子越满」+规约 9「解释的接力」（每章真相只解释上一章并留下新的不可解）。关键拍板：①一章圆满结局=主角以为的真结局（按当时为真写·埋低调 tells 供回读·裁决留三章）；②番外=独立角色创伤摄影师（开局更强/不继承/通关后主线主角可回流刷材料+读痕迹 flag ≤5·摄影师不死不在场）；③前几章同图分区域解锁（禁岛/番外=现有海图扩展区·不开新图）、三章切分层向下；④章节制重写主 SPEC §11 多结局（渔夫/探险家评估降级为周目退场态·归 St2）；⑤每章大机制：二章=幻觉实化（低理智改变敌人行为+事件方向·理智升为内容状态维度）/番外=温度系统/三章=人变鱼（渐变机制·候选=「氧气位」资源反面+上升代价来源）+倒置规则（上升大代价/层级单程=知情选择/死亡后从更深处开始）；⑥**失联真相=单程下行**（十年前整队不是死了是回不来·隐藏结局钥匙）；⑦**死亡口径=断片说**（断片回港+下方留尸·脸永不可见·一二章零解释·三章翻牌「死亡下沉=真相——回港的是还能回去的那部分」·零机制改动只改文案口径）；⑧解锁高亮规约（独立配色+括号解锁原因+`unlockedBy` 数据渲染禁手写·沿 #109 先例实装时配 lint·红字骗你/解锁徽章永远诚实）。两个关键发现：主 SPEC 氮气→理智底座（红字选项/假节点/UI 故障）已备好提案全部机制型幻觉位——**剧情=换弹药不加系统**；一章四潜点↔主 SPEC §5.2 深度带几乎一一对应、沉没之城=「城市遗骸」=三章门已就位。路线图总原则：剧情骑现有轨道（事件/flag/POI/lore 四通道）·机制按需长·beats 交互 session 写/周末引擎只做 [氮醉] 氛围变体·每章机制落地配老区域回流解锁批。**先做好一章（St0–St4），最小第一步=教学部分**（St0=教学关植入半本日志+story flag 脊柱最小版·St1=四锚点 POI 沿 #69 mimic-POI 模板）。三章「另一个世界」围栏更新：叙事方向已定调、机制/数值/演出仍留专门 session（d_reveal 不动）。新 quirk **#117**（剧情剧透纪律+解锁徽章诚实）。无代码改动，regress **28/28** 基线绿确认。

115. **剧情路线图 St0「剧情脊柱」：engine/story.ts 最小版 + 教学关「半本日志」开场钩 + canon 对齐父亲→导师（2026-06-12·交互 session·#114 剧情 SPEC 的最小第一步·作者拍板「先做好一章·最小第一步=教学部分」）**——**(1) `engine/story.ts` 最小版**：章节/节拍状态**从 profile.flags 派生**（零自有状态/零存档字段·沿 #66 outpostStageFlag / #69 setProfileFlags additive 套路·不 bump SAVE_VERSION）；flag 命名口径 `story.ch1.*`（SPEC §8 St0 行锁定）**只在本模块生成**——`CH1_HOOK_FLAG`（教学钩）/ `ch1AnchorFlag(reef|wreck|midwater|vent)`（一章四锚点节拍位·§4.1 表序）/ `ch1EndingFlag(fulfilled|blank)`（圆满/留白结局位·两位可同存=留白是圆满后的更难重访）；派生 `ch1Story()`（hooked/tutorialComplete/anchorsDone/nextAnchor 链序取首个未置位/endings/complete=圆满）+ `chapterUnlocked()` 占位判定（ch1=教学完成（与海图同源门）·ch2=一章圆满·SPEC §1 解锁链）；既有 `flag.tutorial_complete` 收编为常量 `TUTORIAL_COMPLETE_FLAG` 不改名（chart_pois/PortView 钉着字符串）。engine↛ui 由 check-boundaries 规则一自动覆盖。**(2) 教学关植入「半本日志」开场钩**（剧本 SPEC 场景 1b·**一钩两扣不重复埋**）：新 `tutorial.prologue`（zones.json scriptedStartEventId 改指它·两选项 dive_in/look_again **都**经 setProfileFlags 种 `story.ch1.hook` 都接 descent=钩不依赖选哪条·dive 中持久写 profile 死在教学关不丢钩）——开场**种**：导师/半本日志（坐标+萤光背礁鳚草图=St1 锚点①预埋+批注「白天别去找它」=去欺骗提示 §3.1）/失联十年主角不信/科考不挣钱；`tutorial.ending_log` 重写为**「两本日志」**（旧题「父亲的字」）——结尾**收**：「第七次见到它」核心段**原文保留**但归属改为这条沉船的船长（陌生人），与导师日志互证「写的是同一片海」，「能落在这张海图里的，有四个」=四锚点入场券=叙事「抄坐标上海图」与机制 flag.tutorial_complete 解锁海图**同一个动作**；「剩下的页还粘着」=软性回流锁点（§3.5 自然暗示）替换旧「拿到合适的工具?」。**(3) canon 对齐父亲→导师全库 7 处**（剧情 SPEC §2 锁定失联者=导师·清单见教学关剧本 SPEC §8）：aldo.json（「你导师的装备」+「**在这片海的**初次潜水」消解老练科考员走新手关的别扭·ask_father/about_father → ask_mentor/about_mentor）/items.json 潜水刀+航海日志（删「其中一页是父亲的字」——它是陌生船长的日志不再冒认·lore id `lore.father_first_entry`→`lore.ch1.captains_page` quirk #99 不迁移）/tutorial.json/reef.json 桶海绵/state.ts 注释；教学关剧本 SPEC（docs/spec/深海回响_教学关剧本.md）同步修订：状态头 St0 修订记录+§2.1 主角改科考潜水员·导师失联+场景 1b 全文+场景 8 重写+§8 对齐记录，**本批新写/重写文案全标 [待过稿]**（quirk #117：机制+草稿先行·终稿须作者在场逐拍·过稿后划掉标记回写 JSON）。**(4) regress 抓手**（加内容必配 baseline 铁律）：新 `scripts/playthrough-story.ts`（regress 自动注册·28→**29 任务**）——§1 ch1Story/chapterUnlocked 派生纯函数（空档/链序/乱序容错/两结局共存/命名口径回归）·§2 ending_log **真实回港路径**（portEvent 无 run→flag 直进 profile·镜像 PortEventView.finalize+防重播·scenario runner 默认建 run 故这条无 run 分支只有这里守）·§3 存档 round-trip（story flags 随 profile.flags(Set) 往返后 ch1Story 派生逐字节一致）·§4 **canon/字面量守门**（tutorial.json/aldo.json 禁「父亲」回潮·data flag 字面量必须等于 story.ts 生成器输出·scriptedStartEventId 接线·prologue 每选项都种钩都接 descent·旧 lore id 不残留）；scenarios +2（tutorial_prologue__dive_in/look_again）+1 改（tutorial_ending_log__close_book 随 canon 更新）=事件 baseline 307→**309**；verify-tutorial.mjs 起始事件**从 zones.json scriptedStartEventId 再生**（不再手钉 descent）+ 链上 setProfileFlags 纳入模拟 + 断言钩落位；playthrough.ts 教学链加 prologue 选项映射 + 断言 story.ch1.hook 经真实管线落档 + ch1Story 派生抽查。事件 **207→208**〔`--list`/verify-tutorial〕、敌人 9/combat 10/item 24 不变。`npm run regress` **29/29** 全绿（7.9s·含 prod build）。新 quirk **#118**（story flag 单一来源+canon 守门）。**红线自查全过**（quirk #117）：一二章对失联真相/断片说零泄漏零暗示（开场只写主角「不信」=canon §2 明载执念）、无尸体的脸、不动 d_reveal、不越界 St1（四锚点只落 flag 位+圈坐标叙事·POI/事件归 St1）。**遗留**：①教学关新文案（prologue/ending_log/aldo 三处）待作者过稿（[待过稿] 标记在剧本 SPEC）；②下一步=St1 一章 MVP（四锚点 POI 上海图沿 #69 mimic-POI 模板+锚点事件链+原创鱼 4 种 lore 形态+圆满结局 tells 清单——**需作者在场拍 tells 与结局文案**）；③St4 [氮醉] 周末引擎批次在 St0 口径落定后已可滚动（无人值守可做）。

116. **负伤系统 Wave 1 机制脊柱 + Wave 2 scent 第三通道实装（2026-06-12·交互 session·照 docs/spec/深海回响_负伤系统_SPEC.md §3–§5/§10–§11+§6.1·同日教学文案过稿）**——**(1) 数据模型**：新 `types/injuries.ts`（`InjuryTier 1|2`/`InjuryDef`/`InjuryTierEffects`/`ActiveInjury`/`DerivedModifiers`）+ `src/data/injuries.json` 先 bleed/rib 两伤种（**文件顺序有语义**：cause 默认派生取同 cause 第一条·physical→流血·bleed 必须排 rib 前→quirk #119②）；`run.injuries` 必填字段（createNewRun 种 `[]`·hydrateGameState 单点补 `[]`·不 bump SAVE_VERSION·quirk #99/#106·playthrough-save §6 补断言）。**(2) 唯一写入口 `engine/injuries.ts`**：addInjury/worsenInjury/healInjury 三入口（同类升档·重档再中招 saturated 无额外惩罚〔§12.1 当前拍〕·上限 3 超限对「已有最轻伤种」升档、并列取列表先来者=确定性）+ `seedInjuries` 测试 fixture 单点 + `injuryIdForDamageType` cause 派生 + `describeInjury` 徽章三件套（档位/效果行/治疗路径·纯函数）。healInjury 已就位但 medkit 尚未接线（Wave 2/上架契机·见遗留）。**(3) 唯一折算点 `engine/modifiers.ts`**：computeModifiers（乘数相乘/加数相加/布尔取或·无伤=恒等元→全部消费点零行为差）+ effectiveStaminaMax（基线 staminaMax 不改写·读点叠 staminaMaxDelta·地板 1）。**(4) 消费点接线**：combat.ts `actionCosts`（**availability 与扣费同一函数**=「需 N」提示与实扣同口径·ceil）、回合开始 tick（流血·重 −2/行动+system 日志·availability 按 tick 前体力校验=带着重流血硬挥这一下力气可能半途漏光——死亡螺旋是意图 §1）、敌攻理智 ×sanityTakenMult（ceil）、applyStatsDelta clamp 走 effectiveStaminaMax；events.ts `tickTurns` 加可选 `opts.o2CostMult`（其余调用点不传=逐字节不变）；dive-move.ts 移动 tick 氧耗+洋流消耗各乘 mult（`currentMoveCost` 纯函数基准不动·应用点 ceil）；ascent/dive-actions（rest/camp）/events outcome clamp 全接 effectiveStaminaMax。**(5) `EnemyAttack.injuryOnHit`**：鲨咬 chance 0.35→bleed（cause 派生）、石斑侧撞 0.4→`injury.rib` 显式（挤压伤不加 DamageType·§4.1）；**仅带字段的攻击才掷骰=零额外 RNG 消耗**，既有 11 条 seeded 战斗基线零漂移（→quirk #119①），shark/grouper normal_kill 实跑补钉 `injuriesFinal`（seed 1 下恰各 proc 一次轻档·数值断言未动=轻档零效果旁证）。**(6) StatusBar 徽章**（SPEC §9）：伤势 chip（轻=警示色/重=危险色）点开详情浮层（describeInjury 三件套·轻档空效果=「暂无影响——再受同类伤会加重」）；体力条 max 走 effectiveStaminaMax=条轨与引擎结算同源；styles.css 新 .injury-chip/.injury-detail（无新滚动容器·守规则三）。**(7) 机制化**（SPEC §5「禁散读」）：check-boundaries **规则四**——engine 内 `run.injuries`（含 !/? 变体）只许 injuries/modifiers/state/combatScenario 四文件触碰（正则 tripwire·落地当场抓到本批自己注释里的违例=门是活的）；combatScenario `input.injuries` fixture（quirk #106 写全或不写·if 守护）+ `summary.injuriesFinal`；playthrough-combat-scenarios 新 expect `injuriesFinal`（**精确集合匹配**·{}=断言全程无伤）。**(8) baseline 实跑抄（#43 套路）5 新**：`reef_shark__bleed_proc`（seed7·轻档零数值效果）/`reef_shark__bleed_worsen`（seed7+fixture@1·同类升档仍单行+升档后 tick 当场收账 dSta −28 vs −24）/`reef_shark__bleed_heavy_tick`（seed1+fixture@2·tick −2×3+重档再 proc=saturated）/`reef_shark__rib_heavy_o2`（seed3+fixture@2·氧 −6 vs 裸 −3=ceil(1×1.4)×3·体力与裸跑全等=rib 不碰体力）/`reef_grouper_solo__rib_proc_worsen`（seed9·一场战斗走完 无→轻→重+升档后氧耗当场变贵=死亡螺旋最小标本）。**(9) Wave 2 scent 第三通道（§6.1·全绿后顺做）**：`EnemyDef.scent`/`StalkerProfile.scent`/`Stalker.scent`（spawn 派生=prof ?? 遭遇成员任一）；`stalkerScentLocked`（×computeModifiers.scentTrail）→ advanceStalker **旁路分支**（恒「有你的信号」+免疫 §3 装备规避；**decoy 分支在旁路之前=流血·重时仍 guaranteed**·北极星）；守口 patience ×1.5（`STALKER_SCENT_PATIENCE_MULT`·读点折算=止血当场回常）；`scentSpawnReady` 现身线砍半（`STALKER_SCENT_SPAWN_ALERT_MULT` 0.5=SPEC「刷出概率×2」的阈值制翻译·本引擎现身是 alert 越线不掷骰·浅水 §7.5 铁律不动）+ stalkerStep 接线+循血现身专用叙事；startCombat 嗅觉系 unaware→alerted（现役敌种皆 alerted 起步=机制就位暂潜伏）；数据=鲨+梭鱼×2 `scent:true`（起手名单待作者过 §12.2）；猎手 SPEC §2.2 补「第三通道」注记（§6.1 跨 SPEC 承诺）；playthrough-stalker **§20 八组断言**（摸黑/吸声失效但 decoy 仍 guaranteed·轻档不漏味·非嗅觉系不波及·半线循血现身含 moveToNode 全链+对照·守口 ×1.5）。**(10) `npm run regress` 29/29 全绿**（10.5s·含 prod build·combat scenarios 11→16）；负伤 SPEC §11 Wave 1 全勾+scent 勾+头部实装进度注（设计仍待过稿·§12 五条留作者）。**(11) 同日续·教学文案过稿**：作者整体认可 St0 三处新文案（「教程剧情我也觉得可以」）——教学关剧本 SPEC 场景 1b/场景 8/aldo canon 注三处 [待过稿] 清标（过稿记录入档头·两小项〔导师人称、「白天别去找它」批注〕维持现稿不另改·实装 JSON 与 SPEC 一致无需回写）=#115 遗留① 关账。**(12) 同日拍板**：下一步主轴=**St1 一章 MVP**；med_kit=St1 session **起手顺带**（接 heal 字段+Mira 上架+1 条 baseline·定价提案制）。新 quirk **#119**。**遗留**：①负伤叙事/徽章/tick 文案仍 [待过稿]（injuries.json narrative+combat tick 行·quirk #117）；②injuryOnHit 0.35/0.4、scent 半线/×1.5 全是起手值待 baseline 调；③scent 起手名单（鲨/梭鱼）待作者逐敌过（§12.2）；④Wave 2 剩余（瘫痪/retaliation/药物压制层/dread·numb·burn/事件 addInjury op）按 §6.3 章节批次随剧情走、不抢 St1；⑤战斗侧 unaware→alerted 旁路现役无 unaware 嗅觉敌=潜伏，二章敌种设计时记得它在。

117. **St1 一章 MVP（四锚点链+专属双 zone+原创鱼四种+圆满结局）+ medkit 治伤上架 + 负伤 §12 二批拍板 + 文案流程改制（2026-06-12 晚·交互 session·#117·commit `8907275`/`b3efc20`/`5a33364`）**——三块按手off顺序全落。**(1) medkit 治伤接线+上架（`8907275`·St1 起手顺带·负伤 SPEC §8）**：`consumable.medkit` 数据旗标（data-driven 同 decoy.kind·非硬编码 id）——use_medkit 回 30 体力之外对身上**每处**伤按各自 `heal.medkit` 生效（**「全部能治的一起处理」作者拍**·Wave 1 池里 bleed cure 生效/rib none·徽章「急救包可治」承诺逐条兑现）；整包结算 `applyMedkitHeal` 住 `engine/injuries.ts` 唯一写者（规则四触碰面不破·首版直写在 combat.ts 被 tripwire 抓红=机制兑现了自己）；止血⇒scentTrail 随伤消失零接线（#116 读点折算）。Mira 上架：`SHOP_STOCK_CONSUMABLES` + sellPrice 25（买 40=floor(25×0.8)×2·备货 2·作者拍「以后都能调」）；定价后可卖回（offer 20·同 decoy「有价消耗品她收」口径）。基线：新 combat scenario `reef_shark__medkit_cure_bleed`（bleed@2+med_kit×1·T1 治疗当回合 tick 仍先收·seed 3 后续敌招零 re-proc·injuriesFinal 空集=cure 断言本体）=combat 17 条；economy lootValue 44→64/可卖项 2→3/回购门控/货架四处实跑重抄；smoke K1b 加急救包断言·K1d「她不收」样例 med_kit→剧情物。**(2) St1 一章 MVP（`b3efc20`·剧情 SPEC §4.1·作者三拍：③④开专属新 zone·锚点任意顺序〔vent 需前三齐〕·草稿全出我手作者事后逐拍）**：四锚点 POI 上海图沿 #69 mimic「入潜强制开场」模板（新 `ChartPoi.story{anchor,eventId}`·requiresFlags 只门 tutorial_complete=教学尾「四个坐标圈上海图」同一动作）——漆号珊瑚丛（旧灯塔礁浅段 [10,22]·距离 0）/温带商船残骸（墓园·2）/远洋中层（新 zone·2）/海沟热液场（新 zone·3）；**story 坐标=已知点**：`isPoiLit` 恒真（日志抄来的坐标不走灯塔「发现」轴·灯全灭坐标还在——playthrough-chart「无灯塔=海图空」改「只剩 4 日志锚点」新口径）+ `isPoiExplainedByLighthouse` 恒真（**别让剧情锚点误穿 mimic「亮而无主」宏观 tell**·海图诚实轴）。新 zone ×2：`zone.open_midwater`（开阔中层 [55,85]·layered·canFreeAscend·梭鱼 ambush）/`zone.vent_trench`（海沟热液 [85,118]·maze·depthCurveRange [0.4,0.75] 井+廊·canFreeAscend false·盲鳗）+ `ZoneTag` 加 midwater/vent。接线：`startDiveFromPoi` 锚点 flag 未置位（vent 另需 reef/wreck/midwater 三齐）→ 开场设为节拍事件，否则普通下潜=回流重访自然成立；置位只走事件 `setProfileFlags`（quirk #118·dive-start 只读 `ch1Story` 派生零写入）。事件 208→**217**：锚点节拍 ×4 + `ch1.ending_station` 观测站圆满（**按当时为真**·四 tells 低调埋：在站里也戴面罩「热液场的规矩」/十年的站不该这么干净/队友只在余光与玻璃反光/导师每句话都出自日志已有句子——「群落比坐标重要」即沉船半页原句；两选项都置 vent+fulfilled+forceAscend 收束一章→ch2 解锁判定即通）+ midwater/vent 氛围池各 2（磷虾柱/没有参照物的蓝/黑烟柱/灯的间距——薄池·稠密度留周末引擎）；原创鱼四种先事件+lore 形态（`lore.ch1.*`·萤光背礁鳚兑现教学日志「白天别去找它」预埋·锈甲沉船鲷带路穿舱·幻光流帆鱼「成对的眼睛=折光薄膜」·热液岩鳞鱼=日志最后一张草图）。回归：`playthrough-story` 新 **§5**（4 POI 一锚一点/story.anchor∈CH1_ANCHORS/eventId 已注册/requiresFlags 口径/ch1+midwater+vent 三文件 story 字面量只许生成器输出+禁「父亲」/强制开场/任意顺序/vent 门/回流不重播〔普通潜开场可为池抽事件·断言「非节拍」不断言「无事件」〕/resolveOption 真实落账→complete→ch2 解锁）；`playthrough-chart` 锚点 5→9 + reach 表 +4（坐标按 home(0.06,0.5) 实测调到与写死 distance 一致）；scenarios 309→**331**（22 新实跑抄 #43·全确定性：check 双分支/锚点③失败仍置位=投射被经历即节拍/vent 全链 2 步 finalPhase=ascent）。红线自查（quirk #117）：断片说/单程下行零泄漏·头盔「里面当然是空的」无脸·锚点事件纯轻量 stat 代价（一章教学档·事件 addInjury 归 Wave 2）。**(3) 负伤 §12 二批 + 过稿（`5a33364`）**：作者过 St1 批①（锚点①②整体过稿）后拍**「新手剧情不需要找我核实·事后挑错」→ 追问确认扩成全口径：全部系统/剧情文案改「事后挑错制」**（quirk #120·[待过稿] 不再挡实装·重大调性件仍主动贴——本批观测站结局已贴过 tells）。§12 六条全过：**12.1 推翻两档封顶**（「伤该分情况引发瘫痪/别的恶劣状态·不止两级」⇒ 档位制重设计**立项**：每伤种自有档数+escalatesTo 转移·SPEC §3 修订注·**留专门 session 别在内容批顺手做**·Wave 2 余项按此留接口）；**12.2 scent 名单拍定**：鲨+梭鱼+**盲鳗**·幼体随亲代⇒blind_eel/blind_eel_juv `scent:true` 落数据（梭鱼幼体 #116 已带）+ stalker §20g 对照池换章鱼-only+「蓝洞池=嗅觉池」新断言；12.3 惊惧×panicked 暂不叠（二章精神主轴同场）；12.4 瘫痪爬到点亮前哨=**算获救**（Wave 2 接线）；12.5=上述改制；12.6 错乱失真配比留二章（只锁原则）。SPEC 状态头/§3/§6.1/§8/§11/§12 全部就地更新；作者同日自改（二章敌人/环境 palette+§12.10 progression 留门+剧情 SPEC 七/八/九批）随 docs 入库。**每笔 commit 前全量门**·收尾 regress **29/29 全绿**。新 quirk **#120**（事后挑错制）/**#121**（ChartPoi.story 锚点模板约定）。**遗留**：①作者合并验收顺延（#109–#113 在线上·#116+本批 5 commit 待 push——动线清单已交作者）；②档位制重设计 session（12.1·牵动 #116 全部 baseline）；③injuryOnHit/scent 数值仍起手值待 baseline 调；④St2 留白结局+录音道具；⑤midwater/vent 氛围池薄·待周末引擎补批。
 **（2026-06-12 同日续·上线+验收支援+dev 工具点单·`15dce7d`）**：①**积压发布**——`.deploy-token` 显式 URL push `5504b63..0725e15`（#115/#116/#117 六笔·post-push fetch 刷 tracking ref·quirk #103 照做）+ Actions build/deploy 双绿；**考古发现：线上此前一直停在 06-10 14:04 的 `f237bfa`（#106 时代）**——06-10 下午之后 origin 有新 commit 却零 deploy run（deploy.yml 无 paths 过滤·疑似仓库 Actions 被关过一段），作者「dev 免费升级/购物没了」的报告即此（线上版本早于 #109 dev 货架的诞生）；今日两笔 push 均正常触发部署，Chrome 实测页脚 `build 2026-06-12 22:38 (0725e15)`、测试货架/急救包上架全部在位。**再遇『push 了线上没变』先查 Actions run 列表**。②作者答疑：蛙跳/深水前哨=正式功能（深水区 Phase 1/2 前哨脊柱）非测试遗留。③**dev 工具点单三件**（`15dce7d`·quirk #110 家族）：App 页脚 ?dev「删档重开（dev）」（window.confirm 防误触→复用 gameOver handleRestart=clearSave+新档）；`devBuildAtLighthouse` 0 成本直建灯塔设施（跳材料/金币/前置/灯塔等级·已建/未知 no-op·引擎无门门在 UI·真路径零触碰）；LighthouseBuildPanel 每行「测试建造」+每灯塔「一键建满」（只扫该灯塔可见轨=不给家灯塔建 outpostOnly 能源设施）。playthrough-lighthouse 新 §7。regress 29/29 全绿·已推已部署。
 **（2026-06-12 同日续②·作者反馈四件+章节哨站批定调·`e65f56d`）**：①**重开新档转正**（作者拍「不该是 dev 功能」）——页脚按钮去 DEV_TOOLS 门改玩家文案·confirm 防误触不变。②**声呐图选中即入画**——选中相邻节点在视窗外（10% 边距）时平移到「你与它的中点」两头同框（保留缩放档·PAN_MARGIN 钳制·只在选中一刻平移不锁视角），治「离得远要拖图才看到」；并答疑：移动=逐跳相邻、未扫相邻可摸黑走、远房间不能直达。③④**章节哨站批定调**（作者四拍·实装=下个 session 主任务·详深水区 SPEC §10 决策日志 2026-06-12 条）：哨站上大地图点选蛙跳/同 outpost 体系加三新 OutpostDef 落锚点②③④区（区①=灯塔解锁）/解锁=剧情门+建设账单串联（dev 免解）/首扫仪式全显周围点（特殊隐藏除外）后气候原因转暗/**暗点仍随气候消失重现**＝海图可见性三态化（亮/暗/无·诚实轴不破）。regress 29/29 全绿·已推已部署。
 **（2026-06-12 同日续③·作者反馈再四件·`7cb0e6d`）**：①**横岩廊海图点挪开重叠**——(0.44,0.28) 与蓝洞群 (0.46,0.30) 归一化距离 0.028≈十几像素=两点叠一起只能点到一个；挪 (0.38,0.22)（距离 0.113≈52px）·reach 实测仍 1 档·playthrough-chart §2 表同步（备忘「挪 anchor 必跑 §2」照办）。②**Aldo 教学后对话分流**——root 教学选项（ready/not_yet）加 notHasFlag 门控·新增教学后日常节点 `aldo.harbor_morning`（开海图/问导师/坐一会儿）·about_mentor 返回双分流不再送回 briefing；playthrough-story §4 加数据形状守门；文案照挑错制落库。③**电池/灯装备化经济定向**（记深水区 SPEC §10 决策日志·含作者同晚澄清：「浅水近免费」是旧沟通误记——真实口径=耗电不随深度增加+有自然光处不需开灯，0a 拍板不存在被推翻的部分）：灯与电池都是装备要购买·电池买了才显示电量·前期电池勉强支撑灯的使用=暗处开灯就要算电·后期材料制作高容量电池/省电灯·深处灯照不够被迫上声呐=声呐耗电远大于灯（电池成声呐第二道门）——电量升为贯穿全程主资源轴（与三章「电力槽」同向）·牵动 power 数值与基线=成块 feature 待排期。④**剧情碎片可查阅定向**（记剧情 SPEC §3.1）：lore 正文登记表（data/lore.json 类·现 loreEntry 是自由 id 串、正文读完即逝）+ 日志查阅 UI + 「引用必登记」lint——回读性=圆满 tells 的兑现通道·待排期。regress 29/29 全绿·已推已部署。
 **（2026-06-12 同日续④·灯偷看缝焊死·`82a71df`）**：作者问「关灯时开灯当回合能看到吗、耗电吗」——答：切灯免费即时生效（不耗回合·预览当场重算），电费只在回合结算按瞬时开关算 ⇒「开灯瞄一眼再关」=零电费零警觉偷看。作者当场拍规则：**「只要看清了这回合的选项，就按这回合开灯计算电量，和进回合前就开着一样」**⇒ 新 `SensorState.litThisTurn`（真条件字段·setLight(true) 置位·关灯不清·tickTurns 按它补收整回合灯电费后复位·不 bump SAVE_VERSION）；只动电费口，警觉仍瞬时（隐身轴独立·要不要同补收留电池批拍）。playthrough-sensors §8b（瞄一眼=整回合灯费与全程开同额·结算复位不粘连）。regress 29/29 全绿·已推已部署。
 **（2026-06-12 同日续⑤·电池系统终态四点·纯设计入档）**：作者补完电池系统终态（记深水区 SPEC §10 决策日志末条）：①声呐/灯=装备各带电池槽独立耗电·**角色没有电量属性**（run.power/powerMax 退场·电只存在于电池里）②电池=道具（同 medkit 占格·装入槽·**用完不消失**·回港/特殊场景事件充电）③高级电池制作材料含低级电池（升级链吃旧件）④后期科技线装备同吃电池+剧情电量门（「放入电量≥X 的电池才能启动设备/建筑」）。实装注记：电池带电量状态 vs {itemId,qty} 堆叠库存——倾向「满/空两态 itemId」方案（堆叠模型不动）；九批「电力槽」措辞届时回校（方向不冲突）。候选主任务 C 描述已按终态重写。

---

118. **章节哨站批·机制核（三章节 band+OutpostDef / 锚点解锁门 / 显式起跳蛙跳 / dev 免解·2026-06-13·交互 session·#118·深水区 SPEC §10 决策日志 2026-06-12 条「章节哨站批定调」的机制层落地）**——把 #117/§10 定调的「章节哨站」拆成**可机制化、可回归**的核与**需作者在场逐拍**的视觉层，本 session 只落前者（挑错制 #120 已不挡实装·但首扫仪式动画/海图可见性三态渲染是观感件→留作者）。**起手 AskUserQuestion 两 pin**：①方向「全做」（验收/档位制需作者在场→本 session 推能独立落地的主任务 1 机制核）；②**章节哨站蛙跳模型＝各自跳回本锅点区（新 band）**（作者拍·替代「并入深脊柱」「另成解耦网」两选项）。**(1) 三章节 band（`data/depth_bands.json`）**：`band.ch1_wreck`(温带商船残骸·zone.wreck_graveyard·[18,50]·order 11·murky) / `band.ch1_midwater`(远洋中层·zone.open_midwater·[55,85]·order 12) / `band.ch1_vent`(海沟热液场·zone.vent_trench·[85,118]·order 13·murky)——order 取 11/12/13 与 blue_caves 深脊柱（1–7）分段、明示「不在线性梯子上」。**(2) 三章节 OutpostDef（`data/lighthouse_upgrades.json` outposts[]）**：`outpost.ch1_wreck/ch1_midwater/ch1_vent`（坐标落锚点②③④区·result 灯塔 mapX/Y 贴对应 anchor POI·各 3 阶段料账单 gold 升序 40/70/110·50/90/130·70/120/170·vent `current:true` 激流·全 submerged 会衰减）；**锚点①(reef) 由 home 灯塔覆盖·不设哨站**（无 band.ch1_reef）。**(3) OutpostDef 新字段 `requiresAnchor?: string`（`types/lighthouse.ts`）双义**：①解锁门=对应一章锚点节拍（`ch1AnchorFlag(anchor)`）置位前为「暗·已知不可建」、置位后转「可建」；②**章节网标记**——与深脊柱解耦。**(4) 解锁门接线（`engine/lighthouses.ts`·import `ch1AnchorFlag` from story·engine→engine·无环）**：新 `isChapterOutpost`/`outpostUnlocked`/`chapterOutpostForBand`/`isChapterBand`；`advanceOutpost` 锁态拒推进（叙事不扣料金）·`canAdvanceOutpost` 锁态恒 false；新 `devAdvanceOutpost`（#110 家族·跳门+跳料置阶段·建满 promote·不扣银行/库存=真经济零触碰）。**(5) 显式起跳蛙跳（`engine/dive-start.ts`）**：`startDiveFromOutpost` opts 加 `launchOutpostId`（指定起跳前哨·要求半亮≥USABLE·否则忽略退回深脊柱/home）；`deepestOutpostLaunch` **跳过章节前哨**（`isChapterOutpost` continue）=章节前哨不被深 band 误选为起跳点（深脊柱逐字节不变）。**(6) UI（`SeaChartView.tsx`·最小机械层·非视觉演出）**：深脊柱蛙跳列表 `filter(!isChapterBand)`（章节区 band 不混进深列表）；OutpostPanel 章节前哨锁态显示「暗·待解锁」+解锁提示、点亮（或半亮）出「蛙跳下潜」按钮（`onFrogjump(bandId,outpostId)` 显式起跳·闭包 carryPicks）、?dev 后「测试推进（dev）」。**回归**：`playthrough-outpost` §0 加三章节 def 自洽（3 阶段/requiresAnchor 合法/band 双向反查/result id 全局唯一/reef 无哨站/脊柱不带章节标记）+ 新 **§14**（锁态不可建不扣料→锚点置位解锁建满→dev 免解三连点亮→章节蛙跳落本区 zone→章节前哨不污染深脊柱 trench_mouth 预耗氧→半亮门忽略显式起跳）；`smoke-chart-ui` 新 **§S**（OutpostPanel 锁态「暗·待解锁」+解锁提示+深脊柱列表不含章节 band+点亮出蛙跳按钮）。`npm run regress` **29/29 全绿**（5.1s·含 prod build·事件/敌人/combat 计数不变=纯加 band/outpost/字段）。新 quirk **#122**（章节前哨 requiresAnchor 双义+章节网解耦四约定）。**遗留（留作者在场逐拍·SPEC §10③④）**：①**首扫仪式**（点亮后首次扫描全显周围点→气候转暗·沿 #80 survey-sweep 动画）；②**海图可见性三态化渲染**（亮/暗/无·暗点随气候消失重现·chart.ts 派生重构+SeaChartView 暗点渲染·诚实轴不破·mimic 仍唯一谎点）——这两件是观感/演出件，机制核已就位可直接挂。③蛙跳入口的「点海图哨站 marker→选 band」完整视觉交互（本 session 用 OutpostPanel 按钮顶上）。④料账单/坐标全是起手值待作者手感调。⑤合并验收（#109–#117 动线）与档位制重设计（§12.1）仍各自待作者在场 session。

---

119. **章节哨站批·验收续：声呐 reach 调参（海沟可读性）+ dev 一键解锁本区 + 区域揭示视觉定调（demo 过目）（2026-06-13·交互 session·#118 续·接 `20607fd` 之后）**——作者线上验收 #118 给三条反馈，逐条处理。**(1) 海沟开灯仍「一团黑」诊断 + 声呐 reach 调参**：复现确认非渲染 bug——`clarityForNode` 深度差降档下，海沟（vent 85–118m·shaft depthCurve）首跳就 14m、超灯(6m)/声呐(14m) reach，13 节点中 12 个 blind；对照 midwater（55–85m 匀步层）灯本就读得到 5/16＝**问题在竖井落差不在绝对深度**。作者两轮拍板（先「保持现状」后复议「85–118m 照不到不合理」）→ 终拍**抬声呐 reach**：`clarity.ts` `SONAR_DEPTH_REACH` 14→**22**、`SONAR_DEPTH_REACH_MAX` 26→**30**（声呐＝深水的眼·竖井上半段开声呐可读·灯仍照不透＝「深处被迫上声呐」设计；最深陡降 >22m 仍需凑近摸黑＝守「永远有比最深更深的」北极星）。对应剧情口径（作者补）：前期浅洞靠灯看「事件」、后期靠声呐看「洞穴完整形状」——与调参一致。回归：`playthrough-sensors` §12 deep map 加 `deep`(dd26)/`abyss`(dd35) 两节点、12c/12d/12f/12g 断言按新 reach 重写（far dd20 由 none→sonar·deep 基线黑·升级到 30 可见·abyss 升满也黑＝北极星）。**(2) dev 一键解锁本区（作者拍·像 demo 的按钮·不走剧情/材料）**：`engine/lighthouses.ts::devUnlockChapterRegion`——置 `flag.tutorial_complete` + 对应锚点 flag（潜点门开）+ `devAdvanceOutpost` 连推点亮（真路径零触碰·#110 家族）；OutpostPanel ?dev 加「解锁本区（dev）」按钮（章节前哨未点亮时显）。`playthrough-outpost` §14g（解锁→点亮+置双 flag+不扣料+push 灯塔）。**(3) 区域揭示视觉定调（作者过两版 demo 拍「就是这样·实装会更精致」）**：四个「类老灯塔」揭示圈——**家灯塔=海岸线半圆**（同现状）、**章节三哨站=离岸整圆**（半径略小仍覆盖多点）；解锁点亮→圈内点可见；**为看新区·海图边界外扩**；前期彩色圈、后期手绘背景图（圈留描边）。暗点语义再确认：多数彻底不显（无）、少数显示但过不去（暗）＝同现状。**新增需求两条入 SPEC §10**：(A) 升级哨站→搜到更多「暗」点（设施轨·骑 tri-state reveal·属主实装块）；(B) dev 一键解锁（本批已实装）。`npm run regress` **29/29 全绿**（含 prod build·事件/敌人/combat 计数不变）。无新 quirk（声呐 reach=tunable·dev 工具=#110 家族）。**遗留＝区域揭示主实装块（下个 session·作者可在场调风格手感）**：chart.ts 三态化 + SeaChartView 揭示圈（家半圆/哨站整圆）+ 海图边界外扩 + 首扫仪式（#80 sweep）+ 需求(A) 升级搜更多暗点 + **收编旧「深潜·蛙跳（试验）」band 列表与独立面板**（点海图哨站圈→选 band·替代旧港口侧入口）。详见深水区 SPEC §10 决策日志 2026-06-13 条。

---

120. **区域揭示主实装块·配置化重写（2026-06-14·交互 session·#119 续·作者多轮在场调）**——作者反馈「很多大圈重叠成糊」从根修：根因 `BASE_LIGHT_RADIUS=0.72`（归一化世界直径 1.44>全图）⇒ 每灯塔圈盖满图相互重叠；删之、改读**数据驱动区域配置**。**(1) 配置层**：新 `data/chart_regions.json` + `engine/regions.ts` + `ChartRegionDef`（types/chart.ts）——每区＝一座 owner 灯塔的揭示圈·radius/palette/shape 全配置·单一来源·`revealRadius(lighthouse)` 读它（未配 owner 回 `DEFAULT_REVEAL_RADIUS=0.18`）·UI 读 palette/shape/label·Ch.2/外传只加一个 mapId 段、引擎渲染零改。**(2) v4 横向 6 区**（作者多轮 mockup 拍）：家·珊瑚(青·海岸半圆)/残骸(绿)/中层(蓝·大)/热液(琥珀)/海沟(深蓝·小·最右最深)/鲸落(紫·最小·占位)·坐标在 chart_pois/lighthouse_upgrades·半径在 chart_regions。「教学后只家区」由小半径自然涌现（远 POI 不在任何 active 圈=hidden）。**(3) 新章节区**：`outpost.ch1_trench`（`OutpostDef.requiresFlag?` 非锚点章节门）+ `band.ch1_trench`；`outpost.ch1_whalefall` 占位（待新 session 改 flag-gated 区+精简营地）。**(4) 渲染重写**（`SeaChartView` + `ChartViewport` SSR 安全）：圈从配置画分色（柔和渐变+彩描边·替代粗糙白圈）·家=coast 半圆·只渲本图配置区（深脊柱/深渊超渊不在 Ch.1 图）·CSS 补齐 `.chart-map-outpost`/`.chart-light*` 按钮 reset（清默认白框+焦点环）+ 海岸条左延伸（修全解锁缩放露黑缝）。**(5) per-哨站扫描**（`profile.outpostScanSig` per-灯塔·替代单值 `chartSurveySig`）：每灯塔签名＝它点亮的 POI 集+半径·只扫签名变的灯塔（解锁/潮汐只扫受影响那座）·圆形慢扩散·边缘到才浮现/淡出·**动画播完才持久化**（之前立刻写⇒sweepActive 一帧翻 false=扫描秒消失·关键 bug）。**(6) 发现门控**（`OutpostDef.discoveredFlag`·`isOutpostDiscovered` 章节前哨非恒 true）：未发现的章节前哨不在图上留灰节点·剧情节拍置 discoveredFlag 才现「暗·待解锁」·dev 用海图顶「解锁大区」strip（`devUnlockChapterRegion`）/`devRevealOutpost` 模拟。**回归重定基线**：`playthrough-chart`（gating/坐标·新 `fullyRevealedProfile`·roaming/天气断言 robust 化）·`playthrough-lighthouse`（`BASE_LIGHT_RADIUS`→`regionRadius`）·`smoke-chart-ui`（§A gating·§N 发现门控+`OutpostPopup`/`HomeDivePopup` 直渲·§S popup 化）。`npm run regress` **29/29 全绿**（含 prod build）。新 quirk **#124/#125/#126**。**提交注记**：本 session 树含三摊相互依赖（区域揭示↔界面重构 PortLayout↔声呐 WIP·共用 state.ts/styles.css·`NodeSelectView` 渲声呐面板）→ 保绿前提下无法干净拆分，合并为一个绿 checkpoint（含界面重构 #123 双栏布局 + 声呐 WIP）；沙箱无 push 凭证·留作者本机 push。**遗留＝鲸落区剧情（大特性·下个 session·见新 `docs/spec/深海回响_区域揭示配置化_SPEC.md` §10「鲸落区·细化」）**：中层目击巨型生物×3→附近出探索潜点→下潜探索+剧情→找到鲸落区（**flag-gated region**·非 owner 灯塔·引擎待扩 `revealFlag`+`center`）+ 精简营地（`OutpostDef` 功能开关变体·非全功能哨站·作者 2026-06-14 拍）+ 残骸独立目击 + 图鉴 + 鲸落子区。
