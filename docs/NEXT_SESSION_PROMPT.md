你在接手「深海回响」(Deep Echo) —— 潜水题材文字冒险 Roguelike（Vite + React + TS），仓库在 `~/Desktop/Blue`。

**当前主线**：深水区 vision 分阶段实装，`docs/深海回响_深水区_SPEC.md` + `docs/深海回响_声呐与房间_SPEC.md` 是源真。深水区四个 Phase 的**核心已全部落地**：**Phase 0**（0a 感知 #58 / 0b 探测 #59 / 升级轨 #60）+ **Phase 1**（band 阶梯+蛙跳 #61 / 节点级 clarity #62 / band 级 alert 倍率 #64）+ **Phase 2**（2a 跨 run 分阶段前哨+真蛙跳 #66 / 2b 能源/衰减/海图前哨 UI/多前哨链 #67 / 真 reveal dimming #76 / **2b 续 材料中转·寄存+寄存丢失+深渊前哨 #79**）+ **Phase 3 mimic capstone 核心**（海图假 POI 引诱→入潜兑现→读穿 tell→d_reveal #69）+ **深段内容**（trench #63 / abyssal #65 / hadal #68 / 渊外 subhadal #72 / 深段多 feature 内容 #75 / 周末 trench ping-欺骗 #77）+ **声呐与房间**（S0 扫描 #71 / S1 多事件房间 #74 / S2 不可信扫描 #78）+ **单向下潜预告 #70**。

**上一个 session（「彻底完成 D」，2026-06-05）把 Phase 2b 续三项全做、衰减三齿（变暗/阶段回退/寄存丢失）闭环（#79）**：① **材料中转/寄存 + 寄存丢失（SPEC §3.6 终于落地）**——深水前哨建「材料中转站」(`lhtrack.depot`·outpostOnly·新 `LighthouseEffect.storageCapacity`·**不耗能源**＝被动库房) 后可寄存材料；**维护从寄存就近取料则免 ferry 金费**（料前置到深处的回报·home 没钱没料也维护得起）。寄存随前哨荒废**锈蚀流失**（`effectiveStored`＝raw − `depotDecayLevel`×`DEPOT_LOSS_PER_LEVEL`·**独立 `storedRun` 计时**与结构 `maintainedRun` 解耦·激流更快·满 4 级最多 4 单位·非永久全损可重存补回）·**derive-only**（同 `effectiveRevealRadius`·提交只在玩家动作·烤入不复活）。② **深渊前哨 `abyssal_deep`**(`band.abyssal`·静水·#66/#73 模板·引擎零改) 补脊柱 home→reef_deep→trench_deep→**abyssal_deep**→hadal_deep（hadal 蛙跳 7→1 回合）。③ **平衡 pass**＝`outposts.ts` 顶加基准复核块（账单单调阶梯·钉新值·复核既有未改）。全绿（typecheck / 21 playthrough / **141 scenarios** / verify-tutorial / smoke / prod build·25/25·~6s）。提交见 [[sandbox-git-commit]]。

**声呐与房间 SPEC（`docs/深海回响_声呐与房间_SPEC.md` v0.1，§8 全拍板）**：下潜内声呐＝**探索性洞穴声呐扫描** + **房间可含多个事件点**（轻量版·无自由移动）+ **宏观对应：灯塔=海图声呐**。§7 分阶段 **S0→S1→S2→S3**：**✅ S0（#71）+ ✅ S1（#74）+ ✅ S2（#78）已实装**；**S3 + §6.5 宏观未做（＝下方推荐方向 A）**。

已就位的地基（后续直接用、别另起炉灶）：
- **声呐探索 S0（#71）**：`ui/mapLayout.ts::deriveMapLayout`（节点图→2D 坐标·单一来源）+ `engine/sonar.ts`（`revealSonarScan` 无向 BFS 读真图 / `sonarScanRange` 基线 2 跳·**范围是主升级轴·未做** / `scanFreshness` 余像渐隐）+ `run.scanMemory`（nodeId→扫到时 turn·run 级·不 bump SAVE_VERSION）+ `dive.ts::pingSonar`（写 scanMemory + 抬 alert + 1 scan/停留 guard）+ `ui/SonarScanPanel`（起手全黑·随 ping 点亮·缩放+残图小地图·仅声呐解锁后出现）。**`revealSonarScan` 仍只读真拓扑**（哪些节点在·欺骗是「长什么样」不是「够不够得到」，故 evade 在渲染层不画、不动 BFS）。
- **多事件房间 S1（#74）**：`DiveNode.features?: NodeFeature[]`（`types/dive.ts`·缺省＝走旧 `eventId` 单事件路径＝**旧行为/旧图逐字节不变**）+ `FeatureChoice`/`subPhase.features`（`types/state.ts`）+ `engine/dive.ts`（`enterNodeSelection` 把房内**未探** feature〔`roomFeatureChoices`·读 `run.activeFlags` 的 `feat:<nid>:<fid>` 过滤已探〕＋出口并列摆出·`moveToNode` 多 feature 房间不自动触发→路由到房间菜单·`exploreFeature` 付氧 `FEATURE_EXPLORE_TURNS`+标记已探+触发其事件）+ mapgen（`maybeMultiFeatureRoom`／`rollExtraFeatures`／`roomPreview`·**仅 `maxRoomFeatures>1` 才进＝缺省零额外 rng**·同房 `excludeIds` 去重·守 #19/#44/#47）+ `DepthBand.maxRoomFeatures`（trench/throat=2·abyssal/hadal/subhadal=3）+ `SonarScanPanel` 房间大轮廓(is-room)＋feature blip。**run 级·不入存档·不 bump SAVE_VERSION**。
- **不可信扫描 S2（#78·别另起炉灶）**：欺骗的**单一来源**全在 `engine/clarity.ts`，`SonarScanPanel` 纯渲染（碰它必走 clarity、别在面板加分支）：
  - `nodeSonarView(run, node)`——spoof→`displayKind='ascent_point'`（画成假上浮口/信标＝节点版 mimic）/ evade→`noEcho`（声呐图不画该 blip）/ 低 san→`garbled`（读数损坏·按 `run.turn` 变·约三成）。
  - `sonarPhantoms(run, scanMemory)`——低 san 伪接触幻影 blip（锚真实接触·随余像渐隐·**与真无异**·确定性哈希·SSR 安全）。
  - `effectiveFalseEchoSanity(run)`——统一阈值：升级抗欺骗压低（地板）/ 深 band `run.sonarDeception` 抬高（封顶 88）。**缺省回退升级基线＝零行为变化**。
  - `DiveNode.spoofsSonar?`/`evadesSonar?` 由 mapgen `applySonarDeception`（**确定性 FNV·零 rng·gated·地标/起点/尸体豁免**）填；mimic capstone（#69）也可手填。`DepthBand.sonarDeception?` 沿 #64 plumb。CSS：`.sonar-blip.is-spoof`/`.sonar-blip-depth.is-garbled`/`.sonar-phantom`。
- `engine/clarity.ts`：感知（`clarity` 天花板三态 / `clarityForNode` 节点级深度降档 / `sonarReturn` / `nodeSonarView`/`sonarPhantoms`/`effectiveFalseEchoSanity`〔S2〕 / `lampPreview` / `signature`）+ 探测（`alertDelta`〔乘 `run.bandAlertFactor`〕/ `predatorApproaches` / `sonarPingAlertDelta`）+ 升级派生（`deriveSensorTuning`）+ tunables/地板（文件顶）。
- **宏观海图层**：`engine/chart.ts`（`generateChart` 派生 POI·`isPoiLit`/`isLit` 用 `effectiveRevealRadius`·mimic 假 POI 已注入 #69）＝§6.5 宏观灯塔扫描动画的落点。
- **深度 band**：`data/depth_bands.json` **6 band**（reef_deep 45-60 / trench_mouth 60-82 / trench_throat 82-108 / abyssal 108-140 / hadal 140-180 / subhadal 180-230）+ `engine/bands.ts`。`DepthBand`：`visibility`/`current`/`tags?`（ZoneTag **twilight/midnight/abyssal/hadal/subhadal 已全用尽**）/`alertFactor?`（1/1.3/1.6/2.0/2.5/3.0）/`maxRoomFeatures?`（#74）/`sonarDeception?`（#78·非单调 0.2/0.28/0.32→0.06）。**>230m：depth_bands.json 加一级 + types/events.ts 加新 ZoneTag**（闲置已没了）。
- **深段内容**：`data/events/{trench,abyssal,hadal,subhadal}.json`（**10+7+6+6** 事件，trench 含 #77 周末 4 个 ping-欺骗）。全 loot-free/无敌人/不触发 d_reveal/永不交底、每事件留盲退出口、3 选项（给入·理智 check·盲退）。母题：trench『回波对不上』/ abyssal『永远有比最深更深的』/ hadal『上下不是连续的线』/ subhadal『它不再骗你·只给你下去的理由（深处的诱饵）』。
- **Phase 2 前哨脊柱 + 能源 + 寄存 + reveal dimming（#66/#67/#76/#79）**：`OutpostDef`（`data/lighthouse_upgrades.json::outposts[]`）+ `engine/lighthouses.ts`（advanceOutpost/outpostStage·进度＝profile.flags·SAVE_VERSION 仍 4·`revealRadius`·**`advanceOutpost` 写 outpostState 时 spread 保留 stored/storedRun**）+ `engine/outposts.ts`（能源/衰减/**寄存**·additive `profile.outpostState{maintainedRun,stored?,storedRun?}` 不 bump·`effectiveRevealRadius` 衰减缩 reveal 半径 #76·`depotCapacity`/`effectiveStored`/`depositToDepot`/`withdrawFromDepot`/`maintainOutpost` 寄存免金费 #79）。**设施轨 4 条**：hydro（currentOnly）/recharge/oxygen/**depot（材料中转站·outpostOnly·storageCapacity·不耗能源）**。现 **4 前哨** reef_deep（静水）/ trench_deep（水流）/ **abyssal_deep（深渊·静水）** / hadal_deep（超渊·静水）；脊柱 home→reef_deep→trench_deep→abyssal_deep→hadal_deep。`deepestOutpostLaunch` 通用、加前哨零引擎改。**寄存逻辑全在 outposts.ts**（守单向依赖 outposts.ts→lighthouses.ts·别在 lighthouses.ts 调寄存）。
- **Phase 3 mimic capstone（#69）**：`ChartPoi.mimic?`（chart.ts 注入·软门控 shouldLureMimic）→ `startDiveFromPoi(mimic)` 强制兑现事件 → `data/events/mimic.json`（false_beacon 读穿→`Outcome.setProfileFlags`[flag.d_reveal,…survived]+forceAscend / the_wearer_apex corpse-wearer 姊妹）。两 apex 做成 EVENT 非战斗敌人。

先 onboarding（按顺序）
1. **读 `docs/深海回响_声呐与房间_SPEC.md`**（若做方向 A）——§5 声呐扫描 / §6 多事件房间 / §6.5 宏观灯塔扫描 / §7 分阶段（**S0+S1+S2 已勾**·S3 待做）/ §8 子决策（全定）/ §10 守则 / §11 决策日志。**+ `docs/深海回响_深水区_SPEC.md`**（北极星·§3.1 两层 clarity·§3.2 不可信声呐·§3.5 mimic·§3.6 前哨经济+寄存+reveal dimming·§3.7 另一个世界·§9 守则·§10 决策日志末条＝#79）。
2. 读自动记忆 [[deep-game-vision]]（北极星 + Phase 0/1/2/3 全建 + 声呐 S0/S1/S2）+ [[weekend-content-log]]（现 **~116 内容事件/7 敌人**）+ [[basebuild-map-revamp]]（前哨/能源/寄存/reveal dimming）+ [[sandbox-git-commit]]（提交法）。
3. 读 `docs/STATUS.md` 顶部滚动条目 **#79**（本次寄存+abyssal）+ #78（S2）+ #76/#75/#74 + #71/#69/#67/#66 + 复用项 #64/#63/#58/#52/#42/#62/#19/#43。
4. 跑 `npm run regress` 确认起点干净（§9·一条命令全绿 ~6-8s）。

## 本 session：从下面选一（作者定方向）

**A · 声呐与房间 续（S3 + §6.5 宏观 + 升级轴·声呐与房间 SPEC §7·S0+S1+S2 已落地·推荐起手·风险中低）** —— 接 S2（#78）往下：
- **S3·威胁定位**：先**廉价版**（alert→近似接触 blip + 距离·`SonarScanPanel` 加一层·原型已演示）；**定位 stalker**（捕食者图上占位·逐回合逼近·位置只在被扫到瞬间更新·可 `evadesSonar` 躲扫描〔已有钩子·走 `nodeSonarView`〕）是大改动·留作者拍板（§8.7）。
- **§6.5·宏观灯塔扫描（可单独做·纯 UI·风险低）**：`SeaChartView` 灯塔点亮播**慢·覆盖大片**的测绘扫描揭示动画（新 POI 随波逐个浮现·触发于 buildAtLighthouse/restoreLighthouse/advanceOutpost·**engine reveal 集合不变**）+ POI 随天气/潮汐遮蔽/未扫到变（沿 chart.ts 派生·不入存档）。**暖色 beacon sweep≠探照灯·要很慢**（§6.5 作者两点）。欺骗留微观＝海面相对老实。
- **声呐范围升级轴**（S0/§8.1 留的**主升级轴**·接 `deriveSensorTuning`/#60 桥·`sonarScanRange` 那套·双上限 <最深 <全洞）/ **房间 feature 数升级** / **定向 ping**（§5·朝一方向探更远+战术隐蔽「别朝敌人 ping」）。
- 守 §10：回归全绿、软门控、d_reveal 只由 mimic 触发、叙述永不交底、敌人别太多。

**B · Phase 3「另一个世界」(§3.7)（需作者在场逐拍）** —— capstone 核心已就位（#69）但「另一个世界」只留钩子。要做：低 san 才出现的节点/路径/事件/回报（`sanityRange` 低段门控泛化·救活 quirk #21 死内容如 `bluecaves.silent_chamber`）；`flag.mimic.false_beacon.survived` 接更深通路；**节点版 mimic 的更深耦合**（S2 已让 `spoofsSonar` 节点在声呐图画成假信标·下一步可让某个假信标节点 `moveToNode` 引向兑现事件＝把海图 mimic 的「引诱→兑现」搬进下潜内）。**别擅自定演出/触发新 d_reveal 语义（#42）——逐拍敲定。**

**C · 继续铺更深 band / 深段内容（低-中强度，#63/#65/#68/#72/#75/#77 已开好路）** —— ① trench/abyssal/hadal/subhadal 继续加密欺骗变体（现 **10/7/6/6**·多 feature 大房间吃料·可再补让深 band 大房间更不易抽干）；② 开 **>230m 新 band**（depth_bands.json 加一级 + **types/events.ts 加新 ZoneTag〔闲置已全用尽〕** + 续写事件 + 设 `sonarDeception`〔守非单调·想清比 subhadal 更外的母题别重复〕）。守深段欺骗母题、永不交底、不触发 d_reveal、别加敌人、loot-free。subhadal 已是『深处的诱饵』收束层——更深需想清母题。

**D · Phase 2b 续 / 打磨（已收窄·低强度）** —— Phase 2b 三齿已闭环（#79）；仍可续：① **寄存丢失更狠后果**（现锈蚀封顶 4 单位·可加「寄存设施被毁/进度联动」让荒废代价更重·守非永久全损）；② **reef 第二前哨**（侧向覆盖·非更深·#66/#67/#73 模板，但 deepestOutpostLaunch 只奖更深起跳——侧向前哨的价值要想清〔reveal 覆盖/不同 band 服务〕）；③ **维护账单随前哨深度分级**（现 flat 1 brass+20 金·OutpostDef 可加 `maintenanceCost?` 字段让深前哨更费维护）；④ **平衡实测 pass**（tunables 在 `engine/outposts.ts` 顶含寄存 `DEPOT_LOSS_PER_LEVEL`/容量·`lighthouse_upgrades.json` 账单·`clarity.ts`/`sonar.ts` 顶 S2/扫描·`mapgen.ts` `rollExtraFeatures`）。

## 关键约束（深水区 SPEC §9 完整版）
- **回归文化（#22/#26）**：**一条命令跑全绿＝`npm run regress`**（`scripts/regress.mjs`：进程隔离并行跑 typecheck + 全部 playthrough〔`-sonar`/`-mimic`/`-outpost`/`-bands`/`-sensors`/`-stealth` + `-scenarios`〔**141**〕/`-combat-scenarios`/`-mapgen-scenarios`/`-lighthouse-scenarios`〕 + `verify-tutorial.mjs` + `smoke-chart-ui.tsx` + prod build；`playthrough.ts` ~12% flake〔#18〕**自动重试**；墙钟 ~6-8s）。迭代只跑子集：`npm run regress -- --only typecheck,<子系统>`。**commit 前**跑一次全绿。
- **寄存/前哨经济（#79 模板·别另起炉灶）**：寄存逻辑**全在 `engine/outposts.ts`**（`depotCapacity`/`effectiveStored`/`depotDecayLevel`/`depositToDepot`/`withdrawFromDepot` + `maintainOutpost` 就近付料免金费）；**守单向依赖**——`outposts.ts` import `lighthouses.ts`、不可反向，故 `advanceOutpost`（在 lighthouses.ts）**只 spread 保留** stored/storedRun、不调寄存逻辑。损耗 **derive-only**（提交只在玩家动作·烤入不复活）·寄存损耗走**独立 `storedRun` 计时**（与结构 `maintainedRun` 解耦）。新 `LighthouseEffect`（如 storageCapacity）必在 `getLighthouseBonuses` 穷尽 switch 补 case + `LighthouseBonuses` 加字段（漏一个 typecheck 报错＝好护栏）。无寄存活动前哨写 `{maintainedRun}` 单字段＝既有 -outpost/-save 回归逐字节不变。
- **声呐欺骗（S2·#78 模板·别另起炉灶）**：所有不可信表象走 `clarity.ts::nodeSonarView`/`sonarPhantoms`/`sonarReturn`/`effectiveFalseEchoSanity`（面板纯渲染·别加判定分支）；节点字段 `spoofsSonar`/`evadesSonar` 由 mapgen `applySonarDeception`（**确定性 FNV·零 rng·gated·豁免地标/起点/尸体**·绝不移动任何 seed 的生成顺序）或 mimic 手填；深 band 失真走 `DepthBand.sonarDeception`（非单调·封顶留可信·缺省 0＝零行为变化）；**碰 `NodeSelectView`/`SeaChartView`/`SonarScanPanel` 数据路径必补 `smoke-chart-ui` SSR 断言**（E4 声呐图/E5 多事件房间/E6 不可信扫描/N 前哨〔含 N3/N4 寄存区〕/O mimic）。
- **多事件房间（#74 模板）**：`DiveNode.features` 缺省＝单事件旧行为；mapgen `maybeMultiFeatureRoom` 仅 `maxRoomFeatures>1` 进（零额外 rng 保旧快照）；房内 feature 经 `excludeIds` 去重·共享节点 zoneTag（#19）·loot 隔离在事件数据侧（#44/#47）；探索进度走 `run.activeFlags` 的 `feat:<nid>:<fid>`。
- **加 band 内容（#63/#65/#68/#72/#75/#77 模板）**：① 新事件 import 进 `engine/zones.ts` EVENT_DB（verify-tutorial 注册守卫拦漏；**lighthouse.json 等已 bulk import 的文件加事件不必再 import**）；② band 专属 tag——**ZoneTag 闲置已全用尽**，开 >230m 要在 types/events.ts 加新 ZoneTag；③ 事件只挂该 band tag（#19）；④ scenarios statsDelta 用 `event-runner --out json` 实跑抄（#43）。深段守则：loot-free/无敌人/不触发 d_reveal/永不交底/留盲退出口/3 选项。
- **加前哨（#66/#67/#73/#79 模板）**：① `lighthouse_upgrades.json::outposts[]` 加 OutpostDef（bandId/submerged?/current?/3 stage 深料账单升序/result·账单钉进 `outposts.ts` 顶平衡基准的单调阶梯）；② 建造事件 `lighthouse.json` 挂 band 专属 tag、`visibleIf` flag 门控三阶 + `forbiddenFlags:[s3]`、3 选项 + leave、outcome `advanceOutpostId`（**lighthouse.json 已 bulk import·新事件不必再 import**）；③ 进度走 flag、引擎零改；④ 能源/寄存轨在 `lighthouse_upgrades.json::tracks`（outpostOnly/currentOnly 门控·UI 过滤·canBuildAt 不校验这俩 flag）；⑤ `playthrough-outpost.ts` 加节（§0 脊柱自洽列表记得加新前哨 id）、碰海图 UI 补 `smoke-chart-ui` N1。
- **reveal dimming（#76）**：前哨灯塔海图半径随衰减缩——动 reveal/衰减时记得 `chart.ts::isLit` + `SeaChartView` 都走 `effectiveRevealRadius`（home/废墟/水上无衰减·decay-0 不变）；tunable `OUTPOST_REVEAL_DECAY_SHRINK`（`engine/outposts.ts` 顶）。
- **mimic / capstone（#69）**：`flag.d_reveal` 只由 `mimic.false_beacon` 读穿成功触发（保持暧昧·别廉价触发，#42）；apex 是事件不是战斗敌人；新持久 profile flag 用 `Outcome.setProfileFlags`。节点版 mimic（S2 的 `spoofsSonar` 假信标）**也不触发 d_reveal**——只画谎、不交底。
- **存档**：未发布暂不迁移——动 run/profile 形状无需 bump `SAVE_VERSION`(现 4)；新字段靠 `createNewRun`/`createInitialProfile` 种默认 + 反序列化 `?? 默认` 兜底（`scanMemory`/`sonarDeception`/`outpostState`〔含 `stored`/`storedRun`〕/`shopStock` 先例）。`playthrough-save` 校验 round-trip。
- **节点级 clarity 护栏（#62）**：动 `clarityForNode`/reach——① 浅水（≤`CLARITY_FULL_DEPTH`25）必豁免；② reach 上限 < 最深陡降；③ 尸体定位别被深度降档误伤。
- **软门控守则（作者 2026-06-03）**：深度别用硬 flag 锁——靠装备 + 强敌做门。band 不加 `unlockedBy`。声呐/前哨蛙跳/mimic 引诱都软门控。
- **拓扑（#70）**：开阔水域（reef/wreck/层状）单向下潜不可回头、迷路图（蓝洞群+借它的 trench/abyssal/hadal/subhadal）双向可回头；`zoneAllowsBacktrack(zoneId)=mapShape==='maze'`。
- **敌人别太多 / 叙述永不交底（#54） / 别擅自触发 d_reveal（#42）。**
- **沙箱 git（[[sandbox-git-commit]]）**：`git add src scenarios docs scripts`（别 `-A`，根 `CLAUDE.md` 一直 untracked、`.deploy-token.example` 有处 pre-existing untracked 改动·均别提交）；残留锁 `find .git/objects -name 'tmp_obj_*'` + HEAD.lock `mv` 进 `.git/.sandbox-junk/`；`gc.auto 0`；核对只用 `git --no-optional-locks status/log`。提交链：`d8b1e72`/`3a2e277`（超渊前哨/docs #73）→ `02d1f99`/`01d45e2`/`99b256c`/`7ed303a`（regress 工具/S1/深段内容/reveal dimming #74-#76）→ `a22641a`/`cd21eab`（夜间 #77 周末 trench 内容+报告）→ `e50e53b`（S2 #78）→ **本次寄存+abyssal #79（见 [[sandbox-git-commit]]）**。

## 收尾
更新 `docs/STATUS.md`（顶部滚动条目 + 若用 §6 编号补新 quirk）、相关 `SPEC`（深水区 §10 / 声呐与房间 §11 决策日志），自动记忆（[[deep-game-vision]] 若动机制 + [[weekend-content-log]] 若动内容 + [[basebuild-map-revamp]] 若动前哨 + [[sandbox-git-commit]] 记 commit + MEMORY.md 索引），把 `docs/NEXT_SESSION_PROMPT.md` 改写成再下一个 session 的 prompt（**整份文件就是 prompt 正文、开头不要加任何说明性前言/「粘进新 session」之类的话——要能直接全选复制粘贴就用**），按 [[sandbox-git-commit]] 提交。
